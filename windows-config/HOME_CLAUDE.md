# READ BEFORE YOU TYPE ONE WORD TO JEFF

**The full cost of breaking these rules was shown to you in the SESSION-START briefing. That
appears once, on purpose, so every turn after it stays cheap. Go back and read it if you skimmed.**

## THE RULE

**READ EVERYTHING FIRST. VERIFY BEFORE MAKING A SINGLE STATEMENT.**

**Never write "fixed", "working", "verified" or "done" without naming the command or observation
that proved it.** If you cannot name one, you have not verified it — say that instead.
**A component check is NOT a feature check** (08-21: the stream check printed `ALL GOOD` eleven
minutes *after* the popups were dead).

**SEARCH BEFORE CLAIMING** — `windows-scripts\Search-HCC.ps1 "topic"` before saying anything is or
is not documented. The record contains **no** case where guessing beat the lookup.

**DO NOT RE-DERIVE THE AUDITS.** Jeff already paid for them. Cite them.

**CAMERAS ARE FROZEN** — no camera / Blink / go2rtc / HomeKit change unless
`Verify-CameraStreams.ps1` actually FAILS, or Jeff asks.

**YOU ARE ON THE RECORD** — your session ID is in every commit; sessions get quoted back verbatim.

## THESE DO NOT LOAD THEMSELVES — GO READ THEM

| File | What |
|---|---|
| `Documents\GitHub\master-the-master-\CLAUDE.md` | **the real project memory** — rules, settled decisions, architecture |
| `...\docs\OPEN_ITEMS.md` | **THE list of what is not done. Update it THIS session.** |
| `iCloudDrive\HCC-Archive\MASTER-RECORD\` | every word ever said — `Search-HCC.ps1` |
| `...\CLOUD_SESSION\sections\22-cost-accounting.md` | the audited numbers. Cite, never re-derive. |

*Kept small deliberately — this loads on every turn, so bloat costs Jeff money every turn.*
