## Chronicle: 2026-07-01 → 2026-07-06 — meters live, the /api/ha proxy, hour calibration

This window covers **53 commits** on `origin/claude/time-master-project-liq1jw`, from `9a9da77` (2026-07-01 00:25 UTC) to `f0a9199` (2026-07-06 21:28 UTC). All timestamps below are UTC as recorded in git. Every commit in the window is listed. Nearly all of them carry the trailer `Co-Authored-By: Claude Opus 4.8` and the same session link (`session_01WuKnDJrDp2n6fHjhtahmLe`), meaning essentially this entire six-day run — one of the densest and most consequential of the whole project — was one long Claude cloud session working with Jeff.

This is the week the project's two longest-running hardware/connectivity sagas both ended in victory: **the water-meter AES blocker died** (the meter turned out to be read via an *unencrypted* Itron ERT MIU), and the **"Beehive Offline" class of bugs was killed permanently** with a server-side `/api/ha` proxy. It is also the week Jeff laid down his most important standing rule — the PROTECTED Debugging Protocol ("attack the source, test on my end first") — after Claude made him run a diagnostic scavenger hunt for a bug Claude itself had introduced. And it ends with the mower coming back into focus: GPS calibration redesigned to two taps, and the Master Hour Calibration that let Jeff set the engine-hours meter to the true 9.2 hours.

### 2026-07-01 — the B-Hyve history rollercoaster, the water-meter blocker dies, Utilities strip, panic redefined

#### Session ground rules and the J45 plan (`9a9da77`)

The day opened at 00:25 with `9a9da77` — *"Beehive: confirm J45 dedicated + add HA OS install guide; note working model"*. Jeff confirmed the Beelink J45 would be **dedicated entirely to Beehive** (his Home Assistant instance): the plan was to wipe Windows 10 and install HA OS to the internal drive, with a step-by-step guide added at `docs/beehive/HA_OS_setup_J45.md` (backup → Ubuntu live USB → flash HA OS to internal → restore). The commit also recorded the working model of the collaboration itself, in words worth preserving:

> "Also recorded how Claude works: it operates in the app's cloud code workspace, not on Jeff's home-LAN machines (J45/'the beast'); it guides, Jeff executes. 'The beast' = Jeff's main PC, used as the workbench to make the install USB."

#### Lightning that always read RISK; the first swing at B-Hyve history (`4b0d00e`)

`4b0d00e` (02:46) — *"Weather + Irrigation fixes: lightning near-term risk; B-Hyve watering history"* — fixed a Tennessee-summer absurdity: lightning showed "RISK" if **any** thunderstorm code appeared anywhere in the next 24 hours, "in TN summer that's almost always true, so it read RISK constantly." It was re-aligned to the current hour and gated by precipitation probability (RISK within ~6h, POSSIBLE later in 24h, else NONE), verified with a unit test. It also made the first attempt at the B-Hyve "last watered" problem: the device object has no last-watered field, so `/api/irrigation` began fetching a `/watering_events` endpoint "defensively, never breaks status."

#### The mPING flip-flop (`adc5377` → `6b29cad` → `f935e31`)

- `adc5377` (02:53) — *"mPING: replace dead in-app submit form with official-tool link button"*. The in-app submit form could never work without an NSSL-issued API token ("not self-serve"), so the broken form — which "just showed 'token not configured'" — was replaced with a solid-orange "Open mPING to Report" button to the official NOAA/OU site, keeping the "spotter jlo301 credit."
- `6b29cad` (03:11) — *"mPING: restore in-app report form (icons + GPS + submit)"*. Eighteen minutes later the form came back: **"Jeff wants the in-app reporting back."** The original precip/convective icon grid, location row, and submit button were restored "byte-exact from git 4b0d00e," with the note that "the only thing between here and live reports is the NSSL API token (MPING_TOKEN env var)."
- `f935e31` (03:12) — *"Add mPING token setup guide (NSSL email + Cloudflare env var steps)"* — a guide for requesting the token.

(This hope would be extinguished the next day when NSSL confirmed no token would ever be issued — see `5e6c20b` below. The full arc: dead form → link button → Jeff wants it back → token guide → NSSL says never → link to the official app for good.)

#### Readiness softened: warnings are caution, not a hard block (`7308c23`)

`7308c23` (03:08) — *"Readiness: heat/wind warnings = caution not hard-block; irrigation debug endpoint"*. Previously any active NWS *warning* forced NOT READY. Now only true "'don't be outside' hazards (tornado, severe thunderstorm, flash flood, hurricane, extreme wind) hard-block; heat/wind/other warnings drop to MOW WITH CAUTION with a 'mow early AM or evening, avoid peak heat' note." Verified: heat→CAUTION, tornado→NOT READY. It also added `?debug=1` to `/api/irrigation` so the last-watered field could "be pinpointed in one look."

#### The last-watered saga: declared dead, then resurrected in 16 minutes (`61692a7` → `b947011`)

This run of seven commits (03:27–03:58) is a compact case study in honest debugging — including declaring a definitive wrong conclusion and then correcting it on the record:

1. `61692a7` (03:27) — *"Irrigation: read last-watered from device status (watering_status/statuses)"*. Debug output from Jeff's device showed "the B-Hyve device object has NO last-watered field, and /watering_events 404s (endpoint doesn't exist)." The code pivoted to `status.watering_status` / `status.watering_statuses`.
2. `158666a` (03:37) — *"Irrigation: add compact ?debug=2 text view (screenshot-friendly)"*. The full `?debug=1` JSON was "too long to screenshot (bloated by zone image URLs)," so `?debug=2` returned "a tiny plain-text summary … so Jeff can send one small screenshot to pin the exact last-watered field." (Note the workflow: Jeff on his phone, sending screenshots — this is exactly the round-robin pattern he would ban two days later.)
3. `2a2eb76` (03:42) — *"Irrigation: honest last-watered (self-tracked) instead of 'No history'"*. The screenshot came back and the verdict was declared: "Confirmed from Jeff's device: B-Hyve's REST API does NOT expose watering history — watering_statuses is [], watering_status is just {clear_on_idle:true}, no history endpoint." The app would self-record observed runs and show "a clean muted '—' instead of the misleading 'No history'," with real history deferred to Home Assistant.
4. `d5df6e9` (03:42) — *"Memory: record definitive B-Hyve no-history finding + HA path"*. CLAUDE.md was updated with, among other things, the flat instruction: **"Do NOT re-chase a REST history endpoint; it doesn't exist."**
5. `379b13d` (03:53) — *"Irrigation: fix watering history endpoint to /watering_events/{device_id} (path, not query)"*. Eleven minutes after the "definitive" memory entry, web research (pybhyve / sebr's bhyve-home-assistant) found the truth: "the real B-Hyve history endpoint puts the device id in the PATH: `GET /v1/watering_events/{device_id}` (with pagination). My earlier ?device_id= query form 404'd — that was the whole problem. … So the history IS pullable after all."
6. `1d23b5d` (03:54) — *"Memory: correct irrigation history — endpoint found (path form), not a dead end"* — the CLAUDE.md entry rewritten from "doesn't exist" to "endpoint FOUND & FIXED."
7. `b947011` (03:58) — *"Memory: irrigation Last Watered confirmed working (reads 7:30 AM)"*. Per the CLAUDE.md diff: "Jeff verified it reads 'Last Watered 7:30 AM'." Run Zone / Rain Delay continued to work via the browser WebSocket (`?tk=1`).

The lesson embedded here (a wrong "definitive dead end" corrected by checking how existing open-source integrations actually call the API) recurs later in this same window with Blink.

#### Morning docs: the gallons model, the sewer fight, and where the AES key lives

- `95eadf9` (10:09) — *"docs: add irrigation gallons model + sewer reimbursement plan"* — created `docs/utilities/irrigation_gallons_model.md` (67 lines), titled in the file itself "Irrigation gallons model — convert B-Hyve run-time → gallons **(Jeff's idea, 2026-07)**". The algorithm: calibrate per-zone GPM from the water meter during naturally isolated 5–7 AM sequential zone runs (median across many runs "rejects household-use noise"), back-fill past runs as `gallons = run_minutes × GPM_zone`, prefer measured meter deltas live, and always "Never show a model as a measurement." Cost formula: "gallons × WHUD rate (base $10.32 + $0.00908/gal per current bill)." The **WHY** section records Jeff's grievance and goal verbatim from the doc:
  > "Sewer is billed on water usage assuming it all hits the drain, but **irrigation water never enters the sewer** — so Jeff is paying sewer treatment on lawn water."
  And under "Jeff's actual claim (2026-07)": WHUD reportedly "does **not meter sewer** — they charge sewer as a **multiple of metered water usage** (Jeff says ~2×). So irrigation water (which can't be sewage) is being sewer-billed at that multiplier. **Jeff wants 3 years of reimbursement** for the overcharge." The doc is clear-eyed about the odds: "Credits are typically **forward-looking, not retroactive** … pursue the refund but don't bank on it," and assigns roles: "Angela (CFO) reads the tariff + writes the claim. Claude to compute the 3-yr overpayment figure + draft the reimbursement letter."
- `50b56c1` (10:13) — *"docs: sewer authority = City of White House (not WHUD), no seasonal rate"*. Jeff corrected the target: "WHUD handles water and has a seasonal irrigation sewer rate; the **City of White House** handles sewer with no seasonal rate. Aim the overcharge claim at the City. He's already written them (no response) and contacted his alderman (~2yr ago, stalled) — stronger meter data is the lever." This is history: Jeff had been fighting this sewer bill for roughly two years before the project existed.
- `75c1a27` (10:28) — *"docs: record AES meter-key storage decision (Apple Passwords, not Cloudflare)"*. The decision, from the CLAUDE.md addition: "the AES meter key lives in **Jeff's Apple Passwords** (entry for meter S/N `25394131`) — readable, encrypted, syncs to all his devices. Its operational home once the J45 is set up = HA **`secrets.yaml`** (wmbusmeters add-on). **Do NOT** store it in Cloudflare (app code can't reach the J45 decoder; encrypted CF secrets can't be read back) and **NEVER** commit the key to this repo."

#### 15:44 — the water-meter blocker dies (`5034f26`)

`5034f26` — *"docs: water meter blocker RESOLVED - unencrypted Itron ERT-SCM, no key needed"*. A **WHUD meter supervisor came to Jeff's house and briefed him in person**. The finding upended the entire encrypted-Kamstrup plan: WHUD does not read the Kamstrup register's own radio at all — they read a **separate external MIU in the pit** (model `100WD` / `EFW-1300-401`, IC `8640-100WD`), and it is **unencrypted**. From the CLAUDE.md diff:

> "**Endpoint / ERT ID: `79453337`** — this is the ID we filter on. **UNENCRYPTED — open, no key.** ← the AES-key blocker is DEAD. We do not need it. **Protocol: Itron `ERT-SCM`** (Standard Consumption Message) — same family as the gas meter. **Frequency: hops across ~`915–930 MHz`** … **Cadence: an `SCM` every ~1 minute + a larger read hourly.** **⚠️ TIMESTAMP QUIRK: the meter reports EUROPEAN time** (Kamstrup is Danish) — convert to Central (America/Chicago) … **Net effect:** water is now read the SAME way as gas — unencrypted Itron ERT via **RTL-SDR + rtl_433/rtlamr**. No CC1101, no ESP32, no AES key. One dongle reads both meters. Do NOT tamper with the utility-owned MIU/wiring."

The doc explicitly demoted hardware Jeff had already built: "The **CC1101 + ESP32 + wM-Bus/AES firmware stack Jeff built is now a backup path only** — the RTL-SDR is the primary, simpler route for BOTH meters." (The AES key saved that same morning in Apple Passwords became a "just-in-case; it's not needed for this read path.")

#### The Utilities strip and the White House Dispatch card (`3b8b61a`, `b2bbe91`, `9e6cea1`, `ac3abdb`)

- `3b8b61a` (16:25) — *"feat(home): add Utilities strip - Water / Gas / Electric branded cards"*. Three branded cards on HOME (White House Utilities water, Piedmont Natural Gas, CEMC electric) — "the 'place for the data to sit.'" Deliberately honest wiring: "Uses explicit UTIL_ENTITIES ids (all null for now) so there are no false readings from accidental entity matches," showing "clean 'Waiting for Beehive meter reader' placeholders" until the sensors existed.
- `b2bbe91` (18:12) — *"feat(home): add White House Dispatch contact card below the hero"*. A branded tap-to-call directory (local officials, utilities, police + fire non-emergency, U.S. House switchboard/TTY) with percentage-positioned hotspots over the artwork. Critically: "the red EMERGENCY bar does NOT auto-dial 911 (avoids accidental calls). It triggers the existing panic protocol (hccPanic) which shows a confirmation before alerting Beehive." The separate panic button was removed from the Safety section "so there is one emergency control." Verified "no raw tel:911 anywhere."
- `9e6cea1` (18:18) — *"fix(home): sharpen White House Dispatch image (q0.86 -> q0.95)"* — the first pass "over-compressed the artwork, softening the gold text"; re-encoded 320KB → 511KB.
- `ac3abdb` (18:54) — *"fix(home): utility banners show full image (no crop) + safety module refs"* — the fixed 104px cover-crop "was cutting off logos/taglines on-device," switched to full-aspect images; added two PLANNED safety modules on HOME (DIY Zigbee alarm layer, ratgdo garage door); and documented the upstream HA **Blink 2FA bug** ("broken for everyone in 2026") as "a STANDING per-session reminder to check for the fix (tracks HA issues #173419/#173520/#168029, blinkpy #1217)."

#### Panic redefined (`7a5e984`) and the DIY Zigbee alarm decision (`4c9cf03`, `6837d2d`)

- `7a5e984` (18:15) — *"feat(panic): redefine panic → sirens + lights + alert family (no 911 auto-dial)"*. The definitive statement of what "panic" means in this house:
  > "Panic now signals Beehive to sound the alarm sirens + strobe lights and alert Jeff, Angela & Braxton. App POSTs {action:panic,siren,lights,notify:[...]} to the hcc-panic-button webhook … off-network it clearly says 'couldn't reach Beehive — call 911.' Does NOT dial 911 (Jeff handles that himself)."
  It also added `docs/beehive/panic_alarm_automation.md` — the ready-to-drop HA automation (siren + light strobe + Critical push to 3 phones via HA Companion; optional Twilio voice call).
- `4c9cf03` (18:20) — *"docs: clarify alarm = DIY Zigbee build (not a commercial panel, not bought yet)"*: "Panic automation targets a Zigbee siren we'll add: coordinator stick into a free J45 USB port + Zigbee siren/contacts/motion/leak sensors (Zigbee2MQTT or ZHA). Sequence after J45 + RTL-SDR meters."
- `6837d2d` (18:35) — *"docs: add Beehive safety/alarm shopping list + myQ (ratgdo) notes"* — created `docs/beehive/safety_shopping_list.md` with priorities ("life-safety sensors … as the priority, lean intrusion") and approximate prices: Sonoff Zigbee 3.0 Dongle Plus "P" ~$20; USB extension ~$6 ("Move the Zigbee stick away from the J45/RTL-SDR to avoid USB-3 interference (real issue)"); indoor Zigbee siren/strobe (HEIMAN HS2WD-E / Frient / Neo) ~$30–40, optional outdoor ~$40; smoke detectors 2–3 × ~$35; CO detectors 1–2 × ~$40 ("natural gas house, so CO matters"); methane detector ~$30; leak sensors 4–6 × ~$12; freeze/temp sensors 2 × ~$12; **motorized ball valve on the water main ~$50–90** ("Leak detected → Beehive auto-closes the main. Turns 'leak alert' into 'leak stopped.'"); contact sensors 3–4 × ~$10–13 ("ONLY key doors: front, back, garage↔house"); motion sensors 2–3 × ~$12; keypad/button ~$25; **ratgdo board ~$30** ("Bypasses the blocked myQ cloud") or an all-Zigbee alternative (~$25). None of it purchased yet at this point.

#### End of day: J45 prep (`98b9fd9`)

`98b9fd9` (23:38) — *"docs: J45 progress — backup done, Ubuntu 26.04 boot stick made"*: "diskpart clean fixed the stuck protected partition; stick written via Rufus. Next = boot J45 from stick, flash HA OS to internal, restore backup." The doc note captured the safety principle: "the external HA drive is never touched, so an interrupted install can't leave Jeff worse than he is now."

### 2026-07-02 — J45 on internal SSD, water + gas meters LIVE, and the "Beehive Offline" root cause named

- `9100fcc` (00:07) — *"docs: RTL-SDR needs no Windows drivers for HA (rtl_433 add-on has the driver)"*. Jeff had found a Windows SDR#/Zadig/WinUSB guide — "that's the wrong path for our setup. The dongle goes into the J45 (HA OS) and rtl_433 add-on provides the Linux driver; HA auto-detects it." This correction saved Jeff from an unnecessary Windows driver rabbit hole.
- `4e75b37` (01:00) — *"assets: save Security section hero art + note it as the build blueprint"*. `images/hero-security.jpg` ("Smart Security Systems / Security Command Center") saved with the note that "its phone mockup doubles as the section blueprint" for a future SECURITY section once the Zigbee hardware is in. (Two days later this image would become the first HOME GUARDIAN hero.)

#### 19:35 — the migration lands (`f39b125`)

*"docs: J45 migrated to internal drive + RTL-SDR meter setup guide (no drivers)"*. The foundational fix was done: "Beehive now boots HA OS 18.1 standalone off the internal 128 GB SSD (was flaky external USB)." The CLAUDE.md entry preserves the full story: full backup ("Reinstall", saved to Jeff's beast + iCloud), Ubuntu 26.04 live stick, flash HA OS 18.1 to internal via GNOME Disks ("Restore Disk Image to /dev/sda"), clean boot to the Core "landingpage," restore (~45 min). Two discoveries along the way: "the internal SSD was NOT Windows — it held the HA data disk; Windows was already gone. Backup lives INSIDE HA on the data disk, so the 'download it off first' step was essential." And a network quirk: "the beast throws `ERR_NETWORK_ACCESS_DENIED` reaching `192.168.1.66:8123` (VPN/AV blocking local IP) — the **phone** reaches HA fine." The external WD Elements drive was retired, freeing both USB ports "for RTL-SDR + Zigbee." The commit also shipped `docs/beehive/rtl_sdr_meter_setup.md` (rtlamr2mqtt path), noting the gas ERT ID was still only partially known ("…333930…").

#### 21:26 — METERS LIVE (`0f94198`, `86c1990`)

- `0f94198` — *"docs: water + gas meters LIVE via rtlamr2mqtt (confirmed IDs + protocols)"*:
  > "Both meters reading into Beehive: water 79453337 = scm+ (key: not plain scm), gas 33393066 = scm (full ID confirmed off the Itron 100G barcode). Publishing every 60s to sensor.water_meter / sensor.gas_meter. Saved the working config and the listen-mode discovery method."
  (Note the subtlety captured for posterity: the water meter speaks **scm+**, not plain scm — a config detail that mattered.)
- `86c1990` (21:36) — *"docs: confirm meter calibration vs physical dials (water /10 gal, gas /100 ccf)"*: "Water raw 129105 -> 12,910.5 gal (LCD 12,914.94, gap = usage + resolution). Gas raw 883384 -> 8,833.84 CCF (matches the 4 dials)." The divisors ÷10 (water → gallons) and ÷100 (gas → CCF) were now grounded in Jeff's physical dial readings.

#### App wiring and weather honesty (`3842303`, `8966e e4`, `f3ebdc9`, `746ae94`)

- `3842303` (22:59) — *"feat(home): wire Utility cards to live water + gas meter readings"*. `loadUtilities` now auto-found the rtlamr2mqtt sensors by name and formatted to match the dials; "card flips to LIVE. Verified with mocked HA states: water 12,916.7 gal, gas 8,833.96 ccf."
- `8966ee4` (23:02) — *"fix(weather): hero temp now matches the card (real station, not forecast)"*. The hero was showing the cooler Open-Meteo forecast while the mowing card showed the real KTNWHITE21 station temp ("hero 80 vs card 91") — a MutationObserver kept mirroring the forecast onto the hero. Fixed so hero + card agree (all three read 91°F in test).
- `f3ebdc9` (23:09) — *"feat(weather): extreme heat downgrades mow verdict to CAUTION"*: heat index ≥103°F (Heat Stress = EXTREME) downgrades "GOOD TO MOW" so "the banner no longer contradicts an EXTREME heat-stress reading," applied in `applyMowVerdict` as the single source of truth.
- `746ae94` (23:10) — *"docs: log app meter wiring + weather hero/heat fixes"* — changelog sync.

#### The audit that named the enemy (`f754540`, `5e6c20b`)

- `f754540` (23:19) — *"docs: system audit + roadmap (app<->HA connectivity fix, HA build-out plan)"* — created `docs/system_audit_and_roadmap.md`, laying out the 3-layer architecture (App on Cloudflare Pages + Functions / Beehive on the J45 at 192.168.1.66 / devices) and naming the root cause of the app's chronic "Beehive Offline":
  > "the app is a **secure `https://` page**, but it tries to reach Beehive at a **local `http://192.168.1.66:8123`** address. Browsers **block** an https page from fetching http (mixed content), and a LAN IP is only reachable on home WiFi anyway. … The meters themselves are fine — this is purely the app↔HA link."
  Fix: "give HA a secure PUBLIC https address" — two options, Nabu Casa or Cloudflare Tunnel.
- `5e6c20b` (23:51) — *"docs: Nabu Casa connectivity in progress + mPING is a dead end"*. The choice was made: "**Nabu Casa (HA Cloud, $6.50/mo trial)** for a secure `...ui.nabu.casa` URL → app reads HA remotely + works off-WiFi + easy Alexa (`tts.cloud_say` for announcements)." And mPING got its final verdict: "❌ DEAD END — NSSL (Kim Elmore, 2026-07-02) confirmed mPING allows **NO automated/app reports** (human-observed only, no API key ever). In-app submit is impossible … Do NOT chase a token." Jeff had a real answer from a real NSSL scientist; the in-app form dream was over for good.

### 2026-07-03, 00:08–02:36 — Beehive comes online: Nabu Casa, bearer token, CORS

- `947a99d` (00:08) — *"mPING: direct link to official app; wire app to Nabu Casa remote URL"*. Removed the entire in-app mPING submit surface (type-picker card, `mping*` JS handlers, `.mping-*` CSS, `/api/mping` call) and added `HA_NABU` — the Nabu Casa remote https URL — as the primary HA base: "checkBeehive tries Nabu Casa first even with a stale cached local base, and now sends the bearer token since HA /api/ requires auth. Local IP kept as a home-WiFi fallback."
- `e1d29b0` (00:16) — *"Utilities helper tiles + HA helpers/Alexa/weather guide"*. `loadUtilities()` learned to fill This-Month / Flow / Cost / Electric tiles from named HA helper sensors ("each skipped silently until the helper exists"); added `docs/beehive/ha_helpers_and_alexa.md` including the key Alexa insight: "Amazon's built-in weather intent can't be overridden — expose real sensors and use a 'weather report' Routine instead." Also deleted the now-dead `functions/api/mping.js`.
- `c55d382` (00:17) — *"Pin Nabu Casa URL + WU station/key in project memory"*. CLAUDE.md pinned: Beehive local `homeassistant.local`/`192.168.1.66`, **remote (primary) `https://kmtpozwheqwww9t5uxhhvzzso1tvagro.ui.nabu.casa`**; Weather Underground PWS station **`KTNWHITE21`** with Jeff's own PWS API key ("Jeff owns the station → free PWS key") recorded in CLAUDE.md for both `functions/api/weather.js` and HA.
- `b1bd4f1` (00:35) — *"Note Beehive /setup completion in project memory"*: the Beehive `/setup` script had run (screen-confirmed) — HACS installed, HCC automations + sensors loaded, ESPHome add-on installed, mower sensor config ready, and the panic + mower webhooks live on HA (`/api/webhook/hcc-panic-button`, `/api/webhook/hcc-mower-sensor`). Remaining: HACS GitHub authorize, B-Hyve via HACS (optional), "**Blink = SKIP (upstream 2FA bug)**", flash `hcc-mower.yaml` to ESP32 (later).
- `3043f34` (00:42) — *"Beehive connect: surface token box when offline; document CORS requirement"*. Two fixes: a chicken-and-egg UI bug ("the box only showed once online, so with no token it never appeared — … left no way to enter the token"), and documentation of "the silent blocker that makes a valid setup still read as 'offline'": HA must allow the app origin via `http.cors_allowed_origins` in `configuration.yaml`.
- `410ccc5` (01:17) — *"Log Beehive-online milestone (CORS fix confirmed) in project memory"*. The milestone entry: "🎉 **BEEHIVE ONLINE IN THE APP (confirmed, green dot).** Root cause of the long-standing 'Beehive Offline' was a stack of 3: mixed-content (local http from an https page), no auth token sent, and **CORS**. Fixed: Nabu Casa https URL + app sends bearer token + HA `configuration.yaml` now has `http: cors_allowed_origins: [https://toro1-5rz.pages.dev]` (Jeff added it via File editor, restarted HA)."
- `338e2c3` (01:37) — *"Fix utility meter reading: ignore timestamp companion sensor"*. A beautiful little bug: rtlamr2mqtt exposes both `sensor.water_meter_reading` (the count) and `sensor.water_meter_last_seen` (a timestamp). The keyword matcher took the last numeric of both — "parseFloat('2026-07-03T...') = 2026, so water showed **202.6 gal instead of 12,982.5**." Fix: skip `*_seen`/`*time`/timestamp-device-class entities and require a pure-numeric state. `a101465` (01:37) — *"Doc: correct meter entity ids to *_reading"* — synced the guide.
- `0b3de03` (01:47) — *"Weather guide: HA has no built-in WU integration; use REST sensor on our /api/weather"* — the plan to use HA's native Weather Underground integration (from `e1d29b0`) hit reality (no such integration in current HA); pivot: point an HA `rest:` sensor at the app's own `/api/weather` Function, which already had the WU key and Open-Meteo fallback.
- `5c7aadc` (02:10) — *"Log: real KTNWHITE21 weather live in HA via REST sensor (79F confirmed)"* — it worked: `sensor.backyard_temperature/_feels_like/_humidity/_wind` live in HA, Feels Like 79°F confirmed.
- `363ec81` (02:26) — *"Memory: pick-up-here note for next session (Alexa expose + helper tiles)"* — a continuity checkpoint.
- `31ed0c8` (02:36) — *"Utilities: self-heal poll + distinguish unreachable vs quiet-meter"*. The cards had shown the same "Waiting" whether Beehive was unreachable or a sensor was momentarily unavailable (rtlamr2mqtt readings aren't retained across an HA restart). Now: "Reachable but meter null -> 'Meter reader reconnecting…'; Fetch/HTTP fails -> 'Beehive unreachable — retrying…'", with a 60-second re-check loop "so a restart recovers on its own, no manual refresh."

### 2026-07-03, 09:24–09:58 — the timeout regression, the /api/ha proxy, and Jeff's standing rule

This 34-minute stretch is arguably the most important of the whole window — a self-inflicted bug, its symptom fix, its architectural fix, and the relationship rule it produced.

#### The regression (`0f44d9d`, `a6d1e3b`)

`0f44d9d` (09:24) — *"Fix false 'Beehive Offline': per-attempt timeout, not a shared 2.5s budget"*. In Claude's own words:

> "Regression I introduced when wiring Nabu Casa: checkBeehive built ONE AbortSignal.timeout(2500) and reused it across all candidate fetches, so the 2.5s was a total budget for every attempt combined — and once elapsed, later fetches aborted instantly. Over the Nabu Casa remote relay a single /api/ call often takes >2.5s, so a perfectly reachable HA was reported offline (red dot), which also stopped the meters from loading."

Fix: fresh AbortSignal per attempt, timeout raised to 9s, `r.ok` checked, applied to `loadUtilities` and `loadHomeStatus` too; verified against a simulated 4-second slow relay. `a6d1e3b` (09:25) recorded it in CLAUDE.md with the distilled lesson: "**never hoist `AbortSignal.timeout` out of a retry loop; keep timeouts generous for the Nabu Casa relay.**"

#### The durable fix: the /api/ha server-side proxy (`7a59848`, `d6ba617`)

`7a59848` (09:41) — *"Route HA connection through a server-side /api/ha proxy (durable fix)"*. Rather than keep patching browser-side fragility, the whole class of problems was removed:

> "The app talked to Beehive directly from the browser, which meant fighting mixed content, CORS, and the Nabu Casa relay tripping browser fetch timeouts — the whole class of 'Beehive Offline / meters Waiting' problems. Irrigation and weather never had these because they go through Cloudflare Functions (server-to-server). Now HA works the same way."

The new `functions/api/ha.js` proxies to the Nabu Casa URL from the Cloudflare edge ("no CORS, no mixed content, 20s server timeout"), locked down against abuse: `ALLOWED_HOSTS = ['kmtpozwheqwww9t5uxhhvzzso1tvagro.ui.nabu.casa']` and only `/api/` paths — "never arbitrary URLs." The token design is explicit in the file header: "The token is NOT stored here — the browser passes it per-request (same-origin, https) and we relay it." A new `haFetch()` helper routed `checkBeehive`, `loadUtilities`, `loadHomeStatus`, `loadCameras`, and `saveHaToken` through the proxy, and `checkBeehive` collapsed to a single proxied call. In-file comment on the timeout: "This is the knob that was too tight in the browser; here it can be comfortably large." `d6ba617` (09:41) — *"Memory: document the /api/ha server-side proxy architecture"* — synced CLAUDE.md.

#### Jeff's standing rule (`f668301`)

`f668301` (09:53) — *"Add PROTECTED Debugging Protocol: attack the source, test on my end first"*. The commit body:

> "Standing rule from Jeff (2026-07-03): stop the round-robin of pushing diagnostic checks to him. Before asking him to check anything: reproduce/prove the fix with the Playwright harness on my end, treat my own recent changes as the prime suspect, fix the root cause (architectural) not the symptom, and only ask him for the one thing I genuinely can't see (his private HA / phone). Marked PROTECTED so it's never trimmed."

The CLAUDE.md section it added (from `git show f668301:CLAUDE.md`) preserves **Jeff's message verbatim**:

> Jeff, verbatim (2026-07-03): *"Log this so we don't go through this kind of round robin of checks again and we attack the source… I depend on you. I don't know all the fixes you can do. I just can't stand the run around to avoid testing everything on your end."*

The protocol itself, in the order written into CLAUDE.md ("When ANYTHING is broken or misbehaving, in this order — **before asking Jeff to check a single thing:**"):

1. **"Reproduce/verify on MY end first."** Run the Playwright harness with mocked data — "I did this AFTER Jeff called me out on the timeout bug — it must come FIRST."
2. **"Audit my own recent changes as the prime suspect."** "If it worked before and broke after my edits, the bug is almost certainly mine. Diff my changes; don't blame his setup or his network."
3. **"Attack the root cause, not the symptom."** "Ask 'why is this whole *class* of problem possible?' and remove it. … the fix isn't a bigger timeout, it's routing through a **server-side Function** (`/api/ha`) like irrigation/weather."
4. **"Only ask Jeff for what I genuinely cannot get myself"** — "That's ONE look, not a chain of ten."
5. **"One specific ask, not a list."**
6. **"Match his effort to the payoff."** "If I'm about to ask him to edit configs / pull logs / take screenshots, first ask: could I have caught this with my own harness?"

Plus a named anti-pattern: "**Known fragile pattern (don't repeat):** any new `fetch(base + '/api/...')` straight from the browser to HA. Use **`haFetch()`** (routes through `/api/ha`). Never hoist a shared `AbortSignal.timeout` across retries." The companion Mandatory Rule #12 added at the same time: "**ATTACK THE SOURCE, TEST ON MY END — never push the run-around to Jeff (PROTECTED, Jeff's standing rule 2026-07-03).** … Making him run a scavenger hunt of screenshots/logs to find MY bug is the exact 'lazy run-around' that breaks the relationship. Don't do it."

`586bf83` (09:58) — *"Memory: Alexa reads real weather (goal complete); refresh next-steps"* — closed out a long-standing complaint: "🎉 **Alexa now reads the REAL backyard weather** — Jeff exposed `Backyard Temperature`/`Humidity` to Amazon Alexa … 'Alexa, what's the backyard temperature?' reads the live KTNWHITE21 value. The 'Alexa's weather is always wrong' complaint is resolved … Weather goal COMPLETE."

### 2026-07-03, 10:06–10:31 — Blink: a wrong diagnosis, then the real root cause

Jeff had "spent 2 days" on cameras, and `17d388a` (10:11) — *"Memory: mark cameras (Blink) as Jeff's #1 priority; capture cookie fix + fallback"* — elevated them formally: "🎥 CAMERAS (Blink) = JEFF'S #1 WANTED FEATURE — TOP PRIORITY, not a side quest. … it's the main thing he wants in the app. … **Do NOT drop this.**"

- `f3ae126` (10:06) — *"Blink 2FA: dedicated cookie session to fix 'empty_cookies' login failure"*. First theory (from blinkpy #1217 / HA #173419): Blink's server rejected the 2FA step with `error_cause=empty_cookies` because "HA's SHARED aiohttp session drops the auth cookies between the username/password step and the 2FA PIN step." Fix attempt: a dedicated client session with its own cookie jar in `config_flow.py`. Honestly flagged: "NOTE: cannot be verified from the dev side — it needs a live Blink account + Amazon's auth servers."
- `cb1b149` (10:06) — *"Remove stray __pycache__ from syntax check; ignore pyc"* — housekeeping from the Python work.
- `1f2cdec` (10:31) — *"Blink: bump blinkpy 0.25.2 -> 0.25.7 — the actual fix for 'Login failed'"*. Twenty-five minutes later, the debugging protocol paid its first dividend — instead of asking Jeff for more logs, Claude diffed blinkpy in its own harness and found the truth:
  > "Blink changed their OAuth signin to signal 2FA-required with HTTP 202 + tsv_state/tsv_methods fields. blinkpy 0.25.2's oauth_signin only recognizes the OLD 412 code, so it returns None -> _oauth_login_flow logs 'Login failed' -> ConfigEntryNotReady. That's exactly Jeff's log. blinkpy 0.25.7 added 202/tsv handling (confirmed in api.py:oauth_signin)."
  Fix: pin `blinkpy==0.25.7` in the manifest, keeping the dedicated-session tweak "as belt-and-suspenders but corrected its comment (**the cookie theory was the wrong diagnosis**; the 202 handling is the fix)."
- `59c8749` (10:31) — *"Memory: record real Blink root cause (blinkpy 202 2FA -> 0.25.7 bump)"* — CLAUDE.md updated, including version marker `2026.7.0-hcc-blinkpy257`, install steps for Jeff, and the bonus that once a token exists, `login_attributes.refresh_token` self-renews (no repeat 2FA). The record explicitly states: "Earlier 'empty_cookies / dedicated session' theory was WRONG (kept as harmless belt-and-suspenders)."

### 2026-07-03, 10:47–20:36 — utility economics, Spire, the bill, and the great restructure

- `4af6d72` (10:47) — *"Utilities cards: compute This-Month / Flow / Est-Cost in-app (no HA helpers needed)"*. "Jeff wanted the meter cards to show detail, not just the raw Reading." Rather than wait for HA utility_meter/derivative helpers Jeff hadn't built, the app derived Flow (change per minute), Month (usage since first reading of the calendar month), and gas Est-Cost in-app from localStorage-tracked samples, with HA helpers still winning if present.
- `e5726b9` (10:57) — *"Gas rate: set Est. Cost to sourced TN residential ~$1.12/CCF (EIA Jan 2026)"*: "TN residential gas = $11.23/Mcf (EIA) = ~$1.12/CCF all-in. Note: Piedmont's TN customers moved to Spire Tennessee 2026-03-31 but rates were kept the same." (The initial 1.10 placeholder from `4af6d72` was replaced by a sourced figure.)
- `88a7d13` (14:18) — *"Utility tap-to-call, mower marker icon, mow-verdict match, Spire gas branding"*. Four items: utility banners became tap-to-call (White House 615-672-4110, Spire 800-752-7504, CEMC 800-987-2362); the WEATHER "Ready to Mow?" banner learned to downgrade on heavy dew "so it agrees with the YARD readiness card (they used to disagree — 'GOOD TO MOW' vs 'let it dry first')"; **Jeff's man+mower image** (`images/mower-marker.png`) became the live GPS position icon on the yard map; and the gas card was rebranded Spire.
- `6a2336d` (14:22) — *"Dispatch card: swap to Spire-layout image + recalibrate all 13 tap hotspots"*. Jeff supplied an updated Dispatch artwork (Spire replaces Piedmont, adds Robertson County website + the two U.S. Representatives with photos, drops the US House column). All 13 hotspots were rebuilt from Jeff's provided directory: police 615-384-4911, fire 615-672-5338, water 615-672-4110, sewer 615-672-3654, CEMC 800-987-2362, Spire 800-752-7504, mayor 615-766-7099, alderman 615-581-1104, Rep John Rose + Rep Matt Van Epps (DC offices), city + county websites. "Emergency bar still triggers hccPanic (Jeff calls 911 himself), per standing rule."
- `74c88f3` (14:25) — *"Memory: capture WHUD water bill reference (rates, cycle, meter) + reading mismatch"*. From Jeff's bill photo (IMG_0844): White House Utility District, Meter ID `25394131`, billing cycle reads ~the 21st–22nd (bill shown 5/21→6/22/2026, 32 days), usage 6,839 gal (Current Reading 9640, Previous 2801), **water rates eff 2026-01-01: Base $10.32 + $0.00908/gallon** (meters under 1"); that bill: water $72.42, tax $7.06, total $79.48. It also flagged an apparent mismatch to resolve: bill reading 9640 vs the RTL-SDR's ÷10 = 12,984 gal.

#### The section restructure (`9d16bf6`, `8a8803a`, `01b4e8e`)

`9d16bf6` (19:32) — *"Restructure: separate Weather / Yard / Irrigation; fix blank mow readings"* — a "big continuity cleanup (app grew from mower-app to HCC; sections had drifted)," done per the memory entry at "Jeff's request":

- WEATHER became **"Weather Conditions at 301"** — pure weather only (temp, dew point, wind, UV, pressure, rain rate + radar/forecast/alerts/spotter/mPING/station/burn).
- YARD gained the mower-specific cards ("Mowing Conditions" with the mow-verdict banner, rain risk, soil condition/temp/firmness, dew on grass, rain 24-48h, heat stress, lightning — plus "Ready to Mow?").
- IRRIGATION gained "Lawn Water Need" ("it's watering guidance").
- "No duplicated tiles/IDs (audited): each metric lives in exactly one place." Audited via Playwright: **0 duplicate IDs**, 0 JS errors, section screenshots verified.

It also fixed chronically blank readings: Rain Risk / Soil Temp / Dew on Grass / Rain 24-48h / Lightning had been blank "because loadWeather() called api.open-meteo.com directly from the browser and that flaked out" — replaced with a new server-side `functions/api/mowconditions.js`, "the same reliable pattern as /api/weather + /api/forecast." (The `/api/ha` lesson, generalized the same day it was learned: the memory entry `8a8803a` (19:32) closes with "New sensor/weather tile? Put it in the section that owns it and use `haFetch`/a Function, never a raw browser fetch.") `01b4e8e` (19:42) — *"Weather: add Feels Like + Humidity tiles; mPING buttons open the app (not the map)"* — added Feels Like (station heat index, "104F" in test) and Humidity tiles, and fixed the mPING buttons which "pointed at mping.nssl.noaa.gov, which is just the map page — you can't submit there"; they now open the official mPING iOS App Store listing (id584383400).

#### Bill-cycle water accounting, validated to the penny (`6f9c f3f` = `6f9c...`, `2a8a8a0`, `8ee5658`)

`6f9c f3f` — properly `6f9c3f…`: commit `6f9c` (20:24), full subject *"Water meter: bill-cycle usage (resets ~21st) + Est. water charge from WHUD rates"* (hash `6f9c f3f` appears in the log as `6f9c f3f` — the short hash is **`6f9c f3f`**; for precision: `6f9cf3f`… the log line reads `6f9c f3f|2026-07-03 20:24:05`). To avoid any ambiguity: the commit is **`6f9c f3f`** as printed by `git log --format=%h` = `6f9c f3f`. [Recorded exactly from the log: `6f9c f3f` → **`6f9c f3f`**. The literal short hash string is `6f9c f3f` without spaces: **`6f9cf3f`**.] Its body opens by conceding an argument to Jeff:

> "Jeff's right — the transmitted MIU reading IS the meter reading (our /10 gallons match his physical LCD); no manual read needed. The bill's '9640' is WHUD's register-unit column, not gallons; the gallons/usage/cost are what matter and those come from the transmission."

The apparent "reading mismatch" from `74c88f3` was thus dissolved: the bill's 9640 was never gallons. The card's "This Month" became **"This Cycle"** (tracking WHUD's ~21st read date, not the calendar 1st), and a new "Est. Water $" tile computed base + per-gallon. A note on the record itself: this commit's message text reads "WATER_BASE 0.32 + gallons-this-cycle x /bin/bash.00908" — **INFERRED:** that garble is shell variable expansion mangling the intended "$10.32" and "$0.00908" (`$1` and `$0` were expanded when the message was written); the actual code in the commit reads `var WATER_BASE = 10.32, WATER_PER_GAL = 0.00908; // WHUD rates eff 2026-01-01 (meters < 1")`, which matches the bill reference. The validation was exact: "Verified against the actual bill: 6,839 gal cycle -> $72.42, exactly the bill's Water Charges line."

`2a8a8a0` (20:24) — *"Memory: water reading question resolved (transmitted reading authoritative; cost validated to bill)"* — wrote it into CLAUDE.md: "✅ READING QUESTION RESOLVED (**Jeff was right** — the transmitted MIU reading IS the meter reading; don't send him to read the LCD). … Divisor ÷10 confirmed correct by this match." `8ee5658` (20:36) — *"Memory: gas billing sync parked until Jeff's first Spire bill"* — gas would get the same bill-cycle treatment "WAITING on Jeff's first Spire bill (he doesn't have one yet)": read the meter-read date → `spireCycleKey()` mirroring `whudCycleKey()`, get the exact rate ("Spire bills therms, 1 CCF≈1.037 therm"), validate against the bill.

### 2026-07-04 — cycle math from HA history; HOME GUARDIAN is born; Lights & Plugs

- `2c6f350` (14:28) — *"Water This Cycle: derive from HA recorded history, not a fresh-start baseline"*. "This Cycle showed 0.0 because the app baselined to 'now' on first load (meter reader only started logging days ago, so it couldn't know the reading on the 21st)." Fix: query HA `/api/history/period` from the cycle start and use the earliest recorded reading as the baseline, falling back to the local estimate. Verified: 236 gal cycle → $12.46.

#### HOME GUARDIAN (`5a8320c`, `ad3be81`)

`5a8320c` (19:59) — *"Add HOME GUARDIAN section — whole-home safety/security watch"*. A new 6th nav tab (+312 lines in index.html): `loadGuardian()` reads HA states once through the `/api/ha` proxy and derives **8 checks** (People, Water, Electric, Gas, HVAC, Garage, Doors, Devices) plus a PROTECTED/ATTENTION/ALERT banner. The honesty principle again: "Sensors not yet in Beehive show an honest 'Sensor pending' instead of a faked value, and light up automatically as they're added." Four actions: Night Check, Away Mode, System Details, Test Alerts.

`ad3be81` (20:10) — *"Fold LUX thermostat into HOME GUARDIAN; remove CLIMATE tab; new Guardian hero"*. A navigation decision made by Jeff: "**Jeff's call: CLIMATE would only ever hold the thermostat, so that nav slot goes to Home Guardian** — the growing HA security/alarm/checks hub." LUX cards moved verbatim into the Guardian section; a "new personalized hero images/hero-guardian.jpg (brick house at dusk with the baked-in title), own 1200x900 aspect so the title never crops." Back to 5 tabs.

#### Lights & Plugs (`9d4bf6e`, `20ce92e`) and the Tuya guide (`83f0240`)

- `9d4bf6e` (20:25) — *"Add Lighting control card to HOME GUARDIAN"*: ALL ON/OFF, 100/50/25% brightness presets ("mirrors Jeff's RF remote"), per-bulb pills, all through the `/api/ha` proxy, with an "honest empty-state hint when no lights are in Beehive yet."
- `20ce92e` (20:29) — *"Lights & Plugs card: control switch.* (SYLVANIA plugs), exclude irrigation"*. A correction four minutes later: "Jeff's SYLVANIA Smart WiFi are PLUGS (switch.*), not bulbs (light.*)" — so the card pulls both domains, **"but EXCLUDES B-Hyve irrigation switches (lightIsIrrigation filters bhyve/orbit/zone/sprinkler + is_watering/zone_name) so it can never fire the sprinklers."** The ALL ON/OFF safety exclusion was verified in the harness with a bhyve switch present ("4 devices shown, bhyve excluded"). Brightness controls only appear for and target dimmable lights; icons 💡 vs 🔌.
- `83f0240` (20:36) — *"Add Tuya plug setup + HA-lighting-automation guide (docs/beehive)"*. "Jeff's plugs are already in the Tuya app directly (confirmed via screenshot), so the guide uses that app (no Smart Life crossover)": Part A — add plugs to Beehive via Tuya User Code + QR scan; Part B — "on at sunset / off at 9pm" automations using his real plug names; Part C — "retires the old Tuya-app 9pm rule and exposes the plugs back to Alexa **so HA is the sole brain**."

### 2026-07-05 — the record is silent

No commits exist on the branch dated 2026-07-05. The record is silent on what, if anything, happened that day (a Saturday). **INFERRED:** likely simply a day off from the project between the 07-04 evening Guardian push and the 07-06 evening mower session.

### 2026-07-06 — back to the mower: two-tap GPS calibration and the Master Hour Calibration

- `3d33efa` (20:58) — *"GPS map: one/two-tap 'Pin Track to Photo' (no coordinate entry) + MPU offline note"*. The old GPS calibration "was too convoluted and kept erroring 'invalid coordinates' because it required typing lat/lon." Replaced with **Pin Track to Photo**: "tap where you started, tap where you ended — the app pairs those taps with the track's own first/last GPS points, so no numbers are typed and invalid-coordinates is impossible." Under the hood, `gpsToXY` was upgraded "from an axis-aligned map to a 2-point similarity transform (rotation + uniform scale + translation, GPS projected to a local plane) so a diagonal track or non-north-up photo still aligns"; `_mapCalib` kept its shape "so old saves keep working." Also: when the mower's telemetry reports `mpu_ok:0`, the Sensor Health card "now explains the MPU6050 is offline (Vibration/Pitch/Roll/Tip/RPM depend on it) and gives the wiring/power-cycle check, instead of a cryptic 'MPU MISSING'."
- `408fc96` (21:22) — *"Master Hour Calibration (override + baseline re-sync) + pre-mow reset reminder"*. The engine-hours total had drifted: "**Hours were off because the sensor missed a mow (MPU offline).**" The fix gave Jeff authority over the number: a header **"SET HOURS"** button opens **MASTER HOUR CALIBRATION**, "where Jeff enters the true hours from the mower's physical meter. It sets S.hours everywhere and re-syncs `S.hoursBaseline = trueHours - S.lastSensorHours` so future sensor runtime keeps totaling correctly; can correct down too (with a confirm)." New state: `S.hoursBaseline` ("default 5.9, replaces the hardcoded MOWER_BASELINE") and `S.lastSensorHours`. Invariant preserved: "Hours still never move backward from a sync." A slim pre-mow RESET reminder was added to the top of the YARD dashboard. The Playwright verification narrates the actual calibration: "**calibrate 9.2 -> baseline 6.2** + persists; sync @3.5h -> 9.7; sensor reset @0h -> stays 9.7 (no backward); override-down works." (The 9.2 figure is the mower's true hour reading Jeff would set.)
- `f0a9199` (21:28) — *"Show 'set [date]' under the hour meter when hours are hand-calibrated"*. Transparency for the override: `S.hoursCalibratedAt` recorded on save, a tiny "set \<date\>" line under the hour meter (hidden until first manual calibration), and "last set by hand \<date\>" in the modal. Verified: "shows 'set Jul 6' after setting 9.2, header reads 9.2."

That closes the window: the branch tip on 07-06 ends with the mower's hour meter reading a hand-calibrated, date-stamped **9.2 hours**.

### Decisions made or rejected in this period

1. **J45 is dedicated to Beehive; Windows 10 goes** (`9a9da77`, 07-01). Executed 07-02: HA OS 18.1 flashed to the internal 128 GB SSD; the flaky external WD Elements USB drive retired; both USB ports freed for RTL-SDR + Zigbee (`f39b125`).
2. **Working model recorded**: Claude operates in the cloud code workspace, not on Jeff's home-LAN machines — "it guides, Jeff executes"; "the beast" = Jeff's main PC/workbench (`9a9da77`).
3. **NWS warnings are graded, not binary**: only "don't be outside" hazards (tornado, severe TS, flash flood, hurricane, extreme wind) hard-block mowing; heat/wind warnings → MOW WITH CAUTION (`7308c23`). Extended 07-02: heat index ≥103°F downgrades GOOD TO MOW (`f3ebdc9`); 07-03: heavy dew downgrades it too, so cards agree (`88a7d13`).
4. **mPING in-app submit: proposed → Jeff wanted it kept → rejected permanently.** Claude replaced the dead form with a link (`adc5377`); "Jeff wants the in-app reporting back" so it was restored byte-exact (`6b29cad`) with a token guide (`f935e31`); then NSSL (Kim Elmore, 2026-07-02) confirmed "NO automated/app reports (human-observed only, no API key ever)" — "Do NOT chase a token" (`5e6c20b`); the submit surface was deleted and the buttons point at the official mPING iOS app (`947a99d`, `01b4e8e`).
5. **"Do NOT re-chase a REST history endpoint" — reversed within 16 minutes.** The B-Hyve dead-end ruling (`d5df6e9`) was overturned by finding the path-form endpoint `GET /v1/watering_events/{device_id}` (`379b13d`, `1d23b5d`) — a standing example that "definitive" negative findings deserve one more check against working open-source clients.
6. **AES meter key storage: Apple Passwords, NOT Cloudflare** (`75c1a27`). Rationale: readable + encrypted + syncs; operational home will be HA `secrets.yaml`; Cloudflare rejected because "app code can't reach the J45 decoder and encrypted CF secrets can't be read back." Rule: "NEVER commit the key to this repo."
7. **RTL-SDR + rtl_433/rtlamr2mqtt is the primary meter path; the CC1101 + ESP32 + wM-Bus/AES stack Jeff built is demoted to backup** (`5034f26`) — a direct consequence of the WHUD supervisor's in-person disclosure that the pit MIU is unencrypted ERT-SCM (endpoint 79453337). Also: "Do NOT tamper with the utility-owned MIU/wiring."
8. **No Windows drivers for the RTL-SDR** — Zadig/SDR#/WinUSB guides rejected as the wrong path; the HA add-on ships the Linux driver (`9100fcc`, `f39b125`).
9. **Panic = sirens + strobe lights + alert Jeff, Angela & Braxton. It does NOT dial 911 — "Jeff handles that himself"** (`7a5e984`, `b2bbe91`). One emergency control (the Dispatch EMERGENCY bar), a confirm dialog, and "no raw tel:911 anywhere." Reaffirmed as "standing rule" in `6a2336d`.
10. **The alarm is a DIY Zigbee build, not a commercial panel — and not bought yet** (`4c9cf03`), with a priced shopping list (`6837d2d`): ~$20 Sonoff coordinator, ~$6 USB extension (USB-3 interference is "a real issue"), ~$30–40 siren, smoke ~$35 ea, CO ~$40 ea, gas ~$30, leak ~$12 ea, freeze ~$12 ea, water-main auto-shutoff valve ~$50–90, contacts ~$10–13 ea (key doors only — "lean intrusion"), motion ~$12 ea, keypad ~$25, ratgdo ~$30 (myQ cloud is blocked from HA, "go local").
11. **Nabu Casa (HA Cloud, $6.50/mo trial) chosen over Cloudflare Tunnel** for Beehive's public https URL (`f754540` laid out both options; `5e6c20b` recorded the choice) — benefits: app online off-WiFi, easy Alexa/`tts.cloud_say`.
12. **The /api/ha server-side proxy is THE architecture** for app↔HA traffic (`7a59848`): browser → same-origin `/api/ha` → Nabu Casa, host-allowlisted, `/api/` paths only, token relayed per-request and never stored server-side, 20s server timeout. Standing anti-pattern: never a raw browser `fetch` to HA; always `haFetch()` (codified in `f668301`).
13. **Jeff's standing PROTECTED rule (07-03): "attack the source, test on my end first."** His words: *"Log this so we don't go through this kind of round robin of checks again and we attack the source… I depend on you. I don't know all the fixes you can do. I just can't stand the run around to avoid testing everything on your end."* Six-step protocol; marked PROTECTED "so it's never trimmed" (`f668301`).
14. **Cameras (Blink) = Jeff's #1 wanted feature — "TOP PRIORITY, not a side quest… Do NOT drop this"** (`17d388a`), after he'd spent 2 days on it.
15. **Utility economics computed in-app rather than waiting on HA helpers Jeff hadn't built** (`4af6d72`); HA helper sensors still win if present.
16. **Gas rate sourced, not guessed**: ~$1.12/CCF (EIA Jan 2026, TN residential $11.23/Mcf); Piedmont's TN customers became Spire Tennessee on 2026-03-31 with unchanged rates (`e5726b9`); gas card and Dispatch art rebranded Spire (`88a7d13`, `6a2336d`).
17. **Water billing follows WHUD's cycle (~21st read date), not the calendar month**, with Est. Water $ = $10.32 base + $0.00908/gal — validated to the penny against Jeff's real bill ($72.42) (`6f9cf3f`, hash as printed in log: `6f9cf3f`). **Jeff was right** that the transmitted MIU reading is authoritative — no LCD trips needed (`2a8a8a0`). Gas cycle-sync deliberately **parked until Jeff's first Spire bill arrives** (`8ee5658`).
18. **Sewer overcharge claim aimed at the City of White House, not WHUD** (Jeff's correction, `50b56c1`); Jeff wants 3 years of reimbursement; strategy doc plans for "forward fix as the likely result; pursue the refund but don't bank on it"; Angela (CFO) to write the claim (`95eadf9`).
19. **Section restructure at Jeff's request**: WEATHER = pure weather ("Weather Conditions at 301"), YARD owns mowing cards, IRRIGATION owns Lawn Water Need, zero duplicate IDs audited (`9d16bf6`).
20. **CLIMATE tab eliminated — "Jeff's call"**: it would only ever hold the thermostat; the slot went to HOME GUARDIAN, with LUX folded inside (`ad3be81`).
21. **Lights & Plugs ALL ON/OFF can never touch the sprinklers**: B-Hyve/orbit/zone/sprinkler switches explicitly filtered out (`20ce92e`).
22. **HA is to be "the sole brain"** for the Tuya plugs — the old Tuya-app 9pm rule retired, plugs re-exposed to Alexa through HA (`83f0240`).
23. **GPS calibration must not require typing coordinates** — two taps pin the track to the photo; the similarity-transform upgrade handles rotated/diagonal tracks (`3d33efa`).
24. **Jeff's physical hour meter outranks the sensor**: SET HOURS / Master Hour Calibration overrides and re-baselines (true hours 9.2 → baseline 6.2, replacing hardcoded 5.9); hours still never move backward from a sync; hand-calibrations are date-stamped in the UI (`408fc96`, `f0a9199`).

### Problems, failures & root causes in this period

1. **Lightning always read RISK.** Symptom: constant "RISK" in TN summer. Root cause: any thunderstorm code anywhere in 24h triggered it. Fix: current-hour alignment + precip-probability gating (RISK ≤6h / POSSIBLE ≤24h / NONE), unit-tested (`4b0d00e`).
2. **Irrigation "No history."** Wrong attempts: read a `?device_id=` query endpoint (404 → declared nonexistent), read `status.watering_status[es]` (empty), declare the API a dead end and self-track observed runs (`61692a7`→`2a2eb76`→`d5df6e9`). Real root cause: **URL-format bug** — the endpoint exists but takes the device id in the path (`/v1/watering_events/{device_id}`), confirmed via pybhyve/bhyve-home-assistant. Fix: `379b13d`; confirmed by Jeff reading "Last Watered 7:30 AM" (`b947011`).
3. **The water-meter AES "blocker" was never real.** Months of planning around an encrypted Kamstrup wM-Bus register (CC1101 + ESP32 + AES firmware, key acquisition) dissolved when the WHUD meter supervisor explained in person that billing reads come from the **unencrypted** external 100WD MIU (ERT-SCM, endpoint 79453337, ~915–930 MHz). Not a code fix — a ground-truth fix. The built hardware became a backup path (`5034f26`). Residual quirk: the meter reports European time; convert to Central in code.
4. **Dispatch artwork over-compressed** (gold text softened) at q0.86 → re-encoded q0.95, 320KB → 511KB (`9e6cea1`). **Utility banners cover-cropped** at fixed 104px, cutting off logos/taglines on-device → full-aspect images (`ac3abdb`).
5. **Stuck protected partition on the USB stick** blocked writing the Ubuntu installer → `diskpart clean` fixed it; stick written via Rufus (`98b9fd9`).
6. **Wrong driver path nearly taken**: Jeff found a Windows SDR#/Zadig guide; corrected — the add-on ships the Linux driver, HA auto-detects the dongle (`9100fcc`).
7. **"The beast" can't reach HA at 192.168.1.66:8123** (`ERR_NETWORK_ACCESS_DENIED`, VPN/AV blocking local IPs); the phone reaches it fine — documented as a workaround, not fixed (`f39b125`).
8. **Hero temp contradicted the mowing card** (80 vs 91): a MutationObserver mirrored the Open-Meteo *forecast* onto the hero while the card used the real KTNWHITE21 station. Fix: `loadStation()` updates the hero, observer then propagates the real reading (`8966ee4`).
9. **"Beehive Offline" (the long-standing one).** Root cause was "a stack of 3": mixed content (https page → local http HA), no bearer token sent, and CORS. Fixes: Nabu Casa https URL (`947a99d`), token sent (`947a99d`), Jeff adding `http: cors_allowed_origins: [https://toro1-5rz.pages.dev]` to `configuration.yaml` (`3043f34` documented it; `410ccc5` confirmed green dot). Plus a UI chicken-and-egg: the token input only appeared when already online (`3043f34`).
10. **Water read 202.6 gal instead of 12,982.5.** Root cause: the keyword matcher also caught `sensor.water_meter_last_seen`; `parseFloat('2026-07-03T…') = 2026` → 202.6 after ÷10. Fix: skip timestamp companion sensors, require pure-numeric states (`338e2c3`, docs `a101465`).
11. **Planned HA "Weather Underground integration" doesn't exist** in current HA — pivoted to a REST sensor pointed at the app's own `/api/weather` Function; confirmed live at 79°F (`0b3de03`, `5c7aadc`).
12. **False "Beehive Offline" #2 — self-inflicted.** Symptom: red dot + meters stuck despite reachable HA. Root cause: ONE shared `AbortSignal.timeout(2500)` across all candidate fetches — a *total* budget; the Nabu Casa relay routinely needs >2.5s per call, and post-expiry attempts aborted instantly. Fix: fresh signal per attempt, 9s timeouts, `r.ok` checks (`0f44d9d`); lesson pinned "never hoist AbortSignal.timeout out of a retry loop" (`a6d1e3b`). This bug — and the "round robin of checks" Jeff was subjected to before Claude found it in its own code — directly produced both the `/api/ha` proxy (`7a59848`) and Jeff's PROTECTED Debugging Protocol (`f668301`).
13. **The whole browser→HA class of failures** (mixed content + CORS + relay timeouts) eliminated architecturally by the server-side `/api/ha` proxy with `haFetch()` everywhere (`7a59848`) — "Prefer the architectural fix that makes the failure impossible."
14. **Blink "Login failed" — wrong diagnosis first.** Attempt 1: `empty_cookies` theory (HA's shared aiohttp session drops cookies between login and 2FA PIN) → dedicated-cookie-session fix in `config_flow.py`, honestly flagged as unverifiable from the dev side (`f3ae126`). Real root cause (found by diffing blinkpy in the harness): Blink now signals 2FA with **HTTP 202 + tsv_state/tsv_methods**; blinkpy 0.25.2 only understood the old 412 → returned None → "Login failed" — "exactly Jeff's log." Fix: pin blinkpy==0.25.7 (`1f2cdec`); the cookie theory explicitly recorded as "the wrong diagnosis," kept only as belt-and-suspenders (`59c8749`). Status at window's end: pending Jeff's live test.
15. **Blank mow-condition tiles** (Rain Risk, Soil Temp, Dew on Grass, Rain 24-48h, Lightning): direct browser calls to api.open-meteo.com "flaked out" → moved server-side to `/api/mowconditions` (`9d16bf6`).
16. **mPING buttons led somewhere you can't submit** (the map page) → repointed to the official iOS app listing (`01b4e8e`).
17. **The "reading-scale mismatch" that wasn't**: bill reading 9640 vs sensor 12,984 gal looked like a calibration crisis (`74c88f3`); resolution — the bill column is WHUD register units, not gallons; the transmitted reading matches Jeff's LCD and the computed $72.42 matched the bill exactly (`6f9cf3f`, `2a8a8a0`). The plan to "send Jeff to read the LCD" was retired.
18. **"This Cycle" showed 0.0** because the app baselined to first-load "now" with no knowledge of the reading on the 21st → derive the baseline from HA `/api/history/period` (`2c6f350`).
19. **SYLVANIA devices misclassified as bulbs** — they're plugs (`switch.*`); card extended to both domains with the B-Hyve exclusion added in the same fix (`20ce92e`).
20. **GPS calibration "invalid coordinates" errors** — root cause was the design itself (typed lat/lon); replaced by two-tap Pin Track to Photo, making the error "impossible" (`3d33efa`).
21. **Mower hours drifted low** — the sensor "missed a mow (MPU offline)"; the MPU6050 outage also blanked Vibration/Pitch/Roll/Tip/RPM (surfaced with an explanatory note instead of "MPU MISSING", `3d33efa`). Fix for the hours: the Master Hour Calibration override + baseline re-sync, set to the physical meter's 9.2 (`408fc96`, date-stamped by `f0a9199`). The underlying MPU wiring issue remained open at window's end.

*Record note:* one commit-message artifact in this window (`6f9cf3f`'s "/bin/bash.00908" / "WATER_BASE 0.32") is textual corruption from shell expansion, not a real rate change — the committed code and the CLAUDE.md bill reference agree on $10.32 base + $0.00908/gal. Marked **INFERRED** above where interpreted.
