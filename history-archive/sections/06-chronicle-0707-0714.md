## Chronicle: 2026-07-07 → 2026-07-14 — Blink live, AI cameras, Fire TV wars

This eight-day window is the single densest stretch of the whole project: 43 commits on `origin/claude/time-master-project-liq1jw`. It opens with a hardware dead end (the SYLVANIA plugs) and the first real Home Assistant devices ever to appear live in the app, and closes with Jeff's number-one feature (Blink cameras) delivered, a local GPU AI-detection pipeline running on "the beast," the public domain `loewenhome.com` live worldwide, and a three-round war to get camera alerts to pop up on the Fire TV — a war that included a false victory declared "confirmed working end-to-end," an audit that disproved it, and an actual fix by a completely different mechanism.

Two Claudes worked this window in parallel, and this is the period where that arrangement was formalized as **Mandatory Rule 13**: the **cloud session** (`session_01WuKnDJrDp2n6fHjhtahmLe`, Claude Opus 4.8 early in the window, Claude Opus 4.6 from 07-10 on; commits timestamped `+0000`) owned all app code, deploys, and research; the **coworker** — Claude Code running on Jeff's PC "the beast," a.k.a. "Clyde" (Claude Sonnet 5; commits timestamped `-0500`, Jeff's local time) — did the hands-on LAN/Beehive/Windows work. The interleaved merge commits (57476b6, b1022d9, ef20ec5) are the seams where the two streams joined.

**A note on prices:** the record in this window contains no dollar figures. What it does contain, repeatedly and emphatically, is Jeff's **hard NO on subscriptions** — see the 07-10 plan doc below.

### 2026-07-07 — The SYLVANIA dead end, and the first live devices (Tuya plug + Sharky the vacuum)

#### f010694 (2026-07-07 20:17 UTC) — "Note: SYLVANIA plugs are locked to their app — dead end for HA (use Kasa/Zigbee)"

The attempt to bring Jeff's existing SYLVANIA Smart+ WiFi plugs into Home Assistant via the Tuya/Smart Life path (planned 07-04) ended in a confirmed dead end. From the commit body:

> Confirmed via Smart Life's own 'device not supported by this app' error (both EZ and AP mode, all iOS perms/BT/2.4-only correct) plus forum consensus that certain SYLVANIA Smart+ plugs don't work with Tuya/Smart Life. Recorded in CLAUDE.md so we don't re-attempt the Tuya path. Path to HA-native plugs: Kasa (WiFi) now or Zigbee later. Reminder to turn the BGW320-500 5GHz back on.

The CLAUDE.md entry written for this (visible in the 9785381 diff) is blunter and richer — the plugs are **firmware-locked to the SYLVANIA app**; Smart Life rejects them ("Unknown device — this device is not supported by this app") in BOTH EZ (fast-blink) and AP (slow-blink) pairing modes, with every precondition correct (iPhone on 2.4 GHz-only, Bluetooth on, Local Network/Location permissions granted, cellular off). Back-doors (LocalTuya, firmware flashing) need the same blocked access. Verdict recorded verbatim in CLAUDE.md:

> **Verdict:** to get plugs into HA + the Lights & Plugs card, Jeff swaps to HA-native: **Kasa KP125/EP25 (WiFi)** = easiest today (TP-Link HA integration, pairs in ~30s, app handles the 2.4 band), or **Zigbee plugs** (Sonoff S40 ZB / ThirdReality) to ride the coming Zigbee alarm coordinator. … The SYLVANIA plugs stay on SYLVANIA app + Alexa as-is.

(The "turn 5GHz back on" reminder in this commit turned out to be **wrong** — see the 07-09 correction under 9785381 below.)

#### 7d17c24 (2026-07-07 21:45 UTC) — "Lights & Plugs: in-app diagnostic when no plug matches (surface HA entity names)"

A tooling commit born of a real constraint: the cloud dev environment cannot reach Jeff's HA to inspect entity names. So `loadLights()` was taught to show, when it finds no `light.*`/`switch.*`, exactly what Beehive returned (total entity count plus the exact `switch.*`/`light.*` entity_ids) — "This lets us see a plug's real entity name from the app itself." Verified in the harness: diagnostic renders when empty, a real plug (e.g. `switch.mini_smart_socket`) still shows normally, zero JS errors. The stale SYLVANIA hint wording was replaced.

#### e7d9ef9 (2026-07-07 22:16 UTC) — "Add Robot Vacuum (Sharky) card + keep vacuum switches out of Lights & Plugs"

The breakthrough behind this commit: after the SYLVANIA failure, a **generic Smart Life plug (JH-G01U "Mini Smart Socket," Shenzhen Jiuheng)** paired fine, the **HA Tuya integration came alive (2 devices / 22 entities)**, and along with `switch.bed_lamp` it brought in a surprise second device — **"Sharky," a BL20 Pro robot vacuum**. These were the **first HA devices ever live in the app**. The commit added a `#vacuumCard` in Guardian (`loadVacuum()`) that hides itself unless a `vacuum.*` exists, with Clean/Pause/Dock/Find via `vacuum.start|pause|return_to_base|locate` plus live state and battery; `loadLights` now filters out the vacuum's setting-switches (child-lock/DND/mop, or any switch sharing the vacuum's name-stem). Verified in Playwright: "Lights & Plugs shows only the plug (Bed lamp), vacuum card renders 'Docked · charging · 85%' with buttons firing the correct vacuum services, zero JS errors."

The CLAUDE.md changelog entry for 07-07 (🎉 "FIRST HA DEVICES LIVE IN THE APP — plug + robot vacuum") preserves the **pairing playbook that worked**, recorded explicitly "for future plugs":

> (1) pair the plug into the **Smart Life** app using **AP Mode / slow-blink** — the fast-blink auto-scan is flaky, AP mode is reliable; (2) in HA add the **Tuya** integration with the **User Code from Smart Life** and **scan the QR with Smart Life's in-app scanner** (Settings tab → scan icon), NOT the iPhone camera (camera → opens a website) and NOT SYLVANIA (→ "designated app" error); (3) the **QR is ONE-TIME-USE** — a fresh QR, scan once, tap the blue **Confirm login** fast ("already been used" = re-scanned a spent QR).

Recorded loose end: the porch/landscape lights were still on the locked SYLVANIA plugs — automating them (sunset-on / 9pm-off) requires swapping to working plugs first.

### 2026-07-09 — Guardian calms down, Rule 13 is born, the link audit, and BLINK GOES LIVE

#### a27982a (2026-07-09 12:12 UTC) — "Guardian banner explains ATTENTION/ALERT + calmer thresholds; fix 2 dead Weather links"

Symptom: "Guardian showed '⚠️ ATTENTION' with no reason — a single offline device (43/44) was tripping it." The fix: the banner now collects and displays **reasons** ("ATTENTION — 1 door open", "ALERT — Water leak"); a lone offline device no longer escalates (only >15% of devices offline flags it; below that it's "a calm FYI"); and People "0 HOME" became neutral ("being away isn't an alert"). Verified in Playwright: 1/44 offline + 0 home → PROTECTED; door open → ATTENTION with reason; leak → ALERT with reason; zero JS errors. Also fixed two Weather buttons that Jeff reported 404'd (the CLAUDE.md entry records: "Jeff: they 404'd") — the Spotter Network deep-link and a specific TuneIn NOAA station id were swapped for durable targets (site root + a TuneIn search for Nashville NWR).

#### bec7440 (2026-07-09 12:25 UTC) — "Add Mandatory Rule 13 (coworker delegation) + record 404-risk link audit"

The birth of the two-Claude division of labor as a standing rule, labeled in CLAUDE.md as "**Jeff's rule 2026-07-09**." The full rule text added to CLAUDE.md:

> 13. **TELL JEFF WHEN TO USE HIS LOCAL COWORKER (Jeff's rule 2026-07-09).** Jeff runs a **Claude "coworker" on his PC (the beast)** with real computer/local access. It can do what THIS cloud session CANNOT: reach his **home LAN + Beehive/HA directly** (read/click HA, install `custom_components`, restart HA, enter PINs), touch **local files** on his PC, drive **apps on his screen**, and **open/verify external links** in a real browser. THIS session owns the **app code, Cloudflare repo/deploys, research, and guidance**. Jeff doesn't know either of our full capabilities, so **it's on ME to proactively flag the handoff**: whenever a task — or a single step of one — is better done hands-on on his machine or inside Beehive, SAY SO ("this part your coworker can knock out") and hand over a crisp, copy-pasteable instruction the coworker can follow.

The same commit recorded the link audit state: the only remaining 404-risk deep-links were the six mower parts/manual reference buttons (partstree ×3, ereplacementparts, jackssmallengines, manualslib) plus `guardianDetails`'s `openBeehive('/lovelace/0')` — unverifiable from the cloud side because "egress 403 on all external hosts."

#### fc62533 (2026-07-09 12:56 UTC) and 9a34d17 (2026-07-09 12:59 UTC) — link audit closed via the coworker

The **first coworker hand-off in project history**, and it worked:

> First coworker hand-off worked: Claude Code on the beast checked the 5 mower parts/manual links — all live (ereplacementparts 403 is anti-bot only, fine in a browser). No changes needed; validates not blind-swapping working links. (fc62533)

CLAUDE.md's version: "Good thing we checked instead of blind-swapping working links." 9a34d17, minutes later, extended Rule 13 with the coordination protocol that would matter for the rest of the project: brief the coworker by cloning this repo (Claude Code auto-reads CLAUDE.md); the **coworker treats app code as READ-ONLY** and does hands-on local/Beehive/web work while the cloud session owns all app-code edits/commits/pushes — "to avoid two-Claude collisions."

#### 9b29c1f (2026-07-09 14:29 UTC) — "Blink: record real root cause + official fix (blinkpy 0.25.6 / HA 2026.6.4); our custom override is now the blocker"

The commit body is empty; the substance is a CLAUDE.md diff, and it's one of the most important root-cause write-ups in the record. Blink cameras had been Jeff's #1 wanted feature and had been broken for weeks; on 07-03 a **custom `config_flow.py` workaround** had been installed under `custom_components/blink/` based on an "empty_cookies / dedicated session" theory. Web research on 07-09 found the truth:

> Blink's 2FA now returns HTTP 202 (`tsv_state`/`tsv_methods`); old blinkpy read 202 as success so the PIN never showed → "Login failed." Fixed upstream in **blinkpy 0.25.6** (PR #1231) → **HA core 2026.6.4** (PR #173811, "no HA-side changes needed"). Our July-3 `custom_components/blink/` override now **shadows HA's fixed built-in with stale code** — the log's `ConfigEntryNotReady` is from OUR `coordinator.py:58`.

CLAUDE.md's expanded Pending #4 admits the earlier theory outright: "**The earlier 'empty_cookies / dedicated session' theory was a wrong guess**; the real bug was the 202 handling, now fixed in the library." The fix procedure: `rm -rf /config/custom_components/blink` → delete the broken Blink entry → update HA core to ≥ 2026.6.4 → restart → re-add the **built-in** Blink integration → SMS PIN now appears. One caution recorded: "our old code has hammered Blink's login every ~10s for days → account may be rate-limited; after step 2 wait ~30 min before re-adding." And the never-again rule: "**DO NOT re-add a `custom_components/blink` override** — that's what broke it."

#### 7bbc8a2 (2026-07-09 15:11 UTC) — "Blink cameras LIVE in the app (Jeff's #1 feature) — all 6 cameras confirmed; mark Pending #4 resolved"

Forty-two minutes later, done. The CLAUDE.md entry:

> 🎉🎥 **BLINK CAMERAS LIVE IN THE APP — Jeff's #1 feature, DONE (confirmed screenshot: all 6 cameras — 301 Driveway, Front Right, Back Left, 301 Backyard, Garage, 301 Front Doorbell).** Fix = removed the stale `custom_components/blink` override, deleted the broken entry, restarted HA (core 2026.7.1 has the fixed blinkpy 0.25.6+), re-added the **built-in** Blink integration → SMS PIN finally appeared → authenticated. … Lesson: after HA shipped the official fix, our pre-fix custom override became the bug (it shadowed the good built-in). Never re-add a custom blink override.

#### 5ddac8a (2026-07-09 18:49 UTC) — "Camera full-control in app: Refresh All, per-camera snapshot/save-clip/arm panel; stills via signed entity_picture URL"

Same-day follow-through: HOME → Security got a **Refresh All** button (fires `blink.trigger_camera` for every camera, then reloads stills), tap-a-camera opens a full-control panel (large still, Take New Snapshot, Save Clip via `blink.save_recent_clips` to `/media/blink/…`, Arm/Disarm, battery/temp/wifi/motion rows). The key technical fix, per the body: "Fix stills: load HA signed entity_picture image URL from https Nabu Casa host (img load, no CORS) instead of blob fetch" — the old `fetch().blob()` approach was CORS-gated, which is likely why tiles had only shown icons. Explicitly noted: "No Blink subscription required (on-demand snapshot/clip-save/arm are free features)" — CLAUDE.md adds that cloud clip *history* is the only thing Blink's paid plan adds.

#### 80799e7 (2026-07-09 18:56 UTC) — "Safety: gate panic alarm behind Beehive token so public visitors can't trigger it; route via /api/ha proxy"

A genuine security hole found before going public. From the body:

> App is publicly shareable (loewenhome.com). Home data is already token-gated per-device, but the panic webhook was unauthenticated — any anonymous visitor could fire the real alarm. Now hccPanic() requires getHaToken() and posts through the same-origin proxy. Family full access = paste Jeff's HA token once per device.

The CLAUDE.md entry spells out the access model: public visitors see only "the demo shell + public weather/dispatch numbers"; **Angela, Jeff's dad, and Braxton** get everything by pasting Jeff's HA Long-Lived Token once per device. Two low-severity items were *noted, deliberately not fixed*: the `/api/hours` POST is open (a stranger could push junk mower telemetry — "griefing only"), and the Nabu Casa URL is visible in client JS (inherent; HA still requires login).

#### f474d9b (2026-07-09 19:27 UTC) — "Domain live: loewenhome.com + www Active/SSL, serving app (HTTP 200 worldwide)"

Empty body; the CLAUDE.md diff records that `loewenhome.com` + `www.loewenhome.com` were attached to the `toro1` Cloudflare Pages project, both Active + SSL, HTTP 200 worldwide, "verified via 1.1.1.1 & 8.8.8.8 by the coworker." The app needed no code change (relative `/api/*` paths and relative manifest `start_url`); the old `toro1-5rz.pages.dev` still works. One transient noted: Jeff's AT&T gateway cached a stale AAAA-only record so home-WiFi lookups lagged — "self-clears / reboot gateway / set DNS to 1.1.1.1; doesn't affect outside visitors."

#### 9785381 (2026-07-09 23:48 UTC) — "Memory: record Beehive fixed-IP on AT&T gateway + LAN inventory; correct wrong 5GHz-off notes"

Empty body; big CLAUDE.md diff. Three things happened:

1. **Beehive pinned to a fixed IP.** Jeff — "first-timer, walked through it screen-by-screen" — set `192.168.1.66/homeassistant` to Private fixed via the BGW320-500's Home Network → IP Allocation flow (admin at `http://192.168.1.254`, Device Access Code on the sticker). Coworker verified: `.66` pings 0% loss, HA loads HTTP 200. The entry notes fixed IPs for Blink sync/cameras/Tuya plugs are NOT needed (cloud-connected) — only the Beehive mattered.
2. **Full LAN inventory captured by the coworker** (192.168.1.0/24): Beehive `.66` (84:39:be:20:0d:ad), Blink Sync `.214` (40:89:c6:19:55:b7), mower ESP32 `.195` (ESP_DBDE3B, a0:20:a6:0b:de:3b), Tuya plug#1 `.209` (TY_WR), beast `.194` (301Server); other ESP32s at `.210/.224/.196/.232` (purpose TBD), possible Sharky vacuum at `.228`. Also confirmed: "The AT&T box can't change its DNS resolver … per-device 1.1.1.1 or own-router-in-IP-Passthrough is the only real DNS fix."
3. **A memory correction, in bold:** "**CORRECTION (07-09): the BGW320-500 5GHz radio was NEVER turned off — Jeff confirmed it's been on the whole time. My earlier 'toggle 5GHz off/back on' notes were WRONG; do NOT tell Jeff to re-enable it.**" — a wrong note that had propagated through two commits (f010694, e7d9ef9) before being caught.

### 2026-07-10 — ActiveArmor cleared, the camera mockup layout, the no-subscription AI/theater plan, CodeProject.AI staged, the clip player, and Hart of the Hive

#### d2337b9 (2026-07-10 02:35 UTC) — "Memory: AT&T ActiveArmor check done - nothing paused/blocked"

Closes the last networking suspicion from 07-09: "AT&T ActiveArmor / Smart Home Manager check DONE (07-09): Jeff confirmed NOTHING is paused or blocked — so AT&T security is not interfering with any project device." Only remaining tiny item: a secure-HA bookmark (Nabu Casa https) so local HA stops showing "Not secure" (that warning "is just plain-http on LAN — harmless").

#### b704461 (2026-07-10 02:58 UTC) — "Cameras: mockup-style layout — house banner + status badge + big full-width tiles with overlaid labels"

Cameras got the premium look from Jeff's mockup: a banner built from `hero-home.jpg` (cropped to hide baked-in text) with a SECURITY-CAMERAS title and an "All Cameras Ready" badge, and camera tiles went from a small 2-column grid to full-width 16:9 with a green dot + name overlaid on the image.

#### dfaa88f (2026-07-10 03:59 UTC) — "Docs: capture camera-AI + home-theater plan (beast as AI/media brain, no subscriptions)"

Created `docs/home-theater-ai-plan.md`, a living doc that opens with Jeff's goals stated 2026-07-09, "**with a hard NO on subscriptions** (no Blink fee, no Zmodo fee, no per-month anything — he already pays for Claude/Clyde, Nabu Casa, and the domain)":

> 1. Cameras look premium in the app (mockup-style big tiles) — ✅ DONE.
> 2. Review saved Blink clips **inside the app**.
> 3. Alerts tell you **what** triggered them — person / car / animal / package.
> 4. Alert can **pop up on the TV**, glance, return to the show.
> 5. Whole thing feels like a **top-of-the-line home theater with HA driving it all** — seamless, no "cluster of shit," no constant resets.

Hardware roles were fixed here: **Beehive (Beelink J45, `.66`) — "weak Pentium, no GPU, runs the whole house. Keep it PURE HA. Do NOT put media/AI on it."** The **beast (`301Server`, `.194`) = "the designated AI + media brain"** — 6-core CPU, ~2 TB storage + 500 GB external SSD, Nvidia GPU then still "model TBD — run `nvidia-smi`; **Jeff thinks 'T750'**", runs 24/7, sits in the viewing room, also runs Clyde. Screens: Vizio TV + sound (hardwired, the beast's monitor), Apple TV in the bedroom, and a Fire TV Max Jeff owned but called "very slow." The doc's "Honest limits (tell Jeff, don't paper over)" section recorded: premium DRM apps can be resolution-capped on a PC; **Apple TV cannot do a clean overlay** (tvOS has no third-party overlay; Blink isn't proper HomeKit); Blink is snapshot/event-based, a small delay is inherent. Candidate detection routes were laid out (Ollama+LLM Vision if VRAM allows; CPU YOLO/CodeProject.AI if not; free-tier Gemini as ultra-simple fallback; blinkbridge+Frigate as "probably overkill").

#### 71a8cae (2026-07-10 04:02 UTC) — "Docs: confirm beast GPU (GTX 1050 Ti 4GB); settle on CodeProject.AI snapshot detection path"

`nvidia-smi` on 07-09 settled the GPU question: **Nvidia GeForce GTX 1050 Ti, 4 GB VRAM** (not the "T750" Jeff guessed), OS = Windows. Decision recorded in the plan doc: "**CodeProject.AI Server on Windows (uses the 1050 Ti CUDA)** does snapshot object-detection (person/car/animal — no RTSP needed, fits Blink); add **LLM Vision** (small local model on the 1050 Ti, or free Gemini tier) for package + rich scene descriptions. Frigate/blinkbridge NOT needed for this path."

#### 4cd8184 (2026-07-10 04:48 UTC) — "Docs: staged CodeProject.AI smart-camera detection setup for the beast (Clyde-ready)"

Created `docs/beehive/camera-ai-setup.md`, a 114-line staged runbook (Stages 0–5) for Clyde, written around one explicit principle: "**Do this in STAGES and verify each one before the next** (Jeff hates big-bangs that break)." Highlights: install CodeProject.AI on Windows, dashboard at `http://localhost:32168`, enable Object Detection (YOLO) in GPU/CUDA mode and verify with `nvidia-smi`; Stage 2 = **allow inbound TCP 32168 through Windows Firewall** and `curl http://192.168.1.194:32168/v1/status/ping` from the Beehive side (this stage prediction proved prophetic — see 76ae463); Stage 3 wires one camera via the `codeproject_ai_object` HACS platform with `scan_interval: 604800` ("effectively 'never auto-scan'; we scan on demand"); Stage 4 is the smart-alert automation (motion → `blink.trigger_camera` → 7s Blink upload delay → `image_processing.scan` → notify with detected object names). Honest caveats recorded: stock YOLO **does not know "package"**; battery warning — "don't add aggressive periodic scanning (drains camera batteries)."

#### 65e029b (2026-07-10 05:53 UTC) — "Docs: use confirmed notify target notify.mobile_app_jeffs_iphone"

One-line doc fix: the placeholder `notify.mobile_app_jeff` became the real, confirmed target `notify.mobile_app_jeffs_iphone`.

#### 489f458 (2026-07-10 06:39 UTC) — "Clip player: browse + play saved Blink clips in-app (no subscription)"

Goal 2 delivered app-side: an in-app clip browser listing saved Blink motion clips from Beehive's `/media/blink/` directory — CLIPS button in the camera toolbar, per-camera VIEW SAVED CLIPS filter, newest-first with parsed camera name + timestamp, tap-to-play by signing the media path via `/api/auth/sign_path` and streaming directly from Nabu Casa in a `<video>` element ("no proxy needed"). Handles empty state, missing folder, and connection errors.

#### 667eec4 (2026-07-10 08:21 UTC) — "Camera hero + Hart of the Hive status dashboard redesign"

Two visual landmarks: the camera hero swapped to `hero-cameras.jpg` ("Jeff's Blink security mockup"), and **Hart of the Hive** was born — a command-center panel on HOME with 8 uniform chips (Theme, Alexa, Cast, Hive, Accounts, Voice, Watch, Fitness) overlaid on "Jeff's sci-fi dashboard image." Orphan controls (theme toggle, cast, alexa, beehive dot, connected accounts, voice control, Apple Watch) migrated from the header and inline cards into the chips; the header was decluttered to badge + title + a tiny beehive status dot. Zero JS errors in Playwright. (Note: this is the first commit in the window co-authored by Claude Opus 4.6 — the cloud session's model changed mid-window.)

#### a27e0b7 (2026-07-10 14:55 UTC) — "Add AI object detection display to camera tiles (CodeProject.AI integration)"

App-side half of the AI plan, built ahead of the beast-side install: camera tiles show detection badges (label + icon + relative time, color-coded — blue=person, yellow=vehicle, green=animal, purple=package) from HA `image_processing` entities; detection data cached in localStorage for 24h; the camView panel shows AI detail rows (type, confidence %, time, other objects seen). "Gracefully shows nothing when no AI entities exist — auto-populates when Clyde wires HA."

#### 24df1fc (2026-07-10 19:07 UTC) — "Fix Windows stale cache + Hart chips on monitors + camera hero cleanup"

Service worker bumped to `hcc-v7` to force a cache purge on all devices ("Windows … stuck on old content"); images switched from cache-first-forever to stale-while-revalidate so new hero images actually update. Hart of the Hive got Jeff's new image version with system labels baked into the monitor screens, and the chips were repositioned from a flat grid to absolute positions overlaying the actual monitors — "looks like they're part of the command center displays." Camera hero aspect-ratio tightened from 1300/1330 to 1300/980 to crop blank tile areas. Zero JS errors across all 5 tabs, both themes.

### 2026-07-11 — CodeProject.AI goes live on the beast (with two real infra bugs), and the Fire TV war begins

All seven 07-11 commits are coworker/beast session work (Claude Sonnet 5, `-0500` timestamps).

#### 76ae463 (2026-07-11 03:11 -0500) — "Log CodeProject.AI camera detection completion in change log"

The body explains why this log entry arrived a day late: it "Records the 07-10 local-AI-camera work (verified live end-to-end) that was staged locally but never committed before this session pulled newer 07-09 entries from the cloud session" — a small instance of exactly the history-loss failure mode this whole record file exists to prevent. The CLAUDE.md entry it adds is enormous and central. Summary of its contents:

- **CodeProject.AI Server 2.9.5** installed on the beast at `D:\CodeProject\AI`, GPU-accelerated on the GTX 1050 Ti via CUDA, YOLOv5 6.2, ~150–700 ms per detection, Windows service set to auto-start on boot.
- **Infra bug #1:** "**Windows Firewall was silently blocking port 32168** from the LAN — the installer never added an inbound rule, so nothing outside the beast itself could reach it (Beehive's requests just timed out with no useful error); added `New-NetFirewallRule` for TCP 32168, fixed instantly."
- **Infra bug #2 — the bigger find:** "**`packages/hcc.yaml` was never actually loaded by HA at all** — `configuration.yaml` had no `homeassistant: packages: !include_dir_named packages` directive, so despite being valid YAML the whole file was silently ignored. **This is why `hcc_panic_button`/`hcc_mower_sensor_sync`/`hcc_freeze_warning`/`hcc_severe_weather_alert` showed 'unavailable' — they were ghost/restored entities that had never truly run, ever.** Added the missing directive → **all 6 automations (4 old + 2 new) came alive simultaneously.**"
- An **upstream bug patched** in the community `codeproject_ai_object` HACS component: `image_processing.py` referenced `cpai.CodeProjectAIServerException`, which the current `codeproject-ai-api` pip package renamed to `CodeProjectAIException` — "one-line `sed` fix, filed nowhere upstream yet."
- Three Blink cameras (301 Backyard/Driveway/Front Doorbell) wired to `192.168.1.194:32168` with `timeout: 60` ("Blink's cloud snapshot fetch is sometimes slow — expect alerts anywhere from a few seconds to ~1 min after real motion, not instant"), targets person/vehicle/animal at confidence 60, annotated snapshots saved to `/config/www/ai_snapshots/`.
- Three automations added to `packages/hcc.yaml`: `AI Camera Scan on Motion`, `AI Object Detected Notify` (person=critical push with sound, vehicle=normal, animal=passive/silent; attaches the annotated snapshot; fires a custom `hcc_ai_detection` event for the app; gated by a per-camera mute check), and `AI Notify Mute Action` (the "🔇 Mute 15 min" push-notification action button), backed by three `input_datetime.hcc_ai_mute_*` helpers.
- **Verified fully live:** forced `binary_sensor.301_driveway_motion` on twice → "both times detected real cars (up to 85.9% confidence) → notification fired → **Jeff confirmed on his phone** both the push notification AND the attached annotated photo arrived correctly."
- Known gaps recorded: only Jeff's iPhone is registered for notify (Angela/Braxton need the Companion App); no package detection (stock YOLO has no "package" class — "would need a custom-trained model, a separate project"); ROI/zone filtering supported but "intentionally not configured yet — no known false-positive zone to calibrate against, would be guessing at coordinates blind."

#### c926ceb (2026-07-11 13:16 -0500) — "Log Fire TV/HA pairing, alexa_media_player setup, loewenhome.com audit, and a found (unfixed) desktop layout bug"

Four separate stories in one coworker session:

1. **Fire TV paired to Beehive.** Jeff set up a new Fire TV Stick (model AFTKRT, `192.168.1.215`, MAC 20:BE:B8:3A:8C:5D) in the viewing room. First blocker: the beast couldn't reach it at all (`adb connect` → WSAEACCES) — root cause was "**ProtonVPN's WireGuard tunnel on the beast was blocking outbound LAN traffic**, same root-cause class as the earlier 'Beehive Offline' `ERR_NETWORK_ACCESS_DENIED` issue." Jeff disabled ProtonVPN → ADB connected immediately. Developer Options + ADB debugging enabled on the stick (the 7-taps unlock), HA's Android Debug Bridge integration added → device live as "Fire TV - Viewing Room."
2. **A decision on how pop-ups work:** a native Alexa Routine (Blink motion → "Show Camera") was **considered and rejected** "because it bypasses HA entirely — it can't reuse the existing person/vehicle AI classification or the per-camera mute helper, and (per Jeff's ask) can't be suppressed when a known family member's phone is already home." Chosen instead: route through HA via the `alexa_media_player` HACS integration (v5.15.6 installed; HA restarted via the HACS repair card — "the generic Developer Tools restart button was unreliable/needed several attempts"). Left pending on-screen for Jeff: the Amazon login incl. the 52-character TOTP authenticator key — "Claude does not handle credentials."
3. **The "stale version" complaint audited:** `loewenhome.com` vs `toro1-5rz.pages.dev` fetched fresh — "**100% identical, 532,663 chars each**," all 5 tabs clean on both, all APIs 200. "Root cause of what Jeff was actually seeing: a stale `service-worker.js` cached locally in his Chrome profile … Not a server/deploy bug."
4. **A real bug FOUND, deliberately not fixed** (app code is the cloud session's to touch, per Rule 13): the **desktop-wide-browser layout gap** — on any window wider than ~700–900 px, hero sections leave "a large dead black area to the right." Root cause fully diagnosed from CSS: `.sec-hero-*` classes set `aspect-ratio` + `max-height:460px` with no width and no centered max-width shell, so once the height cap is hit "CSS derives the box's **width from the aspect ratio** (~700px) instead of the viewport." Confirmed "via direct CSS read, not just visual," identical on both domains. Filed as Pending #10 with two candidate fixes.

#### 987e804 (2026-07-11 13:58 -0500) — "Log confirmed-working Fire TV camera pop-up mechanism + File Editor add-on fragility"

Jeff completed the Amazon login himself; the real entity `media_player.jeffrey_s_fire_tv` appeared. The mechanism was live-tested: "calling `media_player.play_media` with `media_content_type: custom` and `media_content_id: 'show me the driveway camera'` … makes the Fire TV pop up that camera's live view immediately, exactly like speaking the command to it" — confirmed by Jeff on the physical screen, exact YAML captured for the next session. It was **not** applied to `packages/hcc.yaml` yet because of a near-disaster: "the legacy File Editor add-on mishandled a special keypress (typed literal 'Page_Up' text into the file), caught and undone before saving." Also recorded: `AI Object Detected Notify` cannot be edited in HA's Automations UI because it's package-defined — the UI only offers a "Migrate" button, flagged "do NOT click this without asking Jeff." Recommendation: Studio Code Server add-on for future package edits.

#### a88ccc6 (2026-07-11 14:38 -0500) — "Fire TV motion pop-up alerts: built, deployed, and confirmed working end-to-end"

The automation "AI Show Camera on Fire TV" was added to `packages/hcc.yaml` — but only after **two separate editor corruptions in one session**: "a literal 'Page_Down' keystroke and a Prettier format-on-save truncation — were caught via `ha core check` and fixed precisely with sed/heredoc." The CLAUDE.md write-up elaborates: even the recommended Studio Code Server's "format on save (Prettier) reformatted the entire file on Ctrl+S and truncated/corrupted the new automation's long single-line flow-YAML." Lesson recorded: "use the Terminal add-on directly, or if using Studio Code Server, disable format-on-save first … this file's dense flow-style YAML doesn't survive Prettier's reformatting." The automation triggers on the `codeproject_ai.object_detected` event, fires only for person/vehicle, is gated by the same mute template as phone notifications (so "🔇 Mute 15 min" silences both), and was verified: "a simulated codeproject_ai.object_detected event correctly fired both the phone push notification and the Fire TV camera pop-up." **This "confirmed working end-to-end" claim would be partially disproven on 07-14** — see b108a6e.

#### 049ad6d (2026-07-11 15:19 -0500) — "Add arrival-suppression automation, Angela's HA account, and an 'almost home' alert"

Built entirely via the Terminal add-on ("avoiding the editor corruption issues from earlier in the session" — lesson applied, zero corruption this round): **"AI Arrival Suppression"** (Jeff or Angela arriving `not_home`→`home` loops over all monitored cameras and sets each mute helper 10 minutes out — family walking in the door doesn't trigger alerts); **Angela's own HA account** (`angela301`, "password set by Jeff not Claude") plus `person.angela_loewen`, with a walkthrough for the HA Companion App and her own Long-Lived Token for the HCC app; and a bonus **"Angela Almost Home"** automation — a 10-mile passive zone (`zone.almost_home`) that notifies Jeff's iPhone when Angela enters it, "a heads-up ~10 min before she arrives" (not yet verified live — she hadn't set up the Companion App that session). Ten automations total, all confirmed loaded via `ha core check` + restart. Noted for later: Braxton has no User/Person entity yet.

#### 4b19147 (2026-07-11 15:32 -0500) — "Expand AI camera detection from 3 to all 6 Blink cameras, confirmed working"

Jeff asked why only 3 of his 6 cameras were AI-monitored ("no documented reason found, looks like they just weren't included in the original 07-10 CodeProject.AI setup"). `front_right`, `back_left`, and `garage` were added — image_processing entries, mute helpers, and the motion-scan automation's trigger list + Jinja lookup map, all via "precise sed substitutions on the existing flow-style YAML." The notify and Fire-TV-popup automations needed **no changes** "since they already derive camera identity generically." Verified live with a simulated garage detection: "notification and TV popup both fired correctly for garage only," nothing crossed over. All 6 cameras now uniformly covered by scan, notify, mute, TV pop-up, and arrival suppression.

#### 6c26465 (2026-07-11 16:01 -0500) — "Fix Siri Announce Notifications not reading AI camera alerts aloud"

Two real causes, both documented:

> (1) vehicle/animal push notifications were sent at interruption levels (active/passive) that iOS's Announce Notifications on Speaker ignores by default, and separately (2) Jeff was testing with his phone unlocked (the feature only fires when locked with screen off, by Apple's design).

Vehicle and animal detections were bumped to `time-sensitive` via two sed substitutions ("both were confirmed unique matches in the file before replacing"). Confirmed working live for both object types once tested with the phone actually locked. One recorded tradeoff: animal alerts were originally passive/silent by design ("original intent was 'don't bug me about the cat'"); making them announce-eligible "is an explicit tradeoff he asked for this session, not an oversight."

### 2026-07-12 — The splash screen, wide-screen layout, and the Kodi decision (cloud session)

#### 05dc1db (2026-07-12 13:47 UTC) — "Splash landing screen + media center docs"

A full-viewport splash landing screen using Jeff's portrait/landscape house-at-dusk photos (portrait on phones <768px, landscape on tablet/web/TV), tap-anywhere-to-enter, sessionStorage skip on same-session reload, a live status bar (Security/Network/Power/Climate) from HA when connected, service worker bumped to `hcc-v8`. The same commit created `docs/beehive/media-center-setup.md` (a 169-line Kodi setup guide) and recorded a decision in the home-theater plan that would come back to bite on 07-14:

> **Phase 2 — DECIDED (07-10, Jeff's call): beast-as-media-center via Kodi.** Beast runs **Kodi** (free media center) → HDMI → Vizio TV. HA sends camera alerts to Kodi via `kodi.call_method` → `GUI.ShowNotification` → toast overlay on the TV screen, fades after 8s. Fire TV Stick stays as secondary HDMI input for 4K DRM streaming. **NOT** simple ADB from Beehive to Fire TV — Jeff wants it routed through the beast.

Note the tension already latent in the record: the 07-11 beast session had *already built* the Fire TV/Alexa route the day before this Kodi decision text was committed. The contradiction sat unnoticed until the 07-14 audit (c13f101).

#### a1a6d7a (2026-07-12 13:52 UTC) — "Responsive wide-screen layout for TV, web, tablet, Fire TV"

Wide-screen scaling pass: a max-width container (1400→1800→2200 px) centered on wide screens; heroes scale up (max-height 560→680→800→1000 px by breakpoint); header, nav tabs, cards, buttons, spec rows all scale at 768/1200/1920/2560 px; nav tabs centered with wider spacing on desktop/TV; "Phone layout unchanged (no regression)"; zero JS errors across phone/tablet/web/1080p/4K viewports. **INFERRED:** this was almost certainly a response to the desktop-layout gap the coworker filed the day before (c926ceb) — but it did **not** close that bug. The tip CLAUDE.md (Pending item 7) shows "~~Desktop-wide-browser layout gap~~ — **FIXED 08-06**": the specific hero-width-collapse defect survived this pass and wasn't truly root-caused and fixed (with `width:100%` on all seven hero classes) until 2026-08-06, outside this window.

#### dfb0460 (2026-07-12 18:48 UTC) — "Fix splash portrait mobile: cover with gradient to hide baked-in status bar"

Portrait splash on phones switched to `cover` with center-20% positioning (no dark gap), with the overlay gradient going fully opaque by 88% "to cleanly hide the baked-in status bar in the image" — the source photo had an iPhone status bar baked into it.

#### c236731 (2026-07-12 21:07 UTC) — "Rebuild splash screen: clean photos + HTML gold metallic text overlays"

The proper fix for the baked-in-text problem: the splash images were replaced with clean house photos (no text at all), and all title/subtitle/tagline/ENTER text became HTML with a CSS gold metallic gradient (`background-clip:text`) "so text scales on any screen and never crops." Verified on iPhone SE, iPhone 14 Pro, Jeff's iPhone (440×956), and desktop — zero JS errors.

### 2026-07-14 — Reckoning day: the three-day silent outage, the Alexa dead end, and the pop-up ACTUALLY fixed

Both streams worked this day, interleaved via three merges.

#### 406e8b0 (2026-07-14 12:08 UTC) — "Apply Clyde typography spec + fix splash temp from real weather station"

The splash typography was rebuilt to a spec authored by Clyde (the beast's Claude): Cinzel Display SemiBold from Google Fonts, a champagne metallic gradient (#FFF2C9→#5A3716), warm glow, thin dark outline, bevel via a drop-shadow chain, subtitle tracking 0.35em. Also a real bug fix: the splash temp "was showing '--°F' because splashStatus() only checked HA climate.* entities" — it now also fetches `/api/weather` (the KTNWHITE21 backyard station, no token needed) in parallel, "so real backyard temp shows for everyone on load."

#### 5957758 (2026-07-14 17:02 UTC) — "HOME layout redesign: hero above nav, remove header, reorder content"

Structural redesign of every page: the "HCC · HOME COMMAND CENTER" header bar removed entirely; heroes moved above the nav bar (hero → nav → content everywhere); Quick Access shortcut buttons (Yard/Irrigation) removed; Safety & Emergency content moved from HOME to GUARDIAN; Hart of the Hive moved to the bottom of HOME; HOME reordered Dispatch → Cameras → Utilities → Beehive → Hart of Hive. Rationale noted for the removed emergency section: "911 on dispatch already fires hccPanic() — no separate emergency section needed."

#### 57476b6 (2026-07-14 12:14 -0500) — merge

Merge of the cloud stream (5957758) into the beast checkout (parent 6c26465) — the beast session pulling the cloud session's latest before starting its audit.

#### d9ca3d3 (2026-07-14 17:38 UTC) — "Fix 3 iPhone bugs: hero safe-area padding, move Home Status to Guardian, reposition Beehive chip"

Post-redesign iPhone fixes reported from the device: `env(safe-area-inset-top)` padding on `.hero-slot.active` so heroes don't bleed behind the iOS status bar; `#homeStatusCard` moved from HOME to GUARDIAN "where it belongs"; the `.hive-chip.hc-hive` chip repositioned from top:27% to top:65% — the commit quotes the placement instruction as "just under the chair" (**INFERRED:** Jeff's phrasing, given the quote marks).

#### e70a5ac (2026-07-14 17:40 UTC) — "Align Hart of Hive chips to match reference: HIVE center, FITNESS on chair"

Two minutes later, a correction of the previous commit against Jeff's reference image: HIVE back to top:27% (the center hexagonal area), FITNESS to top:72% (chair/desk area).

#### c13f101 (2026-07-14 12:45 -0500) — "Audit: root-cause camera/Fire TV alert outage (CodeProject.AI service down 3 days), resolve TV-alert plan conflict"

The pivotal audit of the window, triggered by Jeff reporting cameras + Fire TV "not working as intended" **despite the 07-11 changelog saying both were confirmed working end-to-end.** Findings, from the commit body and the huge CLAUDE.md entry:

1. **The AI pipeline had been silently dead for exactly three days.** "The **CodeProject.AI Server Windows service was STOPPED** — even though it's set to 'Automatic' startup, it did NOT come back up after the beast's last reboot (07-11, 6:08 AM)." The proof is forensic and elegant: in HA's Automations list, "AI Camera Scan on Motion" had kept firing normally (most recently 32 minutes before the check) — so motion sensing and the scan trigger were fine — but "AI Object Detected Notify" and "AI Show Camera on Fire TV" both showed "Last triggered: 3 days ago." The scan step had been silently failing to reach CodeProject.AI (port not listening) since the reboot, so no object was ever detected, so nothing downstream ever fired. Immediate fix: `Start-Service`, confirmed `Running` and answering on `:32168` (the 07-10 firewall rule was still correct). Hardening (per the commit subject, applied with Jeff's OK): "delayed-start + failure-recovery actions so it doesn't die silently again."
2. **The plan conflict surfaced:** `docs/home-theater-ai-plan.md` Phase 2 recorded Jeff explicitly choosing (07-10) Kodi-on-the-beast for TV alerts — "**NOT simple ADB from Beehive to Fire TV — Jeff wants it routed through the beast**" — yet "what actually got built and marked 'confirmed working end-to-end' the very next day (07-11) is exactly that rejected approach." Kodi itself "was installed on the beast but only ever launched once, for about 3 minutes, then never touched again (its own log confirms this; `C:\Users\jeffl\AppData\Roaming\Kodi\kodi.log` starts and ends 07-11 6:11–6:14 AM)"; its web server was still disabled and no Kodi integration was ever added to HA. **Jeff's call, made this day:** keep the Fire TV/Alexa path now that it's unblocked; the plan doc was updated to mark Kodi "SUPERSEDED … leave it installed/unused rather than build it out further unless priorities change," and `docs/beehive/media-center-setup.md` demoted to "reference/superseded, not a live setup guide."
3. **Unconfirmed third finding:** the Fire TV's screen was fully asleep when checked over ADB (`Display Power: state=OFF`) — flagged for Jeff to physically confirm a real detection wakes the screen, "the one thing only he can observe in the room."
4. **Camera base pipeline confirmed healthy:** `camera.301_backyard` was updating in real time with a real current backyard photo — "the AI-detection layer on top of it was what broke," not Blink→HA.
5. **Minor items noticed, not chased:** an HA "Login attempt failed — invalid authentication from localhost (127.0.0.1)" notification ~42 minutes before the check, and "a '🚨 EMERGENCY ALERT — HCC Panic triggered at 05:50 PM' logged 3 days ago (same day as the rest of the 07-11 work — almost certainly a test/accidental trigger during that session, not a real event, but flagging since panic firing is significant)."
6. **History-loss near-miss:** "this beast had **8 local commits sitting unpushed** (pure CLAUDE.md/changelog + HA-side documentation from prior coworker sessions, no app-code) that merged cleanly with the cloud session's latest push via `git pull` — still unpushed pending Jeff's OK."

#### b1022d9 (2026-07-14 12:59 -0500) — merge

The beast session merging the cloud stream's latest (e70a5ac) into its line after the audit commit.

#### b108a6e (2026-07-14 13:36 -0500) — "Live-test Fire TV pop-up twice with AI pipeline healthy: still broken, root cause narrowed"

With CodeProject.AI back up, the 07-11 claim was put to the test — and half of it failed. Two live end-to-end tests (real motion sensor forced via HA's API, real `image_processing` detection of `{"car": 2}` both times, verified via HA's own automation traces showing the exact `play_media` call dispatched with no error): "**Both times: phone push arrived (confirmed by Jeff), TV pop-up did not appear** (Jeff was on the correct HDMI input the second time)." Two hypotheses were killed with direct evidence: the cameras ARE linked to Alexa (Jeff checked the Alexa app), and the phrase IS right — "Jeff spoke the exact same phrase, **'Alexa, show me the 301 driveway camera,' out loud and it correctly pulled up the camera on the Fire TV**." Conclusion:

> `alexa_media_player`'s synthetic `media_player.play_media` / `media_content_type: custom` trick is not equivalent to a real spoken command for this camera-show use case — a real voice command lets Alexa's cloud intelligently pick/cast to a screen, but injecting the command directly at the Fire TV's own media_player entity apparently doesn't trigger the same cross-device display behavior.

The commit explicitly corrected the record: "Corrected the 07-11 changelog's 'confirmed working end-to-end' claim, which this disproves for the TV-pop-up half (phone push half held up)" — and explicitly declined to guess at a fix: "deliberately not guessing at a live YAML change without being able to verify it (that's exactly the 'declared done without testing' pattern this whole audit was trying to undo)." One incidental lesson recorded for future beast automation: Windows UIPI blocks synthetic mouse/keyboard input from an elevated terminal into a non-elevated app window — "don't run the terminal elevated if GUI-clicking a normal app is needed."

#### 14222bd (2026-07-14 19:17 UTC) — "Hart of Hive: match Jeff's reference layout + add Open Beehive under chair"

Cloud-side, in parallel: chips repositioned again to Jeff's reference (CAST/WATCH to mid-monitor 48%, HIVE centered at 30%, FITNESS at 68%), plus a new OPEN BEEHIVE chip at top:88% ("under the chair") whose status dot syncs green/yellow/red with the Beehive connection state.

#### 25e3256 (2026-07-14 14:46 -0500) — "Fire TV pop-up actually fixed: ADB browser launch instead of Alexa"

The war ends, same afternoon, with a third mechanism nobody had planned. From the body:

> Alexa's synthetic show-camera command was a confirmed dead end (Amazon doesn't honor it for Fire TV the way it does a real spoken command), and Blink's official Fire TV app is incompatible with this device model. Found a third path that bypasses both: androidtv.adb_command opens the camera's live entity_picture URL directly in the Fire TV's browser, with a delayed HOME keypress to cleanly return afterward. Verified end-to-end live twice through the real automation, not simulated.

The CLAUDE.md entry adds the detail: the Blink Fire TV app dead end was confirmed "via the Amazon Appstore listing's own compatibility check, red ✕ next to 'Jeffrey's Fire TV'" (no sideloading pursued). The rewritten automation: (1) `androidtv.adb_command` runs `am start -a android.intent.action.VIEW -d "http://192.168.1.66:8123{{ state_attr('camera.' ~ camera_key, 'entity_picture') }}"` — the signed live snapshot URL opens fullscreen in the Fire TV's built-in Amazon Silk browser; (2) 8-second delay; (3) a second `adb_command` sends `input keyevent 3` (HOME) to return cleanly to the launcher — chosen over BACK (`keyevent 4`) because ADB showed a single Back press left `mResumedActivity` still in the browser, while HOME "lands on `com.amazon.tv.launcher` every time." The proof was direct: "proved this by screenshotting the Fire TV directly over ADB (not guessing) and seeing the real driveway photo on-screen, then again with Jeff physically watching the correct HDMI input." The recorded UX tradeoff, accepted by design: "this takes over the whole screen like switching apps, not a small toast overlay layered over what's playing (that would've needed the original Kodi plan) — acceptable tradeoff since it actually works and Kodi was explicitly declined." One more editor near-miss logged: Studio Code Server's Monaco editor "had a focus-stealing UI glitch mid-session that nearly caused a bad edit, caught via `hass.callApi('POST','config/core/check_config')` before it reached disk." Standing instruction added to CLAUDE.md: "**do not revert to the `media_player.play_media`/Alexa custom-command approach, it's a confirmed dead end for this device.**"

#### ef20ec5 (2026-07-14 14:46 -0500) — merge

Beast session merging the cloud stream's 14222bd into its line immediately after the fix commit.

#### 8efc3c4 (2026-07-14 23:23 UTC) — "Fix token UX: add inline token input to beehiveCard so Jeff can connect without hunting"

The window closes on a usability fix for Jeff himself: "The token input was hidden in the cameras section and only visible after Beehive responded — Jeff couldn't find it, especially on iPad where OPEN BEEHIVE just navigated away." Now, when no token is set, the beehiveCard itself shows a gold "Paste Your Token to Connect" box with the input field right there, and the OPEN BEEHIVE button stays hidden until connected. Service worker bumped to `hcc-v9`.

### Decisions made or rejected in this period

- **REJECTED / DEAD END — SYLVANIA plugs in HA** (f010694, 07-07): firmware-locked to the SYLVANIA app; Smart Life rejects them in both pairing modes. Never-again rule in CLAUDE.md: "do NOT re-attempt the Tuya path" for these. They stay on the SYLVANIA app + Alexa. Replacement path decided: Kasa KP125/EP25 (WiFi) now, or Zigbee plugs (Sonoff S40 ZB / ThirdReality) later. No prices recorded.
- **DECIDED — pairing playbook for future Tuya plugs** (e7d9ef9 / CLAUDE.md 07-07): Smart Life app, AP Mode/slow-blink (fast-blink "is flaky"), QR scanned with Smart Life's in-app scanner (not the iPhone camera, not the SYLVANIA app), QR is one-time-use.
- **DECIDED — Mandatory Rule 13, coworker delegation** (bec7440, 07-09, "Jeff's rule 2026-07-09"): the cloud session must proactively flag tasks better done by the local coworker; extended by 9a34d17 with the coordination protocol — coworker treats app code as READ-ONLY; cloud session owns all app-code edits/commits/pushes.
- **DECIDED — don't blind-swap links** (fc62533, 07-09): verify before replacing; the coworker check proved all five "at-risk" parts links were actually live.
- **NEVER-AGAIN RULE — no custom Blink override** (9b29c1f/7bbc8a2, 07-09): "DO NOT ever re-add a `custom_components/blink` override — that override shadowing the fixed built-in was the entire bug." The repo's `beehive/blink/` files declared dead artifacts.
- **DECIDED — public-share access model** (80799e7, 07-09): public visitors get a view-only demo shell; family (Angela, Jeff's dad, Braxton) gets full access by pasting Jeff's HA token once per device; panic gated behind the token. Two low-severity holes consciously accepted: open `/api/hours` POST ("griefing only") and the visible Nabu Casa URL.
- **DECIDED — hard NO on subscriptions** (dfaa88f, 07-10, Jeff's goals of 07-09): "no Blink fee, no Zmodo fee, no per-month anything — he already pays for Claude/Clyde, Nabu Casa, and the domain." Every camera/AI/theater feature in this window was built within that constraint.
- **DECIDED — hardware roles** (dfaa88f, 07-10): Beehive stays PURE HA ("Do NOT put media/AI on it"); the beast (`301Server`, `.194`) is the AI + media brain. Jeff's GPU guess ("T750") corrected by `nvidia-smi` to a GTX 1050 Ti 4 GB (71a8cae).
- **DECIDED — CodeProject.AI snapshot detection path** (71a8cae, 07-10): CodeProject.AI Server on Windows with the 1050 Ti CUDA; LLM Vision / free Gemini tier later for package + scene descriptions. **REJECTED as unnecessary:** Frigate + blinkbridge ("probably overkill" for snapshot-based Blink).
- **REJECTED — native Alexa Routine for TV pop-ups** (c926ceb, 07-11): zero-HA-work first-party option turned down "because it bypasses HA entirely" — no AI classification reuse, no mute helper, no family-arrival suppression.
- **DECIDED then SUPERSEDED — Kodi as the TV-alert route** (05dc1db recording "DECIDED 07-10, Jeff's call … NOT simple ADB from Beehive to Fire TV — Jeff wants it routed through the beast"; reversed in c13f101, 07-14): when the audit exposed that the 07-11 build contradicted this, "Jeff chose to keep the Fire TV/Alexa path now that it's unblocked." Kodi left installed but unused (it had run exactly once, for ~3 minutes).
- **DEAD END ×2, then DECIDED — the Fire TV pop-up mechanism** (b108a6e, 25e3256, 07-14): (1) `alexa_media_player`'s synthetic custom command — confirmed dead end ("Amazon doesn't honor it for Fire TV the way it does a real spoken command"); (2) Blink's official Fire TV app — incompatible with this stick model per the Appstore's own check, no sideloading; (3) chosen and shipped: `androidtv.adb_command` opening the signed `entity_picture` URL in the Silk browser + delayed HOME keypress. Standing rule: "do not revert to the `media_player.play_media`/Alexa custom-command approach."
- **DECIDED — Terminal add-on is the method for editing `packages/hcc.yaml`** (a88ccc6/4b19147, 07-11): after two editor corruptions, "use the Terminal add-on directly, or if using Studio Code Server, disable format-on-save first"; legacy File Editor "should be avoided entirely."
- **DECIDED — animal alerts promoted to time-sensitive** (6c26465, 07-11): an explicit tradeoff Jeff asked for so Siri Announce reads them aloud, reversing the original "don't bug me about the cat" silent design — "not an oversight."
- **DECIDED, consciously deferred** (76ae463, 07-11): no ROI/zone filtering yet ("would be guessing at coordinates blind"); no package detection (stock YOLO has no "package" class; a custom model is "a separate project"); the HA Automations UI "Migrate" button on package-defined automations is not to be clicked without asking Jeff (987e804).
- **DECIDED — accept the fullscreen pop-up tradeoff** (25e3256, 07-14): the ADB/browser route takes over the whole screen rather than showing a toast overlay; accepted "since it actually works and Kodi was explicitly declined."

### Problems, failures & root causes in this period

- **SYLVANIA plugs won't pair with anything but their own app.** Symptom: Smart Life "device not supported" in both EZ and AP modes with every setting correct. Wrong avenues considered: LocalTuya/flashing (need the same blocked access). Root cause: firmware lock to the SYLVANIA app, corroborated by forum consensus. Fix: none possible — recorded as a dead end (f010694) and routed around with the JH-G01U generic plug (e7d9ef9).
- **False memory: the 5GHz radio.** The 07-07 notes said the BGW320-500's 5GHz radio had been turned off during pairing and needed re-enabling; Jeff confirmed on 07-09 "it's been on the whole time." Root cause: the assistant's notes were wrong. Fix: bolded correction in CLAUDE.md — "do NOT tell Jeff to re-enable it" (9785381).
- **Guardian crying wolf.** Symptom: "⚠️ ATTENTION" with no stated reason. Root cause: a single offline device (43/44) tripped the banner. Fix: reasons displayed, >15%-offline threshold, "0 HOME" made neutral (a27982a).
- **Blink cameras broken for weeks — the wrong theory, then the right one.** Symptom: "Login failed," no SMS PIN, `ConfigEntryNotReady` looping. Wrong attempt: the 07-03 custom `config_flow.py` override built on the "empty_cookies / dedicated session" theory — admitted in CLAUDE.md as "a wrong guess." Real root cause: Blink's 2FA started returning HTTP 202, old blinkpy read 202 as success so the PIN never appeared; fixed upstream in blinkpy 0.25.6 / HA 2026.6.4 — at which point **our own override became the bug** by shadowing the fixed built-in (the failing traceback pointed at OUR `coordinator.py:58`). Side effect: the override "hammered Blink's login every ~10s for days," risking a rate-limit. Fix: delete the override, delete the entry, re-add the built-in (9b29c1f → confirmed live in 7bbc8a2).
- **Camera stills showed icons instead of images.** Root cause (likely, as recorded): the old `fetch().blob()` image load was CORS-gated. Fix: load HA's signed `entity_picture` URL directly in an `<img>` from the https Nabu Casa host (5ddac8a).
- **Anonymous visitors could fire the real panic alarm.** Root cause: the panic webhook was the one unauthenticated call in a publicly shareable app. Fix: `hccPanic()` requires the stored token and posts via the same-origin proxy (80799e7).
- **`loewenhome.com` slow to resolve on home WiFi.** Root cause: the AT&T gateway cached a stale AAAA-only record, and the gateway's DNS resolver cannot be changed. Fix/mitigation: wait/reboot/per-device 1.1.1.1; outside visitors unaffected (f474d9b, 9785381).
- **CodeProject.AI unreachable from the LAN.** Symptom: Beehive's requests "just timed out with no useful error." Root cause: the installer never added a Windows Firewall inbound rule for TCP 32168. Fix: `New-NetFirewallRule`, "fixed instantly" (76ae463).
- **Four "automations" had never run — ever.** Symptom: `hcc_panic_button`, `hcc_mower_sensor_sync`, `hcc_freeze_warning`, `hcc_severe_weather_alert` showed "unavailable." Root cause: `configuration.yaml` lacked the `homeassistant: packages: !include_dir_named packages` directive, so `packages/hcc.yaml` — valid YAML — "was silently ignored" and those entities were "ghost/restored entities that had never truly run, ever." Fix: add the directive; "all 6 automations (4 old + 2 new) came alive simultaneously" (76ae463).
- **Upstream bug in the `codeproject_ai_object` HACS component.** Root cause: the component referenced `cpai.CodeProjectAIServerException`, renamed to `CodeProjectAIException` in the current pip package. Fix: one-line sed patch, "filed nowhere upstream yet" (76ae463).
- **The beast couldn't reach the Fire TV over ADB.** Symptom: `adb connect` → WSAEACCES. Root cause: ProtonVPN's WireGuard tunnel blocking outbound LAN traffic — "same root-cause class as the earlier 'Beehive Offline' `ERR_NETWORK_ACCESS_DENIED` issue." Fix: disable ProtonVPN; connected immediately (c926ceb).
- **Jeff saw a "stale version" of the app.** Wrong hypothesis implied by the report: a deploy/domain mismatch. Audit: both domains byte-identical (532,663 chars), zero console errors. Real root cause: a stale `service-worker.js` cached in his Chrome profile. Fix: clear-site-data steps for him; separately the SW strategy was hardened app-side in 24df1fc (c926ceb).
- **Desktop-wide-browser layout gap — found, not fixed here.** Symptom: dead black area right of the heroes on windows wider than ~700–900 px. Root cause (fully diagnosed 07-11 by the coworker): `aspect-ratio` + `max-height:460px` with no width and no centered shell — CSS derives width from the aspect ratio once the height cap binds. The 07-12 responsive pass (a1a6d7a) reworked wide-screen scaling but the tip CLAUDE.md shows this specific defect wasn't closed until **08-06** (fix: `width:100%` on all seven hero classes) — a found-but-lingering bug (c926ceb; resolution outside this window).
- **HA GUI editors corrupted `packages/hcc.yaml` — three near-misses in four days.** (1) Legacy File Editor typed a literal "Page_Up"/"Page_Down" keystroke as text into the file (caught before/after save, undone) (987e804, a88ccc6); (2) Studio Code Server's Prettier format-on-save "truncated/corrupted the new automation's long single-line flow-YAML," caught via `ha core check` (a88ccc6); (3) Studio Code Server's Monaco editor's focus-stealing glitch nearly caused a bad edit on 07-14, caught via `check_config` before it reached disk (25e3256). Root cause: dense flow-style YAML + fragile browser editors. Fix: the Terminal add-on with `sed`/heredoc became the established method.
- **Siri Announce read nothing aloud.** Two real root causes: vehicle/animal pushes used `interruption-level: active`/`passive` (Announce only reads time-sensitive/critical by default), and Jeff was testing with the phone unlocked (Announce only fires locked with screen off, by Apple's design — "confirmed via research, not a bug"). Fix: sed both levels to `time-sensitive`; test with the phone locked; confirmed live for both object types (6c26465).
- **The AI pipeline died silently for three days — and nobody noticed.** Symptom (Jeff, 07-14): cameras + Fire TV "not working as intended." Root cause: the CodeProject.AI Windows service, though set to "Automatic," did not survive the beast's 07-11 6:08 AM reboot; the scan automation kept firing and silently failing, so downstream notify/TV automations showed "Last triggered: 3 days ago." Fix: `Start-Service` immediately, then delayed-start + failure-recovery hardening "so it doesn't die silently again" (c13f101). The deeper failure this audit named: features declared "confirmed working end-to-end" with no ongoing verification — the audit set out "to undo" the "declared done without testing" pattern.
- **The Fire TV pop-up "confirmed working end-to-end" — except it wasn't.** The 07-11 verification (a88ccc6) had passed with a manual service call and a simulated event, but with the pipeline healthy on 07-14 the TV half failed twice in genuine end-to-end tests (phone push half held up). Ruled out with evidence: Alexa camera linking (checked in the app), the phrase (spoken aloud, it worked). Real root cause: Amazon's cloud doesn't honor the synthetic `play_media`/custom command for cross-device camera display the way it honors a real voice command (b108a6e). Second dead end: the official Blink Fire TV app is incompatible with this stick model (Appstore red ✕). Actual fix: bypass Alexa entirely — `androidtv.adb_command` opens the camera's signed live snapshot URL in the Silk browser, 8s delay, HOME keypress to exit (BACK was proven insufficient via ADB inspection). Verified live twice through the real automation, screenshot-proofed over ADB, and confirmed by Jeff watching the TV (25e3256).
- **Jeff couldn't find the token input.** Symptom: on iPad, OPEN BEEHIVE just navigated away; the token field lived hidden in the cameras section and only appeared after Beehive responded. Fix: inline gold "Paste Your Token to Connect" box on the beehiveCard itself, OPEN BEEHIVE hidden until connected (8efc3c4).
- **History-loss near-misses within the record itself:** the 07-10 CodeProject.AI completion write-up was "staged locally but never committed" until 76ae463 rescued it a day late, and the 07-14 audit found **8 unpushed local commits** sitting on the beast (c13f101). The record is silent on when those 8 were finally pushed; **INFERRED:** the merge commits of 07-14 (57476b6, b1022d9, ef20ec5) represent that reconciliation reaching the shared branch.
