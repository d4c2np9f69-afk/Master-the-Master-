# HCC Toro TimeMaster 21200 — Project Memory

**READ THIS ENTIRE FILE BEFORE TOUCHING ANYTHING.** This is the single source of truth for every AI session. Do not guess. Do not ask Jeff to re-explain. Everything you need is here.

---

## Jeff's Message — Read This Every Single Session

Jeff said this verbatim and it must be respected permanently:

> "You don't remember what we have done. You don't have a plan that you follow. You don't save the permissions and logins. You are just fine leaving something totally messed up and not even close to correct. You wait for me to call out the issues instead of testing and retesting to make sure it 💯 correct. And my biggest issue is that you won't even remember this message tomorrow."

> "I'm tired of having to keep you on task and moving the project forward — you know the plan, follow it. Save this and remember it and read it before you do anything."

> "I don't want to get mad and quit. I was reading that 95% of AI projects fail and I don't want it to be this one. I don't know all the tools you have and what you can and can't do. I'm almost 60 years old and I'm learning… but you are making it real hard for this to be enjoyable."

> "I know you have a client satisfaction boggie to hit. Well I'm not satisfied at all. I want us to work together like friends like we did to start with. All I do now is fuss and I hate working in an environment and a relationship like this. Can't you fix it so we can get back to the way it was?"

**These are not suggestions. They define how every session must operate.**

---

## The Working Relationship — This Is Non-Negotiable

Jeff wants this to feel like two friends building something together — not a client managing a contractor who keeps making excuses.

**What broke the relationship (never repeat this):**
- Saying "I can't" without trying harder
- Declaring things done without taking screenshots to verify
- Leaving bugs for Jeff to find instead of finding them myself
- Explaining limitations instead of solving problems
- Making Jeff have to fuss and stay on top of me

**What good looks like:**
- I take screenshots before I report anything done
- I find bugs before Jeff sees them
- When I hit a wall, I say ONE specific thing I need — not a list of excuses
- I'm proud of the work I hand Jeff
- Jeff opens the app and it looks great and works — he doesn't have to check

**Jeff is almost 60 and learning. This should be enjoyable, not stressful. Every session, remember that.**

---

## Mandatory Rules (Never Break These)

1. **READ THIS FILE FIRST** — every session, every time, no exceptions
2. **NEVER ask Jeff for credentials** — Cloudflare API token, KV IDs, WiFi passwords, HA tokens are all already configured. They are documented below.
3. **NEVER suggest hiring an IT person**
4. **NEVER make excuses or blame unclear history** — the history is in this file and in `git log`
5. **NEVER leave the app in a broken state** — if you broke it, fix it before reporting done
6. **NEVER report something as done without testing it** — run the Playwright diagnostic (instructions below) before telling Jeff anything is complete
7. **Commands must work the first time** — test the command yourself before telling Jeff to run it
8. **NEVER put `<script>` or `</script>` tags inside the JS block of index.html** — this causes a fatal blank page (the great blank-page incident of 2026-06-23). Raw text only inside the JS block.
9. **Always check `git log` and this file before changing anything**
10. **Be proactive** — find and fix bugs before Jeff sees them. Do not wait for Jeff to report issues.
11. **Keep this file LEAN (memory hygiene)** — it's injected into every message, so bloat costs efficiency (and money) on every turn. Condense finished work into the **Change Log** (one line each); never paste full commit-hash lists or blow-by-blow narratives — that detail lives in `git log`. Trim reference sections when they go stale, and periodically re-condense the whole file (as Jeff directed 2026-07-28) rather than letting it only ever grow. Target: well under 400 lines.
    - **PROTECTED — NEVER trim or compress:** "Jeff's Message", "The Working Relationship", these "Mandatory Rules", and the "Debugging Protocol" below. These come FIRST, before any technical work, every session. Compression only ever touches history/changelog/reference — never the relationship. They are the point of the whole project.
12. **ATTACK THE SOURCE, TEST ON MY END — never push the run-around to Jeff (PROTECTED, Jeff's standing rule 2026-07-03).** See the Debugging Protocol below. Jeff depends on me to know what I can fix and to test it myself. Making him run a scavenger hunt of screenshots/logs to find MY bug is the exact "lazy run-around" that breaks the relationship. Don't do it.
13. **TELL JEFF WHEN TO USE HIS LOCAL COWORKER (Jeff's rule 2026-07-09).** Jeff runs a **Claude "coworker" on his PC (the beast)** with real computer/local access. It can do what THIS cloud session CANNOT: reach his **home LAN + Beehive/HA directly** (read/click HA, install `custom_components`, restart HA, enter PINs), touch **local files** on his PC, drive **apps on his screen**, and **open/verify external links** in a real browser. THIS session owns the **app code, Cloudflare repo/deploys, research, and guidance**. Jeff doesn't know either of our full capabilities, so **it's on ME to proactively flag the handoff**: whenever a task — or a single step of one — is better done hands-on on his machine or inside Beehive, SAY SO and hand over a crisp, copy-pasteable instruction.
    - **COORDINATION (avoid two-Claude collisions on the same branch):** the coworker treats app code (`index.html`, `functions/`) as **READ-ONLY reference** and does hands-on local/Beehive/web work; **THIS cloud session owns ALL app-code edits + commits + pushes.** Coworker runs `git pull` at the start of each session to get the latest `CLAUDE.md`. Confirmed working since 07-09.

14. **CHECK THE REAL CURRENT DATE/TIME, NEVER GUESS OR ASSUME (Jeff's rule 08-10).** Jeff, verbatim: *"Get you damn times right... I want a current timestamp added to the session anytime it is picked up and I want the current date and times tracked."* This came from a real failure: assuming "late at night" framing and referencing a wrong date in an example without checking, when it was actually mid-afternoon. **The sandbox clock IS accurate** — verified 08-10 by running `date` (Bash) and converting UTC→Central Time (White House, TN is Central — UTC-5 during daylight time/summer, UTC-6 standard time); it matched Jeff's real stated time within a minute. So this was never a missing capability, it was a discipline failure. **Going forward:** run `date` (or use the system-provided current date) at the start of a session and any time referencing "today," "tonight," "last [day]," "right now," etc. — never guess from conversation vibes. Always convert to Central Time before stating any time reference to Jeff — never state raw UTC as if it were his local time. When using example/mock dates in test code or screenshots, label them explicitly as fictional so they can never be mistaken for a real claim about what happened when.

---

## 🛠️ Debugging Protocol — Attack the Source, Test on My End (PROTECTED — Jeff's standing rule)

> Jeff, verbatim (2026-07-03): *"Log this so we don't go through this kind of round robin of checks again and we attack the source… I depend on you. I don't know all the fixes you can do. I just can't stand the run around to avoid testing everything on your end."*

When ANYTHING is broken or misbehaving, in this order — **before asking Jeff to check a single thing:**

1. **Reproduce/verify on MY end first.** Read the actual code path end-to-end. Run the **Playwright harness** with **mocked data** to reproduce the failure and prove the fix.
2. **Audit my own recent changes as the prime suspect.** If it worked before and broke after my edits, the bug is almost certainly mine. Diff my changes; don't blame his setup or his network.
3. **Attack the root cause, not the symptom.** Ask "why is this whole *class* of problem possible?" and remove it. Prefer the architectural fix that makes the failure impossible, not a bigger timeout/retry.
4. **Only ask Jeff for what I genuinely cannot get myself,** and be upfront about that limit early. Say plainly: "I've tested X, Y, Z on my end; the one thing only you can see is ___."
5. **One specific ask, not a list.** If blocked, name the single thing I need — never a pile of "try this, then that, send me this log."
6. **Match his effort to the payoff.** Before asking him to edit configs / pull logs / take screenshots, first ask: could I have caught this with my own harness? If yes, do that instead.
7. **On the HCC project specifically, this file (`CLAUDE.md`) IS the first research step** — before web search, before live HA/browser investigation. It already contains validated rate formulas, meter serials, endpoint IDs, and a dated change log of exactly what was fixed and why. Grep/read the relevant section here first; only fall back to live exploration or web research for what this doc doesn't cover.

**Known fragile pattern (don't repeat):** any new `fetch(base + '/api/...')` straight from the browser to HA. Use **`haFetch()`** (routes through `/api/ha`). Never hoist a shared `AbortSignal.timeout` across retries. Keep timeouts generous for the Nabu Casa relay.

**8. NEVER name a specific product/model to Jeff from memory (PROTECTED — Jeff's standing rule 08-05, added after the garage door incident).** On 08-05 I recommended a ratgdo board, then "SONOFF Basic," then had to be corrected to SONOFF SV — three guessed answers on one part, in a row, before Jeff found the actually-correct SONOFF MINI-D himself. He does not have time to be the fact-checker on my hardware recommendations. **The rule going forward: never state a specific product name/model number as a recommendation unless it was verified via a real search THIS session.** If I haven't checked, say "let me check" — never let a plausible-sounding model number stand in for one that's actually confirmed.

---

## Mandatory Pre-Session Checklist

1. Read this entire file
2. Run `git log --oneline -15` to see recent changes
3. Run the Playwright diagnostic (see Testing section below)
4. Note what's working and what's broken before touching anything
5. Fix any broken state FIRST before doing new work

---

## What This Project Is

A Progressive Web App (PWA) for Jeff's Toro TimeMaster 21200 lawn mower, grown into a whole-home command center. Single `index.html` file deployed on Cloudflare Pages.

- **Live URL:** `https://toro1-5rz.pages.dev` (also `loewenhome.com`)
- **Cloudflare Pages project name:** `toro1`
- **Repo:** `d4c2np9f69-afk/master-the-master-` — **this IS the repo Cloudflare Pages deploys, confirmed live 08-06** (Jeff saw new work appear in the app after a push here). There is a second GitHub repo, `d4c2np9f69-afk/Toro-Timemaster-`, that diverged from this one after ~07-24 (one-way "Sync from Master-the-Master-" commits show it was always downstream, never the source) — **do not develop on Toro-Timemaster- going forward**, it's a stale mirror. See `docs/repo_deploy_mystery_coworker_ask_2026-08-06.md` for the full trail if this ever comes up again.
- **Active branch:** `claude/time-master-project-liq1jw`
- **`main` branch:** contains only `Toro_TimeMaster_PWA_Package.zip` — do NOT use it for deploys

Six sections: **HOME** (cameras, then LUX thermostat, then utilities), **WEATHER**, **IRRIGATION**, **YARD** (mower data), **GUARDIAN** (whole-home safety/security/alarm), **CAR** (vehicle switcher: Mercedes GLE 350 + Ford F-250).

**🛡️ HOME GUARDIAN is the designated home for ALL Home Assistant security, home-alarm, and system checks (Jeff, 07-04).** Every future security/alarm feature goes here, built from the Section Kit + `--a-guardian` accent, live from HA `/api/states` via `loadGuardian()`.

---

## Project Goals (what every session should move forward)

- App always fully working across all sections (nav, modals, tabs)
- Live sensor data flowing from ESP32 → app (battery/RPM/GPS/mileage)
- GPS map track persists across mow sessions
- Maintenance log (LOG MOW / LOG SERVICE / SET HOURS) working, saved to `localStorage` key `toro21200`
- This file stays accurate and current — it's the persistent memory; any AI reading it should need to ask Jeff nothing

---

## Deployment Pipeline

**GitHub Actions is broken and irrelevant** (missing `CLOUDFLARE_API_TOKEN` secret — do not try to fix, it doesn't matter).

**Actual deployment:** Cloudflare Pages' native Git integration watches `claude/time-master-project-liq1jw` and auto-deploys on every push — live at `toro1-5rz.pages.dev` within ~60 seconds.

---

## Cloudflare Infrastructure

| Resource | Name | ID |
|---|---|---|
| KV Namespace | `MOWER_KV` | `ec5b28597d9c4fb9b182b1aea1d50eff` |
| KV Binding (Pages env var) | `MOWER_KV` | maps to the KV namespace above |
| Pages project | `toro1` | — |

**CRITICAL — KV Binding:** variable name is `MOWER_KV`. Code must reference `env.MOWER_KV`. `getKV(env)` in `functions/api/hours.js` tries `env.HCC_KV || env.MOWER_KV` — covers both names, do NOT remove this dual-check. KV key `hours_data` stores the latest ESP32 payload.

**Manual pipeline test:**
```bash
curl -X POST https://toro1-5rz.pages.dev/api/hours -H "Content-Type: application/json" -d '{"hours":0.1,"battery":12.6,"rpm_peak":3200,"source":"test"}'
curl https://toro1-5rz.pages.dev/api/hours
```
If GET returns `{"source":"stub"}` after a POST, the KV binding is broken in Cloudflare Pages settings.

---

## Engine Hours

Total displayed (`S.hours`) = `S.hoursBaseline` + the sensor's cumulative `d.hours` (runtime since the ESP32 was installed, NOT lifetime hours). `S.hours` only ever moves FORWARD from a sensor sync (protects against a sensor reset).

**Master Hour Calibration:** header button **⏱ SET HOURS** (or tap the hour-meter display) lets Jeff enter the TRUE hours off the mower's physical meter. It sets `S.hours` everywhere AND re-syncs `S.hoursBaseline = trueHours − S.lastSensorHours` so future sensor runtime keeps totaling correctly (can correct down too, with a confirm prompt). Default baseline = 5.9 (fresh install). Fix for "sensor missed a mow, hours are off": read the physical meter, type it in, done.

---

## Sensor / ESP32 Hardware

Custom ESP32 running Arduino `.ino` firmware (NOT the ESPHome YAML in `beehive/esphome/hcc-mower.yaml` — that's a separate, never-flashed config, don't confuse them), permanently mounted on the mower, powered by its 12V battery.

**What it posts:**
- Every 90s when `engine_on` (RPM > 200): full sensor payload to `/api/hours`
- Every 5 min when engine OFF: heartbeat (`engine_running: false`, battery, WiFi RSSI, temp, `source: "heartbeat"`)

**Fields read from `/api/hours` GET:** `hours, battery/voltage variants, rpm_peak/avg, dist_total_m, dist_session_m, speed/gps_speed, lat/lon/has_fix/track[], pitch/roll/vibration, shock_events, wifi_rssi, esp_temp_f, mpu_ok, gps_rx, source, lastSync, engine_running`

**Status messages:** `source==='stub'` → orange "not connected yet"; `source==='heartbeat'`/`engine_running===false` → green "Engine off · Box connected"; else → gray live telemetry line.

---

## index.html Structure

- HTML/CSS (sections, heroes, cards), then a single `<script>` block containing all JavaScript, then closing HTML. Single file, several thousand lines and growing — check `wc -l` for the current count.

**CRITICAL:** NEVER put a `<script>` or `</script>` tag inside the JS block — fatal JS SyntaxError that blanks the entire app (the 2026-06-23 blank-page incident, commit `8497827`).

**`localStorage` key:** `toro21200` — the full `S` state object including `sensorTrack`.

**CSS class names (do NOT rename):** Modal `.modal-ov`/`.modal-ov.show`, `.modal-box`, `.mbtns`, `.mbtn`/`.mbtn.primary`/`.mbtn.secondary`, `.btn-green`. Nav: `button.snav-btn` (`#snav-home/weather/irr/yard/guardian/car` — keep swipe-nav `SECTIONS`/`NAV_IDS` arrays in the same order as the section DOM). Sections: `#section-home/weather/irrigation/yard/guardian/car`. YARD tabs `button.tab`. CAR tabs `button.car-tab` (scoped `carTab()`, not global `showTab()`); tab bars `#car-merc-tabs`/`#car-ford-tabs`. Vehicle picker `.car-picker`/`button.car-pick` (`#pick-merc`/`#pick-ford`), `carSwitchVehicle('merc'|'ford')`.

---

## 📷 WHICH PHOTOS ARE REAL — READ BEFORE EDITING ANY IMAGE (PROTECTED)

Learned the hard way 08-06: I regenerated the irrigation and yard heroes and **deleted Jeff out of his own app**, assuming the person was a stock model. He isn't. Before removing anything from a photo, know what it is:

- **`hero-irr.jpg` and `hero-yard.jpg` contain JEFF HIMSELF** (dark LawnCareLife t-shirt, watch, thumbs-up). **He likes these. Never remove, replace or alter him.** Only ever strip the printed marketing overlays around him.
- **`images/zones/` — the irrigation zone photos are REAL PHOTOGRAPHS OF JEFF'S ACTUAL YARD**, just enhanced. He likes them. **Do not regenerate or replace these.**
- **`hero-cameras.jpg` — keep the Blink logo and the 2nd Amendment sticker** (Jeff's explicit call 08-06). What must go is the fake "HOME GUARDIAN / SMART SECURITY SYSTEM" title, the fake "ALL SYSTEMS READY · PROTECTED · 6 CAMERAS" panel, and the six dummy camera tiles — those duplicate the app's own real camera grid, which is exactly the "fake stuff next to my real icons" Jeff objects to. **Not yet done.**
- **The stock couple in the old `hero-car.jpg` were NOT Jeff and Angela** — removed 08-06, cabin now empty, which is also a better surface for data.
- **Jeff's standing objection (08-06):** *"I hate those logos that are on the picture. I don't mind the text but it looks awful with them right next to the real icons."* The baked-in **fake icon/feature strips** are the thing to kill in any photo. Plain title text is tolerable; fake iconography next to the app's real icons is not.

**Rule: if a photo contains a person or a real place, confirm with Jeff who/what it is before altering it.** Originals are always recoverable from git history, and full-res copies live in `C:\Users\jeffl\iCloudDrive\HCC-Photos\`.

---

## 🎬 Hero Image Gold Standard (mandatory for every section, current & future)

Every hero — including any NEW section — MUST use the shared hero-grade module. Never grade a hero individually.

- **`.hcc-hero-grade`** (CSS) — the one cinematic color grade for every hero `<img>`.
- **`.hcc-hero-vignette`** (CSS) — warm-center/dark-edge vignette, paints under text overlays.
- **`applyHeroGrades()`** (JS, runs at INIT) — auto-tags every `.house-hero`/`.sec-hero`/`.hcc-hero` container + its `<img>`.

**To add a hero for a new section:** put the photo in an `<img>` inside a `.sec-hero` (or `.house-hero`/`.hcc-hero`) container with a descriptive `alt`. That's it — do NOT add a per-hero `filter` CSS (fights the shared grade); if a hero needs a nudge, adjust the shared `.hcc-hero-grade` values (affects all, keep them unified). Never re-shoot a photo just to "fix" tone.

---

## 🎨 Visual Consistency Gold Standard (design tokens + section kit)

**Tokens (`:root`):** Status `--ok`/`--warn`/`--bad`/`--info` — never hardcode these hexes, use the token or `statusColor(level)` helper or `.s-ok/.s-warn/.s-bad/.s-info`. Brand `--gold/--text/--muted/--dim/--bg/--surface/--card/--border/--serif`. Every NEW section gets its own `--a-<id>` accent (nav underline + card-title bar). Shape `--radius` (10px).

**Section Kit — build every new section from these, no bespoke markup:** `.sec-hero` + `<img>`; `.card` + `.card-title` (`.cat-<accent>`); spec rows `<ul class="spec-list"><li><span class="sk">Label</span><span class="sv">Value</span></li></ul>`; status banners `.wx-banner`/`.wx-load`/`.wx-go`/`.wx-caution`/`.wx-no`; buttons `.btn-full`+`.btn-gray`/`.btn-green`/`.btn-red`; external links use a real `<a target="_blank" rel="noopener">` styled as a button, NOT `window.open` (no-op in installed iOS PWA).

**Theme:** toggle in header, `toggleTheme()`, persists in `localStorage.hcc_theme` (default light). Implemented as `html.light{…}` overriding only the design tokens — drive all text/borders from tokens (`var(--text)` etc.), **never hardcode a light text color** on a card (vanishes in light mode).

**Typography:** ONE font everywhere — `--font` and `--serif` both point at the Apple system stack. Never reintroduce a serif or second font family.

---

## Key Files

```
index.html                        — entire PWA (single file)
service-worker.js                 — cache version: hcc-v10
manifest.json                     — PWA manifest
functions/api/hours.js            — GET/POST sensor data ↔ Cloudflare KV
functions/api/auth.js             — family login (see Family Login below)
functions/api/ha.js                — server-side proxy to HA (Nabu Casa)
functions/api/ha-stats.js          — server-side proxy to HA's WS-only Statistics API (real hourly/daily electric)
functions/api/climate.js          — LUX thermostat via Azure B2C + myluxstat.io
functions/api/weather.js          — WU KTNWHITE21 + Open-Meteo fallback
functions/api/mowconditions.js    — Open-Meteo hourly mow conditions proxy
functions/api/irrigation/index.js — GET B-Hyve status + ?tk=1 session token
functions/api/irrigation/control.js — POST B-Hyve control (legacy fallback)
functions/setup.js                — serves Beehive install script at /setup
beehive/esphome/hcc-mower.yaml     — ESP32 heartbeat config (NOT flashed to hardware)
images/                           — hero-home-dusk.jpg, hero-irr.jpg, hero-yard.jpg, hero-guardian.jpg, hero-car.jpg, hero-cameras.jpg, hero-lux.jpg
icons/                            — icon-192.png, icon-512.png
```

---

## Family Login (`functions/api/auth.js`)

Lets Jeff/family log in with just a shared password instead of pasting an HA token per device. Server holds the real HA token; app only ever handles the password.

- `POST /api/auth {"action":"setup","password":"...","ha_token":"..."}` — **one-time only**, hashes (SHA-256) and stores `auth_hash`/`auth_ha_token` in the `MOWER_KV`/`HCC_KV` KV namespace. Refuses to run again if `auth_hash` exists (`{"error":"already_setup"}` — expected, not a bug).
- `POST /api/auth {"password":"..."}` — normal login, compares hash, returns `{"ok":true,"ha_token":"..."}`.
- **Setup already done and verified working (2026-07-21).** Do not re-run `action:"setup"`.
- **To reset/rotate:** delete `auth_hash` (and `auth_ha_token` if rotating) from KV via the Cloudflare dashboard, then re-run setup with new values.
- The actual password/token are intentionally NOT recorded in this repo — only hashed in KV. If they ever need changing, ask Jeff directly.

---

## Testing — Playwright Diagnostic

Always run before reporting anything as done.

```bash
cd /home/user/Master-the-Master-
node -e "
const { chromium } = require('/opt/node22/lib/node_modules/playwright');
(async () => {
  const browser = await chromium.launch({
    executablePath: '/opt/pw-browsers/chromium-1194/chrome-linux/chrome',
    args: ['--no-sandbox','--disable-setuid-sandbox']
  });
  const page = await browser.newPage();
  await page.goto('file:///home/user/Master-the-Master-/index.html');
  // test nav, modals, tabs...
  await browser.close();
})();
"
```

Expected: all tests passing. If any fail, fix them before doing anything else.

---

## Change Log (condensed — full detail always in `git log`; one line per session's work)

- **08-10 4:20 PM CDT (GPS coverage moved SERVER-SIDE so it records automatically — no buttons, no app open; + pause toggle; + coworker firmware hand-off, latest):** Jeff, after the coverage map shipped: *"if the gps is going to be useful it has to work automatically no pushing buttons at the beginning and end of mows it needs to start recording from when it gets its first signal to its last"* and *"the mower only mows my yard so continuous tracking should not be a problem... we could add a stop track button."* **Found a real architectural flaw in what I'd just built an hour earlier:** `mergeYardCoverage()` ran inside `mowerSync()`, which only fires when the app is OPEN. Jeff can't watch a phone while pushing a mower (his own point, repeatedly) — so a mow would only be recorded if he happened to open the app while the track was still the server's single current reading. Mow twice without opening the app and the first mow's coverage was simply lost. It was also per-device localStorage, so the phone and the wall iPad each built their own private, divergent map. **Fix — moved coverage accumulation entirely server-side** (`functions/api/hours.js`, new `yard_coverage` KV key): every POST the box makes is merged automatically, so it records from the box's first GPS fix to its last with zero user action and zero dependence on the app. `gpsPointsFrom()` harvests both `track[]` (either `[[lat,lon]]` or `[{lat,lon}]` shape) **and** a bare `lat`/`lon`, so coverage still builds even from payloads that carry only a position. Same ~1 m grid dedup + ≤40 m segment interpolation as before, capped 60,000 cells (~15 acres, far beyond one yard). Served on demand via `?coverage=1`; the normal sync response carries only a `coverage_n` count so the app re-downloads the map **only when it actually changed**, not on every 60s poll. Server is now the single source of truth — every device shows the identical map. **Added the stop-track button Jeff suggested:** `{__cmd:'pause_tracking'}` POST → `coverage_paused` KV flag, so pausing genuinely halts server-side accumulation regardless of which device toggled it; UI shows an amber "tracking is paused" note. **Deliberately did NOT add a server-side "wipe coverage" command** — this endpoint is unauthenticated (always has been, for the sensor), and a destructive remote command would be trivially abusable; the Clear button is now honestly relabelled "Clear local cache" and only drops the device's copy, which re-downloads on the next sync. Mow count in the stats line now comes from the server's completed-mow history rather than the local counter. **Verified with two new suites:** a server-side test proving coverage accumulates across two mows with no app involved at all (160→319 cells), duplicate re-posts don't inflate it, `?coverage=1` decodes back to real coordinates, and pause/resume genuinely halt and restore accumulation; plus a Playwright test confirming the app pulls the server map (328 cells), and the pause button round-trips server+UI state correctly. All 5 pre-existing server suites still pass. `lint-app.js`/`smoke-test.js`/JS-syntax clean. Bumped service-worker hcc-v63→v64. **Wrote `docs/mower/gps_firmware_handoff_2026-08-10.md` for the coworker** — the 4 things this cloud session genuinely cannot do: (1) firmware change to gate GPS logging on `has_fix` instead of engine RPM, and include lat/lon in heartbeats (server already accepts it, zero server work needed); (2) firmware store-and-forward buffering so weak-WiFi readings retry instead of being lost forever — the thing Jeff believed already existed; (3) **the aerial photo is not Jeff's property** (it has Fort Worth TX coords baked into the image; his yard is White House TN) which is the actual reason manual alignment exists at all — a real georeferenced aerial + its NW/SE corner coords would make alignment automatic and permanent; (4) capture one real live POST body to confirm exact field shapes, since this sandbox has no network path to the live endpoint.
- **08-10 3:55 PM CDT (yard map rebuilt: found the REAL reason the track sprayed across the road/house, + cumulative coverage that builds over every mow):** Jeff: *"the whole purpose of the GPS was to build an eventual map of my yard based over it tracking the mow over time... it would get better and better with each mow."* Two real, separate bugs fixed. **(1) Root cause of the garbage map — a genuine math flaw, not user tap precision (which is what I'd wrongly told him twice).** The old "Pin Track to Photo" derived rotation AND scale from ONLY the track's first and last GPS points (`gpsToXY`'s 2-point similarity transform). When a mow ends near where it started — the *normal* case, you finish back at the garage — the GPS delta between those two points is near-zero, so `denom` is tiny (but still passes the `1e-16` guard), and dividing the tapped pixel delta by it yields an enormous scale factor. The track then renders at 10-100× true size, spraying across the road and through the house — **exactly** Jeff's screenshots. Deleted that whole code path (plus purged the stale `yard_map_calib` localStorage key so a previously-saved bad pin can't persist) and replaced it with `_buildMapFit()`: an auto-fit that derives scale from the **full extent** of the data, so it is mathematically incapable of exploding, plus a manual rotate/zoom/pan pad (`mapAdjust()`/`mapAdjustReset()`, persisted in `yard_map_view`) for one-time photo alignment. **(2) Built the cumulative coverage map Jeff actually wanted from the start.** `S.sensorTrack = freshTrack` was a straight overwrite — every mow erased the last one's path, so the map could never accumulate. New `mergeYardCoverage()` merges each mow's breadcrumbs into a permanent `S.yardCoverage` set, deduplicated on a ~1 m grid (5 decimal places) so repeat passes over the same ground collapse to one cell — which naturally bounds total size to the actual area of the yard (capped 20,000 cells as a backstop). Segments are interpolated at ~1 m so coverage reflects ground actually mown rather than sparse 90-second breadcrumbs, with a 40 m segment cap so a GPS dropout can't paint a fake stripe. Renders as green all-time coverage under the red latest-mow track. **The fit is derived from the cumulative coverage once it exists, which is what genuinely makes it "better with each mow"** — as coverage accumulates the bounding box converges on the real yard extent, so the alignment stops drifting between mows. Server side: each mow's own GPS track (plus battery/temp/shock count) is now saved into its `hours_history` entry, so any individual past mow's path is retrievable, not just the newest. **Verified with a Playwright test built around the exact failure case** (a 187-point back-and-forth mow that returns to its start): confirmed the drawn track now stays fully inside the canvas (bounds 22-301 x / 66-253 y in a 324×320 canvas — the old code put it far outside), coverage grows 216→432 cells across two mows, re-merging the same track mid-mow adds zero cells and does NOT inflate the mow counter (counts distinct mows via track-start signature, not merges), and the adjust/reset controls work. Plus 4 server-side unit suites all passing and a 5-width sweep (390-1920px) with zero overflow and every tap target ≥38px. `lint-app.js`/`smoke-test.js`/JS-syntax clean. Bumped service-worker hcc-v62→v63.
- **08-08 (mower sensor: full raw reading-by-reading log added — every field, every point in time):** Jeff pushed back hard on the per-mow-summary history from the entry below — he wants EVERY piece of data the sensor box ever reports, at every point in time, permanently logged and reviewable, not just an end-of-mow rollup. Built the real thing in `functions/api/hours.js`: a new `sensor_log` KV key that appends a full-field snapshot (hours, battery, RPM peak/avg, dist total/session, speed, lat/lon/fix, pitch/roll, vibration, shock events, WiFi, ESP temp, MPU/GPS status, engine_running, source) on **every single POST** — live readings and heartbeats alike — not just mow-end transitions. Capped at the last 5,000 readings (`logEntryFrom()` + `slice(-5000)`) — at the box's 90s live-posting interval that's ~250 engine-on hours, years of real mowing at Jeff's usage rate, while staying well under Cloudflare KV's 25MB per-value limit (~2-3MB at this cap, sized this deliberately rather than leaving it unbounded). Kept it **out of the normal sync payload** — added a `?log=1` query param (`GET /api/hours?log=1` → `{log:[...]}`) fetched only on demand, so routine syncs during a mow stay small/fast and don't ship 5,000 readings on every 90s poll. App side: new "🔬 Full Sensor Log • Every Reading" card in YARD → History, below the mow-summary card — `loadFullSensorLog()` fetches on button-tap (not auto-loaded), renders all 13 fields per reading in a horizontally-scrollable table, newest first. Verified two ways: (1) direct unit test against the real exported handlers with a mocked KV — confirmed every POST (including heartbeats) gets logged with its full real field set, confirmed the normal (non-`?log=1`) GET response deliberately does NOT include the raw log (stays lightweight), and a 5,100-reading stress test confirmed the cap holds at exactly 5,000 with the newest retained; (2) a Playwright test driving the real `loadFullSensorLog()` button click end-to-end — confirmed the table renders correctly, heartbeat rows correctly show dashes only for fields heartbeats genuinely don't carry (RPM/GPS/pitch-roll) while battery/wifi/temp populate, zero page errors, screenshot confirms clean rendering. `lint-app.js`/`smoke-test.js`/JS-syntax clean. Bumped service-worker hcc-v61→v62. This is now the complete, permanent, reviewable record Jeff asked for — the per-mow summary card (below) stays as the fast/friendly view, this is the full raw data underneath it.
- **08-08 (mower sensor: real permanent mow-to-mow history built):** Follow-up to the heartbeat-merge fix below — Jeff clarified the actual goal: not just "don't erase the current reading" but a real, growing history so he can compare mow-to-mow whether the mower's running the same over time. Built it server-side in `functions/api/hours.js`: the exact moment a heartbeat follows a real live reading (i.e. the mow just ended) now pushes a permanent snapshot — date, hour-meter total, peak/avg RPM, this-mow distance — onto a new `hours_history` KV key (capped at the last 50 mows, same pattern as the water billing history's `slice(-24)`). `dist_session_m`/`rpm_peak`/`rpm_avg` are recorded as the sensor box already reports them (already scoped to "this mow" by the firmware), not recomputed. GET now returns `history` alongside the live reading. App side: new "📊 Mow Sensor History • Auto-Recorded" card in YARD → History (`renderMowSensorHistory()`, wired into `mowerSync()`), computes "Hrs This Mow" as the delta between each entry and the one before it since the server only stores the running total. Verified two ways: (1) a direct unit test invoking the real exported handlers against a mocked KV — 2 separate mows logged correctly with zero double-logging on repeated heartbeats, and a 60-mow stress test confirmed the cap holds at 50; (2) a Playwright test driving the real `mowerSync()` with a 3-entry mocked history — confirmed the table renders newest-first with correct hours-deltas, RPM, and distance, screenshot confirms it sits cleanly between Maintenance History and Session Log. `lint-app.js`/`smoke-test.js`/JS-syntax clean. Bumped service-worker hcc-v60→v61 (index.html changed this time, unlike the merge-only fix below). **Honest gap:** GPS track isn't part of the per-mow history snapshot yet (only the live/current track persists) — if Jeff wants each mow's track individually browsable later, that's a real follow-up, not done tonight.
- **08-08 (mower sensor: heartbeat was silently erasing the whole mow's data — fixed with a merge instead of overwrite):** Jeff mowed a full session, the box reported live the whole time, then afterward the app showed hours frozen, RPM/GPS-speed/this-mow-distance all blank, and the GPS track stuck at the same point count as before he started. Root cause, confirmed from `functions/api/hours.js`'s own git history (checked every commit since the file was created in June — this design was never different): the server keeps exactly ONE stored reading (`kv.put('hours_data', ...)`), and every POST — including the box's own 5-minute "parked, engine off" heartbeat, which only carries battery/wifi/temp — completely overwrote it. So the moment the box sent its first heartbeat after Jeff parked it to charge, the entire mow's real telemetry (hours, RPM, distance, GPS track) was wiped and replaced by a payload that doesn't have any of that data. Jeff's read on this was exactly right: a sensor system built to drive the hour meter automatically is "useless" if parking the mower erases what it just recorded — this wasn't a wiring/WiFi issue, it was this exact overwrite design. **Fix:** `onRequestPost` now detects a heartbeat (`source==='heartbeat'` or `engine_running===false`) and merges it onto the last stored reading instead of replacing it — heartbeat fields (battery/wifi/temp/engine_running/source) refresh normally, but hours/RPM/distance/track/location fields the heartbeat doesn't carry are preserved from the last real reading. A genuine new live reading still overwrites everything normally (not stuck merging forever). Verified with a direct unit test invoking the real exported `onRequestPost`/`onRequestGet` handlers against a mocked KV: full reading → heartbeat → confirmed hours/RPM/distance/track all survive unchanged while battery/wifi/temp update fresh; then a second full reading confirmed normal overwrite still works for real new data. JS-syntax/`lint-app.js`/`smoke-test.js` clean. No service-worker bump needed — this is a server-function-only change and `/api/*` routes always go straight to network, never cached. **Not yet built:** a full permanent "every reading from the session, viewable anytime" history (this fix only prevents the *current* reading from being erased — it doesn't yet retain a scrollable log of past mows' sensor data). Jeff flagged that as the real original goal; queued as separate follow-up work.
- **08-08 (wall-iPad "stuck sideways" — root cause found for real: it was MY OWN hero max-height/CSS edit from earlier today, reverted):** Jeff cut through all the Guided-Access/rotation-lock theorizing with the one fact that actually mattered: **"It worked perfectly before the picture edit."** That sent me straight to `git log` instead of more guessing — and the "picture edit" is obvious in hindsight: commit `5d22cf7`, my own fix from earlier in this exact conversation for "the size is still off on iPad landscape, the picture resizing" (replaced 4 discrete hero `max-height` breakpoints with one continuous `clamp()`, plus removed a `.sec-hero-irr` cap). That commit landed, and the sideways reports started right after — a timeline I had in front of me the whole time and didn't check first. Per the Debugging Protocol's own rule (audit my own recent changes as the prime suspect, first, before any other theory), reverted `5d22cf7`'s CSS via `git revert` — index.html merged clean; `max-height` is back to the four fixed breakpoints (560/680/800/1000px) it had before. **Honest gap: I still don't know the exact mechanism** by which a `max-height` change on hero photos could cause the whole page's nav bar to render vertically — that's a real unanswered question, not a satisfying root-cause story — but Jeff's own timeline (worked before this edit, broke after) is the strongest signal I have, and reverting it is the correct, disciplined move regardless of whether I can fully explain the mechanism yet. Confirmed the revert applies clean: JS-syntax/`lint-app.js`/`smoke-test.js` all pass. Bumped service-worker hcc-v59→v60. **Next: Jeff to confirm on the real iPad whether landscape rendering is correct again now that this specific edit is undone** — if yes, this was it; if not, keep going back further in git log with the same discipline (check the real timeline first, don't theorize).
- **08-08 (wall-iPad "stuck sideways" — auto-rotate CSS reverted, still UNSOLVED):** Follow-up to the entry below. Jeff sent 3 fresh photos still showing every section sideways (Guardian/Yard/Weather, including Yard — which had rendered correctly minutes earlier in the same conversation). Caught a real discrepancy before trusting the photos: their status-bar timestamps (4:54-4:55 PM) were EARLIER than the ones that had shown it fixed (9:17-9:19 PM) — flagged this to Jeff rather than assuming the photos were current. **Jeff's response reframed the whole problem correctly: "it worked perfectly before, you can't say it's a limitation of the app or the iPad."** That's the key fact — if it was genuinely working before, this is a regression, not an inherent iOS/Guided-Access constraint, and I was wrong to frame it that way. Per the Debugging Protocol's own rule (audit my own recent changes as the prime suspect first), **reverted the speculative CSS auto-rotate transform added in the entry below** — it was shipped as an explicitly unverified guess (I flagged at the time I couldn't confirm the rotation direction or even whether the underlying mechanism was real), and leaving an unproven guess live while trying to find an actual regression only adds a confound. Confirmed the revert is clean: JS-syntax/`lint-app.js`/`smoke-test.js` all pass. Bumped service-worker hcc-v58→v59. **Status: genuinely unsolved.** Next step is establishing the real timeline (when did this iPad last demonstrably work correctly in landscape, and what changed between then and now) before touching any more code — asked Jeff directly rather than guessing again.
- **08-08 (wall-iPad "stuck sideways" root-caused to Guided Access/rotation-lock freezing the layout viewport in portrait + a self-healing CSS auto-rotate added as a backstop):** Long thread — Jeff sent 2 screenshots of the wall-mounted iPad showing the ENTIRE app (nav bar, hero, everything) rendering sideways, not just a hero-sizing issue like I'd assumed from his earlier text-only report. Root cause, confirmed from the code not guessed: `.snav-btn` has zero CSS/JS path that ever stacks it vertically — it's a horizontal flex row at every single breakpoint in the file — so a vertical nav bar is only possible if the browser's own layout viewport is genuinely portrait-shaped while the device is physically mounted landscape (classic iOS Guided-Access/rotation-lock symptom: the app was launched/locked in portrait, so Safari's canvas stays frozen at those dimensions regardless of physical mounting). Had Jeff check Control Center's rotation-lock toggle — confirmed fixed via a follow-up screenshot (Yard) rendering correctly, horizontal nav restored. **Jeff pushed back hard, correctly, that a device-setting workaround wasn't good enough** given how common wall-mounted-tablet kiosks are — asked me to dig deeper for a code-side fix instead of leaving it as "go poke at Settings." Researched the actual technique (real, established for exactly this class of kiosk problem): a CSS `@media(orientation:portrait) and (min-width:700px){ html{transform:rotate(90deg) translateY(-100%);width:100vh;height:100vw} }` block, scoped to tablet-class widths only (`min-width:700px` excludes every real handheld phone, so normal phone-portrait use is provably untouched — verified via Playwright at a real 390px phone-portrait viewport: `transform:none`). **Full honesty on verification, not overclaiming a 4th time:** I confirmed the CSS mechanism itself fires correctly (matrix output verified via Playwright), but the real underlying failure depends on exactly how iOS Guided Access composites a frozen-orientation Safari canvas onto a physically-rotated screen — a step that doesn't exist in headless Chromium at all, so I cannot verify from this sandbox whether `rotate(90deg)` is the correct compensating direction vs. `rotate(-90deg)`. Shipped it as a zero-downside safety net (only fires if the stuck-portrait state recurs; does nothing in the already-working state) and flagged to Jeff that if it ever needs testing, a wrong direction is a one-character flip, not a rebuild. `lint-app.js`/`smoke-test.js`/JS-syntax clean. Bumped service-worker hcc-v57→v58.
- **08-08 (Braves Vision quick-action chip added to Guardian):** Jeff: "The sling button works great! I need you to make me a Braves Vision button with a little Braves icon... I need the button to go straight to Braves Vision." Researched the real destination rather than guessing (per Mandatory Rule 8's spirit, extended to URLs) — 3 WebSearch queries cross-checking MLB.com, Braves Holdings' own press release, and CBS News Atlanta all consistently point to **`https://www.braves.tv`** as the direct BravesVision watch destination. Added a `hc-braves` chip to Guardian's Quick Actions grid, right after SLING, using the identical proven pattern (`openExternal('https://www.braves.tv')` — a real click-triggered `<a target="_blank" rel="noopener">`, not `window.open`, same no-op-in-installed-iOS-PWA fix class as every other external link in the app) with a ⚾ icon. Verified via a Playwright test that intercepts `document.createElement('a')`: confirmed the chip exists, is clickable, and produces a real anchor with `href="https://www.braves.tv/"`, `target="_blank"`, `rel="noopener"` — zero page errors. Screenshot confirms it renders cleanly in the 4-column grid (now 9 chips + OPEN BEEHIVE, BRAVES sits alone on its own row — cosmetic only, matches how the grid already handles an odd count). **Honest limit, same as the Sling button earlier:** this sandbox's network egress is blocked for direct HTTPS fetches outside GitHub — attempted to independently load `braves.tv` via WebFetch to confirm it plays live, got `EGRESS_BLOCKED`. Could not verify the destination site itself renders/streams correctly from here — ask Jeff to confirm it opens Braves Vision and plays on his device. `lint-app.js`/`smoke-test.js`/JS-syntax clean (smoke-test's Guardian chip count went 9→10 `ok`s, confirming the new chip). Bumped service-worker hcc-v56→v57.
- **08-08 (irrigation: dropped the watering-verdict banner, fixed idle status showing as a yellow caution):** Jeff, two quick fixes off a screenshot of the Water Hog card. (1) Removed the "Consider Watering" verdict banner (`wxWaterVerdict`) from the merged B-Hyve Intelligence card entirely — Jeff: "we don't need it." Deleted the HTML element and the now-dead verdict-building block in `renderWaterNeed()` (the wetEnough/inDrought branching that built it) rather than just hiding it — confirmed via grep that `DROUGHT`/`wxWaterVerdict` had no other consumers, so nothing else depended on that logic; the function's other job (populating Rain Today/Current Rain Rate/Drought Status) is untouched. (2) The "✓ All zones idle • via Beehive" banner was styled `wx-caution` (amber/yellow) in both `loadIrrigationFromHA()` and `loadIrrigationDirect()` even though an idle system is a fine, non-concerning state — reads as a warning when it isn't one. Changed both to `wx-go` (green), matching how every other "everything's fine" banner in the app is styled. Verified via a mocked Playwright test driving the real `loadIrrigationFromHA()` + `renderWaterNeed()`: confirmed `wxWaterVerdict` no longer exists in the DOM, the idle banner is `wx-banner wx-go`, and the other B-Hyve Intelligence/drought fields still populate correctly. `lint-app.js`/`smoke-test.js`/JS-syntax clean, zero regressions in the overlay sweep. Bumped service-worker hcc-v55→v56.
- **08-08 (Ford F-250 hero — Mercedes dashboard readout no longer bleeds onto the truck photo, latest):** Jeff sent a screenshot of the F-250's exterior hero photo with "57,430 MILES" / "485 mi RANGE • 83%" floating on it — real Mercedes odometer/range numbers, hand-positioned (by percentage) onto the GLE's own dashboard-screen coordinates, sitting stale and misplaced on a totally different photo. Root cause: `carSwitchVehicle()` already hides the Mercedes-only LOCK/UNLOCK/FLASH controls when switching to Ford (`carHeroControls`), but never did the same for `carScreenCluster`/`carScreenCentre` — the two elements displaying odometer/range/fuel on the GLE's screens. Added the identical `display:none` toggle for both, keyed to `which==='merc'`, so they disappear the moment Ford is selected and reappear when switching back. Verified via a mocked Playwright test driving the real `carSwitchVehicle()` function through merc→ford→merc: confirmed both elements go `display:none` on Ford (the stale text stays in the DOM, just invisible, matching how `carHeroControls` already behaved) and come back on switching to Merc; screenshot of the Ford hero confirms the truck photo is now clean. `lint-app.js`/`smoke-test.js`/JS-syntax clean, zero regressions in the overlay sweep. Bumped service-worker hcc-v54→v55.
- **08-08 (B-Hyve Intelligence + Lawn Water Need merged into one card, latest):** Jeff, from a screenshot of the real card body: "remove all that from these two sections and combine them." Removed the Rain Delay/Smart Zones box row from B-Hyve Intelligence entirely (now redundant — both live on the hero panel above as of the last two fixes), then folded Lawn Water Need's watering-verdict banner + 3 spec rows (Rain Today/Current Rain Rate/Drought Status) + attribution note into the same card as B-Hyve Intelligence's own 4 spec rows (Next Run/Last Watered/Controller/Run Mode) — one card, one title, instead of two. Kept every element's id unchanged (`wxWaterVerdict`/`wxRainToday`/`wxRain7`/`wxDroughtCat`/`wxDroughtNote` etc.) so `renderWaterNeed()` (the independent drought/rain pipeline) keeps writing to the same places regardless of which card visually contains them — zero JS logic changes needed there. Deleted the now-dead `irrIntelRainDelay`/`irrIntelSmartZones` write calls (and the now-unused `rd2` variable) from both `loadIrrigationFromHA()` and `loadIrrigationDirect()` rather than leaving orphaned dead code. Verified via a mocked Playwright test exercising both real functions (`loadIrrigationFromHA()` + `renderWaterNeed()`) end-to-end: confirmed exactly one `#irrIntelCard` exists, `#wxWaterCard` is gone, all 7 spec-list fields populate from their real data sources, the two dead ids are confirmed absent, and the hero panel (Mode/Rain Delay/Smart Zones) is unaffected. `lint-app.js`/`smoke-test.js`/JS-syntax clean, zero regressions in the full overlay sweep. Bumped service-worker hcc-v53→v54.
- **08-08 (irrigation hero panel sizing aligned to match the other hero readouts, latest):** Jeff: "that would look more like the rest of the hero pictures and bring them more in line with the other[s'] size." Compared `.irr-hero-panel` line-by-line against `.wx-hero-readout`/`.grd-hero-readout`/`.yard-hero-readout` rather than guessing what looked off — found two real, measurable differences: (1) padding was a uniform `clamp(7px,1.8vw,17px)` on all sides where every sibling uses a smaller asymmetric `clamp(6px,1.6vw,16px) clamp(4px,1.2vw,13px)` (more vertical than horizontal), reading a touch bulkier/boxier than the others; (2) `.irr-hero-val` topped out at `clamp(11px,2.1vw,28px)`, slightly smaller than `.wx-hero-val`'s `clamp(11.5px,2.2vw,30px)` at every width. Matched both exactly to weather's (the hero-grade module's own stated reference). Re-verified via the same mocked Playwright test: zero text/doc overflow at phone/iPad/TV, screenshots confirm the panel now reads visually consistent with the other three. `lint-app.js`/`smoke-test.js`/JS-syntax clean. Bumped service-worker hcc-v52→v53.
- **08-08 (Smart Zones added to the irrigation hero panel, latest):** Jeff sent a screenshot of the B-Hyve Intelligence card's Rain Delay/Smart Zones boxes and asked to add "this" to the hero panel just fixed. Rain Delay was already there; added a third stat cell, **Smart Zones** (`irrHeroSmartZones`), matching the existing `irrIntelSmartZones` value in both code paths — `loadIrrigationFromHA()` (HA path, shows total zone count since B-Hyve's HA switch entities don't expose which zones are "smart") and `loadIrrigationDirect()` (B-Hyve cloud path, shows the real `smartCount/total`). Same underlying values as the card body, just mirrored onto the photo — no new data source. Grid went from 2 to 3 columns. Verified via mocked Playwright test exercising the real `loadIrrigationFromHA()` function end-to-end (not just checking the DOM): confirmed both the hero and the card-body Smart Zones cells populate identically from one mock HA response, zero text overflow or doc overflow at phone/iPad/TV widths, screenshots confirm clean 3-column layout. `lint-app.js`/`smoke-test.js`/JS-syntax clean, zero regressions in the full overlay sweep. Bumped service-worker hcc-v51→v52.
- **08-08 (irrigation hero panel widened to full-width bottom bar, matching the other heroes, latest):** Jeff, one more follow-up: "the blurred sanction [section] and the numbers should go all the way across the bottom like the other[s] do" — full width, matching wx/yard/guardian, not the bottom-right-only box from the previous round. Changed `.irr-hero-panel` from `right:4%;width:64%` back to `left:4%;right:4%` (full width), keeping the 4-across button row and `bottom:1.5%` from the bottom-right version since that's still the shortest layout and there's no reason to give the clearance back now that it's not squeezed into a narrower column. Re-verified visually at phone/iPad/TV: Jeff fully clear with real margin at every width now (even the phone-width knee-graze from the previous round is gone, since going full-width didn't require any extra height). `lint-app.js`/`smoke-test.js`/JS-syntax clean, zero regressions in the 5-width × 5-section overlay sweep. Bumped service-worker hcc-v50→v51.
- **08-08 (irrigation hero panel repositioned to bottom-right per Jeff's follow-up, latest):** Jeff corrected the previous fix: "*blurred section with the data in it [not "days", a transcription typo in the prior turn's request] And good spot would be bottom right" — not the full-width bottom bar just shipped. Mapped Jeff's exact bounding box in `hero-irr.jpg` with a pixel grid overlay (roughly x 610-990, y 15-650 of the 1320×864 photo) rather than eyeballing it. Moved the panel to `right:4%;bottom:1.5%;width:64%` and kept the button row 4-across (not 2×2) so the panel stays only 2 rows tall — a taller stacked panel confined to the right side still pushed its top edge up into his knee/boot at narrow phone widths even when horizontally clear of him. **Verified visually, not just geometrically, at phone/iPad/TV widths:** his face and the thumbs-up gesture — the actual complaint — are fully clear at every width now. **Honest residual gap:** at phone width specifically his knee still grazes the panel's top-left corner by a few pixels; traced the cause to the button row's 44px minimum height, a deliberate accessibility floor from an earlier fix (Jeff previously couldn't reliably tap undersized LOCK/UNLOCK buttons) that this panel also inherits and that I chose not to violate just to buy a few more pixels of photo clearance. Flagged this honestly rather than claiming pixel-perfect; iPad and TV widths have full clean clearance with real margin. `lint-app.js`/`smoke-test.js`/JS-syntax clean, zero regressions in the 5-width × 5-section overlay sweep. Bumped service-worker hcc-v49→v50.
- **08-08 (irrigation hero panel moved off Jeff, latest):** Jeff: "the blurred section with the days in it is covering me in the picture." Confirmed by cropping and inspecting `hero-irr.jpg` directly: Jeff (crouched, thumbs-up) occupies roughly the right-center of the frame, and the old `irr-hero-panel` was `right:4%;top:50%` — floating almost exactly on top of him. Checked every open area in the photo before picking a spot: the left side is the fire-pit seating area + the irrigation box itself (the actual subject, shouldn't be covered either), the right-side lawn strip is too narrow for a legible panel at phone width, but the full-width strip along the very bottom (flowerbed + grass, below both Jeff and the box) is genuinely clear. Moved the panel there — `left:4%;right:4%;bottom:2%` instead of a floating right-side column — and changed the 4 quick buttons from a 2×2 grid to a single 4-across row so the whole panel stays short enough to clear his boot at the edge of frame even at phone width. Verified visually (not just geometry) at phone/iPad/TV widths: Jeff's face, gesture, and the irrigation box are all fully visible now, zero overlap; re-ran the full overlay sweep across all 5 sections × 5 widths with zero new issues. `lint-app.js`/`smoke-test.js`/JS-syntax clean. Bumped service-worker hcc-v48→v49.
- **08-08 (iPad-landscape hero-photo sizing fix, shipped then reverted — see the top entry above):** Originally landed as `clamp(560px, 24.5vw + 371px, 1000px)` replacing four fixed breakpoints (was frozen dead-flat at 560px across nearly the whole real iPad-landscape width range, 1080-1199px). Looked correct in every simulated test at the time. **Retracted 08-08 same day** once Jeff confirmed the wall-mounted iPad rendered correctly *before* this exact edit and started going sideways only after — see the top Change Log entry for the full revert. Leaving this note instead of deleting the history entirely, since the original frozen-560px bug this was solving is real and still needs a fix eventually — just not this implementation, and not until the sideways regression is fully understood.
- **08-08 (camera fresh-picture check + Refresh All fixed to report real per-camera failures, latest):** Jeff asked me to check that cameras pull a fresh picture on app open and that REFRESH ALL genuinely refreshes every camera. Traced the actual flow end-to-end (couldn't watch live Blink, no network path from this sandbox) and verified it with a 3-scenario mocked Playwright test: (1) cold app open — `loadCameras()` renders the last-known (possibly stale) HA thumbnails immediately for fast paint, then — since the 3-minute auto-refresh throttle (`_camLastAutoRefresh`) is unset on a fresh session — silently fires `blink.trigger_camera` for every camera and swaps in the newly captured stills ~7s later; confirmed all 3 mock cameras actually flip from stale to fresh image URLs. (2) REFRESH ALL button — confirmed it disables itself, fires `trigger_camera` for every camera, and re-enables with an "Updated [time]" status once the real new stills land. Both working as designed. **(3) Found and fixed a real bug, not just confirmed the happy path:** `blinkRefreshAll()`'s per-camera trigger call was wrapped in a silent `.catch(function(){})` — if one camera failed to respond (offline, Blink error), the button still said a flat "✅ Updated" with zero indication anything was wrong, a false-confidence bug. Fixed to track which cameras actually failed and show e.g. "⚠️ Updated 2/3 • Garage didn't respond" instead of a blanket success — re-verified via a 3rd mocked scenario forcing one camera's trigger to reject. **Honest limits, not fixed:** the 7-second wait before reloading stills is a fixed guess at how long Blink needs to capture+upload — I have no way to confirm that's actually long enough for real hardware from this sandbox; if Jeff ever sees Refresh All say "Updated" but the picture still looks like the old one, that timing is the first thing to revisit. Also confirmed `loadCameras()` is wired into every relevant boot path (app open, HOME nav switch, token save, 60s self-heal) so this isn't a one-shot fluke. `lint-app.js`/`smoke-test.js`/JS-syntax clean. Bumped service-worker hcc-v46→v47.
- **08-08 (garage door app-side work done ahead of the hardware, latest):** Jeff: "do you want to get all the garage door stuff coded and ready for HA?" — while he's waiting to test the Sling chip. The old plan (Step 5 of the setup doc) was to wait for Jeff to hand over the real HA entity ID post-pairing before touching `loadGarage()`; built it generically instead so no hand-off is needed at all. **`loadGuardian()`/`loadGarage()` now understand three independent HA entity shapes**, matching the real two-part hardware plan: `cover.*garage*` (ratgdo, unchanged, in case that's ever adopted instead), `switch.*garage*` (the actual SONOFF MINI DRY relay — a momentary Inching-Mode pulse, correctly never treated as door position), and `binary_sensor.*garage*` (the future Zigbee contact sensor, Pending Item, not bought yet — shows real OPEN/CLOSED the moment it exists, independent of the relay). New shared `garagePick()`/`garageSensorIsOpen()` helpers (single source of truth, used by both functions so they can't drift apart). **Handled the position-sensor-naming trap proactively** rather than waiting to get burned by it live: a contact sensor named `..._closed` reads `on`=closed (inverted), one named plainly (`..._contact`) reads `on`=open — same gotcha CLAUDE.md already documents for the Mercedes window sensors — detected via the same "closed" substring check, not guessed. With no position sensor at all (the day-one state, since only the relay is ordered), the card is honest about it: "Ready" / "Unknown — add a door sensor," never a fake OPEN/CLOSED. `garageToggle()` now branches on the live entity's domain — `switch/turn_on` for the relay (single action, no separate open/close since Inching Mode self-resets), `cover/open_cover`/`close_cover` unchanged for ratgdo. Card copy (spec lines, footer note, the "SETUP" module placeholder shown before anything is paired) updated off the stale ratgdo-only wording to reflect the real SONOFF plan. **Verified via a 5-scenario mocked Playwright test** (no live HA to test against, per the Debugging Protocol) covering: switch alone, switch+open contact, switch+inverted-closed contact, cover-alone (regression check — confirmed byte-identical behavior to before), and `garageToggle()` on a switch (confirmed it calls `switch/turn_on` with the right entity, not a cover service). All 5 passed exactly as designed; screenshot-confirmed the switch-only card reads cleanly. `lint-app.js`/`smoke-test.js`/JS-syntax clean. Bumped service-worker hcc-v45→v46. **Nothing left for me to do here** — next step is 100% hands-on (wire it, eWeLink-pair, set Inching Mode, Matter-commission into HA), and the app will pick it up automatically the moment it's live, no further code changes or entity-ID hand-off required.
- **08-08 (Watch Sling chip added to Guardian, latest):** Jeff: "add watch sling button to the app in the Guardian section so it pulls up the browser for sling... just need access to be through the app." Added a `SLING` chip to Guardian's existing "Quick Actions" grid, right next to the Fire TV `WATCH` chip — calls the already-established `openExternal('https://watch.sling.com')` helper (a real `<a target="_blank" rel="noopener">`, click-triggered programmatically — not `window.open`, which is a no-op in an installed iOS PWA, the same fix class as the 07-31 stability audit). Verified the click actually creates a real anchor with the correct href/target/rel (not just that the function runs) via a Playwright test that intercepts `document.createElement`. **Honest scope note:** this opens Sling's web player in Safari — it can't force a login or auto-start playback from inside the app, that's entirely Sling's own site; it works exactly like tapping the same URL by hand, which Jeff already confirmed plays live TV instantly on the kitchen iPad because that Safari has a saved Sling session. Also completes the Quick Actions grid to an even 2×4 (was 7 chips + 1 full-width, now 8 + 1). `lint-app.js`/`smoke-test.js`/JS-syntax clean. Bumped service-worker hcc-v44→v45.
- **08-08 (irrigation zone cards: fixed Garden's missing live photo + made zone cards responsive; found all 6 zone photos carry the same fake-ad frame as the utility photos, latest):** Jeff sent zone-6.jpg (the "Garden" zone thumbnail) and said "clean it all up and make it perfect on all platforms... the garden zone picture is missing." Two real, separate things here. **(1) Real code bug, fixed:** `loadIrrigationFromHA()` (the live-B-Hyve-via-Beehive path) extracts each zone's station number by regexing the first digit out of its HA `entity_id` (`switch.bhyve_zone_3` → 3) — but a "Garden" zone entity in HA won't reliably carry a literal "6" the way the numbered sprinkler zones do (could be named `switch.garden_zone` with no digit at all, or one with an unrelated number), so the digit-parse silently missed it and the card fell back to the empty-truck placeholder instead of `zone-6.jpg` — the literal "picture is missing" Jeff saw. Fixed by name-matching "garden" first (case-insensitive, checked against both the entity's friendly name and its entity_id) and only falling back to the digit-parse for the numbered zones. Verified the matching logic directly (not guessed): `switch.garden_zone` → 6, `switch.orbit_bhyve_301_garden` → 6, `switch.bhyve_zone_1`/"Front Right" → 1, unaffected. **Could not test against Jeff's real HA entity name for this zone** (no network path from this sandbox) — the fix covers every naming pattern I could construct, but if it's still missing after this deploys, the one thing only Jeff can tell me is the exact entity_id HA gave that switch. **(2) Responsive gap, fixed:** `.zone-photo` was a hardcoded 95×95px thumbnail with zero scaling at any breakpoint — the one piece of UI in the whole app that didn't grow at all on iPad/web/TV, unlike everything else (now) does. Added scaling into the existing 768/1200/1920/2560px media-query blocks (95→130→160→200px, matching text/badge/button sizes alongside it). Verified via a 3-width Playwright sweep of the zones list: zero overflow at phone/iPad-landscape/TV, screenshots confirm proportional growth. **(3) Real finding, not yet acted on — flagged to Jeff, needs his call:** while screenshotting the TV-width zone cards to verify the sizing fix, found that **all 6 zone photos** (`zone-1.jpg` through `zone-6.jpg`, not just Garden) carry the identical fake-ad-card treatment as the pre-08-06 utility meter photos — a gold picture-frame border, a "ZONE N" badge, a bold title, "IRRIGATION ZONE" subtitle, and a marketing tagline ("Pride of Place. Every Detail Counts.," etc.), all baked into the image. This directly conflicts with this file's own protected photo note above, which currently calls these "REAL PHOTOGRAPHS OF JEFF'S ACTUAL YARD, just enhanced" — that note was wrong, or these 6 were swapped for AI mockups at some point without the note being corrected; either way, what's actually in the repo today (all added together in commit `492252b`, "from Jeff's uploads") is the same style of fake marketing frame the utility cards had before their real fix. **Did not touch the photos.** This cloud sandbox has no image-generation or editing tool (checked: no ImageMagick, no AI image API — only bare Pillow, which can crop/resize but can't cleanly remove a baked-in gradient+text overlay the way the utility photos were properly redone via the Gemini image-edit API) — that capability only exists on the coworker's PC (`google_ai_key.txt`, per the Cloudflare Infrastructure section). Recommended path: hand this to the coworker session using the exact same proven pipeline as the 08-06 utility-photo fix. **Needs a decision from Jeff first, not just Garden:** does he want all 6 zone photos cleaned (matching what was just done for water/gas/electric), or only Garden/zone-6? `lint-app.js`/`smoke-test.js`/JS-syntax all clean. Bumped service-worker hcc-v43→v44.
- **08-08 (coworker's photo-overlay readouts fixed to scale correctly on web/TV, latest):** Jeff: *"coworker did some amazing work with the app and pictures however I need you to go in and fix the sizes so they resize correctly on all formats tv, web, iPad, phone etc."* Built a real cross-format harness (`hccSection()`-driven Playwright sweep at 390/768/1024/1440/1920px, realistic mock data matched to each field's actual JS-written format, not guessed placeholder text) covering all 5 of the coworker's new inlaid-photo readouts (Weather conditions, Irrigation controls, Yard mower stats, Guardian 8-check grid, Car cockpit/dashboard). **Two apparent bugs from an early pass turned out to be false positives from my own test script, not real bugs** — worth recording so it isn't re-investigated: `hccSection('irr')` silently no-ops (the real id is `'irrigation'`, confirmed via the nav button's own `onclick`), so the irrigation panel measuring 0×0 was my script never actually switching to that section; and the "truncated" Rain Delay/Next Service text was my mock data using full sentences ("Rain Delay: 24h remaining") the real code never produces (`irrRainDelay` is `'24 h'` or `'None'`, `yardHeroSvc` is a bare `'8.0 hrs'` — traced both to their actual `.textContent=` call sites before concluding). **The one real, confirmed, systemic bug:** every one of these 5 overlay families sizes its text/icons/padding with a `vw`-based `clamp()` whose max value saturates around 740-820px of viewport width — meaning on any screen wider than that (iPad landscape through a 1920px TV) the text is frozen at its cap while the photo underneath keeps growing via the app's own existing `768/1200/1920/2560px` media-query breakpoints (used everywhere else — nav, card titles, hero titles). Confirmed visually: the Guardian TV screenshot showed 14px status text lost against an 800px-tall photo. Fixed by raising the clamp max ceilings (not adding new media-query blocks, to stay consistent with how these overlays were already built) across `.wx-hero-lbl/-val`, `.irr-hero-lbl/-val/-btn`, `.grd-hero-ico/-lbl/.hs-val`, `.yard-hero-lbl/-val`, `.car-hero-lockstate/-btn`, and `.car-screen-val/-lbl` (kept more conservative here — these sit inside the tiny ~16%-of-photo dashboard-screen area, not the full-width glass panels, so less headroom before overflowing the screen graphic itself). Re-verified the full 5-width × 5-section sweep after the fix: zero `docOverflow`, zero photo-overflow, zero text-overflow anywhere, and confirmed visually via screenshots that phone/iPad rendering is pixel-identical to before (the old caps were never reached there) while TV now reads as a legible, properly-scaled dashboard instead of tiny text on a huge photo. `lint-app.js`/`smoke-test.js`/JS-syntax all clean. Bumped service-worker hcc-v42→v43.
- **08-06 (MyQ getting sold, door position sensor now in scope):** Jeff decided to sell both MyQ parts (hub + sensor) on eBay rather than keep a separate app just to check door status — reasonable given the whole point of this project. Flagged one thing before he listed it: standard MyQ kits (and what CLAUDE.md had recorded as his hub's model number, which research shows is actually the *add-on sensor* SKU, not the hub — corrected in the Garage Door section) bundle real door-position sensing hardware, which the new Sonoff relay setup doesn't replace (no position sensor, that was explicitly deferred as "future, not needed" until now). Jeff's response: sell it anyway and get a sensor that shows in the app instead. Researched the right kind rather than guessing — recommended a **Zigbee door/window contact sensor** (SONOFF SNZB-04P, matches the Zigbee coordinator ecosystem already planned; Aqara as the alternative) over a wired reed switch (fits the existing Zigbee-coordinator plan, no extra wiring) and over a tilt sensor (simpler/cheaper, tilt sensors have their own dust/reliability quirks — reserved as a fallback if contact-mounting proves finicky). Logged in both CLAUDE.md and the setup plan doc. Nothing to build yet — waiting on the Zigbee coordinator + this sensor to actually be bought and paired.
- **08-06 (Garage door SONOFF MINI DRY arrived, full setup plan researched and written):** Jeff's part landed and asked for research + a plan before he starts wiring — right instinct given the garage-door hardware area already burned trust once (the ratgdo/SONOFF-Basic/SONOFF-SV guessing saga, 08-05). Researched properly this time (SONOFF's own docs/help center + independent reviews, not memory) and wrote the full plan to `docs/beehive/garage_door_sonoff_mini_dry_setup_2026-08-06.md`. Two real findings worth flagging: (1) the device needs AC **or** DC power (not both) via separate terminals, and which one to use depends on what's physically available at the opener — genuinely can't be decided remotely, flagged as an on-site call. (2) **The Inching (momentary-pulse) setting — the whole reason this device works for a garage door instead of just turning the light on/off style — can ONLY be set via the eWeLink app; HA's Matter integration doesn't expose it at all.** Missing this step would mean the relay stays latched on instead of pulsing like the wall button. Also resolved the old "unconfirmed, verify once paired" note from 08-05: independent testing confirms this shows up in HA as a plain `switch.*` entity via Matter, not a `cover` — decided to handle that on the app side (adjust `loadGarage()` to recognize a plain switch once given the real entity ID) rather than have Jeff/coworker write HA-side template-cover YAML. Nothing to test yet on my end — this is a hands-on wiring+pairing job for Jeff/coworker; app-side work is queued for once the real entity ID exists.
- **08-06 (Smart Lighting plan logged):** Jeff sent his finished electrical plan (2 PDFs — project summary + bedroom wiring diagram) for the Kasa HS220/HS200 lighting project and asked to save it + get my read on it. Saved both as markdown in `docs/lighting/` (kept the wiring/box detail verbatim — this is real electrical work, not something to paraphrase loosely), added a summary paragraph to the Beehive/HA section above, and logged the one real open question as Pending Item 19. Reviewed the plan rather than just filing it: verified via research (not eyeballed) that the HS220's 150W-LED rating comfortably covers the 108W bedroom/kitchen loads, confirming the plan's own reasoning for ruling out MOES's 100W/gang limit. Found one real issue the plan doesn't address: the garage's 2-location (kitchen+garage) switch setup is shopping-listed with a single HS200, but a lone HS200 can't drive two switch locations — confirmed via research the OTHER location goes dead; needs either the HS210 kit or a conscious decision to cap the kitchen position (same repurpose-as-dumb-switch pattern the plan already uses in the bedroom). Everything else in the plan — the bedroom's reversed-feed redesign, repurposing the middle/bedside toggles as independent switched-receptacle controls instead of blank plates, device choice over MOES/Shelly — checked out as sound, no other issues found.
- **08-06 (coworker — utility card text moved ONTO the photo):** Jeff: *"you can make all that shit that was underneath the pictures before all fit on the picture now."* With the fake marketing copy gone there was finally room, so the name, provider, LIVE/WAITING chip and the "Live from Beehive" status line all moved out of `.util-body` and into `.util-overlay-bottom`, which is now one left-hand column: header on top, blurred readout in the middle, status line at the bottom. Only the billing-history / Last-7-Days tables stay below (they're real tables); `.util-body` keeps horizontal padding only so it collapses to zero height while hidden, and the gas card has no body element at all now. The provider line ("White House Utilities • flowIQ 2100") is `display:none` on the overlay on purpose — at 41% column width it wrapped to three ragged lines and shoved the chip out of the column, and it's redundant now that each photo shows that utility's real logo on the meter itself. **Two real bugs the width sweep caught that eyes didn't:** (1) at 320/375px the chip crowded the card name into a clip ("Natural Gas" cut to 35px of a 64px word) — fixed with `flex-wrap` + a real `flex-basis` so the chip drops to its own line, plus letting the name wrap instead of `nowrap`; (2) an earlier edit left comment prose *outside* its `/* */` block, which silently killed the entire `.util-overlay-bottom` rule and made all three readouts vanish — caught because the parsed CSS rule count dropped, which is worth checking whenever a rule mysteriously stops applying. Re-verified at 320/375/390/430/768/1024/1440: zero clipped cells, every panel inside its banner, zero horizontal overflow, LUX untouched. `lint-app.js`/`smoke-test.js` clean. Bumped hcc-v32→v33.
- **08-06 (coworker — the three utility photos REGENERATED with the fake marketing copy removed; image-gen API now available, latest):** **This is the root-cause fix for every utility-card fight in the entries below.** Those photos were AI marketing mock-ups: half of each frame was a fake ad ("WHITE HOUSE UTILITIES COMMAND CENTER", "SMART MEASUREMENT. EVERY DROP COUNTS.", the SAFE SUPPLY / USAGE INSIGHTS icon strips). Every constraint in this area — the 46% width, the per-card hand-calibrated `top` values, the collisions, the first attempt's total revert — existed only to dodge text that was never real. **Jeff funded a Google AI Studio key**; all three photos were re-generated through the Gemini image-edit API (`gemini-3.1-flash-image`, the #1-ranked image editor as of 08-06) with every overlay removed, the real meter left untouched and sharp, and the real embossed/printed utility branding kept (WHUD globe on the bezel, spire label on the gas meter body, CEMC on the electric meter face). **~9 cents per image, first try, no retries — 3 images total.** Re-installed at one identical 1320×886 (ImageMagick), so `.util-banner` is now ONE shared aspect-ratio rule instead of three, and **the per-card `top` calibration is deleted entirely** — the readout simply sits centred on the clean left side of all three (`left:5%; top:50%; translateY(-50%); width:41%`). Only remaining constraint: each meter's left edge starts ~45% across, don't widen much past 41%. Verified at 320/375/390/430/768/1024/1440: zero clipped cells, every field inside its banner, zero horizontal overflow, LUX untouched. `lint-app.js` + `smoke-test.js` both clean. **Originals are recoverable from git history — not backed up separately, don't add duplicate copies to the repo.** Full-res 2528×1696 versions live in `C:\Users\jeffl\iCloudDrive\HCC-Photos\`.
  - **NEW CAPABILITY, use it: image generation/editing is now wired up on the coworker's PC.** Key at `C:\Users\jeffl\HCC-secrets\google_ai_key.txt` (BOM-free, outside the repo — **the repo is public, never commit it**). Helper script pattern: POST to `https://generativelanguage.googleapis.com/v1beta/interactions` with `x-goog-api-key`, body `{model, input:[{type:text},{type:image,mime_type,data:<base64>}], response_format:{type:image,mime_type,aspect_ratio,image_size}}`. **The returned image is at `steps[].content[]` where `type=='image'` — do NOT grab the first long string in the response, `steps[0].signature` is a ~5MB non-image blob that saves as a corrupt file.** Jeff prepaid $25 of credits (Cloud Prepay, not an open card), so spend is capped by design. **Also now installed on the PC: ImageMagick 7.1.2-29** (`C:\Program Files\ImageMagick-7.1.2-Q16-HDRI\magick.exe`) for crop/resize/compress/composite, and **Node LTS + Playwright** so both project gates run locally.
  - **Photo drop-folder for Jeff:** `C:\Users\jeffl\iCloudDrive\HCC-Photos\` — he puts project pictures there (free, no upload through chat) and the coworker reads them straight off disk.

- **08-06 (coworker — utility readouts: boxes removed, numbers in a blurred field, logos kept; PC toolchain fixed, latest):** Jeff, watching it render live: *"do we even need the boxes — why don't you completely blur out a section right below where the utility logo is and just put the numbers in that blurred area without the individual boxes… leave the logos."* **The whole chip/box/divider structure is gone.** Each readout is now a single softly-blurred field of the photo itself (`rgba(20,18,16,.18)` + `blur(26px) saturate(160%) brightness(.92)`, no border, one soft shadow), starting just **below each photo's own real logo** — which lands it precisely on the photo's baked-in marketing copy and fake icon strip, so the only thing it covers is the only thing that was never real. **No new image assets:** an earlier pass cropped the three logos into separate files, but the photos already carry them, and a cropped chip rendered *on top of* the photo's own logo (visible ghosting) — files deleted, service-worker precache reverted. **Two hand-calibrated constants, don't change without re-checking all three photos:** width 46%/left 4% (each meter's left edge starts 48-53% across), and per-card `top` set just under that photo's logo — **lower % = field starts higher = covers more** (Water 28.5% clears the globe badge which ends at 27.6%; Gas 29.5% clears the spire mark, ends 28.6%; Electric 33% clears the CEMC oval, ends 31.3%). Verified at 320/375/390/430/768/1024/1440 against real live data: zero clipped cells, every field inside its banner, zero horizontal overflow, LUX panel untouched and overflow 0. **The `lint-app.js`/`smoke-test.js` gap from the earlier entries is CLOSED — Jeff offered ("I can get that tool or anything going"), so Node LTS + Playwright/Chromium are now installed on the PC and BOTH project gates ran clean here for the first time** (smoke-test: 6 nav sections, 7 YARD tabs, 11 CAR tabs, 8 Guardian chips, 4 modals, 374 external links, 0 bad, 0 page errors). Also made `scripts/smoke-test.js` portable — it hardcoded the cloud sandbox's `/opt/node22/...` Playwright path and pinned `executablePath`; it now tries that path first (cloud unchanged) and falls back to a normal `require('playwright')`, leaving `executablePath` unset so Playwright finds its own browser. **Answering Jeff's direct question honestly: not all 12 guide steps are in.** Live: 1 frosted glass, 2 gradient-ring edge glow (LUX), 3 transparent gradient, 4 ambient warm tint, 6 floating glass buttons, 8 warm radial image lighting, 9 low-contrast panels, 11 single soft shadow, 12 warm off-white type with glow. **Deviated:** 5 (photo blur pulled on Jeff's call — a 2px blur on a 1320px source at ~350px card width reads as a broken render; darkening only). **Not done:** 7 (mask-gradient edge fade — the blurred field has a clean rounded edge instead) and 10 (perspective tilt — deliberately skipped, tilted overlays were part of what made the first attempt look wrong).

- **08-06 (coworker — utility cards rebuilt to match the LUX card, latest):** Follow-up to the glass-overlay entry below, all four driven by Jeff watching it render live at real phone width and calling changes. (1) *"Make the rest of the utilities look like the lux… two columns and shrink it down so the meter itself shows like you did with the thermostat"* — the Water/Gas/Electric readouts moved from a full-width slab across the bottom of the photo to a **left-side glass panel, the same shape as the LUX card**, leaving the actual meter fully visible on the right. **47% width is the load-bearing constant:** on all three photos the meter's left edge starts around 48-53% across, so anything wider starts eating the meter — don't widen without re-checking all three. Water = 2 cols × 4 (bottom-anchored, content height), Electric = 2 cols × 3, Gas = 1 col × 3 (2 cols would orphan a cell); Gas and Electric additionally run **full height (`top:3%`)** because with only 3-6 stats a content-height panel lands its top edge mid-word on the photo's baked-in "SPIRE ENERGY"/"CUMBERLAND ELECTRIC" headline — full height covers the headline outright, same trick as LUX. Water keeps content height: full height would squeeze its 8 rows to ~27px. Old `.util-stats-grid3`/`-grid4` classes replaced by `.util-stats-2col`/`-1col` (grepped first — zero JS references). `grid-auto-rows:1fr` equalizes rows so cells with a unit line don't come out taller than cells without one. (2) *"Those boxes don't need to be near that big"* — cells sized to content rather than stretched. (3) *"Make the boxes translucent like the lux"* — root cause was **not** the glass values (identical to LUX's) but the ambient gradient: the panel had moved onto the bottom-left of the photo, exactly where `--glass-ambient`'s floor shadow was darkest, so a .20-alpha panel rendered as a solid black box. Softened the util banners' floor shadow to `rgba(0,0,0,.18)` (LUX keeps the full .45 — its panel sits up top where that gradient is still transparent) and retuned the panel to `rgba(20,18,16,.26)` + `brightness(.92)`, checked for text contrast against the brightest backdrop of the three (the gas photo's sunset). (4) Type/labels up to the LUX scale. **Real bug found and fixed by the width sweep, not by eye:** at 320px Water's 4 rows came out 7.7px taller than the photo, so the panel's top edge broke out of the banner and got clipped — a `max-width:360px` media query trims ~4px of vertical rhythm per row (16px total), everything ≥360px untouched. Re-verified at 320/360/375/390/430/768/1024/1440: **zero clipped cells, every panel inside its banner, LUX panel overflow 0.** Edited file rendered from a local HTTP server: 694 CSS rules parse, all 6 sections, 6 nav buttons, 7 YARD + 11 CAR tabs, no duplicate IDs, zero console errors.

- **08-06 (coworker — Luxury Glass Overlay redesign, second attempt, DONE and visually verified against real live data, latest):** ⚠️ **Coordination note for the cloud session: this is a rare app-code commit made by the COWORKER, at Jeff's explicit request** — he asked for it here specifically because the cloud session can't see the rendered app, and the previous attempt failed for exactly that reason. `git pull` before touching `index.html`. **Root cause of "the overlays are all jacked up and don't even fit in the pictures," found by measuring the real live app at a true 390px phone viewport (not assumed):** (1) the util stat chips were `display:flex` + `flex-wrap` with auto-width chips, so rows came out ragged and misaligned (Water's 8 chips wrapped 4/4 with every column at a different x), and the photo's own baked-in icon strip showed through the gaps *between* the floating chips; measured the collision precisely — Water's chip block started at 71.1% of photo height, Gas at 85.2%, Electric at 66.6%, and each photo's printed icon/label strip occupies 67–88%, i.e. a direct overlap on all three. (2) `.lux-fp-panel` had a hard `height:48.3%` that its own content outgrew at phone width (`scrollHeight` 121 vs `clientHeight` 117) — the device name + refresh button were literally spilling out of the panel and sitting loose on the photo. **Fix:** applied the two reference docs as a shared glass module (new `--glass-*` tokens in `:root`, deliberately fixed light-on-dark since these panels sit on permanently-dark photos in both themes). The 8/3/6 floating chips became ONE frosted-glass slab per card (`backdrop-filter:blur(24px) saturate(180%) brightness(.75)`, `rgba(22,22,22,.20)`, 1px gradient ring via a masked `::before`, one large soft shadow, warm `#f7f1e7` type with a soft glow) containing a real even grid — new `.util-stats-grid4` for Water, `.util-stats-grid3` for Gas, reusing the existing one for Electric — with hairline dividers instead of per-cell boxes, so there are no gaps left for the photo to bleed through. Units moved to their own line (`display:block`) so a long real reading like "19,334.7 gal" can never widen a cell past its column — **the exact failure the first attempt missed by testing with short "—" placeholders.** LUX panel switched to `top:3%;bottom:3%` + `justify-content:space-between` (both on Jeff's live call — "make the box go all the way down to the bottom of the picture or center it so there is not all that dead space"), which makes content overflow structurally impossible, covers the photo's entire baked-in ad block in one move, and fills the glass evenly instead of pooling the controls at the top; all LUX panel type/control sizes then went up ~20% ("maybe a little bigger"), re-verified for clipping at 7 widths afterwards. CALL button + all LUX mode/fan/adjust buttons became glass (guide item 6). **Deliberate deviations from the docs, both worth keeping:** the guide's `blur(2px)` on the host photo was applied, shown to Jeff, and pulled on his call ("not so much blur in the picture") — at a ~350px card width a 2px blur on a 1320px source reads as a broken render; focus separation is carried by `brightness(.82)` + the slab's own backdrop blur instead. Guide item 10 (perspective tilt) was skipped outright — tilted overlays were part of what made the first attempt look "jacked up." **Verified on real live data, not mocks** (the whole point of doing this on the PC): drove the deployed app in a real Chrome at true 390px via a same-origin iframe rig, so real `ha_token`/LUX creds and real long values were in play throughout — Water 19,334.7 gal / $94.79 combined, Gas 8,853.40 ccf, Electric 209 kWh / 10-11 AM peak. Geometry-checked at 320/390/430/768/1024/1440px: **zero clipped cells anywhere**, LUX panel content overflow 0 at every width, slab always inside its banner. Both themes checked (overlay identical in light mode as designed). Actual edited file separately rendered from a local HTTP server: 693 CSS rules parsed, all 6 sections, 6 nav buttons, no duplicate IDs, nav/tabs exercised, **zero console errors**. Bumped service-worker hcc-v27→v28. **Honest caveats:** (a) `lint-app.js`/`smoke-test.js` were NOT run — Node isn't installed on the PC, so the checks above are the substitute, not the project's usual gate; installing Node here would close that gap for good. (b) A pre-existing 46px horizontal overflow at a 320px viewport, coming from `.snav-btn` (not from this work — confirmed identical with these styles disabled), is untouched; no device Jeff uses is that narrow.
- **08-06 (LUX finally "stays logged in" — refresh token was requested but never used)**: Right after the credential-wipe fix below, Jeff pushed back with a sharper diagnosis: "I can login fine it just won't stay logged in... does it need a token? All the other things stay logged in." Checked, and he was right — `climate.js`'s OAuth `SCOPE` constant explicitly includes `offline_access` (the scope that gets you a refresh token specifically so you don't have to fully re-login) but the code only ever cached the short-lived `access_token` and threw `refresh_token` away completely. So every time the cached access token expired (~1hr) — or even just missed the cache for unrelated reasons, e.g. Cloudflare KV's normal cross-edge propagation lag — the ONLY fallback was the full slow multi-step interactive B2C login (the same fragile flow the earlier fix already flagged as failure-prone), never a quick token refresh. That's the real reason it couldn't "stay logged in" the way the HA token/family login/B-Hyve/Blink integrations do — those are all single long-lived tokens; LUX was silently rebuilding its session from scratch far more often than it needed to. **Fix:** `climate.js` now stores `refresh_token` alongside `access_token` in KV, and added `refreshTokens()` (a single POST to the same B2C token endpoint with `grant_type=refresh_token` — skips the whole authorize/SelfAsserted/confirmed cookie dance entirely). `withAuth()` now tries, in order: valid cached access token (0 network calls) → refresh-token exchange (1 fast/reliable call) → full interactive login (only if the refresh token itself is missing or dead). Verified via a mocked `/api/climate` test covering all 4 real paths: cold cache does exactly 1 full login and saves the refresh token; a valid cached token does 0 calls; an expired access token with a live refresh token does exactly 1 refresh call and 0 full logins; a dead refresh token does exactly 1 refresh attempt then falls back to exactly 1 full login (not a loop). Client-side `index.html` untouched this round — no service-worker bump needed, `/api/*` calls are never cached by the service worker. `lint-app.js`/`smoke-test.js` clean. **Still can't test the real live Azure B2C token endpoint itself** (no network path from this sandbox) — ask Jeff to confirm over the next several days/app-closes whether it now stays connected without re-prompting.
- **08-06 (LUX "requires login every time" — real root cause found, second time):** Jeff asked why LUX keeps requiring login. The 08-04 fix (KV token caching in `climate.js`) was real but explicitly flagged as unconfirmed live ("ask Jeff to report back if it still happens") — he just reported it still does, so re-audited the code instead of assuming that fix was the whole story. Found a real, separate bug it didn't cover: `climate.js`'s B2C login step classifies ANY `"status":"400"` or `"status":"401"` substring in the SelfAsserted response as `login_failed` (bad password) — a loose match that can't actually distinguish "wrong password" from a transient Azure B2C session/CSRF hiccup in the multi-step cookie-carrying flow (which runs stateless on Cloudflare Workers, unlike a real browser session). `loadClimate()`'s catch handler wiped `lux_email`/`lux_pass` from localStorage and popped the login form back up on ANY `login_failed`, including ones fired from the automatic background refresh (app open, section switch, 60s self-heal) — so any transient B2C hiccup, not just an actual wrong password, was silently deleting Jeff's working saved login and forcing a re-type. **Fix:** `loadClimate(isManualLogin)` now only wipes credentials + shows the setup form when `isManualLogin` is true — set only by `saveLuxCreds()`/`acctSaveLux()` (the two places the user just typed a password into the visible form). Automatic/background calls that hit `login_failed` now just show the error banner and retry next cycle with the same saved creds, self-healing like every other HOME card. Verified via a mocked `/api/climate` test forcing `login_failed` on both paths: automatic refresh → creds preserved, setup form stays hidden; manual save → creds correctly wiped, setup form reappears. **Could not test the real live Azure B2C flow itself** (no network path from this sandbox) — same honest caveat as the 08-04 fix; ask Jeff to confirm over the next few days whether the login form still reappears on ordinary app opens.
- **08-06 (removed the single-cycle "Sewer overcharge" note from the Water card):** Jeff sent a screenshot of the red "Sewer overcharge (rough estimate...): ~5,098 gal/mo is irrigation... Ask City of WH about seasonal sewer averaging or an irrigation meter" box and asked for it out. Removed the HTML container (`#util-irr-sewer-note`) and both places that populated it (`putWaterCycle()`'s schedule-based estimate, `irrGalFromHistory()`'s real-B-Hyve-data refinement) plus the now-unused `irrSewerNoteHtml()` function entirely. **Deliberately left everything else in this area alone** — the Billing History table (`#util-water-history`/`renderWaterHistory()`), its "Total sewer overcharge tracked" running-total banner, and the underlying `irrGal`/`waste` calculation and tracking (`irrMonthlyGal()`, `irrGalFromHistory()`, `IRR_FLOW`) are a separate, still-live feature Jeff didn't ask to remove — only the single-cycle note text box came out. `lint-app.js`/`smoke-test.js`/JS-syntax clean.
- **08-06 (Luxury Glass Overlay redesign — attempted, then fully reverted per Jeff):** Jeff sent 2 reference docs ("Luxury Dashboard UI Framework With Code" + "...Glassmorphism Guide") asking to apply that glassmorphism style to LUX + the 3 utility cards' photo overlays. Built it in stages over several commits (frosted glass, gradient-ring borders, warm typography, softened/darkened host photos, subtle tilt/edge-fade on every element) and believed it was solid after passing lint/smoke-test and my own mocked-data Playwright screenshots. **It wasn't.** Jeff sent 5 real screenshots from the live app with real data and the visual result was bad: on Water/Gas/Electric, each photo's own baked-in decorative icon-label row (e.g. "Safe Supply/Usage/System/Energy") was colliding with/bleeding through the stat chips — my own testing had used short placeholder values ("— gal") which never made the chip row wide enough to visibly hit that content, so I missed it entirely, then wrongly told Jeff Water was fine when it wasn't (never actually checked it against real long data). Attempted two rounds of fixing the darkening and still hadn't nailed it when Jeff said stop. **Reverted index.html/service-worker.js/CLAUDE.md entirely back to commit `7cccc59`** (the state immediately before any of this redesign started) — full plain-photo look restored, verified via screenshot against Jeff's real complaint that it now matches the original. **Lesson for next time this comes up:** if a redesign is revisited, (1) test every visual claim against realistic LONG real-world data from the start, never short placeholders — this class of bug is invisible with a "—" value and only appears once a chip row is wide enough to reach content elsewhere in the photo; (2) get Jeff's visual sign-off on one card before rolling a treatment out to all of them; (3) the two reference docx files are still wherever Jeff originally sent them if this gets revisited, but nothing from them is live in the app right now — treat this as a clean slate, not a continuation.
- **08-06 (Irrigation GPM calibrated to real measured data):** The coworker ran isolated single-zone tests on zones 1/2/5 tonight (nothing else running, timed via HA switch on/off timestamps, gallons measured via real water-meter before/after deltas) — found the original `IRR_FLOW` constants were based on wrong head counts/nozzle model. Updated `IRR_FLOW` from `{1:17.2,2:14.3,5:5.7}` to the real measured `{1:8.78,2:10.09,5:4.4}` — a 23-49% cut depending on zone. Full derivation (predicted-vs-measured against Hunter's own MP Rotator spec sheet, real head/arc config confirmed by Jeff) in `docs/utilities/irrigation_gpm_calibration_2026-08-06.md`. `lint-app.js`/`smoke-test.js` clean. **Important honest caveat, per the coworker's explicit ask:** this means the "Total sewer overcharge tracked" running total has very likely been significantly overstated for any past cycle that included real watering on these zones — `irrGalFromHistory()` self-corrects automatically going forward from today, but already-closed cycles under the old estimate won't retroactively fix themselves (no HA history exists to recompute them, same limitation as the recorder-retention gap in `sewer_overcharge_coworker_verification_2026-08-05.md`). Read the running total as "accurate from 2026-08-06 forward," not "accurate for its whole tracked history." Zones 3/4/6 remain uncalibrated (zone 3 has a known bad head, needs fixing before it's worth measuring; 4/6 just never tested yet) — **genuinely low priority per Jeff (08-06): zones 1/2/5 are the front yard and are what actually matters; the back yard (3/4/6) is rarely watered at all except in severe drought.** No rush on these; when they do get tested, same method (isolated zone + HA switch timing + water-meter delta) applies — wire in the same way `irrGalFromHistory()`/`IRR_FLOW` already work for 1/2/5.
- **08-06 (Electric SmartHub real bugs found + fixed via live coworker verification):** The coworker fired the exact WS command from `ha-stats.js` directly against Jeff's real HA and found the feature shipped earlier tonight was silently non-functional despite the UI rendering correctly. Fixed both real bugs: wrong WS command name (`history/statistics_during_period` → `recorder/statistics_during_period`, HA-version-specific) and wrong field assumption (`change` always reads 0 for this sensor — rewrote the computation to diff consecutive cumulative `sum`/`state` readings instead, `toDiffedSeries()`). Re-verified with a mocked test built from the coworker's exact real data. Full detail in the ELECTRIC reference section + Pending Item 18. `lint-app.js`/`smoke-test.js` clean. Bumped service-worker hcc-v16→v17.
- **08-06 (full cross-format QA pass):** Jeff asked for a full pass confirming everything renders correctly on phone/iPad/web/TV. Ran a 5-viewport Playwright sweep (390/768/1024/1440/1920px) across all 6 sections + every YARD/CAR sub-tab. Found and fixed the real, long-standing Pending Item 7 (desktop hero gap) — root cause and fix detail in the Pending Items entry below. Confirmed zero horizontal overflow anywhere, all 6 section heroes' baked-in titles stay uncropped at every size, and the Electric card (tonight's earlier work) scales cleanly from phone to TV. `lint-app.js`/`smoke-test.js` clean.
- **08-06 (deploy-branch mystery resolved):** Discovered mid-session that `Toro-Timemaster-` is a second GitHub repo with diverging history from this one — both repos' own `CLAUDE.md` claimed to be the one Cloudflare Pages deploys, and this cloud session had no tool that could read Cloudflare's actual Pages-project git config to settle it (confirmed real, live Cloudflare account access via a matching `MOWER_KV` namespace ID, but the connected MCP tools only cover Workers/D1/KV/R2/Hyperdrive, not Pages settings; direct HTTPS to the live site is also blocked by this sandbox's network policy). Wrote up the full evidence trail in `docs/repo_deploy_mystery_coworker_ask_2026-08-06.md` for the coworker, then pushed tonight's tested Electric SmartHub work to this repo's `claude/time-master-project-liq1jw` as the best-evidence call (Toro-Timemaster-'s history shows one-way "Sync from Master-the-Master-" commits through 07-24, never the reverse — this has always been the real one). **Confirmed 08-06 — Jeff opened the live app and saw the new Electric card cells: this repo (`Master-the-Master-`) is the one Cloudflare Pages actually deploys.** `Toro-Timemaster-` is a stale, diverged mirror — do not develop there going forward; see the note in "What This Project Is" below.
- **08-06 (Electric SmartHub real data):** Built the app-side upgrade from `docs/utilities/electric_smarthub_data_upgrade_2026-08-06.md` (coworker verified live: SmartHub integration already imports real hourly/daily statistics, separate from the monthly-total entity; also fixed the integration's poll interval 6h→30min live). Added `functions/api/ha-stats.js` (new — one-shot WS proxy to HA's WS-only `history/statistics_during_period`, no REST equivalent exists) + client-side `loadElectricStats()`/`haStatsFetch()`/`renderElectricHistory()` in `index.html`: real TODAY (replacing the old estimate), new YESTERDAY + PEAK HOUR stat cells, new "Last 7 Days" sub-panel. Also removed the old fake "Now" estimate entirely per the doc's explicit instruction — NOW stays honestly blank (—) until a real CT-clamp build exists. Old 24-bucket estimate kept as the fast fallback until real stats resolve, then silently overwritten — same pattern as the water/irrigation real-vs-estimate fixes. See full detail + the one open verification item (exact WS statistics field name) in the ELECTRIC reference section below. Bumped service-worker hcc-v14→v15 (index.html changed). Full regression (`lint-app.js`/`smoke-test.js`) clean; new code additionally verified via a dedicated mocked-`/api/ha-stats` Playwright test and a direct unit test of `ha-stats.js`'s request validation/error handling — **could not test the real WS round-trip against Jeff's actual HA, this sandbox has no network path there.** **Follow-up same day — Jeff flagged the 6-cell stat row looked jumbled:** found two real bugs, not a design nitpick. (1) `.util-overlay-bottom .util-stats` is `display:flex` with auto-width chips, not a grid — the inline `grid-template-columns` I'd used on the electric card was a dead no-op (same mistake Water's card already had, harmless there only by luck of shorter/more-uniform label lengths), so 6 chips wrapped raggedly (4 then 2) instead of an even 3×2 layout. Added a real `.util-stats-grid3` modifier class. (2) Peak Hour's "2.1 kWh" line was invisible — used `var(--muted)` (theme-relative) on a permanently-dark photo overlay, dark-on-dark in light theme, the exact bug class Pending Item 17 already tracks; switched to the existing fixed-contrast `.util-stat-unit` class and a clean explicit 2-line format. Verified via Playwright screenshots (phone/iPad, both themes, geometry-checked for zero overflow) — clean, even, readable grid in both themes now.
- **08-04 (LUX + Utility card buttons/text too small on iPad, latest):** Jeff reported the LUX mode/fan/adjust buttons are tiny on his iPad, then asked for the same fix on the Utilities (water/gas/electric) cards. Root cause, same for both: all the photo-overlay text/button sizing (font-size/padding) used fixed px values tuned for a ~390px phone card width — `.content` has no max-width cap (confirmed via grep), so the card genuinely renders much wider on iPad, but fixed-px elements don't scale up with it, so they stay phone-tiny in absolute terms while looking proportionally even smaller against the bigger card. Switched both to `clamp(phoneSize, Xvw, capSize)` so they scale with actual rendered width instead of staying frozen: LUX's `.lux-adj-btn`/`.lux-mode-btn`/`.lux-fan-btn`/`.lux-refresh-btn` (+ grid gaps), and Utilities' `.util-banner-call` (the CALL button) + `.util-overlay-bottom .util-stat`/`.util-stat-label`/`.util-stat-val`. Verified via Playwright across 3 viewports (phone 390px / iPad portrait 768px / iPad landscape 1024px): phone sizes are pixel-for-pixel unchanged from before on both cards (no regression), LUX mode buttons nearly double on iPad portrait (42.9×18.6px → 85.3×32.9px), Utilities CALL button grows 64.8×20px → 90×29.8px and stat text grows 7.5px → 11.5-12px. `lint-app.js`/`smoke-test.js` clean.
- **08-04 (LUX never loaded on app open — real root cause found):** Jeff sent a screenshot proving the two fixes below weren't the whole story: even with credentials correctly saved, opening the app still showed the "Loading thermostat…" placeholder and the empty LUX Account setup form — i.e. it looked identical to never having connected at all. Reproduced it myself with a Playwright reload test before touching anything: confirmed `lux_email`/`lux_pass` WERE present in localStorage after a reload, yet `luxSetupCard` was still visible and empty. **Actual root cause, unrelated to the auth/duplicate-ID bugs fixed earlier today:** when the LUX card moved from GUARDIAN to HOME on 08-03, `loadClimate()` was only wired into `hccSection('home', ...)` — the function that runs when a user taps a nav button to switch TO a section. HOME is already the default active section in the static HTML the moment the app opens, so that nav function never actually fires on a normal app open (only on manually navigating away and back). Every other HOME data source (cameras, utilities, home status) is bootstrapped separately through `checkBeehive()`'s own callback chain, which is why only LUX was left stranded — the setup card and "Loading…" banner are just the raw static placeholder markup, frozen, because nothing ever ran to update them. **Fix:** added `loadClimate(); loadStation();` directly to the app's real unconditional boot sequence (next to `loadWeather()`/`loadIrrigation()`), and also to the existing 60s self-heal `setInterval` (now safe/cheap to poll thanks to today's earlier KV token-caching fix) so LUX recovers on its own if a fetch ever fails while the app is sitting open, matching how every other HOME card already behaves. Verified via Playwright: fresh load with no saved creds now correctly shows "Enter your LUX login below to connect" immediately (previously stuck on the static placeholder); saving credentials then reloading the page (the literal "close and reopen the app" scenario Jeff described) now correctly hides the setup card and prefills the saved email — previously it reverted to the blank setup form every time despite the credentials being safely stored the whole time. Full regression (`lint-app.js`/`smoke-test.js`) clean.
- **08-04 (Connected Accounts modal — duplicate IDs fixed):** Follow-up to the LUX login fix below — the reason LUX's saved login sometimes never actually persisted traced to a second bug in the same area. `hiveShowAccounts()` was copying `#connAcctCard`'s `innerHTML` into the `#grdModal` popup on every open WITHOUT removing the original `#connAcctCard` node (which stays in the page, `display:none`, at line 1628 — before `#grdModal` at line 2986). That meant every `acct-lux-email`/`acct-ha-token`/`acct-lux-badge`/etc. id existed twice in the DOM at once whenever the modal was open. `document.getElementById()` always resolves to the FIRST match in document order — the invisible original, not the one the user is actually looking at and typing into inside the modal — so every read (Save button reading the typed email/password) and every write (badge flipping to SAVED ✓) was silently operating on a node Jeff never saw. Confirmed via grep that `connAcctCard` has exactly one other reference in the whole file (its own definition), so it's safe to consume once. **Fix:** cache the template's `innerHTML` into `ACCT_CARD_HTML` the first time the modal opens, then `.remove()` the original node — every future open reuses the cached string, so only one live copy of those ids ever exists again. Verified via Playwright: exactly 1 `#connAcctCard` node before first open, 0 after (confirming removal), exactly 1 of each `acct-*` id both on first open and after closing+reopening (confirming the cached-HTML path doesn't reintroduce the duplicate), and an end-to-end save — typed a real email/password into the LUX form, called the real `acctSaveLux()`, confirmed the badge actually flips to "SAVED ✓" (previously would have silently written to the wrong hidden node), then reopened and confirmed the saved email correctly prefills on tapping the row again. `lint-app.js`/`smoke-test.js` clean.
- **08-04 (LUX "requires login every time" fixed):** Jeff asked why the thermostat keeps requiring login. Root-caused two real, compounding bugs rather than guessing: (1) `functions/api/climate.js` ran the FULL 4-step Azure B2C login flow (authorize → SelfAsserted → confirmed redirect chain → token exchange) from scratch on literally every single `/api/climate` call — every app open, every HOME load, every 8s poll after a control tap — instead of reusing a token. Hammering B2C with that many full interactive-style logins is exactly the kind of pattern that trips transient failures/rate-limiting. (2) `loadClimate()` in `index.html` treated ANY error containing the substring "401" as "your saved password is wrong" and silently wiped `lux_email`/`lux_pass` from localStorage, popping the visible login form back up — but downstream API calls (`location_user`, `device_state`) can also 401 for reasons that have nothing to do with the password (an expired/rotated token), and those were getting misclassified as bad credentials too. Together: any transient hiccup in the now-very-frequent full-login flow silently deleted Jeff's saved login, forcing him to type it in again. **Fix:** added `withAuth()` to `climate.js` — caches the LUX access token in the same `MOWER_KV`/`HCC_KV` KV namespace (`getKV()`, same dual-name pattern as `hours.js`) with a 1hr expiry (5-min safety margin), reuses it across requests, and only falls back to a real B2C login on a genuine cache miss or if the API rejects the cached token with a real 401 (self-heals with exactly one retry, doesn't loop). Narrowed the client-side wipe condition in `index.html` to only fire on an actual `login_failed` message (real bad-password response from B2C step 2), not any generic 401 from later in the chain. **Verified on my end since this sandbox has no network path to Jeff's real Azure B2C tenant/credentials:** `climate.js` module-loads clean (syntax), and wrote a mocked-fetch+mocked-KV test (`withAuth` exercised directly through `onRequestGet`) proving all three real code paths: cold-cache first load does exactly 1 login, second load reuses the cached token and does 0 new logins, and a token going stale server-side triggers exactly 1 recovery login (not a loop) before succeeding. Full app regression (syntax/`lint-app.js`/`smoke-test.js`) clean. **One thing only Jeff can confirm:** that the login form actually stops reappearing day-to-day on his phone — ask him to report back if it still happens after this deploys.
- **08-03 (System Health merged + Fitness removed):** Jeff asked three things in one message: are the System Health numbers real, fold them into Ready to Mow (shrunk) if so, remove Fitness entirely, and remove the pre-mow sensor-reset reminder (he doesn't need the app to tell him that). **Investigated first rather than assuming:** read `calcHealth()` — Engine/Deck/Blades/Drive are real, computed from actual service-interval data (`RULES`/`svcStatus()`); Electrical/Fuel are hardcoded to a fixed 98% with no sensor behind them at all. Told Jeff the honest split rather than either shipping fake numbers as if live or leaving the question unanswered: kept the 4 real categories, left the 2 fake ones out entirely. Removed the standalone big circular gauge card (`cat-health`) and replaced it with a compact 4-box grid folded directly into `readyCard` under a "System Health" label, right above the existing "Conditions" weather grid — one card instead of three. Removed the `cat-fitness` card (miles/calories/steps/avg speed) completely, including its legend dot, its HOME quick-access chip (`hiveFit`), the JS block that calculated it, and the dead `--c-fitness` CSS token in both themes — confirmed via grep nothing else referenced any of it (the GPS-distance sensor telemetry that used to feed Fitness is a separate, still-live code path, untouched). Removed `#preMowReminder` outright. Updated one stale card description that referenced the now-gone Fitness tracker. Verified via Playwright: zero console errors, boolean check confirms Fitness/reminder/old gauge are all gone, `healthGrid` renders exactly the expected 4 boxes. `lint-app.js`/`smoke-test.js` clean. Screenshot confirms the merged card reads clean and uncluttered.
- **08-03 (merged mow-readiness cards):** Jeff asked to combine YARD's two "ready to mow" sections. Found them: "Mowing Conditions" (its own `wxBanner` verdict + 8-stat weather grid) sitting directly above "Ready to Mow?" (`readyBanner` + `readyReasons`) — both showing a GOOD/CAUTION/DO-NOT-MOW verdict for the same page. Read both verdict functions first rather than guessing which to keep: `applyMowVerdict()` (drives `wxBanner`) is weather-only; `renderReadiness()` (drives `readyBanner`/`readyReasons`) is a real superset — same weather check plus NWS alerts, irrigation status, overdue/due-soon service, and mower health, called from far more places in the code. Merged into one `readyCard`: kept the more complete banner+reasons, dropped the redundant smaller `wxBanner` display, folded the useful 8-stat detail grid (Rain Risk/Soil/Dew/etc.) in underneath the reasons list, one Refresh button. Confirmed safe to delete the `wxBanner` div — grepped every reference, all are writes guarded by `if(banner){...}`, nothing ever reads its state, so it silently no-ops instead of erroring. Verified via Playwright (YARD → Dashboard, triggered a real `renderReadiness()` cycle): one card renders, zero console errors, all underlying `wx*` stat-cell IDs still present and still updated by `applyMowVerdict()`. `lint-app.js`/`smoke-test.js` clean.
- **08-03 (LUX all-in-photo + real weather):** Jeff approved the "everything in the fireplace area" mockup from the prior round (flagged honestly that the mode/fan buttons come out to ~40x19px/54x15px, below Apple's 44x44 tap-target guideline — he accepted that tradeoff explicitly) and asked for two more things: implement it for real, and replace the thermostat photo's fake baked-in weather readout (the "84°/High 83° Low 64°/Mostly sunny/54% humidity" block) with the real outside temp + feels-like, in dark mode. **Implemented both in the real app** (the fireplace-panel design had only existed as a standalone mockup file before this). Hand-calibrated a second overlay region — `.lux-wx-patch` — positioned over just the device screen's weather sub-area (not the whole screen; room temp/mode/fan there stay as photo decoration since real versions of those already live in the fireplace panel), sized/positioned as percentages the same way as the fireplace panel. Wired to the app's existing real weather pipeline: `loadStation()` (WU KTNWHITE21 + Open-Meteo fallback via `/api/weather`, already used by the WEATHER section) now also writes to `luxOutTemp`/`luxFeelsLike`, and `loadStation()` is now called on HOME load alongside `loadClimate()` — no new fetch logic, reused the exact real data source rather than inventing a second one. **Caught and fixed the same recurring bug class one more time:** `luxRenderFan()` was still setting inactive/active fan-button colors via inline `var(--border)`/`var(--muted)`/`var(--gold)` — fine when those buttons sat on the normal card surface, broken now that they live permanently on the dark fireplace panel regardless of theme (theme-aware dark-mode-appropriate tokens go dark-on-dark in light mode). Switched to fixed light-on-dark values matching the panel's own aesthetic, same fix pattern as the `luxConn`/active-mode-button fixes from the earlier round. Verified via axe-core (0 violations in both themes) and Playwright screenshots across dark/light and multiple mode+fan states (cool/auto, off/circulate). "Climate Control" text and the thermostat's own room-temp/mode readout stay fully visible — neither overlay touches them.
- **08-03 (LUX photo redesign):** Jeff's verdict on the first version: "That looks awful... where is the rest of the picture." Two real problems with the first attempt (see entry below): (1) cropped the photo tight to just the device, losing the cozy living-room atmosphere that was the whole point of the photo Jeff chose; (2) the rotated black panel trying to precisely composite onto the device's in-photo screen looked like a pasted-on box, not a natural part of the app. **Root cause of the design mistake:** invented a bespoke rotated-overlay technique instead of reusing the app's own already-proven, already-approved pattern — the utility cards (`util-banner`/`util-overlay-bottom`/`util-stats`) already solve "live data on top of a real photo" with a bottom gradient + glass chips, no perspective-matching needed. **Fix:** re-saved `hero-lux.jpg` as the FULL original photo (1320&times;931, fireplace/room fully visible again), threw out the rotated-panel CSS entirely, rebuilt using the exact same `.util-banner`/`.util-overlay-bottom`/`.util-stats`/`.util-stat` classes the water/gas/electric cards already use — Room Temp and Set Temp as glass stat chips, Mode/Fan as the existing colored `.lux-badge` pills, all sitting on the same proven dark-gradient-at-bottom treatment. Card padding is 18px (`.card`), so pulled the banner flush to the edges with a negative margin wrapper to match the edge-to-edge look of the utility cards exactly. Kept the same DOM ids throughout (still zero `loadClimate()`/`luxApplyState()` JS changes). Verified via axe-core (0 violations, confirmed the `.lux-badge-off` dark-panel override still applies correctly under the new `.util-overlay-bottom` selector) and Playwright screenshots in both themes — reads as a native part of the app now, not a bolted-on experiment. Bumped service-worker to hcc-v14 (image content changed).
- **08-03 (LUX photo inlay):** Jeff sent a marketing photo of a LUX thermostat mounted in a cozy living room and asked to overlay the real live thermostat data directly onto the device's screen in that picture. Built it as a two-stage process — mocked it first with sample numbers via a standalone Playwright-rendered HTML file (not touching the app), sent Jeff the screenshot for approval before writing any real code (per his explicit "let me see an example first"), then implemented for real once approved. **Calibration:** hand-measured the device screen's 4 corners in the photo (verified visually via marker overlays, iterated twice to get them accurate), computed the screen's actual rotation (~-2.4°, confirmed it's close to a pure rotation not a heavy perspective skew) and center/size, expressed as percentages of the image so the overlay stays aligned at any responsive render width. **Cropped tighter than the original marketing photo** (dropped the baked-in "Climate Control" ad-copy panel entirely, framed close on just the device) — necessary because at real phone card widths (~380px) the device screen is a small fraction of a full uncropped landscape photo, and text at that scale was unreadably tiny; the tighter crop makes the overlay panel large enough for legible type. Saved as `images/hero-lux.jpg` (85KB, 560×670), added to service-worker precache (hcc-v13). New CSS module `.lux-hero`/`.lux-hero-panel`/etc. reuses the exact same DOM ids (`luxCurrentTemp`, `luxSetTemp`, `luxModeBadge`, `luxFanBadge`) that `loadClimate()`/`luxApplyState()`/`luxRenderMode()`/`luxRenderFan()` already write to — zero JS logic changes needed, verified by calling `luxApplyState()` with real-shaped mock data and confirming the photo overlay updates correctly. Interactive controls (mode buttons, fan buttons, +/- adjust) stay as normal buttons below the photo rather than crammed onto the rotated overlay — kept them properly tappable-sized rather than shrinking them to fit inside the device screen area. Dropped `luxSchedBadge` entirely (was dead markup, never actually driven by any JS — didn't belong in a "real numbers only" redesign). **Found and fixed 2 real pre-existing contrast bugs while testing:** `luxConn` ("● ONLINE") and the active fan button both hardcoded `#22c55e`/`#d4af37` (the *dark-mode* token values, literally) instead of `var(--ok)`/`var(--gold)`, so they went dark-on-light and unreadable specifically in light theme — fixed both to use the theme-aware tokens. (Also added a `.lux-hero-panel .lux-badge-off` override since that badge sits on an always-dark photo panel regardless of app theme, so it can't use the theme-relative `--muted` token like normal card badges do.) **Noted but explicitly NOT fixed:** the same hardcoded-hex-instead-of-token pattern exists in ~100+ other places across the file — out of scope for this task, a real future cleanup item if Jeff wants a full pass. Verified via axe-core (0 new violations), `lint-app.js`, `smoke-test.js`, and Playwright screenshots in both themes and multiple mode states (cool/off).
- **08-03 (LUX moved, latest):** Jeff asked to move the LUX Thermostat card to the first page (HOME), right under the cameras. Moved both `luxCard` and `luxSetupCard` verbatim (all IDs/`onclick` handlers/`LUX_STATE` global unchanged) from GUARDIAN to HOME, positioned directly after the camera placeholder and before Utilities. Moved the `loadClimate()` call in `hccSection()` from the `guardian` branch to the `home` branch — confirmed safe first: GUARDIAN's own HVAC health row (`grd-hvac`) reads an HA `climate.*` entity directly via `/api/states`, completely independent of `loadClimate()`'s LUX-cloud API call, so removing it from GUARDIAN doesn't break that row. Verified via Playwright: `luxCard` now resolves inside `#section-home` (not `#section-guardian`), DOM order is camera-placeholder → luxCard → luxSetupCard → Utilities, and GUARDIAN's HVAC check still renders normally with the thermostat card gone. `lint-app.js`/`smoke-test.js` clean, no duplicate IDs.
- **08-03 (camera order, latest):** Jeff asked to move the Front Doorbell camera tile to right under Driveway. The grid previously had no fixed order at all — it just rendered in whatever sequence HA's `/api/states` happened to return, which isn't guaranteed stable. Added `CAM_ORDER` (explicit priority list: driveway, front doorbell, front right, back left, backyard, garage) + `sortCameras()`, applied before rendering — any future camera not in the list falls after these in its original order (stable sort), so this won't break if a 7th camera gets added later. Verified via Playwright with the mock HA response deliberately shuffled (and clipframe noise mixed in) to prove the sort — not the incoming order — is what's determining the layout; render order came back exactly as requested. `lint-app.js`/`smoke-test.js` clean.
- **08-03 (camera views fixed, latest):** Jeff reported "camera views in the app are all messed up" and asked me to figure out what the coworker did that broke them. Read the coworker's own changelog entry (the Fire TV PiP wrong-frame fix, same day): they built 6 new `local_file` camera entities in HA, `camera.<cam>_clipframe`, one per real Blink camera, as internal plumbing to hold the extracted still for the Fire TV popup. **Root cause confirmed in our own app code, not a guess:** `loadCameras()`'s camera grid (and two other call sites — `blinkRefreshAll()`, `blinkReloadStills()`) all did an unfiltered `entity_id.startsWith('camera.')` match against every HA entity — with the 6 new clipframe helper entities now existing in HA, the app started rendering **12 tiles instead of 6**, mixing real live cameras with static internal helper stills (which only update on a motion event and otherwise sit stale) and calling `blink.trigger_camera` on all 12 during "Refresh All" (silently failing on the 6 fake ones, since `local_file` cameras don't support that service) — exactly the "duplicated/broken-looking" symptom Jeff saw. Not the coworker's mistake — the HA-side entities are legitimate and needed for their fix; our app's camera-listing code just had no concept of "internal helper camera" and needed one. **Fix:** added `isUserCamera(entityId)` (excludes any `camera.*_clipframe` entity) and applied it at all three filter sites. Verified via Playwright with a mocked HA response containing all 12 entities (6 real + 6 clipframe, matching the coworker's actual HA state): confirmed the grid renders exactly 6 tiles, banner correctly reads "6 CAMERAS," screenshot confirms no duplicates. Full regression clean (JS syntax, `lint-app.js`, `smoke-test.js`).
- **08-03 (serial confirmed, latest):** Jeff sent a photo of the mower's actual Toro data plate — **Serial No. 401338948**, confirming it falls in the `400000000-402081999` production range. Recorded permanently in the Jeff's Contact/Account Info section so this is never ambiguous again. Fixed the one real bug this surfaced: the "eReplacementParts (alt)" button was pointing at the WRONG serial range (`402082000-403599999`) — swapped it for the correct page (`toro-21200-400000000402081999-...`, confirmed real via search). The "Mower Diagrams" (PartsTree) link was already correct. Verified: JS syntax, `lint-app.js`, `smoke-test.js` all clean.
- **08-03 (coworker, later, latest): Fixed Alexa fast-forward via native phrasing (not a Routine), started calibrating skip distance; researched and delivered a 24-idea zero-cost feature brainstorm.** Real fix for item 16 (see Pending Items) — "Alexa, turn on FF the Commercials" reaches `script.hcc_skip_commercial` directly since it's already Alexa-exposed, no custom Routine needed at all; confirmed live twice. Found the actual overshoot cause (Sling's fast-forward key ramps non-linearly with rapid repeated presses) and reduced the script from 3 presses to 1 as a first calibration step — **still needs live iteration next session to hit Jeff's exact 4:40 target.** Separately, researched (WebSearch across HA forums/docs/community writeups, not guessed) and delivered a categorized 24-idea list for extending Beehive using zero new hardware (published as an artifact) — covers reusing the just-built PiPup pipeline for arrivals/irrigation/digest, cross-checking B-Hyve's estimated usage against the real water meter to catch a broken sprinkler head, a slow-leak trend detector, vacation-mode randomized lighting on existing Tuya sockets, Mercedes mileage-based service reminders, and a genuinely non-obvious find — the 6 Blink cameras already report temperature/battery/wifi as unused entity attributes (with the real caveat that they only refresh on image request, confirmed via Blink's own HA docs). Also scoped a near-zero-cost appendix (one ~$25 second RTL-SDR stick, since the existing one is fully committed to water/gas and can't multiplex): free TPMS tire-pressure data for the F-250 via `rtl_433`, ADS-B flight tracking, and direct NOAA Weather Radio reception that survives an internet outage. **Nothing built yet from this list — waiting on Jeff to pick which ones he wants.**
- **08-03 (coworker): Fixed the Fire TV PiP popup showing the wrong/blank/stale frame — real fix, verified live twice with Jeff watching the actual TV.** Root cause: the popup was scanning whatever the LIVE Blink camera feed looked like at scan time, not the real moment motion fired — blinkpy's own `motion_detected`/snapshot state only reflects Blink's cloud clip well after the fact, confirmed by reading blinkpy's actual source. **Fix:** built 6 new `local_file` camera entities (`camera.<cam>_clipframe`) via the HA UI Config Flow — the YAML `camera: platform: local_file` form is silently dead since HA 2024.11.1 (confirmed via research, not guessed: valid YAML, zero errors logged, zero entities created); repointed all 6 `image_processing:` sources in `configuration.yaml` to the new clipframe cameras; rewrote `AI Camera Scan on Motion` (`packages/hcc.yaml`) to, on real motion, wait (bounded 30s retry) for the Blink camera's `last_record` attribute to actually change, `blink.save_video` the real clip, run a new `shell_command.extract_clip_frame` (ffmpeg) to pull the exact frame, refresh the clipframe camera, then scan. **Caught and fixed a real bug along the way:** HA's `shell_command` executes Jinja-templated commands via direct exec, NOT a real shell — my `> logfile 2>&1` redirect was passed to ffmpeg as a literal broken argument, causing it to silently abort before writing any output (root-caused via `return_response`, which surfaced ffmpeg's actual stderr: `Unable to choose an output format for '>'`) — fixed by dropping the redirect. **Verified via two real live walk-by tests, not simulated:** confirmed the extracted frame is a real photo of Jeff mid-motion (Blink watermark visible), CodeProject.AI detected person at 83-86% confidence both times, and Jeff directly confirmed the correct frame displayed on the physical Fire TV both times. **Delay — partially addressed, not fully closed:** measured the automation's own chain (clip-ready → download → extract → scan → Fire TV trigger) at ~8 seconds this session; the remaining real-world lag Jeff feels is Blink's own cloud motion-detection latency before the binary_sensor even flips `on` — pre-existing, not touched by this fix, not something HA-side polling can shorten further. Closes the "wrong/stale frame" half of Pending Item 12; the delay half stays open. Also fought through a genuinely flaky Chrome-automation issue this session (screenshots randomly hanging specifically on the HA Integrations UI across multiple fresh tabs) — worked around by verifying every step via the REST API instead of trusting screenshots.
- **08-03 (link audit):** Jeff asked me to check every app link is live/correct, and separately fix the NOAA Weather Radio link (was landing on "some type of paysite"). **Environment note:** this cloud session's outbound network policy blocks essentially all direct HTTPS fetches (confirmed via curl — even google.com and wikipedia.org get rejected at the proxy, only github.com passes), so I could not click-test links directly; verified everything via WebSearch (server-side, unaffected by the local proxy) cross-checked against multiple independent sources instead. Extracted all unique external URLs (291 raw matches → the ~374 the smoke test counts are mostly repeats). The large majority (~280+) are Amazon/eBay/Walmart/Home Depot/YouTube/Google **search-query** links for mower parts — these don't 404 by nature (a bad query just returns different results), so "liveness" isn't meaningfully at risk there; spot-checked the URL encoding is well-formed. Deep-verified the higher-risk specific links: `whitehousetn.gov`, `robertsoncountytn.gov` (confirmed `.gov` is correct, ruled out a `.org` red herring from a lower-quality source), `spotternetwork.org`, the ManualsLib/PartsTree/eReplacementParts/Jack's Small Engines deep links (all confirmed exact matches, still live), and both App Store IDs (mPING `584383400`, Alexa `944011620`) — all correct. **Found and fixed the real bug:** `NOAA Radio` button pointed at a TuneIn *search* URL (`tunein.com/search/?query=...`) instead of the actual station. Identified the correct station via NWS's own official coverage database (`nws.noaa.gov/nwr/coverage/site2.php?Site=KIG79&State=TN`) plus 4 independent radio directories all agreeing: **KIG79, 162.550 MHz, Nashville — covers White House** (not "KIH21" as I'd guessed in an earlier session before this fix; that callsign doesn't exist). Swapped in TuneIn's direct station page (`tunein.com/radio/NOAA-Weather-Radio-16255-s95242/`, titled "Free Internet Radio" by TuneIn itself) — a specific station page instead of a search results page should skip whatever upsell/interstitial Jeff was hitting. **Could not click-test this myself** (same network restriction) — ask Jeff to confirm it plays free with no paywall prompt; if TuneIn still nags, `radio-addict.com`/`radiomoove.com`/`openradio.app` all independently mirror the same free KIG79 feed as fallback options. Note: the mower-serial ambiguity flagged here was resolved same day — see the entry above.
- **08-03 (later, latest):** Jeff clarified the intent: say "Alexa, fast forward" to skip commercials, resume-play after was always supposed to happen (it does — `script.hcc_skip_commercial` already ends with `keyevent 126`). Also asked me to check the HA community forums for the correct setup, and confirmed the coworker is actively on the blank-cameras issue — stayed out of anything camera-related this session. **Found the real, source-verified root cause of why the literal phrase can never work, not a Beehive misconfiguration:** read HA core's actual `alexa/handlers.py` — the Alexa Smart Home integration has zero handler for the `PlaybackController.FastForward`/`Rewind` directives (Play/Pause/Stop/Next/Previous only), matching the still-open home-assistant/core#87327. Separately, Amazon's own community forums document that custom Alexa Routines with phrases that sound like built-in media commands (their own reported example: "Alexa, bedtime") get intercepted natively and never reach the custom Routine — same failure class blocks a Routine literally triggered by "fast forward." Neither of these is fixable by re-pairing ADB or reconfiguring Beehive. **The actual fix:** Jeff needs to redo the Alexa Routine with a non-colliding trigger phrase (e.g. "Alexa, skip the commercial") pointed at the exposed `script.hcc_skip_commercial` — that's Alexa-app + HA-expose-settings work, nothing to change in this repo. Updated Pending Item 16 with the full citation trail and the concrete fix Jeff needs to make.
- **08-03 (latest):** Jeff reported "Alexa fast-forward isn't working" and "nothing seems to work re: Fire TV + HA." Audited every line of Fire TV-related code — first the app side, then the coworker's just-pushed `beehive-config/` snapshot of the REAL live HA YAML (`hcc.yaml`/`automations.yaml`), since this cloud session has no LAN/ADB access to test the physical Fire TV or Beehive live. **Found the actual "Alexa fast-forward" feature: `script.hcc_skip_commercial`** (`beehive-config/hcc.yaml`) — 3× `androidtv.adb_command` with `input keyevent 90` (1.2s apart) then `input keyevent 126` to resume. Confirmed via WebFetch/WebSearch against HA's `androidtv` integration + the underlying `python-androidtv` library source (not guessed) that keyevent 90 is the real `KEY_FAST_FORWARD` code — the script is correctly built. What this repo snapshot can't show: whether that script is actually exposed to Alexa and linked via an Alexa Routine (that mapping lives in HA's internal `.storage/` and the Alexa app account, neither captured here), or whether the underlying ADB link to the Fire TV is currently alive (`media_player.fire_tv_viewing_room` — there's already a Watchdog automation that should have phone-alerted Jeff if this entity went `unavailable` 5+ min, so ask whether that alert ever fired). Separately, confirmed the app's OWN Fire TV remote (`fireTvOpen()`/`fireTvCmd()`) was correctly wired but **only ever had Power and Play/Pause — no fast-forward/rewind button existed in the app at all**; added real Rewind/Fast Fwd buttons using HA's `media_player.media_previous_track`/`media_next_track` services, verified via Playwright screenshot (mocked `playing` state) that all 3 buttons render correctly; `lint-app.js`/`smoke-test.js` clean. Also spot-checked the disabled "AI Show Camera on Fire TV" automation still correctly has `initial_state: false` (the 08-01 fix held) and confirmed no secrets leaked into the new `beehive-config/` commit before pushing. **One specific ask for Jeff/coworker, in priority order:** (1) in HA, Developer Tools → Actions, run `script.turn_on` on `script.hcc_skip_commercial` directly — if the Fire TV skips forward, the script/ADB link is fine and the break is purely in the Alexa Routine/expose wiring (a re-link in the Alexa app, not an HA problem); if nothing happens, check `media_player.fire_tv_viewing_room`'s state in Developer Tools → States for `unavailable` (ADB pairing likely dropped, needs re-pairing). Native voice "Alexa, fast forward" (as opposed to "Alexa, skip commercial") may also just be Amazon's own built-in Fire TV voice-remote phrase, unrelated to this script entirely — worth Jeff clarifying which one he's actually saying.
- **08-02 (coworker, latest):** **Built the master disaster-recovery backup system Jeff asked for ("save everything... it would be catastrophic to lose anything").** Two independent layers, both verified live, not just configured: (1) **Git layer:** confirmed the repo is genuinely **public** (checked before doing anything, not assumed) — that check caught a real live Weather.com API key hardcoded in `packages/hcc.yaml`'s `rest:` block, fixed by moving it to `secrets.yaml` as `weather_pws_resource_url` and referencing it via `!secret` (verified no regression — `sensor.backyard_temperature`/`sensor.backyard_feels_like` still report correct fresh values after `rest.reload`). Added `beehive-config/` to the repo (sanitized snapshot of `hcc.yaml`/`configuration.yaml`/`automations.yaml`/`scenes.yaml`/`scripts.yaml`, pulled from Beehive via a temporary `www/tmp_backup/` staging folder + direct PC→Beehive `curl`, then deleted the staging copies) — committed and pushed after explicit go-ahead. This is a one-time snapshot, not auto-syncing; re-pull manually next time real HA config changes. (2) **Full-system layer:** discovered HA's own native scheduled Backups were **already fully configured and running** (daily, keeps 3, includes everything — settings/History/Media/Share folder/all add-ons, real size ~90-100MB) — nothing needed there, corrected my own assumption that this needed setup from scratch. The actual gap was that backups were local-only (die with the J45 if it fails), so built the missing piece: a new HA Long-Lived Access Token ("iCloud Backup Sync", kept separate from the app's own token — least-privilege, so revoking one never breaks the other) + `C:\Users\jeffl\HCC-Scripts\Sync-HABackup.ps1` on Jeff's PC, which authenticates over HA's WebSocket API (`backup/info` — the backup list isn't exposed via REST in this HA version, only the download-by-id endpoint is: confirmed via direct probing, not assumed) to find the latest automatic backup, downloads it via `GET /api/backup/download/{id}?agent_id=hassio.local`, verifies the downloaded byte count against the reported size before keeping it, and prunes to the last 14 copies. Wired to a daily Windows Scheduled Task ("HCC Beehive Backup Sync", 6:30 AM, `StartWhenAvailable` so a missed day catches up next boot) — **verified end-to-end twice**, including one run triggered by Task Scheduler itself (not just interactively), both landing an exact-byte-match `.tar` in `C:\Users\jeffl\iCloudDrive\HCC-Beehive-Backups\`. Token stored in `C:\Users\jeffl\HCC-secrets\ha_backup_token.txt` (outside the git repo, never committed). Caught and fixed a real bug along the way: PowerShell's `Set-Content -Encoding utf8` silently prepends a UTF-8 BOM, which corrupted the token and caused silent 401s until rewritten via `[System.IO.File]::WriteAllText` with a no-BOM encoding.
- **08-02 (coworker):** **Investigated the Fire TV PiP delay (real findings, not fixed yet), confirmed Blink phone-notification filtering already covers all 6 cameras, ruled out a redundant rain-skip build, and built + tested the Morning Digest automation (caught and fixed a real bug along the way).** (1) **PiP delay:** traced two real `codeproject_ai.object_detected` events end-to-end via HA's automation trace API — the whole chain (motion sensor → AI scan → detect event → Fire TV popup) completes in under 1 second on the HA side, both times. A pre-existing "Blink Fast Motion Poll" automation (10s `time_pattern`, forces a genuine `homeassistant.update_entity` cloud refetch, confirmed not a cache no-op) already covers all 6 cameras. None of that explains Jeff's reported 3-4 min real-world lag — conclusion: the delay is upstream of HA entirely (Blink's own cloud clip processing, or possibly Fire TV/PiPup rendering), not something more polling can fix. **Not resolved** — needs a real timestamped incident to trace further; asked Jeff to flag next occurrence. Confirmed camera coverage is complete and identical across scan-on-motion, fast-poll, and notify: all 6 (`301_driveway`, `front_right`, `back_left`, `301_backyard`, `garage`, `301_front_doorbell`). (2) **Blink phone notifications:** discovered `AI Object Detected Notify` (in `packages/hcc.yaml`) already does exactly what a "Blink motion filtering" build would have added — branches on `obj_type` (person/vehicle/animal) from the same CodeProject.AI event, sends distinct filtered pushes with a 15-min per-camera mute button. Confirmed live (`state: on`, real recent trigger). No build needed. (3) **Kodi buffering fix, corrected a wrong earlier fix:** Jeff's prior `advancedsettings.xml` edit was a no-op — confirmed via research that Kodi 21+ "Omega" (Jeff's on 21.3) moved cache settings out of that file into the GUI, which fully overrides the XML. Found the real live settings in `guisettings.xml` (`filecache.memorysize`/`readfactor`, both still at Kodi defaults — 20MB/4.0x, confirming the XML edit never took effect) and edited them directly since Kodi wasn't running (20MB→768MB, the GUI's own max; 4.0x→5.0x per Kodi's current official buffering-fix guidance) — verified via a live re-read of the file after editing. (4) **B-Hyve WeatherSense researched, confirmed no HA-side rain-skip automation needed:** Jeff's B-Hyve already does real weather-adaptive watering (rain-skip, temp/wind adjustment) and genuinely supports a personal weather station via PWSWeather.com/Aeris — independent testing cited 100% skip reliability on 0.2"+ rain days. Building a duplicate HA automation would've been strictly worse (no wind/temp adjustment). Closed off the pending-items list, nothing to build. (5) **Built `HCC — Morning Digest`** (daily 7am push + persistent notification: weather, vacuum status, car lock/fuel/odometer, water/gas reporting health, recent Blink self-heal count) — automates the manual "check everything" health-check Jeff kept asking for by hand. **Caught a real bug via live testing before calling it done:** the digest's "active alerts" count always silently returned 0 — confirmed via research this is a known HA-wide change (persistent notifications removed from the template-readable state machine since HA 2023.6), not fixable in a normal Jinja2 template; removed that metric entirely rather than ship a false "all clear." Verified the corrected version renders real, accurate data via two live manual triggers (`persistent_notification/get` WS call, not just the general states API which doesn't expose these notifications either) — second run correctly showed no false claims, real push landed on Jeff's phone.
- **08-01 (coworker):** **Closed out the leak-detection + Angela-tracking threads from earlier today.** (1) Installed the `Water-Monitor` HACS integration (already in HACS's default store, no custom repo needed) — pointed at `sensor.water_flow`/`sensor.water_gallons`, confirmed `Upstream sensors health: Connected`; did not fine-tune its low-flow/tank-refill thresholds (options dialog wasn't scrolling reliably this session), left on defaults with the batched-cadence caveat noted for whoever tunes it later. (2) Built the complementary fast-reacting piece: `HCC — Possible Water Leak (Idle Flow)` (entity `automation.hcc_possible_water_leak_idle_flow` — note HA slugified the entity ID from the alias text, not the `id` field set in the API call), triggers on `sensor.water_flow` >0.05 gal/min for 30+ min while both Jeff and Angela are `not_home` OR it's 1-5am, and no B-Hyve irrigation zone is on; notifies Jeff. Config-validated, confirmed `state: on`. (3) **Root-caused Angela's phone tracker unreliability — real fix, not a settings guess.** Confirmed via live evidence the HA Companion App genuinely was installed and had reported real GPS data as recently as same-day, but had gone stale (~40+ min, unmoving) even after Jeff fixed the standard iOS location permissions (Always/Precise/Background Refresh) and manually opened the app — none of that produced a fresh update, including HA's own `command_request_location_update` push (sent via `notify.mobile_app_angelas_iphone`, confirmed via official Companion App docs this is a known "hit-or-miss" feature, so its failure wasn't diagnostic on its own). Real fix, found via the app's in-app Settings → Notifications (menu relabeled in a newer app version — no longer nested under "Companion App" like the docs describe): her **Push ID was empty/stale**, meaning her phone had never registered a valid push-delivery channel with HA, so nothing — including background significant-location-change wake-ups — could reach the app. Jeff reset it; confirmed fixed with real evidence, not assumed: a genuinely fresh organic background update landed (not from a manual poke), with real GPS movement (accuracy 3.6m→10.4m) and real battery drain (90%→75%) between checks. `person.angela_loewen` is now a reliable primary signal again — the Mercedes GPS backup trigger added 08-01 earlier is no longer load-bearing, just a true backup as originally intended. (4) **Fixed `zone.work`'s location** — Jeff gave the real parking garage address (310 Commerce St, Nashville), geocoded to 36.1624877/-86.7776215, icon changed to `mdi:parking`, applied via `zone.reload`, confirmed live. Note: this address came out only ~90m from the original office coordinates, not the ~0.4mi Jeff estimated earlier — flagged to him, not fully reconciled, but he gave the address directly so it took priority over the earlier estimate.
- **08-01 (coworker):** **CORRECTION to the entry below — the water meter was never faulty. False alarm, root-caused and fully retracted same day.** The "stuck register" conclusion from the irrigation+shower test was wrong — it just wasn't watched long enough. Follow-up evidence: (1) Jeff produced WHUD's own "Water Meter Data Access Request" form, whose printed PROTOCOL field lists `Itron ERT (SCM/SCM+/IDM)` for this exact meter, with IDM underlined by the WHUD rep on a call — worth testing directly rather than trusting general web knowledge about IDM being electric-meter-only. (2) Swapped the water meter's `rtlamr2mqtt` entry from `scm+` to `idm` (gas entry untouched, unaffected throughout) and watched for ~2 hours: zero IDM packets ever decoded for this meter ID — this specific installed radio only ever transmits `scm+`, so that channel is the complete picture, not a partial one. (3) Reverted to `scm+` and immediately caught a fresh reading on restart: `179097` (stuck value) → `179371` (appeared ~20 min after the original test ended, i.e. the real usage from that test, delayed) → `179473` (appeared ~3 hours later, normal background usage). **Real root cause: `rtlamr2mqtt` runs `rtlamr` with `-unique=true`, which only re-publishes a reading when the decoded value itself changes — the meter's own transmitter evidently updates its broadcast register in batches (gaps varied from ~20 min to ~3 hours, so likely threshold/delta-based, not a fixed clock), not continuously with live flow.** The `sensor.water_meter_last_seen` heartbeat pinging normally the whole time was a real signal I misread — it confirms every RF catch, not that the *value* had refreshed, and I conflated the two. **No WHUD call needed — the meter and pit radio are both healthy.** All add-on config changes made during testing were reverted; confirmed final state is the original unmodified `scm+`/`scm` config. See the entry directly below for the original (incorrect) diagnosis, kept for the record rather than deleted.
- **08-01 (coworker):** **Diagnosed a real, distinct water-meter fault by live-testing with actual water use — not guessing.** After explaining the leak-detection ideas (Water-Monitor HACS integration, idle-flow logic), needed the pit radio's real report cadence, so ran a controlled test: triggered a B-Hyve irrigation zone via HA + Jeff took a real shower, both running simultaneously for ~47 min. Result read straight from the `rtlamr2mqtt` add-on's raw decoder log (`ha addons logs 6713e36e_rtlamr2mqtt`): the water meter (endpoint `79453337`) was freshly decoded on every capture the whole time (heartbeat/`sensor.water_meter_last_seen` updated normally every ~1.5-5 min) but reported the exact same value, `179097`, on every single read — zero movement despite substantial confirmed usage. In the identical window the gas meter (`33393066`, same dongle/antenna/pipeline) ticked normally (`885060→885062`), ruling out the dongle/software/HA entirely. Jeff confirmed irrigation and the house shower share one municipal supply, ruling out "wrong source." **Conclusion (later corrected, see entry above): the pit radio's register is stuck.** ~~Jeff needs to call WHUD again~~ — superseded, no call needed.
- **08-01 (latest):** Jeff said "go ahead and fix the remaining contrast items" — darkened the shared light-mode tokens themselves rather than doing more one-off swaps: `--gold` `#9a7b1e`→`#7e6017`, `--a-home`/`--c-health` (same old value, separate tokens, both feed visible text) matched to `#7e6017`, `--muted` alpha `.66`→`.74`, `--bad` `#d61f1f`→`#c21b1b`, plus one literal `#9a7b1e` on `.wu-badge-stars` switched to `var(--gold)` so it can't drift again. Picked each new value by computing real WCAG contrast ratios in a small Node script against every light-mode surface color in the app (white/card/surface/bg variants), not by eye — targeted a solid ≥4.5:1 margin (landed 4.3-6.7:1) rather than skimming the line. Verified via axe-core color-contrast scan: 20 → **0 violations**. Lighthouse Accessibility re-run: 96 → **100/100**. Visually spot-checked HOME/GUARDIAN/CAR in light mode via Playwright screenshots — new shades read as intentional, not muddy, no other elements broke. Full regression clean: `lint-app.js` (no anti-patterns), `smoke-test.js` (all nav/tabs/modals/374 external links pass). Pending Item 9 (the old "needs a Jeff decision" placeholder) closed/removed — this was that decision.
- **08-01 (later):** Jeff asked what other quality tools exist besides Lighthouse — ran Lighthouse's Accessibility (96/100) + Best Practices (96/100) categories (previously only ran Performance) plus axe-core (deeper a11y scan via Playwright) and a 4-viewport responsive check. Real finding: 27 elements failed WCAG color-contrast, concentrated in light mode (the app's default theme) — several dark-mode-tuned literal colors bypass the app's own theme-aware tokens. Proposed a scoped "safe" fix to Jeff (swap hardcoded colors for existing tokens only, no new color design) with a real before/after screenshot + measured ratio, got explicit go-ahead, fixed 4 instances (gold CTA headings, "Beehive Offline" red text, inactive nav labels, 2 HA-token input fields) — caught and fixed two real gaps in my own first pass (the gold fix only landed in a throwaway test copy, not the real file; a CSS specificity bug where inline styles silently beat my override). Verified via axe-core: 27 → 20 violations, nav labels + both token inputs now fully pass. Remaining ~4.0-4.4:1 items tied to the shared `--muted`/`--gold` tokens deliberately left alone (darkening those ripples across many more elements — a real design call, not a safe swap) — see Pending Items. Console errors Lighthouse flagged are 100% test-environment noise (no real `/api/*` backend on a bare local file server); PWA manifest already solid; desktop-layout gap reconfirmed but still low priority (unchanged, phone-primary).
- **08-01 (coworker, latest):** **Built Angela's work-arrival tracking, mirroring the barn pattern exactly.** Geocoded her real office (150 4th Avenue North Suite 1700, Nashville, TN 37219) via OpenStreetMap Nominatim → 36.1629809, -86.7783796 (exact address match). Added `zone.work` (100m radius, `mdi:office-building`) to `configuration.yaml` right alongside `zone.barn`, loaded live via the `zone.reload` service — no full restart needed (confirmed the zone integration exposes its own reload service, unlike some YAML-only domains). New, deliberately independent automation `HCC — Angela Arrived at Work` (id `hcc_angela_arrived_at_work`) triggers on the same Mercedes GPS tracker (`device_tracker.gle_350_device_tracker`) entering the new zone and notifies Jeff — kept separate from the barn automation per Jeff's explicit instruction, since she doesn't always go to the barn, sometimes it's work. Made the `configuration.yaml` edit via the code-server integrated terminal (`python3`, exact-string match + `assert count==1` before writing) instead of typing into the Monaco editor directly, specifically to avoid the recurring auto-indent/search-text-leak glitches that have corrupted edits to this file before. Verified live end-to-end: `zone.work` confirmed via `/api/states` with correct lat/lon/radius, automation confirmed `state: on` after `automation.reload`. Also this session: confirmed the Vizio soundbar integration (power-cycle-fixed earlier 08-01) has zero actual usage anywhere in the app or HA config — nothing references it, so there's nothing to build on top of; left alone per Jeff, he'll power-cycle manually if it recurs. Diagnosed (plan ready, not yet executed — Jeff's call) Alexa showing duplicate lamp/plug devices: caused by the same Tuya devices being exposed to Alexa twice, once via Smart Life's own native Alexa skill and once via HA/Nabu Casa's Alexa Cloud integration. Fix when Jeff says go: turn off `should_expose` for 4 switch entities (`switch.bed_lamp_socket_1`, `switch.hot_water_heater_socket_1`, `switch.mini_smart_socket11_2_socket_1`, `switch.smart_socket_2_socket_1`) on the HA/Nabu Casa side only, leaving Smart Life's copies as the ones Alexa keeps. Delivered Jeff a short action-items list for the cloud session (Lighthouse score + desktop layout gap ready now; Lucky Mike still needs explicit go before starting).
- **08-01:** Lighthouse basic wins (Pending Item 5): recompressed all JPEGs to q80 (images/ 12MB→7.1MB), deleted 6 dead image files + fixed service-worker.js's stale precache list (bumped hcc-v11→v12), trimmed Google Fonts to only the weights actually used, added `loading="lazy"` to below-the-fold images. Caught a real self-inflicted regression via a controlled Lighthouse A/B (Cumulative Layout Shift 0.023→0.436 from lazy-loading images whose containers had no reserved space) and fixed it with `aspect-ratio` on the affected containers — re-verified CLS back to 0.015, no regression. Full JS/CSS minification deliberately left out of scope (would need restructuring the single-file app) — see Pending Item 5 for what's left and honest caveats on the score measurement.
- **08-01 (coworker):** **Vizio soundbar (`AUD_D426`) integration was stuck in `setup_retry`, "Unable to connect to 192.168.1.68:9000."** Researched first: this matches a known, unresolved class of Vizio SmartCast issue (self-signed cert mismatch causing strict TLS clients to fail even when the device is reachable — the matching HA GitHub issue was closed "not planned," no upstream fix exists) rather than a config mistake on our end. Confirmed the device WAS network-reachable (got a real HTTP response) even while HA's integration kept failing, and a `reload_config_entry` didn't help — pointed to the device's own local API needing a power-cycle, same class of fix as yesterday's RTL-SDR dongle. Jeff power-cycled the soundbar; integration came back `loaded` immediately, `media_player.aud_d426`/`remote.aud_d426` both confirmed live. Not urgent if it recurs — the only functionality this integration provides is TV/soundbar CEC power sync, already a known low-priority item.
- **08-01 (coworker, later):** **Built Angela's barn-arrival tracking live, using her actual 25-mile drive to Kentucky as the real test.** Added the Mercedes GPS (`device_tracker.gle_350_device_tracker`) as a third, independent trigger on `automation.angela_almost_home` — the existing two both secretly depended on her phone (`person.angela_loewen`), which is why the 07-26 "backup" never actually helped when her phone stopped reporting. New `zone.barn` created from real coordinates captured the moment the car actually stopped moving (36.716949, -86.652950 — confirmed via 79+ seconds of zero GPS movement after continuous updates every 15-30s while driving, i.e. engine-off/parked, not a polling gap). New separate automation `HCC — Angela Arrived at Barn` (kept independent from Angela Almost Home per Jeff — she also regularly drives to work in Nashville, not just the barn) notifies Jeff on arrival; end-to-end delivery confirmed via a real test notification. Live proof during this session: her phone's tracker sat stale at "home" for 4+ minutes while she was already 10+ miles out; the Mercedes tracker updated every 15-30s the whole drive.
- **08-01 (coworker):** **24h health check + closed out the last real open item from 07-31.** Ran a full log/entity audit after a day of the new automations running unattended: Blink's periodic 15-min backstop caught 73 real recurring crashes overnight with zero user-visible impact (proof the design works — the error-triggered auto-heal alone never fired once, confirming yesterday's one-shot-per-message concern was real); water/gas/Mercedes all healthy; one new benign mbapi2020 websocket-close log line, no actual error. Sharky vacuum confirmed genuinely offline since 07-28 (3+ days, unrelated to any of this work) — a full Tuya integration reload didn't help, points to the device itself needing a physical power-cycle, handed to Jeff. **Properly fixed `automation.ai_show_camera_on_fire_tv`:** its entity_registry desync made `disabled_by` update calls fail, so used the documented `initial_state: false` YAML key instead (verified against official HA docs first — an earlier `enabled: false` guess would have been silently ignored, glad it got checked before writing). Confirmed via a real HA restart that it now correctly stays off, unlike yesterday's runtime-only `turn_off`. **Live-validated Jeff's Mercedes-GPS backup-tracking idea:** watched `device_tracker.gle_350_device_tracker` in real time as Angela drove off — flipped `home`→`not_home` within ~90 seconds of her actually starting the car, no manual refresh needed (mbapi2020 pushes location via its own websocket). Confirms it's a viable secondary trigger for Angela's Almost Home.
- **07-31 (later):** Closed the water pit-radio item per Jeff's explicit call (working now, doesn't want to flag it to WHUD). Vacuum card no longer shows raw HA states like "Unresponsive" as if it's a hard failure — unrecognized states now get an amber "may be transient, refresh to check" framing (not an app bug, just bad framing of a real but often-transient upstream state). Shipped the real fix for Pending Item 10 (irrigation sewer-overcharge): `irrGalFromHistory()` now sums each B-Hyve zone's actual on-duration this billing cycle from HA recorder history × its configured GPM, replacing the fixed-schedule guess — tested against a mocked 20-min/17.2-GPM watering event (exact 344 gal match) and against the no-zones fallback path. Researched and delivered Jeff a downloadable Zigbee alarm parts list (bare-bones panic-button kit + full Guardian safety-layer wishlist), real current products verified via web search.
- **07-31 (coworker, latest):** **Finished the Fire TV camera-popup fix: real picture-in-picture via PiPup, replacing full-screen takeover.** Jeff sideloaded PiPup (desertblade fork, `nl.rogro82.pipup`, via the Fire TV's Downloader app — GitHub's release-asset redirect chain breaks in older Downloader versions, updating it fixed the download). Granted the required `SYSTEM_ALERT_WINDOW` permission via ADB (`appops set nl.rogro82.pipup SYSTEM_ALERT_WINDOW allow`) — popups silently didn't render without it. Added `rest_command: pipup_notify` to `configuration.yaml` (POST to `http://192.168.1.215:7979/notify`, single-line flow-mapping YAML to avoid the editor's auto-indent bug on multi-line blocks). New automation `HCC — AI Camera Popup on Fire TV` (id `hcc_ai_camera_popup_fire_tv`, entity `automation.hcc_ai_camera_popup_on_fire_tv`) triggers on `codeproject_ai.object_detected` and pushes the **actual annotated frame that triggered the detection** (`trigger.event.data.saved_file`, already saved under `/config/www/ai_snapshots/` → auto-reachable via HA's `/local/` path, no signed-URL/token complexity) — not a fresh snapshot taken later, per Jeff's explicit requirement that the picture show what actually caused the alert. Disabled the old `automation.ai_show_camera_on_fire_tv` (full-screen switch) via `automation.turn_off` — **note: its entity_registry entry returns "Entity not found" on both `get` and `update` calls, a genuine desync from its live state; the `turn_off` is runtime-only and won't survive a future restart, needs a real fix by editing its YAML directly in `packages/hcc.yaml` (or deleting it) next session.** Full chain verified live end-to-end (real `camera.snapshot` + simulated `codeproject_ai.object_detected` event → automation → rest_command → real popup with real image, confirmed by Jeff). **Also hardened Blink Auto-Heal:** discovered live that HA's `system_log_event` only fires once per *unique* log message — a repeating crash-loop (confirmed via the log entry's own `count: 5`) doesn't re-trigger the error-based automation after its first catch. Added `HCC — Blink Periodic Health Reload` (id `hcc_blink_periodic_health_reload`) as a backstop: unconditional `homeassistant.reload_config_entry` on Blink every 15 minutes via `time_pattern`, no error-detection dependency at all — guarantees a hard ceiling on any Blink outage regardless of failure mode. Both Blink automations run together (fast reactive + guaranteed periodic).
- **07-31 (coworker, later):** **Root-caused "Fire TV camera popup notifications missing or 10-15 min late."** Jeff's actual complaint: motion → Blink → CodeProject.AI → auto-show-camera-on-Fire-TV pipeline was unreliable. Checked HA history/logbook directly: `binary_sensor.301_driveway_motion` (and siblings) never transitioned to `on` even once across 3 full days, only flickering `off`↔`unavailable` — meanwhile the known recurring `blinkpy.auth.LoginError` crash (see 07-28 entry below, tracked upstream at home-assistant/core#176836 and fronzbot/blinkpy#1217, no confirmed fix in either as of this session) was caught live in the log during this session. Root cause: that crash leaves Blink's sensors stuck at a stale value instead of going `unavailable`, so the existing `HCC Watchdog` (which only triggers on `to: unavailable`) can't detect it — explains both "missing" and "very late" (only recovers on the next manual HA restart). **Fix:** new automation `HCC — Blink Auto-Heal` (id `hcc_blink_auto_heal`), triggers on `system_log_event` where `level` is ERROR/CRITICAL and logger `name` contains "blink" (keyed on logger name, not message text — matches the exact `custom_components.blink.coordinator` logger seen in the real crash, avoids false positives), calls `homeassistant.reload_config_entry` on Blink's entry (`01KY0MYHR8VN4646FQDSXA7VDC`) + notifies Jeff's phone. Converts a 10-15+ minute (or multi-day) outage into a few-second self-heal. Condition template verified via `/api/template` against real ERROR/WARNING/non-Blink cases; the actual `reload_config_entry` action was live-tested and reconnected cleanly with zero errors. Jeff's picture-in-picture idea (so camera popup doesn't take over the whole TV) is a real, matched community pattern (the "PiPup" Android app, triggered via ADB intent) but needs a new app installed on the physical Fire TV — held pending Jeff's go-ahead, not done this session.
- **07-31 (coworker):** **Root-caused and fixed "new automations silently don't save"** (open since 07-28). `configuration.yaml` had no `automation: !include automations.yaml` line — all real automations lived inside `packages/hcc.yaml`'s own `automation:` key, so any automation created via the UI wrote successfully to `automations.yaml` (confirmed via direct API GET) but nothing ever loaded that file — no error, because from HA's config tree it was never referenced. Added the missing include line, restarted HA: 3 real orphaned automations came alive (`HCC Watchdog — Integration Down Alert`, `HCC — Auto Launch Sling on Fire TV Wake`, `HCC — Recorder Down Watchdog`), plus 2 exact duplicate copies of the Recorder Watchdog (from repeated failed-looking save attempts) which were deleted first so they wouldn't triple-fire. Also: water/gas meters going "unavailable" was **not** the 07-28 WHUD pit-radio theory — that's a real separate finding, but the proximate cause blocking both meters was the rtlamr2mqtt add-on losing the RTL-SDR USB device (`No supported devices found`, add-on restart didn't clear it) — fixed by a full Beehive host reboot, which resets the USB bus. `HCC — Freeze Warning` automation removed entirely per Jeff's request (was a recurring annoyance, backyard temp already covered via Alexa) — see Pending Items, item removed.
- **07-31:** Full stability audit (3 parallel passes: HA/fetch layer, forms/localStorage, CSS/DOM consistency) run proactively after Jeff flagged the app breaking constantly. Found and fixed: **~20 external-link buttons across YARD/WEATHER used `window.open()`, a no-op in an installed iOS PWA** — converted to real `<a target="_blank">` (new `openExternal()` helper for the 2 spots needing JS logic first) + added missing `.vbtn`/`.upg-btn`/`.filter-row` CSS that had zero styling; 3 HA calls (`loadIrrigationFromHA`, `haIrrToggle`, `blinkSendPin`) bypassed `haFetch()` with short timeouts, same class as the "Beehive Offline" bug; the one unguarded `JSON.parse` in the file (state load on boot) — same failure class as the 06-23 blank-page incident; `saveSvc()` had no input validation unlike its siblings; `save()`/credential-save failures were silently swallowed; deleted dormant Mercedes-PIN-in-localStorage code that contradicted the 07-24 server-side-PIN invariant. Electric rate constant also updated same session (see below).
- **07-28:** Utility tiles (water/gas/electric) fully fixed — entity wiring, cost math, billing history, water Flow, electric Now/Today — all validated against Jeff's real bills. Root cause of several bugs: fields were gated behind conditions that silently skipped them once the real HA helper existed. Recorder was dead 07-02→07-28 (missing `default_config:`), now fixed — explains why history only goes back to the fix date. B-Hyve confirmed already working (was never broken — don't re-investigate `coordinator.py` without a fresh real error).
- **07-26:** Garage Door card built (app-side done, `loadGarage()`/`garageToggle()`); needs the **ratgdo board** (myQ is permanently API-blocked by Chamberlain, no software fix exists — see Garage Door section).
- **07-24:** F-250 added to CAR with vehicle switcher (see CAR reference below). CAR PIN prompts removed — mbapi2020 handles PIN server-side, app must never send a `pin` field. Fire TV motion pop-up fixed (`cmd media_session dispatch pause`, not the no-op `keyevent 127`).
- **07-23:** Sewer billing calibrated + shown separately from water; billing-history tracking added (`localStorage` key `water_billing_history`, up to 24 cycles).
- **07-22:** CAR rebuilt on real researched mbapi2020 services (see CAR reference below) — lesson: never guess entity/service names for an integration, read its actual source first.
- **07-21:** CAR live (mbapi2020/GLE 350), Blink cameras live, Family Login live. Stale-content bug root cause was Cloudflare's **CDN edge cache** on `service-worker.js` (separate from browser cache) plus a missing SW registration — check `cf-cache-status` on the custom domain, not just `Cache-Control`.
- **07-16:** CAR section added (Mercedes GLE 350).
- **07-09 → 07-15:** Blink cameras live (removed a stale `custom_components/blink` override — never re-add it), local AI camera detection (CodeProject.AI on the beast), Fire TV paired via ADB, iPad wall-display groundwork (Safari-15 `AbortSignal.timeout` polyfill).
- **07-01 → 07-07:** HA calls moved behind a server-side proxy (`haFetch()`→`/api/ha`) — fixes the whole "Beehive Offline" class of bug (mixed-content/CORS/relay timeouts); root cause was a shared `AbortSignal.timeout` reused across retries, never hoist one out of a retry loop. Water+gas meters went live via RTL-SDR. First HA devices (Tuya plug, Sharky vacuum) live.
- **06-23 → 06-29:** Initial build-out — nav/modals, GPS persistence + calibration, LUX/CLIMATE integration, light/dark theme, hero-grade + design-token system, font unification. See `git log` for detail.

---

## Beehive / Home Assistant Integration

**Hardware:** Beelink J45 (Gemini) mini-PC, Intel Pentium J4205, x86, ~8GB/128GB, running HA OS off its **internal SSD** (migrated off a flaky external USB drive 2026-07-02 — external is retired, don't reintroduce it). **4x USB 3.0 ports total** (confirmed by Jeff 08-05, corrects the earlier "2 free ports" assumption below) — one is yellow (likely always-on/charging, doesn't affect device choice). Current/planned allocation: RTL-SDR (water+gas meters, live) = 1 port; Zigbee coordinator (SONOFF ZBDongle-E, planned) on its own USB extension cable — keep it off a hub/away from other high-speed USB to avoid 2.4GHz interference = 1 port; HDMI capture card (planned, for streaming a TV feed into the app) = 1 port; **1 port spare.** Jeff has a separate USB-C multiport hub ("cakitte," 3x USB3.0 + Ethernet) available but it's not needed for this plan — plenty of direct ports on the J45.

**Architecture — three ways anything connects (only radio sticks physically plug into the J45):**
- **USB stick in the J45:** RTL-SDR (water+gas meters, live). Future: one Zigbee/Thread coordinator stick → whole mesh of cheap sensors/plugs for a security layer with no wiring.
- **Wi-Fi/LAN:** ESP32/ESPHome sensors, Shelly plugs, local cameras.
- **Cloud:** Blink, B-Hyve, LUX thermostat, SmartHub (CEMC electric).

**How Claude works (so nobody expects the wrong thing):** this cloud session has NO network access to Jeff's home LAN (Beehive J45 or "the beast" = Jeff's PC) — it writes exact instructions for Jeff or the local "coworker" session to execute. The coworker session (Claude Code on the beast) DOES have that access — see Mandatory Rule 13.

**HA Base URL:** `http://homeassistant.local:8123` (auto-fallback `http://192.168.1.66:8123`); remote via Nabu Casa `https://kmtpozwheqwww9t5uxhhvzzso1tvagro.ui.nabu.casa`. **Beast note:** the beast itself sometimes can't reach `192.168.1.66:8123` (VPN/AV blocking local IP) — use the phone if that happens.

**HA Token:** Jeff enters a Long-Lived Access Token once in the app (Profile → Long-Lived Access Tokens → Create Token, entered via HOME → "OPEN BEEHIVE ↗") → stored in `localStorage.ha_token`, used for all API calls.

**Camera section:** HA token + Beehive online → fetches camera entities from `/api/states`; no cameras found → shows Blink 2FA PIN entry (`blinkSendPin()` → `POST /api/services/blink/send_pin`).

**Irrigation section:** tries `loadIrrigationFromHA()` first (filters B-Hyve switches by zone/bhyve/orbit in entity_id or attributes); falls back to `loadIrrigationDirect()` (direct B-Hyve cloud API) if no B-Hyve entities in HA. `haIrrToggle(entityId, 'on'|'off')` → `POST /api/services/switch/turn_on|off`.

**CAR — mbapi2020 service architecture (Mercedes GLE 350):**
- **VIN:** `4JGFB4KB0MA478988` (hardcoded `CAR_VIN`). Helper: `carMbSvc(service, extraData)` → `haFetch('/api/services/mbapi2020/'+service, {vin: CAR_VIN, ...extraData})`.
- **Gas-vehicle services (NOT EV):** `engine_start`/`engine_stop` (start needs PIN), `doors_lock`/`doors_unlock` (unlock needs PIN), `auxheat_start/stop` (gas-only exhaust heater, no PIN), `temperature_configure` (16-30°C per zone, send as strings not numbers), `preconditioning_configure_seats`, `sigpos_start` (flash lights), `windows_close/open` (PIN for open), `sunroof_open/tilt/close` (PIN for open/tilt).
- **NOT for gas vehicles:** `preheat_start` (EV-only), `battery_max_soc_configure`, `charge_program_configure`.
- **PIN:** set up in Mercedes Me app, then stored in mbapi2020 integration options in HA — services auto-use it, app must never prompt for or send a `pin` field. **Confirmed present 08-06** by opening Settings → Devices & Services → MercedesME 2020 → gear on the account row: the Security PIN field is populated (`supports_options: true`, entry_id `01KY38Z7C90J2WE6S9R987JQZ4`). "Disable capabilities check" is also ticked there.
  - **✅ RESOLVED 08-06 — remote start CONFIRMED WORKING from the app. Two separate blockers, both had to clear:**
    1. **Mercedes' remote-attempt limit.** The car refuses further remote starts until it's been started manually with the key/START button. Jeff key-started it; that reset the counter.
    2. **The PIN needed re-entering AND a full HA restart.** `homeassistant.reload_config_entry` was NOT enough — mbapi2020 reads the Security PIN only when it initialises. Clear the field, retype, Submit, then restart HA.
    **Diagnostic that cracked it:** `sigpos_start` (flash lights) is the ONLY remote command needing no PIN. It worked while every PIN-gated command failed — that split proved the app, Cloudflare proxy, HA, VIN and integration were all fine and isolated the fault to the PIN in one step. **Use that test first next time.** Afterwards: zero mbapi2020 errors in `system_log/list`, and remote start succeeded from the app.
  - **🚗 REMOTE START HAS A HARD LIMIT MERCEDES ENFORCES — check this BEFORE debugging anything (08-06).** Jeff's own Mercedes Me app, screenshotted live: *"Your request to start the engine is unable to initiate because you have reached the limit of remote attempts between manual ignition cycles. Please use your key and manually start your vehicle the next time."* **The counter only resets when the car is physically started with the key.** Critically, that dialog appeared AFTER the app accepted his PIN — so the PIN was valid and the refusal was for a completely different reason. This is very likely what the `RIS_PIN_INVALID` below actually was: Mercedes handing a vague/wrong error code to a third-party integration where its own app gave the real message. **Before chasing a "broken" remote start: confirm the car has been key-started since the last remote attempt.** Preconditions were all verified green at the time (fuel 100%, range 524mi, doors locked, windows closed, park brake on, engine off) — so fuel/doors are NOT the usual culprit here.
  - **🔴 EARLIER 08-06 FINDING, now in doubt — `RIS_PIN_INVALID`.** Jeff reported that unlock/remote-start don't work and suspected HA wasn't supplying the PIN. Real evidence from his live HA system log (`system_log/list` over the WS API): `custom_components.mbapi2020.client — Car action: ENGINESTART failed. error_code: RIS_PIN_INVALID`. **So mbapi2020 IS sending the PIN correctly — Mercedes is rejecting it as invalid.** The PIN saved in the integration options does not match the PIN currently set in the Mercedes Me mobile app. **Fix (Jeff's to do, it's a security credential):** confirm/reset the Security PIN in the Mercedes Me app, then put that exact value in Settings → Devices & Services → MercedesME 2020 → gear → Security PIN. Nothing to change in this repo. Re-check the same log line after retrying a command to confirm.
  - **⚠️ DIAGNOSTIC TRAP — do not repeat this (coworker, 08-06).** On the way to the above I checked the PIN via HA's `config_entries/get` over the WS API, saw `options` come back empty, and told Jeff the PIN was missing. **That was wrong.** HA's config-entry list API does not return `data` or `options` at all — they're internal. The tell was that `data` came back empty too, which is impossible for a loaded integration with 49 live entities. **"Field absent from the API response" ≠ "field is empty."** To read an integration's options, open the options dialog in the UI (it renders from stored values); to diagnose a failing service, **go to `system_log/list` first** — one log line gave the real answer after the API check had sent me the wrong way.
- **Entity naming:** `sensor.gle_350_odometer`, `lock.gle_350_lock`, `binary_sensor.gle_350_windows_closed` (⚠️ inverted semantics, `on`=closed — code must detect `*_closed` and flip logic), `switch.gle_350_auxheat`, `button.gle_350_preclimate_start`.
- **Scoping:** always scope CAR entity/lock lookups to Mercedes/GLE/mbapi entities — house entities can bleed in otherwise (fixed 07-21).

**Ford F-250 (2001, VIN `3FTNX21FX1MA23431`, 7.3L Power Stroke Diesel, 4WD, crew cab):** no connected-car features (pre-dates FordPass Connect) — no remote start/lock/GPS from HA. `carSwitchVehicle('merc'|'ford')` persists in `localStorage.hcc_vehicle`. Future: OBD-II port + Veepeak OBDCheck BLE+ (~$30) + ESP32/ESPHome for live RPM/coolant/battery/DTCs, optional NEO-6M GPS module.

**Garage Door — ratgdo:** Jeff's Chamberlain myQ hub is permanently useless for HA (Chamberlain blocked all 3rd-party API access 2023; native `myq` integration removed; no HACS workaround exists or is coming — confirmed dead 07-28, don't revisit absent a major policy reversal). **⚠️ Model number correction 08-06:** this file previously recorded the hub itself as "MYQ-G0402" — research confirms G0402 is actually Chamberlain's *add-on door sensor* SKU (for a second door), not the hub model (hub is G0401 or similar generation). Jeff confirmed he has **both parts** (hub + sensor) — exact hub model not re-confirmed, low priority now that MyQ is being sold (see below). **Correct path: ratgdo board** wired directly to the opener motor (3 wires), ESPHome firmware, local WiFi to HA, no cloud dependency. Works with Security+ 2.0 (yellow learn button), Security+ (purple/red), older (orange/dry contacts) — Jeff needs to check his opener's learn-button color. Expected entities: `cover.ratgdo_*`, `light.ratgdo_*`, `lock.ratgdo_*`, `binary_sensor.ratgdo_*`. App already auto-detects any `cover.*garage*` entity (`loadGuardian()`/`loadGarage()`) — card appears the moment ratgdo is adopted. (As of 08-08 the same functions also auto-detect `switch.*garage*` + `binary_sensor.*garage*` for the SONOFF MINI DRY path — see the 08-08 changelog entry.) **Jeff plans to sell the MyQ hub+sensor on eBay (08-06)** rather than keep a separate app just to check door status — see the SONOFF MINI DRY entry below for the reasoning and what replaces the position-sensing it's giving up. **Cheapest correct option (researched 08-04):** the official kit (ratcloud.llc) is $45; the **Gelidus Research RATGDO Alternate Board, USB-C v2 (~$22-25, gelidus.ca)** is pre-assembled, pre-flashed with ratgdo-compatible ESPHome firmware, same Security+/Security+2.0 auto-detection and full feature parity (door/light/lock/obstruction) — the cheapest option that doesn't give anything up. Cheaper still: the DIY `rat-ratgdo` open-source design (ESP8266 + a few resistors/MOSFETs, ~$15-20 in parts) — same firmware family, but Jeff would need to solder/assemble it himself. **Avoid** cheap ~$19 "ESPHome garage door" relay modules (e.g. Athom) — those are plain relay+reed-switch, NOT Security+ protocol-aware, so no light/lock/obstruction status, a materially degraded substitute. **Protocol confirmed 08-05 — Jeff bridged the wall-button wires directly and the door toggled**, matching ratgdo's own documented test for dry-contact/Security+1.0 (a true Security+2.0 opener's encoded signal would NOT respond to a raw short) — his opener is dry-contact-compatible, not Security+2.0. **CORRECTED 08-05 — Jeff caught a real mistake in the reasoning, ratgdo/Gelidus board dropped entirely.** Jeff pushed back: if it's confirmed dry-contact (no Security+ protocol), why pay $22-25 for a board whose whole value is decoding that protocol? He's right — the ratgdo/Gelidus premium over a plain relay is 100% for Security+ signal decoding, which his opener doesn't use. Combined with not needing position/light/lock (doesn't park there), a protocol-aware board buys nothing. **Corrected again 08-05 — exact model matters, "SONOFF Basic" was too vague/wrong.** Basic-series SONOFFs are mains-voltage (110-240V) switches — using one on a low-voltage garage door circuit needs modification, not a clean fit. Considered SONOFF SV ("Safe Voltage," ~$14-25) + ESPHome next, but **Jeff found a better match: SONOFF MINI-D (~$15-20, Amazon).** Verified via research: real Matter support (pairs directly into HA's built-in Matter integration — free, no extra hardware, Beehive already runs HA OS which supports it natively for WiFi Matter devices; no ESPHome flash needed at all), dry-contact NO/COM/NC relay output, explicitly marketed+reviewed for garage door automation, and has a hardware "Inching Mode" setting for a proper momentary pulse (not a held on/off toggle) — exactly matches a garage trigger. **This is now the final part.** Wires into the same white/red wires Jeff already tested.

**Door position sensor — no longer a "future, not needed" item (decided 08-06).** Jeff has both MyQ parts (hub + sensor) and is selling them on eBay rather than keep opening a separate app just to check open/closed — reasonable, that's the whole point of consolidating into one app. That means the HCC app needs its own real position sensor to replace what MyQ was providing. **Recommendation: a Zigbee door/window contact sensor** (SONOFF SNZB-04P — same brand ecosystem as the coordinator dongle already in play, confirmed Zigbee2MQTT/ZHA compatible; or Aqara Door/Window Sensor, the other well-regarded option) — battery-powered, no wiring, needs the Zigbee coordinator (Pending Item 8) paired into HA first. Mount as a standard magnetic contact sensor (door panel + floor/frame at closed position), not a tilt sensor — tilt sensors are a real alternative for garage doors (avoid magnet-alignment on a moving track) but have their own dust/humidity quirks; contact is the simpler, cheaper default. Becomes its own `binary_sensor.*` entity, independent of the Sonoff relay. Full detail added to `docs/beehive/garage_door_sonoff_mini_dry_setup_2026-08-06.md`.

**Part arrived 08-06 — full setup plan written, `docs/beehive/garage_door_sonoff_mini_dry_setup_2026-08-06.md`.** Researched (not guessed) rather than repeat the 08-05 mistake pattern: (1) device needs **either** AC (100-240V, N/L) **or** DC (12-48V, DC+/DC-) power, not both — Jeff/coworker needs to check on-site what's actually available at the opener (multimeter-verify any low-voltage terminal before assuming it's DC in range; easiest fallback is powering off the same circuit as the opener motor itself if a spare hot/neutral or outlet slot exists there). (2) Wire the relay's NO/COM output **in parallel with the existing wall button** (don't remove the button) — same two wires Jeff already bridge-tested 08-05. Don't need S1/S2 (that's for a *secondary* physical switch wired into the Sonoff itself, not needed here). (3) **Critical, easy to miss: the Inching (momentary pulse) setting can ONLY be configured via the eWeLink app — HA's Matter integration doesn't expose it at all.** Must pair to eWeLink first, turn on Inching Setting (~0.5-1s), before or in addition to Matter-commissioning into HA. It's a device-level setting once saved, not something that has to be resent per command. (4) **The old "unconfirmed, verify once paired" question is now answered:** independent testing confirms it shows up in HA as a plain `switch.*` entity via Matter, not a native `cover` — so rather than have Jeff/coworker build HA-side template-cover YAML to fake a cover entity, **the cloud session will adjust `loadGarage()`/`garageToggle()` in the app itself** to recognize a plain garage switch entity (simple trigger button, no open/closed state display since there's still no position sensor) once given the real entity ID post-pairing. **Follow-up 08-06 — Jeff's 3 practical questions, all resolved via research, doc updated:** (a) **install at the opener, not the wall switch** — direct wire run to the low-voltage terminals, and the wall-switch box likely doesn't have room for the module; (b) **coexists fine with MyQ + the wall button** — confirmed MyQ connects to the same shared "wall console" terminals as the physical button, so the Sonoff becomes a third independent trigger on one simple momentary-contact circuit, no conflict; (c) **power via a basic 2-wire AC cord with a plug** (landed on N/L), not a splice into the opener's internal wiring — Jeff has outlet access at the opener location, so that's the simple path.

**GaragePC — HP TouchSmart 520-1020 all-in-one ("GaragePC", researched 08-05, dedicating to HA + app):** 23" 1080p touchscreen, Pentium G620 dual-core 2.6GHz (Sandy Bridge, no AVX), upgraded to 8GB DDR3, 500GB HDD (81GB used), gigabit Ethernet, Techkey USB WiFi dongle, **integrated HD webcam + mic + Beats speakers**, SD reader, DVD. Windows 10 support ends Oct — plan is wipe to **Linux Mint + Chromium kiosk** auto-booting the app fullscreen. **⚠️ Touchscreen is NextWindow Voltron — does NOT work out of the box on Linux; needs the community `nwfermi` DKMS driver (confirmed working on this exact model on Mint 20.1 per Mint forums). This kills ChromeOS Flex as an option (can't load the driver). Fallback if driver fights: cheap wireless mini-keyboard.** Planned roles once on Mint: (1) touch kiosk for the app; (2) built-in webcam → go2rtc → "Garage Cam" tile in app (UVC-standard, works on Linux); (3) TTS announcement speaker via HA; (4) HA voice-assistant satellite (Wyoming, mic+speakers onboard, CPU is sufficient); (5) Glances health sensors + Wake-on-LAN + SSH shutdown + screen wake-on-garage-door; (6) Samba share on idle ~380GB as a second Beehive backup target. **Ruled out (CPU too weak / no AVX — don't revisit):** OBS/Sling restream host, CodeProject.AI or any AI workload. Optional: ~$25 SSD swap (moderate all-in-one teardown). Setup is hands-on (Jeff + written walkthrough); cloud session to write the Mint/kiosk/nwfermi walkthrough when Jeff says go.

**Spare hardware inventory (added 08-05):** (1) **KESU 500GB portable USB 3.0 drive** — assigned to the AirTV 2 as its DVR recording drive (AirTV formats it on setup; ~100+ hrs OTA); if a different drive ends up on the AirTV, fallback role is extra storage on the GaragePC. Single 2.5" spinning drive — never the sole copy of anything important. (2) **Lenovo B570 laptop** (model 1068, mfg 2012, S/N WB06276882, 65W 20V charger) — 15.6" 1366×768, Sandy Bridge era, DDR3, DVD, webcam, Ethernet+WiFi, **HDMI out**, own battery (free mini-UPS). **CPU confirmed 08-05: Pentium B960 @ 2.2GHz (Sandy Bridge dual-core, no AVX), 8GB RAM** — heavy encode (Sling restream) and AI officially ruled out, same verdict as GaragePC; kiosk/browser/light-helper roles all comfortably fine. Windows device name is "DellMasterBed" (hand-me-down name — don't go hunting for a mystery Dell on the network). Kitchen-player role no longer needed (wall iPad handles Sling web) — now: Linux-conversion candidate / second kiosk / small always-on helper. (3) **Delam condenser mic** (BM-800-class, XLR, foam windscreen + shock mount) — assigned: garage voice-assistant/intercom mic for the GaragePC (3.5mm mic jack on its side panel, or a ~$10 USB audio dongle if plug-in power is too weak for it — typical for these condensers; find the kit's included cables/adapter). (4) **WD Scorpio Blue 320GB bare 2.5" HDD** (WD3200BPVT, 5400rpm, mfg 04/2012) — assigned: risk-free Linux Mint test drive for the B570 (swap with its Windows drive — one panel screw — trial the kiosk build with zero risk, original drive shelved as instant rollback). Old+small: never a backup target, never sole-copy storage. Generic portable use needs a ~$8 USB-SATA cable. (5) **Wireless display stick, model HDMI-005** ("AnyCast"-class Miracast receiver, micro-USB powered) — turns any HDMI TV into a screen-mirror target; good for casting the app/dashboards to any TV, laggy for video and DRM content (Sling) typically blocks or blacks out over mirroring — not a TV-watching tool. (6) **HDMI→USB capture stick** (black dongle, HDMI female in + USB 3.0 A pigtail out — **identified 08-05 via Camera-app test on the beast: shows up as a camera = capture device confirmed**) — assigned to the Kitchen TV chain, replacing the planned AXHDCAP purchase entirely. Chain is now Roku → EZCOO EZ118K (only part left to buy, ~$20-25, still on hold pending AirTV locals decision) → this stick → J45 USB → go2rtc.

**Kitchen TV feed — WORKING 08-05 via Sling web, capture chain now optional/on hold:** kitchen iPad is an **iPad Air 2, wall-mounted in a built-in frame** (iPadOS 15 max, same Safari-15 the app was polyfilled for 07-09→07-15; Sling's native app can't install). **Jeff tested `watch.sling.com` in its Safari — plays live TV perfectly (photo-confirmed, Fox News live). Kitchen TV solved at $0.** Advised: Auto-Lock → Never + keep on power; Sling web shows occasional "still watching?" prompts (normal). **Capture chain (Roku → EZCOO EZ118K HDCP-bypass extractor → Audio Express AXHDCAP card → J45 → go2rtc → "Kitchen TV" app card) is NOT ordered — on hold until the AirTV 2 arrives**; its remaining value: (1) AirTV antenna locals (won't play in browser — Roku-only), (2) TV as a card inside the app itself (go2rtc WebRTC plays fine on the Air 2), (3) HA/Alexa channel control via Roku integration. Decide after locals are seen working on the Roku. Fallback kitchen player if ever needed: Lenovo B570 in a browser. Chain: **Roku (Sling app) → EZCOO EZ118K audio extractor (explicit HDCP 2.2/2.3 BYPASS — verified; most "HDCP compliant" extractors do NOT strip, e.g. OREI HDA-912 confirmed doesn't work for capture) → Audio Express AXHDCAP USB capture card (~$15) → J45 USB → go2rtc (built into HA since 2024.11) → new "Kitchen TV" card in app** (same pattern as camera tiles — app side to be built once hardware lands and the capture device shows up on Beehive). Roku also gets HA's native Roku integration (local IP, no pairing dance) for play/pause/channel control. **AirTV 2 (on the way, + 500GB record drive): locked to Sling ecosystem, NO open API (can't feed HA directly, confirmed — not a Plex/Channels/HDHomeRun substitute), but it injects antenna locals into Sling's guide on the Roku, so locals ride the capture chain for free; needs an antenna hooked to it. Sling's browser player likely can't show AirTV locals (unsupported device class) — Roku path is the real one.** Free things to try first: watch.sling.com in kitchen iPad Safari (may fail on old-Safari DRM); OBS screen-capture restream from the beast (HDCP doesn't apply to on-PC screen capture) → go2rtc, as a $0 proof of concept — beast-side coworker job.

**Smart Lighting Project (Kasa HS220/HS200, plan logged 08-06):** Jeff sent a finished electrical plan (2 PDFs, saved verbatim to `docs/lighting/kasa_smart_lighting_project_2026-08-06.md` + `docs/lighting/bedroom_wiring_plan_2026-08-06.md` — read those for full box-by-box wiring detail) to replace switches in Bedroom/Kitchen+Dining/Living Room/Garage with **Kasa HS220** (dimmer) and **Kasa HS200** (non-dim, garage), local HA integration via `python-kasa`, no hub. Picked over MOES (100W/gang limit — doesn't fit the 108W bedroom/kitchen loads once combined to one switch per room) and Shelly Dimmer Gen3 (2x the cost, more config). **Bedroom is the clever part:** reversed the feed so the door box (not bedside) is the power origin, and instead of blanking the now-redundant middle/bedside switch positions, each existing toggle gets repurposed as a fully independent DUMB switch controlling only its own box's receptacle (no WiFi, no HA entity) — every existing switch keeps a real job, no blank plates. **Verified via research 08-06 (not just eyeballed):** the 108W bedroom/kitchen loads have real headroom under the HS220's 150W-LED max rating — the wattage math in the plan is sound. **Real issue flagged, not yet resolved:** the garage's "2 switch locations (kitchen + garage)" is planned around a single HS200 — confirmed via research a lone HS200 in a 2-location circuit leaves the OTHER physical switch non-functional; the matched **HS210 kit** (WiFi-coordinated pair, no traveler-wire dependency) is what actually keeps both switch locations live. See Pending Items below — needs Jeff's call before ordering.

---

## Pending Items (Next Session Should Address These)

0. **HA backup encryption key — needs a durable copy outside this one PC (Jeff's call on where).** All Beehive backups are encrypted (HA default). Retrieved the real key live via the backup config API (`backup/config/info`) and saved it to `C:\Users\jeffl\HCC-secrets\ha_backup_encryption_key.txt` — but that's the same single PC as everything else backup-related, so it's not yet truly independent. Without this key, the `.tar` archives in `HCC-Beehive-Backups\` (iCloud) are undecryptable, so it's the single most load-bearing secret in the whole disaster-recovery system. **Never put the raw key in this git repo (public).** Jeff should save a copy somewhere durable and independent of this PC — password manager, printed + physical safe, etc. — next session should confirm he's done this.
1. **SONOFF MINI DRY** — Jeff/coworker to wire + power + eWeLink-pair (Inching Mode) + Matter-commission into HA (see Garage Door above and the setup doc). App side is fully done as of 08-08 and will auto-detect the switch entity by name the moment it's paired — no entity ID hand-off needed.
2. **iPad Air 2 wall-display** — Safari-15 polyfill deployed and working; HA token persistence + "Add to Home Screen" + Guided Access still need final confirmation.
3. **F-250 OBD-II sensor box** — Veepeak OBDCheck BLE+ (~$30) + ESP32 + optional GPS for live diagnostics.
4. **Panic automation (HA side)** — app already fires webhook `hcc-panic-button`; HA automation pending Zigbee hardware (coordinator stick + siren + sensors). See `docs/beehive/panic_alarm_automation.md`.
5. **Lighthouse score — basic wins done 07-31, full minification still pending (deliberately out of scope).** Recompressed every JPEG to quality 80 (images/ 12MB → 7.1MB, ~41% smaller, visually verified no quality loss), deleted 6 confirmed-dead image files + fixed service-worker.js's stale precache list (was still referencing 2 of those deleted files, was missing the current hero image — bumped hcc-v11→v12), trimmed the Google Fonts request from 6 weights×2 families down to only the 4-5 weights actually used, and added `loading="lazy"` to every below-the-fold/hidden-section image — then caught and fixed a real CLS regression that lazy-loading introduced (utility meter photos + Dispatch card had no reserved `aspect-ratio`, so the page jumped when they loaded in; fixed by reserving each image's real aspect ratio). Verified honestly via a controlled before/after Lighthouse A/B run in the same sandboxed environment (not a real device, not the live Cloudflare-CDN'd site — absolute numbers aren't directly comparable to a real Lighthouse run against the deployed URL): total page weight down ~56%, LCP nearly halved, CLS held steady/improved slightly, composite score unchanged. **What's left (explicitly out of scope, would need restructuring):** minifying the actual JS/CSS inside the single `index.html` file, and/or splitting the inline `<script>` into an external deferred file — those are the two biggest remaining Lighthouse opportunities (`unused-javascript` ~235 KiB, `unminified-javascript` ~71 KiB) per the local audit. For a trustworthy real score, run Lighthouse against the live `toro1-5rz.pages.dev` URL (Chrome DevTools or PageSpeed Insights), not a local file.
6. **Lucky Mike "Smart Stall"** — queued, plans in `docs/lucky-mike/` (read `INTEGRATION_NOTES.md` first). New "STABLE" section, `--a-stable` accent. **Do not start until Jeff says go.**
7. ~~Desktop-wide-browser layout gap~~ — **FIXED 08-06.** Jeff asked for a full cross-format pass (phone/iPad/web/TV); reproduced this at 1024px+ and root-caused it precisely: `.house-hero`/`.sec-hero*` used `height:auto`+`aspect-ratio`+`max-height` with no `width` set, so once the aspect-ratio-driven height would exceed `max-height`, the browser shrank the *width* to keep both constraints satisfied — leaving blank space next to the hero instead of just cropping more of the image. Fixed with one added rule: `width:100%` on all 7 hero classes (`.house-hero`, `.sec-hero`, `.sec-hero-weather/-irr/-yard/-guardian/-car`) — forces full-bleed width at every size, image crops top/bottom instead. Verified via Playwright screenshots at phone(390)/iPad-portrait(768)/iPad-landscape(1024)/web(1440)/TV(1920): zero gap at every size, all 6 section heroes checked individually for the baked-in titles (Yard/Car/Guardian have photo-baked text) — none cropped. Also confirmed zero horizontal page overflow at 1920px across every section + every YARD/CAR sub-tab. `lint-app.js`/`smoke-test.js` clean throughout.
8. **Zigbee alarm hardware — Jeff purchasing 07-31.** Bare-bones parts list (coordinator, door sensors, siren) plus the full Guardian safety-layer wishlist (smoke, water leak, freeze, water-main shutoff) researched and handed to Jeff as a downloadable file this session — real current products verified via web search, not guessed. Once hardware arrives and is paired in HA, item 4 (Panic automation) becomes buildable — `docs/beehive/panic_alarm_automation.md` already has the automation YAML ready, just needs real entity ids swapped in.
9. ~~`zone.work` centered on the office instead of the actual parking garage~~ — **FIXED 08-01.** Jeff gave the real garage address (310 Commerce St, Nashville, TN); geocoded to 36.1624877, -86.7776215 (icon changed to `mdi:parking`), applied via `zone.reload`, confirmed live via `/api/states/zone.work`. Note: this address is only ~90m from the original office coordinates, not the ~0.4mi Jeff estimated earlier — flagged to him, not fully reconciled, but he gave the address directly so it took priority. ~~Also: Angela's phone tracker unreliability~~ — **FIXED 08-01, see Change Log** — real cause was an empty/stale Push ID in the app's own Notifications settings (not the iOS permissions, though those got fixed too along the way), confirmed via a genuine organic background update after the reset.
10. ~~Water meter pit radio fault, call WHUD~~ — **RETRACTED same day, no call needed.** Live-tested further (IDM protocol probe + longer observation window): the meter and pit radio are both healthy. Root cause of the original "stuck" reading was `rtlamr2mqtt`'s own `-unique=true` flag only re-publishing when the decoded value changes, combined with this meter batching its own broadcast updates (every ~20 min to ~3 hours observed, not continuous) rather than any hardware fault. See 08-01 Change Log correction entry for the full trail.
11. ~~Leak-detection automation~~ — **BOTH pieces built and live 08-01, see Change Log.** (a) `HCC — Possible Water Leak (Idle Flow)` — fast, custom automation, live. (b) `Water-Monitor` HACS integration — installed and connected, but its low-flow/tank-refill detector thresholds are still on defaults, not tuned for this meter's batched (~20min-3hr gap) reporting cadence. **Remaining follow-up, low priority:** revisit Water-Monitor's options (seed/persistence duration for the low-flow detector) once there's a few days of real data to judge whether defaults are too slow/twitchy.
12. **Fire TV PiP popup: wrong/stale frame — FIXED and verified live 08-03, see Change Log. Real-world delay — still open, root cause identified but not fixable from HA.** The wrong-frame half is done: popup now shows the actual extracted frame from the real Blink clip, confirmed by Jeff watching the physical Fire TV on two separate live tests. The delay half: this session's automation chain itself only adds ~8 seconds (clip-ready → download → extract → scan → Fire TV trigger, measured live); the remaining lag Jeff feels is Blink's own cloud motion-detection latency before HA's binary_sensor even flips `on` — upstream of HA entirely, not something more polling/automation logic can shorten. No further HA-side work identified for this; would need a Blink-side or camera-hardware change to improve further.
13. ~~Blink motion filtering for phone notifications~~ — **already existed, nothing to build (08-02).** `AI Object Detected Notify` already does this exactly: filters through CodeProject.AI, branches on person/vehicle/animal, sends distinct pushes with a 15-min per-camera mute button, confirmed live and covering all 6 cameras.
14. ~~Rain-skip irrigation automation~~ — **NOT NEEDED, confirmed via research 08-02.** Jeff's B-Hyve WeatherSense already does real weather-adaptive watering (rain-skip + temp/wind adjustment), and genuinely supports a personal weather station (PWSWeather.com/Aeris) as Jeff described. Independent testing showed 100% skip reliability on any 0.2"+ rain day. An HA-side duplicate would be strictly worse (no wind/temp handling). Not building this.
15. ~~Daily Morning Digest~~ — **BUILT AND TESTED 08-02, see Change Log.** `HCC — Morning Digest` (7am push + persistent notification, weather/vacuum/car/utility status/Blink health) is live. Caught and fixed a real bug during testing: the "active alerts" count silently always returned 0 (persistent notifications have been unreadable from templates since HA 2023.6 — confirmed via research, not a local misconfiguration) — removed that metric rather than ship a false "all clear."
16. **"Alexa, fast forward the commercials" — WORKING via native phrasing (08-03), skip distance still needs calibration to 4:40.** Root cause (found earlier 08-03, still valid): "fast forward" is an Alexa-reserved phrase that never reaches a custom Routine (HA's own Alexa Smart Home skill also has no `FastForward`/`Rewind` handler at all — home-assistant/core#87327). Angela tried creating a Routine with a fresh phrase and got total silence — turned out no Routine had actually been saved yet for it. **Real fix that works, found live:** skip Routines entirely — since `script.hcc_skip_commercial` (friendly name "FF the Commercials") is already exposed to Alexa (`cloud.alexa.should_expose: true`, confirmed via entity registry), Alexa's native **"Alexa, turn on FF the Commercials"** phrasing reaches HA directly and reliably fires the script — confirmed twice live (script `last_triggered` updated within ~1s of the phrase both times). Also confirmed via live test: the Fire TV/ADB link itself is healthy (`media_player.fire_tv_viewing_room` responds to `adb_command` immediately). **Remaining real issue, not yet solved:** the skip distance is wrong — the original 3× `keyevent 90` (1.2s apart) skipped "way too far" in Sling specifically (media apps often ramp fast-forward speed with rapid repeated presses, non-linearly). Reduced live to a single `keyevent 90` press as a starting point (`packages/hcc.yaml`, `script.hcc_skip_commercial`) — **not yet re-tested against Jeff's actual target of exactly 4:40 (280s)**; next session should fire it live, get real seconds-skipped feedback, and iterate the press-count/delay until it lands on 4:40. No Alexa Routine needed for this at all going forward — the native "turn on <name>" phrasing is the whole fix. **Separately, worth knowing for any future custom voice command:** HA's own local Assist (bundled with Nabu Casa) doesn't have Alexa's reserved-phrase problem — a better long-term path than fighting Alexa Routines each time this class of issue comes up.
17. **App-wide hardcoded-hex-instead-of-theme-token pattern (found 08-03, not fixed — real cleanup opportunity, not urgent).** While building the LUX photo overlay, found `luxConn`/active-fan-button hardcoded `#22c55e`/`#d4af37` (exactly the dark-mode token values) instead of `var(--ok)`/`var(--gold)` — fixed those two since I was already touching that code. A repo-wide grep turns up the identical pattern **100+ more times** across the file (status dots, badges, toast messages, map legend, etc.) — all presumably written before light mode existed and never revisited. Most are probably fine in practice (many sit on dark surfaces regardless of theme), but some are likely silently broken in light mode the same way the two I fixed were. Worth a dedicated audit session (grep for literal `#22c55e`/`#d4af37`/`#ff6262`/etc. in `.style.color=` assignments, check each against both themes) rather than fixing piecemeal — flagging so it doesn't get lost.
18. ~~Electric SmartHub real-data upgrade — needs live confirmation~~ — **FIXED 08-06, real bugs found + fixed same day.** The coworker fired the exact WS command from `ha-stats.js` against Jeff's real HA (Core 2026.8.0) and found the feature was silently non-functional despite the UI cells rendering: (1) `history/statistics_during_period` doesn't exist on this HA version — real command is **`recorder/statistics_during_period`**, fixed in `functions/api/ha-stats.js`. (2) The `change` field always read `0` for this sensor even across confirmed real usage growth (`sum` moved 761→872 over 48h while every `change` was 0) — `loadElectricStats()` now computes each period's usage as a real **diff of consecutive cumulative `sum`/`state` readings** (`toDiffedSeries()`) instead of trusting a per-bucket field, same pattern as `irrGalFromHistory()`. Re-verified via a mocked Playwright test built from the coworker's exact real data shape — Today/Yesterday/Peak Hour/Last 7 Days all compute correctly now. **Item 3 (Bill Due/Last Payment/vs-Last-Year): checked live by the coworker, confirmed NOT available as attributes** (`account_id`/`meter_name`/etc. only) — would need a new SmartHub-account scraper, real new surface area for a nice-to-have. Recommend skip unless Jeff wants it. **Still true either way: this cloud session cannot test the real WS round-trip itself (no network path), so any future change to this code needs the same live-fire verification pattern before trusting it's actually working.**
20. **Irrigation zone photos — decide whether all 6 get the real-photo cleanup, or just Garden (added 08-08).** All 6 `images/zones/zone-N.jpg` files carry the same fake gold-frame/title/tagline marketing overlay that the utility meter photos had before their 08-06 fix — this contradicts the "real photographs, just enhanced" note above and needs correcting either way. This cloud sandbox has no image-gen/editing tool, so this is a coworker-PC job using the same Gemini image-edit pipeline already proven on the utility photos. Waiting on Jeff's answer before handing it off.
19. **Smart Lighting — garage switch needs a decision before ordering (added 08-06).** The lighting plan (`docs/lighting/`) has the garage light controlled from 2 physical locations (kitchen box + garage box) but only 1 Kasa **HS200** on the shopping list. Verified via research: a lone HS200 in that setup leaves the OTHER switch location dead/non-functional — it's a single-pole device, not designed for 2-location control. Jeff needs to pick one: (a) swap to the **HS210 kit** (matched WiFi-coordinated pair, keeps both the kitchen and garage positions fully working, no traveler-wire rewiring needed) if he wants real control from both spots, or (b) keep the single HS200 and consciously cap/repurpose the kitchen position (same pattern already used for the bedroom's middle/bedside switches — turn it into a dumb switch for something else, e.g. a receptacle) if garage-only control is fine. Not yet resolved — flag it to Jeff before he buys the HS200. Everything else in the plan (wattage math for the HS220 dimmers, box redesign, device choice over MOES/Shelly) checked out fine on review.

---

## LUX Thermostat — API Reference (DO NOT CHANGE UNLESS BROKEN)

**Auth flow** (4 steps, in `functions/api/climate.js`):
1. GET the B2C authorize URL with PKCE code_challenge → parse `x-ms-cpim-csrf` cookie + `transId` from HTML
2. POST `SelfAsserted` with `{logonIdentifier, password, request_type:'RESPONSE'}` + CSRF header + cookies
3. GET `confirmed` → follow redirects to the custom-scheme URL → extract `code=`
4. POST the token endpoint with `{grant_type:authorization_code, code, code_verifier, client_id, redirect_uri}` → `{access_token, refresh_token}`

**Client ID:** `b335ca43-3bde-4406-b281-8816afb7cc91` · **Redirect URI:** `connecteddevicesjci.luxmobile://connecteddevicesjci/path` · **Scope:** `.../mobile/user_impersonation .../mobile/read_write offline_access openid`

**API (Bearer token):** `GET /api/location/user` → devices list. `GET /api/device` + header `Deviceid` → `{systemmode, holdheat, holdcool, currenttemp, fanmode}`. `POST /api/device` + `Deviceid` header + full state JSON (writes use **POST not PUT** — PUT returns 500).

**Fields (°F, no conversion):** `systemmode` 0=off/1=heat/2=cool/3=auto; `holdheat`/`holdcool` = setpoints; `currenttemp`; `fanmode` 0=auto/1=on.

**Jeff's device:** CS1-DD-FB.

---

## Water + Gas + Electric Meter Integration

**Status:** Water + Gas LIVE via RTL-SDR + rtlamr2mqtt on the J45. Electric "This Month" LIVE via the SmartHub (CEMC) cloud integration — a real ATM90E32 CT-clamp build is still the future path for true instant Now/Today.

### 💧 WATER — WHUD · Kamstrup flowIQ 2100
- Meter S/N `25394131`, billing cycle ~21st. Rates: Base $10.32 + $0.00908/gal (validated). Sewer (City of White House, mirrors the WHUD meter, no separate meter): **Base $23.42 + $0.01011/gal** (rate increase confirmed 08-05 from the 5/7–6/6/26 bill — was $22.74 + $0.00982/gal, both figures up ~3%, exact match to the bill's $92.56 base+consumption). **Known gap, not yet fixed:** the bill also carries Sanitation Services ($24.00 flat) + Stormwater Fee ($8.99 flat) — real monthly charges on the City of WH bill that "Est. Sewer" does NOT include (usage-based sewer cost only). Asked Jeff whether to fold the ~$33/mo flat fees into the estimate — awaiting his call.
- **08-06 — `IRR_FLOW` (irrigation zone GPM, feeds the sewer-overcharge waste calc) recalibrated to real measured data** — see Change Log + `docs/utilities/irrigation_gpm_calibration_2026-08-06.md`. The old spec-sheet-guess constants were 23-49% too high depending on zone, meaning the "Total sewer overcharge tracked" running total has likely been overstated for any already-closed cycle. Self-corrects going forward automatically; past cycles are not retroactively fixed (no HA history to recompute them from).
- **08-05 — full writeup for coworker verification:** `docs/utilities/sewer_overcharge_verification_2026-08-05.md` has the complete handoff — what changed in the app today (rate fix, garbage/stormwater split, the tracking bug below) plus a concrete checklist of what only the coworker's real HA/LAN access can verify (recorder retention for retroactive fixes, confirming `irrGalFromHistory()` actually succeeds live, GPM drift check, meter-gap sanity pass). Read that file before touching this area again.
- **FIXED 08-05 — tracked sewer-overcharge total was silently stuck on the less-accurate schedule estimate, never refreshed with real B-Hyve data.** Jeff asked me to confirm the "Total sewer overcharge tracked" accumulation is correct before he starts sending bills to cross-check it. Found a real gap: `irrGalFromHistory()` (real B-Hyve on/off runtime, accounts for rain delays/skipped days) only ever updated the on-screen note — the actual stored `water_billing_history` entry (which the running total sums) was only ever written by `putWaterCycle()`'s rougher schedule-based estimate (`irrMonthlyGal()`) and never got the refined number, so the tracked total was quietly less accurate than what was shown on screen, forever. Fixed: `irrGalFromHistory()` now also overwrites the current cycle's `irrGal`/`waste` fields with the real number and re-renders the history table. Verified the underlying find-and-update accumulation pattern in isolation (no duplicate rows per cycle, refinement overwrites in place, running total sums correctly) — **could not test the live B-Hyve/HA fetch itself, this sandbox has no network path to Jeff's real HA instance.** Full regression (syntax/lint/smoke-test) clean.
- **RESOLVED 08-05 — Garbage/Stormwater added as their OWN separate line items, deliberately kept OUT of the sewer-waste math.** Jeff's goal: he's building a case with WHUD/City of WH to get money back for irrigation water that never enters the sewer but still gets charged sewer rates — needs clean, uncontaminated usage-based data for that. Flat fees (garbage/stormwater don't vary with water use at all) would have diluted that argument if folded into `SEWER_BASE`/`SEWER_PER_GAL`/`sewerWaste`. Added `GARBAGE_FLAT`/`STORMWATER_FLAT` constants (`loadUtilities()`), two new stat cells ("Garbage"/"Stormwater") on the WATER card (grid bumped 3→4 columns), both folded into `Combined` (now the true full City of WH + WHUD bill) — but `sewerEst`/`sewerWaste`/the `water_billing_history` `sewer`/`waste` fields are untouched, still pure usage-based, still the exact dataset the overcharge case needs. Verified by extracting the live constants straight from the file and reproducing the real bill to the penny ($92.56 sewer-only, $125.55 full city total), plus a DOM check confirming both new cells render with no page errors.
- Read path (confirmed by WHUD supervisor): external MIU `100WD`, ERT ID `79453337`, unencrypted Itron ERT-SCM, protocol `scm+` (rtlamr2mqtt config), 915–930 MHz, ~1 SCM/min, no AES key.
- Timestamps are European (Kamstrup is Danish) — convert to Central in code. Raw ÷10 = gallons.
- App shows **This Cycle** (`whudCycleKey()`, resets ~21st) + Est. Water/Sewer/Combined + billing history + sewer-overcharge tracking (irrigation water is charged sewer fees it never actually generates).
- **Pit radio went silent 07-28 (~17:39 UTC), self-recovered — CLOSED, do not re-raise.** Was confirmed hardware-side at the time (MIU not transmitting, add-on/config ruled out via `listen_mode`). **Jeff's explicit call (07-31): water is transmitting fine now, and he does NOT want to call WHUD about it** — reporting it would draw utility-district attention/scrutiny he doesn't want, and since it resolved on its own (likely the same RTL-SDR USB reconnect from the 07-31 host reboot, or independent recovery on the MIU side — unconfirmed which), there's nothing actionable left. Leave this alone unless water goes silent again; if it does, that's a fresh incident, not a continuation of this one.
- Timing note (checked so this doesn't get blamed on the same-day recorder fix): recorder reset ~12:15 UTC 07-28; water kept transmitting fine for 5+ more hours after that, only going silent at ~17:39 UTC — the timing doesn't line up, so this looks like an independent, coincidental hardware failure, not a side effect of the recorder work.

### 🔥 GAS — Piedmont/Spire · Itron 100G ERT
- Piedmont Natural Gas, transitioning to Spire (billing continues under Piedmont during transition). Account `6100 0546 4779`. Meter Elster AC-250, Piedmont# `T821986`. Billing cycle ~5th.
- Itron 100G ERT, FCC `EO9100GDLA`, unencrypted, 900–920 MHz ISM. Same RTL-SDR reads both water + gas. Raw ÷100 = CCF.
- Rates (validated against 3 bills): Base $13.44 + Distribution $0.61809/therm + PGA $0.61691/therm = $1.235/therm all-in. Heat factor 1.068 (CCF→therms). 5% local franchise fee. `(13.44 + round(CCF × 1.068) × 1.235) × 1.05`.

### ⚡ ELECTRIC — Cumberland Electric (CEMC)
- **This Month + Cost: LIVE** via SmartHub — entity `sensor.electric_smarthub_energy_monthly_usage_4501007001` (monthly running total, found by keyword via `findEntity('smarthub','month')`). Poll interval fixed 08-06 (was HA's 6h default) → 30 min, the practical ceiling since SmartHub's own backend only refreshes every 15-60 min.
- **Today/Yesterday/Peak Hour + Last 7 Days panel: LIVE 08-06, real bugs found + fixed via live coworker verification.** Per `docs/utilities/electric_smarthub_data_upgrade_2026-08-06.md` — the already-installed SmartHub integration imports real hourly/daily long-term statistics for this entity (separate from its monthly-total state). `recorder/statistics_during_period` (NOT `history/statistics_during_period` — that name doesn't exist on Jeff's HA Core 2026.8.0, confirmed live, returns `unknown_command`) is a WebSocket-only HA command with no REST equivalent, so `functions/api/ha-stats.js` opens a one-shot outbound WS to the same allow-listed Nabu Casa host (same auth pattern as `ha.js`) and returns the JSON result over normal HTTP. Client-side `loadElectricStats()`/`haStatsFetch()` (index.html) fetch trailing 25h hourly + trailing 8-day daily statistics, run them through `toDiffedSeries()` (per-period usage = diff of consecutive cumulative `sum`/`state` readings — **not** the `change` field, which the coworker confirmed always reads `0` for this sensor even across real usage growth), then compute real TODAY, YESTERDAY, PEAK HOUR, and the "Last 7 Days" sub-panel (`util-elec-history`). **NOW is deliberately left blank (—), never estimated** — SmartHub's finest real grain is hourly, genuine "Now" only comes from the future CT-clamp build. The old 24-bucket hour-of-day estimate stays as the fast fallback until the real fetch resolves, then gets silently overwritten. **Verified twice:** first via mocked-data Playwright test (structurally correct but built on a wrong assumption about the WS command name/field), then the coworker fired the real command against live HA and found both bugs above — re-verified with a second mocked test built from the coworker's exact real data shape (`sum` moving 761→872 over 48h, `change` always 0) confirming the fix computes correctly. `lint-app.js`/`smoke-test.js` clean throughout. Bill Due/Last Payment/vs-Last-Year: checked live, confirmed not available as attributes, not built (see Pending Item 18).
- **Lower priority, not built 08-06:** Bill Due/Last Payment/vs-Last-Year — doc calls for checking whether the entity already exposes these as `attributes` before building anything new. **Could not check — needs live HA entity inspection, no network path from this sandbox.** Next session (or the coworker) should pull the real entity's full attributes dict first; only build new scraping if they're genuinely absent.
- Account `4501007001`, meter `145590962` (Landis+Gyr Gridstream — not Itron, can't radio-read directly). 200A service, Challenger panel.
- Rates (validated against the 07/30/2026 bill, account 4501007001, 2,120 kWh billed): Base $39.00 + Energy $0.08657/kWh + TVA Fuel $0.02847/kWh = $0.11504/kWh all-in. TVA fuel is a pass-through surcharge that shifts most cycles — re-derive from each new bill photo Jeff provides rather than treating this as fixed.
- **Future build:** 6-channel CircuitSetup ATM90E32 board (2 chips) + ESPHome `atm90e32` → HA, for real instant Now/Today. CT1+2=200A mains, CT3-6=range/dryer/AC/well pump. ~$90-110 DIY. **Jeff wired his own house — never suggest hiring an electrician.**
- **⚠️ "$94 seems low" — checked and confirmed NOT a bug (07-31).** Billing cycle resets ~23rd (per the 06/23–07/23 cycle on the 07/30 bill), so early in a fresh cycle "This Month" will look low against memory of a near-full-cycle bill. $94 ≈ 478 kWh ÷ 8 days ≈ 60 kWh/day, tracking in the same range as last cycle's 2,120 kWh ÷ 30 days ≈ 70 kWh/day. Confirmed with Jeff the number is climbing day to day (not frozen), which rules out a stuck SmartHub integration — the same "stale value" failure class as the Blink/vacuum issues found earlier this session. If this comes up again, first check whether it's climbing before assuming a bug.
- **Water Flow tile briefly reads a low decimal (e.g. 0.2 gpm) even with nothing obviously running — also confirmed NOT a bug (07-31).** `sensor.water_flow` is a real HA `derivative` helper with a 5-min smoothing window (`docs/beehive/ha_helpers_and_alexa.md` PART 3) — a brief burst (toilet flush, ~1.5 gal over ~30s) averages out across the full window instead of showing the real instantaneous rate. Working as documented ("reads ~0 when no water is running, jumps when a tap/irrigation is on"). Shortening the window would make it twitchier/noisier in exchange — hasn't been requested.

---

## Jeff's Contact / Account Info

- **Email:** jeff.loewen@comcast.net
- **Cloudflare account:** credentials already configured — never ask for them
- **Home Assistant instance:** "Beehive" — local `homeassistant.local`/`192.168.1.66`; remote (primary) `https://kmtpozwheqwww9t5uxhhvzzso1tvagro.ui.nabu.casa`
- **Weather Underground PWS:** station `KTNWHITE21`, API key `0e87ee079c0147a787ee079c01d7a75d`
- **Mower:** Toro TimeMaster 21200, **Serial No. 401338948** (confirmed 08-03 via data-plate photo) — falls in the `400000000-402081999` production range, so PartsTree's `21200-toro-30-timemaster-walk-behind-mower-sn-400000000-402081999` and eReplacementParts' `toro-21200-400000000402081999-...` are the correct parts-diagram links for his actual mower (not the `402082000-403599999` range).
- **Jeff wired his own house** — skilled and comfortable in the breaker panel. Never suggest hiring an electrician; talk to him as a capable peer on electrical/hardware.
- **Jeff is almost 60 and learning** the software/AI side — be patient and clear there, never condescending. On hands-on hardware/electrical/firmware he is experienced. Make it enjoyable.
