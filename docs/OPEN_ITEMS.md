# ✅ HCC — THE TODO LIST

**Only things somebody is going to DO.** Findings, lessons, stop signs and finished work do not
live here — see the pointers at the bottom. Jeff, 2026-09-16: *"If it's not a direct action item
move it where it needs to go to be referenced if needed, but not in the damn todo list."*

**RULE: strike an item in the same commit that finishes it, with the command or observation that
proved it.** A list that disagrees with reality makes the next session confidently wrong.

---

## 🔨 ACTION ITEMS

**🟢 Nothing here is waiting on me.** Both rows were closed 2026-09-16 without asking Jeff
anything — one answered from the record, one by building the thing that was actually missing.

| # | What | Owner | State |
|---|---|---|---|
| 🟡 **#205** | ✅ **CONFIRMED BY TEST 2026-09-24 09:01 — on Jeff's GENUINE HP 90 W brick (PPP012H-S, 19 V 4.74 A, swapped 08:23 with a clean shutdown) the HP ran 33 min of Sling with ZERO resets, peaking at 88 °C — HOTTER than the 85 °C at which it died on the 65 W brick.** So it was power, not heat (live log `D:\PXE\backup\kitchenpc-90w-test.txt`, 1662 samples, 0 disconnects). **Remaining: order the 180 W (HP P/N 462603-001 / 463954-001, 19.5 V 9.2 A, 7.4×5.0 mm) — 90 W is still half the design rating; and clean the vents/fan (88 °C).** Also verified this session: a spare 90 W "Replacement PPP012D-S" exists — do not use it over the genuine one. 🎯 **ROOT CAUSE FOUND 2026-09-24 08:18 — THE HP IS RUNNING ON A 65 W LAPTOP CHARGER.** Jeff's photo of the brick: *"Replacement AC Adapter, PA-12 FAMILY, MODEL 1650-05D, P/N 5U092, OUTPUT 19.5 V ⎓ 3.34 A"* = **65 W** — an HP *laptop* adapter with the same 7.4 mm HP plug, so it fits. The TouchSmart 520-1020 needs **150–180 W** (replacements searched 09-24 06:4x: 19.5 V 7.7 A = 150 W, genuine 19.5 V 9.2 A = 180 W). Jeff: *"It's pretty warm."* **This explains every observation:** idle/headless/kiosk/memtester stay under 65 W and survive; Chrome-on-GPU at boot and Firefox+Sling+audio exceed it, the rail sags and the machine drops with NO log (a power loss cannot write a log). **Fix = the correct 19.5 V ≥7.7 A (150 W) or 9.2 A (180 W) adapter, 7.4×5.0 mm HP centre-pin — Jeff buys; verify price in the BROWSER before naming one.** Until then: light use only, brick uncovered. The software changes (Chrome→Firefox, GPU policy) were chasing a symptom — keep them, but they are not the fix. **Lesson: the brick was listed as a suspect at 23:5x on 09-23 and at 03:48 on 09-24, and its label was a 10-second look; I ran RAM, thermal, GPU and browser tests for hours first.** 🔴 **REOPENED 2026-09-24 08:14 — "FIXED" WAS PREMATURE. It hard-reset under FIREFOX too:** boot `3eea1f53` ran 77 min (cold boot 06:57, Firefox holding the GPU, Chrome uninstalled), Jeff started Sling ~07:55, and the live log shows **load 3–4.5 and the CPU package climbing steadily 55 → 72 → 80 → 84–85 °C over ~20 min, then gone at 08:14** (journal ends mid-activity, no shutdown lines). **So the pattern is SUSTAINED HEAVY LOAD, not Chrome:** Chrome+GPU at boot, Firefox+GPU+DRM video+audio after 20 min. Idle/headless, memtester, and a 2-min CPU burn all survived. **Two hardware suspects, both $0 to check by hand:** (1) **cooling** — 15-year-old all-in-one, 84–85 °C is the hottest seen, dust in the fan/vents is the classic cause; (2) **power** — the external 19.5 V brick (genuine is 180 W; many replacements sold for this model are 150 W) sagging under combined CPU+GPU+HDD+backlight+speaker-amp load. Last night's resets at 61–63 °C argue that it is not ONLY CPU temperature. *Previous (superseded) verdict follows.* ~~**FIXED 2026-09-24 ~06:10 — CHROME'S GPU ACCELERATION WAS HARD-RESETTING THE HP.**~~ **Proof:** cold boots into the desktop **0 of 4 survived** with Chrome on the GPU (reset 82–209 s; live-streamed journal shows the kiosk Chrome starting at 82 s and the box gone at 93 s, no kernel message); **3 of 3 survived** with it off (**598 s, 83 min, 12+ min**, kiosk on screen — `wmctrl -l` shows `HCC KIOSK — Home Command Center`). **The fix, machine-wide:** Chrome policy `/etc/opt/chrome/policies/managed/hcc-205-no-gpu.json` = `{ "HardwareAccelerationModeEnabled": false }` (name verified against Chromium's `policy_templates.json`; **proven effective**: with it the gpu-process runs `--use-gl=disabled`, with the file moved aside it does not) — covers kiosk, screensaver, Braves/Sling and every shortcut. Belt-and-braces `--disable-gpu` also on `hcc-kiosk-start` (backup `.bak-205`, one-line diff). **Made permanent in `windows-scripts/hcc-desktop-standard.sh`** (writes the policy when hostname is KitchenPC; `bash -n` OK). Default target back to `graphical.target`. ⚠️ **STILL UNEXPLAINED, stated honestly:** (a) boots -11/-10 (21:49/21:52) died ~40 s in, at X start, BEFORE the kiosk's 20 s delay — so something else may have been in play then; (b) the 23:19 reset inside the PXE rescue env had no Chrome at all; (c) why Chrome+GPU ran for an hour on boot -12 and then started killing it. **Watch item, not a fault:** software rendering on a 2011 Pentium may make VIDEO (Braves, Sling) choppy — if Jeff reports that, it is this trade, not a new bug. 🔴 **Session error logged:** my GPU-policy test launched Chrome without `--password-store=basic` and put an `Unlock Login Keyring` box on the HP's screen (closed via `wmctrl -c`); and a backup I made with `cp -n` read 0 bytes minutes later (rebuilt from the live file). *Original row follows.* **KITCHENPC HARD-RESETS ITSELF 1–5 MIN AFTER BOOT — SINCE ~21:50 ON 2026-09-23. MEASURED, CAUSE NOT YET PROVEN.** Three instances: ~21:52 (dropped before I could connect), 23:19:16 (live rescue env, ~5 min in), 23:39:38 (installed system, **70 s** in). **It is a HARD reset, not a reboot:** `journalctl -b -1` ends mid-activity (CIFS mounts, `blueman`) with no `Stopping`/`reboot.target` lines. `last -x` is empty on this build (instrument note in `ACCESS_MAP`). Nothing thermal/MCE in dmesg; `x86_pkg_temp` **63 °C at 92 s uptime** — warm, not conclusive. ⚠️ **NOT the partition table** — that fault is fixed and proven (#202); this survived the fix. 🔎 **LEAD, not a finding (`feedback_test_before_declaring_fault`): the machine ran 7 hours today (14:00–21:00) without this, then went to Jeff's BED at ~21:50 — an all-in-one with its intake/exhaust against bedding.** Also possible: PSU brick, or the ~12 hard power cycles tonight. **TEST RUNNING:** a 3-second temp/uptime logger on the Beast (`D:\PXEackup\kitchenpc-temp-log.txt`) — if `pkg` climbs toward 90–100 °C right before `UNREACHABLE`, it is thermal; if it dies at ~65 °C, it is not. ✅ **BISECTED 23:44–23:55, and the thermal lead is DEAD:** (1) **watchdog ruled out** — no `*wdt` module, no `/dev/watchdog`, `RuntimeWatchdogUSec=0`; (2) **heat ruled out** — every reset landed at **61–63 °C**, and a 2-core CPU burn headless drove it to **78 °C at 2593 MHz for 120 s with no reset**; (3) **the last three desktop boots lasted 74 s / 76 s / 84 s** and each journal ends mid-activity (CIFS mounts, `blueman`) — no oops, no panic, no kernel error; (4) 🔑 **HEADLESS BOOT (`multi-user.target`, no X) SURVIVED 389 s AND COUNTING** at load 0.5. **So the reset is triggered by something the DESKTOP stack brings up, not by CPU load, not by time, not by heat.** Candidates being bisected one at a time over ssh (each step 120 s): bare `Xorg` (GPU init + backlight) → Chrome → Bluetooth radio → audio. ⚠️ **KitchenPC is deliberately left booting to a TEXT CONSOLE until this is found** — re-run `hcc-desktop-standard.sh` (it sets `graphical.target`) or `systemctl set-default graphical.target` to restore the wall panel. **Lead now: a desktop-only load on the power path** — the external brick/cord after the 21:50 move to the bed — but the bisection names the subsystem first. ✅ **RE-READ 2026-09-24 03:30 (next session), from the machine's own journal:** (5) **bare Xorg SURVIVED** — `Xorg.1.log` runs uptime 579 s → 1008 s (~7 min, 5× the 74–84 s failure window), ending `Server terminated successfully` only because of (6); (6) **the 00:05 CT end of that boot was NOT a reset** — `systemd-logind: Power key pressed short` at 05:05:14 UTC → clean `systemd-poweroff`; a human pressed the button, and it was powered back on 00:06:28; (7) **the current boot (headless, `multi-user.target`) has run 3 h 24 min clean with `bluetoothd` + the CSR dongle UP** — so neither Xorg/GPU/backlight nor the Bluetooth radio alone triggers it. 🎯 **REPRODUCED + CAUGHT 2026-09-24 03:59–04:16.** Cold boots into `graphical.target` reset **4 of 4** (at ~82 s, ~95 s, ~89 s, 209 s); starting the desktop on a system already up 3.7 h ran 4 min clean. **Live-streamed journal to the Beast** (`D:\PXE\backup\kitchenpc-livelog.txt`): the kiosk's Chrome started at **up=82 s** and the machine vanished at **up=93–94 s** — 63 °C, load 4.4, no kernel message, nothing. Ruled out this session: **RAM** (`memtester 5500M` ~11 min, 6.1 GB in use, all compare tests `ok`, no reset), **touchscreen driver** (installed 15:52, before the 3-h good boot; loaded in the surviving bare-X test and in headless boots), **half-installed packages** (`dpkg --audit` clean), **disk/partition layout** (`fdisk`: sda1 1 MB leftover bios_grub slot, sda2 `/boot` 2 G `*`, sda3 LVM — sane for MBR). **TESTING 04:18: `--disable-gpu` added to `/usr/local/bin/hcc-kiosk-start`** (backup `hcc-kiosk-start.bak-205`); one variable. Watch log `D:\PXE\backup\kitchenpc-nogpu-test.txt`. ✅ **RESULT 04:30: with `--disable-gpu` the cold desktop boot SURVIVED 598 s and counting** (kiosk up, 13 Chrome processes, 62 °C) vs 0 of 4 without it. Two more cold boots running as confirmation (`kitchenpc-nogpu-test2/3.txt`). Also found: **no package changed between 20:40 and 21:49** (dpkg: unattended upgrades 14:45 + 15:18 CDT, Chrome 154.0.8037.57 installed 17:48 CDT — all before or during the good boot), so the trigger's onset is not explained by an install. Full 09-23 change history rebuilt from the transcripts: `docs/computers/kitchenpc_change_log_2026-09-23.md`. 🔎 The iCloud MASTER-RECORD stops at **09-22 18:04** — none of the 09-23 HP work is in it. 🔴 **THE "MOVED TO THE BED" LEAD IS DEAD — AND SO IS MY 03:35 READING OF IT.** Jeff, 03:40 09-24: *"No that is not correct I told him I was in bed and tired he had screwed the thing up from the start and I was 10 hours into fixing it."* His *"Im in bed"* (22:24 CT) was **Jeff, exhausted after ten hours repairing our mess — not a fact about where the HP sits.** Two sessions have now turned that sentence into a hardware lead; **do not do it a third time, and do not claim to know the machine's location.** What stands on its own, from the machine's journal: **boot -12, 17:35→20:40 CT, 3 h 05 m with the FULL desktop** and no reset. 🔴 **CORRECTED 03:50 — I first wrote "every autostart file predates it". FALSE:** the autostart files are dated 17:43–19:19 and boot -12 logged in at ~17:35, so **that boot never launched them together at login; boot -11 (21:49) was the first login that ever did.** And the last-line pattern: boots -11/-10 died **40 s after power-on, at the auto-login/`Xorg_wrap` start**; -7/-6 66–69 s (CIFS mount lines); -5/-4/-3 76–77 s (only my SSH probes). **CIFS was the last line in only 2 of 9** — last night's "CIFS before every reset" was wrong too. Resets begin at boot -11, 21:49, right after the GPT→MBR/GRUB work — but **kernel and command line are byte-identical on good and bad boots** (`7.0.0-34`, same `Command line:`), and **it also reset inside the PXE rescue env at 23:19**, a different OS that never read the disk's autostarts. ➡️ **So it follows the HARDWARE, not the desktop config.** The CPU burn exercised cores but ~no RAM; the desktop and the rescue env (2.9 GB ISO unpacked in RAM) are both memory-heavy, headless idle uses 625 MB of 7.3 GB. RAM = 2 × Hynix `HMT351S6BFR8C-H9` 4 GB @1067. **TEST RUNNING 03:38: `memtester 5500M 2`** (log `/var/tmp/memtester.log`, watcher `D:\PXE\backup\kitchenpc-memtest-watch.txt`) — silent, no screen/audio. 🔴 **CORRECTION: the 23:58 message to Jeff said the Bluetooth radio, CIFS mounts and Chrome were "running now, one at a time". They were NOT** — the journal shows exactly one `sudo` test in that window (`timeout 700 Xorg :1`, 04:58:06 UTC) and **zero** CIFS or Chrome lines before the power key; `findmnt -t cifs` is empty on the current boot too. **Still untested: CIFS mounts, Chrome kiosk, the XFCE session (blueman applet, onboard, EasyEffects/PipeWire audio), CIFS mounts — and WHERE the machine now sits** (if the 00:05 power-off was the move off the bed, the next desktop boot is also the location test). | **— (JEFF: say if Braves/Sling video stutters on the HP)** | Opened 09-23 23:45 · fixed 09-24 ~06:10. |
| 🟢 **#204** | **BRAVES + SLING ARE BACK — ON THE LINUX DESKTOPS ONLY, NOT IN THE SHARED APP. 2026-09-23.** Jeff: *"I would like to run the HP like the iPad does with the app up all the time"* and *"It's Braves vision through Major league baseball."* 🔑 **WHY THIS DOES NOT VIOLATE THE "BRAVES HERE MUST NOT RETURN" STOP SIGN:** that removal was **iPadOS-specific** — Jeff, verbatim: *"It was because it wouldn't work on the iPad but it should have no problem on the HP."* `index.html` is shared with the wall iPad, so putting the button back in the APP would break it there a third time. **Solution: `.desktop` launchers on KitchenPC and GarageLaptop only.** The app is untouched; the iPad never sees a button it cannot use. **Do not add these to `index.html` without device-gating them away from iPadOS.** 📺 **BRAVES = `braves.tv`, VERIFIED LIVE** (HTTP 200 → redirects to `mlb.com/live-stream-games/subscribe/braves`, checked from KitchenPC 2026-09-23). **BravesVision runs on MLB's own platform**, so an MLB account signs in. 🔑 **Braves.TV has NO BLACKOUTS** — *"all regular season non-national exclusive Braves games available to fans everywhere and without blackouts"* — which is why it beats **MLB.TV, which WOULD black out in-market Braves games; Tennessee is Braves country.** 💰 **CHECK BEFORE SUBSCRIBING: BravesVision is on Xfinity channel 1254 and Jeff is an Xfinity customer** — he may already have it. 📡 **Some games are FREE over the air** via the Braves/Gray Media partnership on local stations — which the **AirTV antenna already receives**. 📺 **SLING = `watch.sling.com`**, the same thing photo-proven working on the wall iPad's Safari. ⚠️ **UNTESTED AND WORTH ONE TEST: whether Sling's browser player shows AirTV locals on a DESKTOP.** `BEEHIVE_REFERENCE.md` says *"likely can't ... unsupported device class — Roku path is the real one"* — but that was reasoned from the iPad and never tried in desktop Chrome, which presents as a different device class. **If locals appear, it changes the AirTV/capture-chain plan.** 🖥️ **KitchenPC now runs like the wall iPad:** `/usr/local/bin/hcc-kiosk-start` + autostart brings `loewenhome.com/?kiosk=1&rot=60` up full screen at every login and keeps it there; the app's own X returns to the desktop; `xautolock` + `/usr/local/bin/hcc-screensaver` bring it back after 10 min idle. **Both scripts mirror the Beast's `Start-HCCKiosk.ps1` deliberately: `--app=` not `--kiosk` (so the X can close it), NO `--user-data-dir` (a fresh profile loses the HA token and parades broken panels), and `?kiosk=1&rot=60` so the app rotates its own sections against burn-in.** ⚠️ **A first port got all three wrong and had to be rewritten — read the Windows script's header before changing either.** 🔴 **`pgrep -f` TAKES AN EXTENDED REGEX:** the marker `loewenhome.com/?kiosk=1` never matched its own window because `?` means *optional*; escaped to `loewenhome\.com/\?kiosk=1`. **And a `pgrep`/`pkill` pattern that appears in your own SSH command line MATCHES ITSELF** — that produced a false "running: 1" three times today and killed my own session twice. **Check the window manager (`wmctrl -l`), not a process grep.** | **JEFF (test Braves + whether Sling shows locals)** | Shortcuts on both Linux desktops; app deliberately unchanged. |
| 🟢 **#203** | ↔️ **+ STEREO WIDTH 09:57 — Jeff: *"Better that is good."*** `stereo_tools#0` `stereo-base` **0.5** (0.3 tried first), placed after the EQ, before the echo. **Current live preset = `KitchenPC-FINAL-2026-09-24-wide.json`** (repo copy `windows-scripts/kitchenpc/`). Chain: bass enhancer → EQ → stereo tools → exciter (bypassed) → reverb → limiter. 🎚️ **+ ECHO 09:51 — Jeff: *"That good."*** Reverb added before the limiter: `room-size` **Large/smooth**, `decay-time` **1.6 s**, `amount` **−21 dB**, predelay 30 ms, bass-cut 300 Hz, treble-cut 6 kHz (Jeff asked for it bigger — kitchen + dining are one open room — then less echo). Live preset = `KitchenPC-FINAL-2026-09-24-with-echo.json` (repo copy in `windows-scripts/kitchenpc/`); the no-echo version is kept as `KitchenPC-FINAL-2026-09-24.json`. Reverb/stereo key names were read from the easyeffects binary, not guessed. 🎚️ **RE-TUNED 2026-09-24 09:46 — Jeff: *"That is perfect."*** Final = `~/.local/share/easyeffects/output/KitchenPC.json` (= `KitchenPC-FINAL-2026-09-24.json`, repo copy `windows-scripts/kitchenpc/easyeffects-KitchenPC-FINAL-2026-09-24.json`): bass enhancer 15 · EQ hi-pass 65 Hz, **+6 @110, +3 @160, −7 @400 (boxiness), +4 @3 kHz (clarity), +6 shelf @8 kHz** · EQ out −2.5 · limiter in +2, threshold −1, **gain-boost OFF** · exciter bypassed. Built ONE change at a time with Jeff judging each by ear, from a MEASURED speaker response (pink noise + internal mic): speakers roll off below ~100 Hz (nothing at 63), boxy hump +12 dB @400, treble falls above 4 kHz. 🔴 **Four traps found today:** (1) last night's "bass all the way up" = +14 dB stacked below 150 Hz + bass-enhancer amount 36 + NO limiter → distorted, "two things playing at once"; (2) limiter **gain-boost ON** = pumped to max loudness, "awful" — never enable it; (3) **the taskbar slider did nothing on `easyeffects_sink`** — fixed by making the HARDWARE sink the default (`pactl set-default-sink alsa_output…`); EasyEffects still captures every app stream, and the slider now drives ALSA Master (proven: 40% → −23.25 dB); (4) ⚠️ **a sink `.monitor` is tapped BEFORE the sink volume — it cannot measure a volume change; use `amixer sget Master`.** **Hidden "Beats bass speaker" pin ruled out by A/B measurement:** mono pin 0x10 (via mixer 0x1a ← selector 0x19 ← DAC 0x13) powered and enabled live — ON−OFF difference within ±0.4 dB at 80 Hz–4 kHz → nothing is wired to it; left OFF. Dolby/Atmos remains impossible (below). HP clock set to America/Chicago + 12-hour format the same session. **Still owed: confirm it all survives one reboot.** *Original row:* **KITCHENPC AUDIO + BLUETOOTH — DONE AND PROVEN BY EAR, 2026-09-23.** Jeff: *"Sound good now."* 🔑 **THE ONE THAT WILL SAVE A FUTURE SESSION HOURS — an iPhone will NOT offer a Linux PC in its audio picker, no matter how correct the audio profiles are, because iOS filters that list by Bluetooth CLASS OF DEVICE.** The HP advertised `0x7c0104` = *"Computer, Desktop workstation"* and was invisible as a speaker while every A2DP endpoint was correctly registered. Fix = class `0x240414` (Audio service + Rendering | major Audio/Video | minor **Loudspeaker**). ⚠️ **`Class =` in `/etc/bluetooth/main.conf` DOES NOTHING on BlueZ 5.85** — it must be set on the adapter with `hciconfig hci0 class 0x240414` after bluetoothd starts; `hcc-bt-loudspeaker.service` does that at every boot. ⚠️ **The phone must FORGET and re-pair** — iOS caches the old class. Result: phone → HP over A2DP, playing through the built-in speakers. 🔊 **AUDIO STACK — the server install left NO audio server running at all.** `pipewire`, `pipewire-pulse` and `wireplumber` were installed but all **inactive**; the tray plugin saying "PulseAudio not connected" is what exposed it. Sound had appeared to work only because the earlier test drove ALSA hardware directly, bypassing everything the desktop uses. Enabled + started; `pactl` now reports *PulseAudio (on PipeWire 1.6.2)*. 🎚️ **GAIN STAGING, measured not guessed:** playback had **Master −24 dB + Speaker+LO −18.75 dB stacked ≈ −43 dB**; all three stages (incl. `PCM`) now 0 dB. Capture was **+50 dB** (Capture 30 + Internal Mic Boost 20) and clipped at 83%→100% peak; cutting the **boost** (noisiest stage) rather than the capture level gives **peak 20.4%, rms 1549 — strong with headroom**. `alsactl store`d. **Mic proven by measurement, speakers proven by Jeff's ear.** 🎛️ **EasyEffects 8.1.2 + LSP plugins**, preset `~/.local/share/easyeffects/output/KitchenPC.json`, autostarts: hi-pass 70 Hz (small drivers cannot move that air — feeding it only causes distortion), +5 dB @180 Hz, −2.5 dB @500 Hz (boxiness), +2.5 dB @3.5 kHz (presence), +3 dB shelf @9 kHz, bass-enhancer harmonics, exciter, limiter −1 dB. Chain verified live: `ee_soe_bass_enhancer/equalizer/exciter/limiter`. ⚠️ **EasyEffects only processes what is routed through it — `pactl set-default-sink easyeffects_sink` is required or audio silently bypasses the whole chain.** ⚠️ **Presets moved themselves from `~/.config/easyeffects/` to `~/.local/share/easyeffects/` on first load.** ☎️ **CALLS: `hfp_hf`/`msbc` enabled in `/etc/wireplumber/wireplumber.conf.d/51-hcc-bluetooth.conf`** — not yet tested with a real call. 🔴 **TEXTS ON THE HP ARE NOT POSSIBLE and should not be re-attempted: Jeff is on iPhone, Apple does not expose iMessage/SMS to non-Apple devices, and no maintained Linux MAP client exists.** 🔴 **NO DOLBY / ATMOS — hardware, not driver: codec IDT 92HD91BXX exposes exactly ONE analog playback device, no SPDIF, no HDMI, 2 channels.** Passthrough needs a digital output this machine does not have; no licensed Atmos renderer exists for Linux. Do not re-pitch. | — | Also done: `onboard` on-screen keyboard (auto-shows on text fields), `blueman` tray, network handed to NetworkManager so the tray X cleared and WiFi can be joined from the desktop. |
| ✅ **GRANDFATHER CLOCK — LIVE 2026-09-25 07:29 (Jeff: *"That is awesome nice job"*); first automatic chime 07:30:00 on the speaker** | 🔊 **Speaker = iDeaUSA iDeaPLAY W200** (2.1, 10 W sub, BT 4.1, 10 h battery → keep it on USB power; 2 EQ modes, 1 press toggles, no indicator). `chime_config.json` speaker = `Headphones (iDea W200)` (A2DP stereo, NOT the HFP "Headset"). 🎚️ **Audio = the 1960 recording with vinyl crackle removed** (ffmpeg adeclick x2 on the Lenovo; clicks 17–41 per piece → 0, peak/length unchanged) **+6 dB low shelf @180 Hz + 2 semitones lower** (resample 55/49 — Jeff: *"a little more bass or deeper tone"* → *"Go deeper"* → approved). Versions kept: `HCC-Scripts\chimes-original`, `chimes-clean`, `chimes-bass`, `chimes-deep` (= live `chimes`). 🔊 **Breakup fixed 08:1x:** Jeff "it's kind of breaking up a little" → `chime_listener.py` output buffer 0.4 s (`latency=BUF`) → *"It's perfect now … I didn't touch the speaker."* Plan geometry: clock #79 is 27.7 ft from the Beast (#2) through master bed → foyer → hallway → living room — **Jeff: distance is fine** ("definitely close enough for Bluetooth"); the cause was the PC-side buffer. ⏳ Open: `w200-idle-watch.log` (started 07:06, 3 h) decides whether the W200 auto-powers-off when idle. *Build history:* | Jeff: *"Can HA drive a Bluetooth speaker and play the Westminster chimes on the hour and half hour?"* (first asked 09-19 10:41; 09-19 proved HA OS can't do BT audio and Echos won't play custom URLs; the Echo SSML-announce test was never answered). Jeff 22:44: **quiet 10 PM–6 AM** → 23:18 **moved to 10:01 PM** so the 10 PM hour strikes (condition `before: 22:01:00`, verified); 22:46 quarters too (*"we can always take it off if it is too much"*); *"The beast can reach the clock."* **Built:** `input_boolean.grandfather_clock` (**OFF**), `input_boolean.grandfather_clock_quarter_chimes` (on), `automation.hcc_grandfather_clock_…` (id `hcc_grandfather_clock`; /15 min, 06:00–22:00, fires event `hcc_chime {kind, strikes}`) → Beast `HCC-Scripts\chime_listener.py` (sounddevice+websocket-client, pip --user; scheduled task **"HCC Grandfather Clock"** at logon, restarts) plays ONLY on the speaker named in `chime_config.json` (default "H5 HeadSound" — **unconfirmed which speaker**), never the TV. **Proven 22:51:28:** HA event → Beast log "not connected - silent". Audio: 1960 archive.org public-domain recording cut by measured onset timing into `HCC-Scripts\chimes\` (quarter/half/threequarter/hour + strike1..12). ⚠️ Pieces cut by TIMING — pitch check was unreliable on bells; **quarter/half/three-quarter boundaries need Jeff's ears.** 🗣️ **+ 6 AM WEATHER (Jeff 23:26, female voice 23:28):** `automation.hcc_grandfather_clock_6_am_weather_spoken_after_the_chime` (06:00, clock + `input_boolean.grandfather_clock_morning_weather` on, 5 s delay, `weather.get_forecasts` daily → event `hcc_speak`) → Beast voices it with Nabu Casa TTS **JennyNeural** (`chime_config.json` "voice") and queues it behind the chime. Chain proven 23:29:32 (TTS fetched, speaker absent → silent). 🔴 **Bug caught before it shipped:** WASAPI refused 44.1 kHz chimes + 24 kHz speech ("Invalid sample rate") — every real chime would have FAILED once the speaker connected; fixed with `WasapiSettings(auto_convert=True)`, guarded by `chime_listener.py --selftest` (silent, 16/16 ok). 🌙 **+ HOLIDAYS / 10 PM FORECAST + MOON / SPOKEN WARNINGS / ANGELA (Jeff 23:35–23:44):** built-in **Moon** (`sensor.moon_phase`) + **Holiday** (`calendar.united_states_tn`, US+TN, public + unofficial) integrations added via config flow; 6 AM report appends "Today is <holiday>." (switch `…_holidays`, "(observed)" dupes dropped; rendered "Today is Halloween." for 10-31, empty for 09-25); `automation.hcc_grandfather_clock_10_pm_overnight_forecast_moon_…` (22:00, next 8 hourly periods: most common condition, min temp, first rain hour + moon; switch `…_night_forecast`); **spoken warnings** (switch `…_speak_warnings`, independent of the clock switch): `HCC-Scripts\HCC-WeatherWarning.py` speak() on each NEW NWS warning — Jeff: **Tornado + Flash Flood 24/7, other 4 types 06:00–22:00 only** (verified: a Severe T-storm at 23:44 → phones only); fixed its Windows-only `%-I` bug (the "until" time went out as a raw ISO stamp); lightning 10-mi automation speaks **daytime 06–22 only, max once/hour** (`input_datetime.lightning_last_spoken`); `hcc_angela_15_minutes_out` also speaks "Angela will arrive in about 15 minutes." (switch `…_angela_arriving`, same 24/7 hours as its Echo announce). All paths proven to reach the Beast (silent — no speaker). **OWED: Jeff names/pairs the speaker → `--test` each piece after 6 AM → Jeff approves → turn the clock ON.** | **JEFF (speaker + listen) → CLAUDE (switch on)** | 2026-09-24 |
| 🟡 **HOUSE MAP — LIVE STATUS LIGHTS, BUILT 2026-09-25 ~09:05, NOT PUSHED** | Jeff 08:47: *"made the map live and showed all the active devices, so you could look at the map and see if anything is down. It could have lights that show status"* + 08:50 *"You can add the mower to the garage"*. **Built:** `docs/house-plan/plan-live.js` — green/amber/red/grey light on each dot mapped to HA (47 devices), red flashing ring when DOWN, top-left LIVE bar with counts (tap = jump to the first problem), a **Needs attention** group at the top of the key, reason line in hover + inspector. Mower = **#80** in the garage, judged from `/api/hours` `lastSync` (parked box posts every 5 min; silent while mowing is normal). Clock **#79** written into `plan-data.js` too. Only works from the app origin (claude.ai sandbox can't reach HA — there it shows a note); **app button GUARDIAN → "HOUSE MAP · LIVE DEVICE STATUS"** → `/docs/house-plan/`, with **← HCC app** back link. SW `hcc-v126`: `/docs/` now network-first (was cache-first = stale plan JS forever). Rules honoured: meters amber only after 6 h quiet (SESSION_START: 20 min–3 h gaps normal), Blink battery flags NOT used (battery experiment), no camera/Blink calls — HA states only. **Proven:** Playwright vs the REAL `/api/states` snapshot at 1536×864 → *"LIVE 45 reporting 0 down 2 off"* (the 2 = A/C master off), 0 page errors; injected faults (front door unavailable, sink leak on, guest-bath battery low, water meter 8 h quiet) → 2 down / 2 need a look, correct reasons; no-token → idle note; 390 px OK; `run-all-gates.js --live` 20/21 then package gate fixed (stale iCloud HCC-PROJECT copies refreshed) → clean. Artifact v9 published. **Owed:** Jeff says push → then open it from the app on the real device. Not monitored (no HA entity): Beast, switch, UPS, RE200s, TVs without integrations, appliances, meters' electric. |
| 🟡 **MOWER GPS — MOW 2026-09-24** | ✅ **Hours + RPM confirmed by Jeff** ("the hours tracked well and the rpm's are spot on"): box 5.575 → 6.65 h (+1.08), app 13.3 h, RPM 3650/3555, mow_end 15:53 CT, 111-point track stored in `hours_history`. ❌ **GPS distance wrong:** box logged 0.92 mi total (1,101 m before a gas/water stop at ~15:22 + 376 m after); Jeff says his whole yard is **~4 mi** with a **30" mower, yard ~8,500 sq ft** — those two inputs disagree (8,500/2.5 ft ≈ 0.64 mi of passes; 4 mi ≈ 53,000 sq ft), track bounding box ≈ 200×145 ft incl. house. Unresolved which is off. **Three defects, all mine (mower subsystem = this PC's):** (1) **a gas stop split the mow in two** — firmware `mowEnded = wasRunning && !running` fires on any stop; need a ~15-min engine-off grace before mow_end (server-side merge possible without reflash); (2) app "GPS Distance (This Mow)" shows only the last segment (0.23 mi) and the red "latest mow" line draws the live `track` (2 pts) instead of the saved 111-pt history entry; (3) **30 s GPS sampling undercounts stripe mowing** — fix = read GPS at 1 Hz while running and integrate distance continuously (firmware + reflash; 150-pt RTC track buffer also too small for denser tracks). 🔴 **~~RESOLVED 17:45~~ RETRACTED 17:46 — I misread Jeff.** "My Apple Watch is dead wrong" was NOT a concession; Jeff 17:46: *"You are telling me I only walked a mile but my watch says 4 miles."* ✅ **SETTLED 17:48 — Jeff: *"That was weedeating mowing and blowing."*** The watch's 4 mi is ALL yard work; the box rides only on the mower, so its **0.92 mi mowing total is consistent with 8,500 sq ft / 30"**. GPS distance OK; the real defects are (1) gas-stop split and (2) This-Mow/red-line display. (3) 1 Hz is optional map detail only. Geometry says the MOWER path over 8,500 sq ft / 30" is ~0.8–1 mi; the watch may include all walking. Text below is the retracted reasoning: 8,500 sq ft ÷ 2.5 ft ≈ 0.64 mi of passes → ~0.8–1.0 mi with overlap/turns; **box total 0.92 mi is plausible — GPS distance was likely RIGHT.** So (3) drops to OPTIONAL (map detail only), no reflash needed; (1) and (2) remain real and are fixable server/app-side (merge a mow_end followed by a restart within ~15 min; This-Mow = dist_total delta since the previous merged mow; red line from the last history entry). | ✅ **App/server fixes LIVE e60ecbb (23:2x)** — (1) gas-stop merge + (2) This-Mow/red track shipped; next mow proves them. (3) optional | 2026-09-24 |
| 🟡 **A/C OFF BY JEFF 2026-09-24 1:36 PM + A/C MASTER SWITCH BUILT 14:15** | 🔘 **BUILT (Jeff: "go ahead and build"; Angela had to come tell him she was cold):** `input_boolean.air_conditioner` ("Air Conditioner", exposed to Alexa via entity-registry `cloud.alexa` + `cloud/alexa/sync`; `switch.ac_relay` deliberately NOT exposed) + `automation.hcc_ac_master_switch` — ON = enable + trigger the UNCHANGED thermostat automation; OFF = disable it + open the relay. **Proven:** master fired 19:15:28Z on a helper change. ⚠️ **NOT proven: the Alexa voice path** — an alexa_media text command to `media_player.master_bedroom` never registered (`last_called` false), so Jeff/Angela must say "Alexa, turn off the air conditioner" once and report Alexa's answer. **App — LIVE 2026-09-24 23:2x, commit e60ecbb, sw hcc-v124, live-e2e clean:** A/C card gets one ON/OFF button; `switch.ac_relay`, `switch.garage_garage_door_opener` and `switch.301_alarm_light` removed from GUARDIAN Lights & Plugs — **"ALL ON/ALL OFF" was pulsing the garage door and killing the A/C.** Helper currently OFF, matching Jeff's "just leave it off". ⬇️ Earlier: Jeff: *"Can you turn off the AC"*. `switch.ac_relay` turned **off** AND `automation.hcc_ac_relay_thermostat` **disabled on purpose** (otherwise it re-closes the relay within 5 min at >=73 F). Verified via HA states 1:36:16 PM, indoor 71.1 F. **Both OFF = Jeff's choice, NOT a fault — do not re-enable until Jeff says turn the A/C back on;** then re-enable the automation (it drives the relay itself). 📊 **ENERGY CHECK 2026-09-24 (Jeff asked: does the relay use as little as the thermostat?):** whole-house CEMC kWh/day (HA stat `smarthub:smarthub_energy_sensor_4501007001_16290`) vs daily mean outdoor temp from **KTNWHITE21 via WU history** (key = the 32-hex inside `HCC-secrets/weather_underground_api_key.txt` — the file also holds notes, so `.Trim()` of the whole file = 401). LUX era 07-15..09-07 fit: **kWh = 34.0 + 2.07×CDD65** (n=55, R² 0.54; Open-Meteo temps gave 29.3 + 2.13×CDD, R² 0.81). **Relay week 09-16..21: 403 kWh actual vs 436 LUX-predicted = −7.5%** (−3.4% on Open-Meteo). Verdict: at least as efficient as the LUX. Caveats: LUX setpoint unknown, whole-house meter, 6 days. Cycles 09-16..21: 18→29/day, runtime ~14–16.5 h/day, shortest normal-day cycle 14 min. Scripts: session scratchpad `ac-energy.mjs` + `an.py`. | Jeff | 2026-09-24 |
| 🟡 **#3/#118 PROGRESS 2026-09-24** | 🔐 **BITWARDEN ON EVERY COMPUTER — 20:37–20:55 (Jeff: *"fix the Bitwarden on all computers or I can't get in anything"*).** Proven: Beast Chrome inline menu pops on a REAL mouse click (lowes.com, 3 entries; a CDP/synthetic click shows nothing — not a fault). Beast **Edge (default)**: Bitwarden OK but Edge's own "Saved passwords" box opened ON TOP offering the OLD 14-char Comcast pw → policy `PasswordManagerEnabled=0` set 20:45 (Jeff's yes) — ⚠️ that only stops NEW saves; the 309 already-saved still pop. All 309 predate the 08-19 import → safe to clear, **JEFF presses delete** at edge://wallet/passwords. Installed by policy + verified: **KitchenPC Firefox** (`/etc/firefox/policies/policies.json` ExtensionSettings force_installed, AMO guid `{446900e4-…}` v2026.9.0; backup `.bak-20260924-204944`; xpi present after a headless start), **Lenovo Firefox** (xpi present), **Lenovo Edge** (`/etc/opt/edge/policies/managed/hcc-bitwarden.json`, installs on next Edge launch — unverified), **Acer Chrome** (HKLM forcelist; installs on next launch — unverified). ⚠️ **NOT turned off, on purpose — logins saved AFTER the 08-19 import may exist only in the browser:** Beast Firefox 6 (windscribe, x.com, ebay, ambientweather, kroger, 1 moz-extension), Acer Edge 4 of 310 (kroger, paypal, loewenhome, dash.cloudflare), KitchenPC Firefox 1 (sling, 09-24). Move those into Bitwarden FIRST, then disable those managers. **JEFF: sign in to the Bitwarden extension once per new browser** (HP Firefox, Lenovo Firefox/Edge, Acer Chrome). ✅ **COMCAST EMAIL PASSWORD ROTATED 13:57** — new 4-word passphrase generated + saved + read-back-verified in Bitwarden BEFORE Comcast saw it (`HCC-Scripts\Run-NewComcastPassword.ps1`; builds the item JSON itself and base64-encodes in-process — PS 5.1 pipes a UTF-8 BOM into `bw encode`). Entry: **"Xfinity Comcast email (CURRENT - 2026-09-24)"**. Reset via idm.xfinity.com → SMS code to …1844 (Jeff typed it) → pasted twice → landed on xfinity.com/account = accepted. 8 old Jeff Xfinity entries + 1 blank leftover → Bitwarden trash (30 days); **Angela's untouched**. ⚠️ Owed: Outlook (Store `olk`) + iPhone Mail will prompt for the new password; an item named just "Email" (Comcast username, no website, iPhone import 08-19) still holds the OLD password — left in place, ask Jeff. 🔴 **Jeff's rules today:** NO paper copies of site passwords (only the master pw in the safe); a printed all-passwords sheet at the END of the rotation is OK. **BITWARDEN DEDUPE FINALLY RUN** (promised 08-19, 08-23, 09-03, 09-04, 09-11 — never done; Jeff: *"I was told it was but I don't think it was"* — he was right). ✅ **55 duplicates removed, 0 lost:** 1 by hand (idm.xfinity.com, NO username), 9 exact copies (`bw-dedupe-exact.py`), 45 identical-password copies whose website addresses were first MERGED into the kept entry (`bw-dedupe-merge.py`). All in the Bitwarden trash (30 days); undo lists `~\bw-dedupe-undo-20260924-1255.txt` / `-1257.txt` (`bw restore item <id>`). Vault 552 → ~497 logins. 🔑 **How it finally ran:** `HCC-Scripts\Run-BitwardenDedupe.ps1` — Jeff types the master password into a standard Windows `Get-Credential` box → `bw login --passwordenv` → cleared; session revoked by `bw lock`. **Two fixes were needed:** the CLI server was `https://bitwarden.com` → set to `https://vault.bitwarden.com` (Bitwarden docs); and the console hidden-password prompt REFUSED the correct password twice (the web vault accepted it) — never use the console prompt again. ⚠️ **`bw-dupes.py`'s "KEEP newest" rule is WRONG for this vault** — every item is dated 08-19, the two imports are an hour apart (Edge 10:26 = id suffix `b4ab00fe77…`, iPhone 11:20 = `b4ab010d34…`), so "newest" only means "from the iPhone". It would have deleted the likeliest current Comcast password. Decide by EVIDENCE (length, reuse count) — `bw-dedupe-merge.py` part B prints it. **OPEN: (1) 25 sites still hold CONFLICTING passwords** (evidence in `~\bw-dedupe-result.txt`, read with `-Encoding Unicode`); (2) 🔴 **no Xfinity entry holds a 32-char password — the 08-19 Comcast reset password is under another name (comcast.net / connect.xfinity.com?) or in the hand-trashed no-username idm entry** — find it before touching Xfinity again; (3) 🔴 **massive reuse: one password is on 235 other sites, another on 102** — the rotation (free, Tier 1 first: Comcast, Apple, Amazon, Nabu Casa, smart-home clouds, banks) is the real fix; breach report 09-24 = 11 breaches, 5 with passwords incl. the 2025 Synthient credential-stuffing list. Also: Jeff's iPhone AutoFill is set correctly (Bitwarden only, verified by screenshot 12:15); turn on Face ID in the Bitwarden app. | **CLAUDE (next unlock) + JEFF (password box)** | 09-24 13:10 |
| 🔵 **#207** | **USE FOR THE 500 GB DRIVE IN THE DRAWER — Jeff 2026-09-24 11:34: *"find the use for that ... 500 gig external hard drive that's just sitting in a drawer doing nothing."*** ~~Most likely the KESU~~ → **Jeff 11:36: it is the DEKYDA 2.5" USB 3.0 enclosure** (Amazon 2022-08-11, bought ~2 weeks after the second MX500 on 2022-07-25 — so the drive inside is very likely the ORIGINAL 2.5" drive pulled from the Lenovo or the Acer when they got their SSDs; type SSD vs HDD unconfirmed). ⚠️ It may hold that machine's old Windows install + personal files — inventory before any reuse, erase nothing unread. (The KESU 500 GB from `BEEHIVE_REFERENCE.md` is a separate spare.) **Proposed: an OFFLINE DISASTER-RECOVERY DRIVE kept in the SAFE** — the HA backup encryption key ring + the latest few `HCC-Beehive-Backups` tars → closes the open item that the key currently sits in the SAME iCloud account as the backups it opens (CLAUDE.md Pending 0). $0; refresh by plugging into the Beast. Step 1: plug into the Beast → inventory existing contents (erase nothing unread) + SMART health. | **JEFF (plug it in) → CLAUDE** | Opened 09-24 11:35. |
| 🔵 **#206** | **KITCHENPC RECESSED FLUSH INTO THE WALL — Jeff 2026-09-24 10:10: *"completely recessed in the wall to where it is flush"*, with a frame that covers the cut drywall.** Plan agreed in chat: a flat trim frame (e.g. 1×3 poplar / flat MDF casing, rabbeted) lapping ~½" onto the HP bezel and 1–1½" onto drywall, fixed to the wall framing, not the HP. 📱 **THE WALL iPAD IS NOT RETIRED — IT MOVES.** Jeff 10:41 *"The iPad is coming out"* (of THIS wall), then 10:42: *"No I'm putting the iPad in the wall right above where the thermostat is going."* ⚠️ I first recorded it as retired and marked CLAUDE.md Pending 2 moot — **both corrected the same minute; the iPad stays a live wall display**, so its Safari-15 polyfills and the iPadOS-driven "BRAVES HERE must not return" stop sign stay exactly as they are. (Checked 10:41: nothing in `beehive-config/` references the iPad.) 📍 **LOCATION = the wall where the kitchen iPad is now** (Jeff's photo 10:32; the HP replaces the iPad, whose recessed black-bordered hole gets absorbed into the new opening). **Framing per Jeff 10:36: ONE stud cut, header + footer, opening edge 5" from the door stud** — Jeff: *"It won't matter."* ~~Photo scale put the wall at ~26"~~ → **MEASURED by Jeff 10:47: 30" door frame to corner** (my photo estimate was 4" short) — the ~25"-wide frame fits with room to spare. **The stud being cut is the one the triple-gang switch box (lower left) is attached to, on its right side:** keep the footer ABOVE the switch box so the stud stub below still carries the box, and reroute the box's feed cables (likely running up that stud) around the opening — nothing in the 1" air gap behind the HP; a **black** frame matches both the HP bezel and the current iPad border. **Measured by Jeff: body 23" W × 15¼" H × 3" D** (10:11 + 10:22). ✅ **FRAME FINAL 2026-09-24 10:52** (Jeff's tape photo of the top-right corner, scale ~370 px/in off the tape marks, cross-checked against HP's 23" 16:9 display in the 23 × 15¼ body; Jeff's 28" diagonal = the body, 27.6" computed): bezel **MEASURED by Jeff 10:54–10:56: 1¼" top, left and right; 2½" bottom (the Beats speaker bar)** — sums check (1¼+1¼+20½ = 23; 1¼+2½+11½ = 15¼); ½" overlap leaves ¾" of bezel clear of the glass; bottom stays ¼". Rails: sides **1×2**, ½" overlap; top **1×3**, ½" overlap; bottom **1×3**, **¼" overlap only** (speaker bar). 🚫 **NO VENT SLOTS — Jeff 10:57: *"I don't want vent slots."*** Hidden venting instead (it ran 79–88 °C in open air): (a) frame stood off the wall on ~⅛" spacers = invisible shadow-gap intake at the bottom / exhaust at the top, or (b) a louvered grille WITH a lint filter in the laundry-side access panel. After install: watch the HP temperature for a few days of normal use vs today's 88 °C peak. ✅ **Jeff 10:57: he will PULL AIR OUT THE BACK (laundry side).** Exhausting INTO the laundry = no lint issue (make-up air comes in around the frame from the kitchen); only if it ever draws laundry air IN does it need a filter. ✅ **Jeff 10:59: a small computer fan IN the access panel, blowing out.** ~~Power it from one of the HP's USB ports~~ → **Jeff 11:08: the new receptacle will be the same USB-charger type, and the fan runs off THAT — "I don't wanna pull any more power out of the HP than I have to"** (right call on the 90 W brick). ~~Fan then runs 24/7~~ → **Jeff 11:09: the fan goes on a SMART PLUG, switched by temperature.** Plan: (1) HP CPU temp into HA via **Glances** (glances web server on the HP + HA's built-in Glances integration — the "Glances health sensors" role already planned for this machine in `BEEHIVE_REFERENCE.md`); (2) ~~smart plug~~ → **Jeff 11:10: reuse the SONOFF dry-contact relay now running the A/C (`switch.ac_relay`, driven by `automation.hcc_ac_relay_thermostat`) once the new A/C + ecobee free it** — $0; it switches the fan's power line; clone the A/C thermostat automation pattern with the HP's temp as the input. ⚠️ It stays on the A/C until the swap; the fan runs from the USB receptacle (always on) or waits until then; (3) one HA automation with hysteresis — fan ON above ~65 °C, OFF below ~55 °C (starting values; tune after install). ⚠️ Run `Search-HCC.ps1` before creating the automation (CREATE gate). Build the sensor any time; the automation once the plug exists. a spare 12 V PC fan usually runs slow + quiet on 5 V via a USB adapter ($0 if one is in the shop); check the airflow arrow points into the laundry; larger + slower = quieter. (The HA smart-plug fan idea is superseded.) 🔌 **POWER (Jeff 11:03): a new receptacle for the HP fed from the light-switch box** (the triple gang below-left). Tap the ALWAYS-HOT line that feeds that box's receptacle — never a switched leg, or the light switch hard-cuts the HP (the exact fault class of #205). **FINAL (Jeff 11:04–11:06): receptacle RIGHT ABOVE the opening, FACING THE LAUNDRY SIDE** (below-the-switch was weighed and dropped — it would put the brick's DC lead through the wall cavity; NEC 400.12-type cord-in-wall issue). Brick plugs in at the access panel; no cords in the wall, nothing visible in the kitchen. **The feed:** the triple gang's left position is a USB-charger receptacle Jeff added where an old switch was, when the kitchen + dining lights were combined onto one Kasa dimmer (`docs/lighting/kasa_smart_lighting_project_2026-08-06.md`: "2 of the 3 switch positions come out") — so the box already carries the ALWAYS-HOT line the new receptacle needs. So: brick up on/beside the header (never in the 1" gap behind the HP), and the exhaust fan's pull near the TOP so it clears both the HP's and the brick's heat; all reachable through the access panel. **Sight opening 22" × 14½", outside 25" × 19½"**, flat black, mitered, fixed to framing not the HP. Optional check: if the grille starts >½" above the bottom edge, bottom overlap can be ½". *Superseded provisional numbers follow.* **PROVISIONAL (bezel + speaker grille unknown):** wall opening **23½" W × 17¼" H** (¼" side clearance, 1" air gap top + bottom), full cavity depth; one stud cut → header + sill + cripple (**wall is NOT load-bearing — Jeff 10:26**, so flat 2×4 header/sill blocking is enough; ⚠️ Jeff 10:27: ceiling + floor joists run **N–S**, and `plan-data.js` puts this kitchen↔laundry wall at y=22 running **E–W — ACROSS the joists**. Jeff's call stands, but confirm in the attic that the joists pass over it unbroken (lap elsewhere, e.g. the hall walls); if they lap ON it → proper header, e.g. doubled 2×8 on jack studs, over the 23½" opening); flat trim frame (no rabbet — HP face is flush with drywall), sides ~1¾" (½" on bezel + ¼" gap + 1" on drywall), top/bottom ~2½" with vent slots over the air gaps, outside ~27" × 22¼". Awaiting photo: bezel width to glass, speaker-grille position, back mounting points. → in a 2×4 wall (3½" stud + ½" drywall = 4") that leaves ~1" behind it → right-angle Ethernet/power plugs, brick outside the cavity, 1" gap = the air channel. **Constraints:** do NOT cover the glass (NextWindow OPTICAL touch — corner cameras look across the glass); keep the bottom rail off the Beats speaker bar (front-facing, along the bottom); vent slots in top + bottom rails (ran 79–88 °C); side buttons/ports become unreachable → BIOS power-on-after-AC-loss + WoL + remote restart. **HP's official VESA adapter VK554AA is discontinued** (Newegg out of stock, eBay 0 listings 09-24). Ready-made alternative priced 09-24: Hammer And Nail Studios recessed niche $540 (VESA-only, so still needs a custom bracket). ✅ **INTERIOR wall — the LAUNDRY room is behind it; Jeff will fit an ACCESS PANEL on the laundry side** (09-24 10:13) → cables + power brick + service (SSD, fan cleaning, maybe the side power button) from behind. ⚠️ Keep the panel closed in use: dryer lint + humidity must not feed the HP's fan; breathe/vent through the kitchen-side frame slots only. **Needed from Jeff: body width + height without stand; photo of the back (and sides) with the stand off.** 🔌 **POWER BUTTON will be unreachable (Jeff 10:14).** Replacement plan: OS side already armed (`ethtool` → `Wake-on: g`, checked 09-24); **BIOS still owed, on SSD day: `S5 Wake on LAN` → Enable (Jeff's 09-23 17:15 photo showed DISABLE; the "enabled" claim that followed was never confirmed) and `After Power Loss` → Power On (was Previous State).** Then PROVE it before framing in: shut down → magic packet to `38:60:77:9f:9b:7a` → boots. Then an app / HA "Turn on Kitchen PC" WoL button; a smart plug on the brick (spare one first, $0) for hard-freeze recovery. Also found: the 520 has a rear **subwoofer-out jack** (TechSpot review) — codec pin 0x10 — a powered sub would give real bass. | **JEFF (measure + photo) → CLAUDE (opening + frame drawing)** | Opened 09-24 10:12. |
| 🔵 **#201** | 🧰 **SSD-DAY CHECKLIST (Jeff, 2026-09-24 09:55: *"I will clean it all when I put the ssd in"*) — ONE visit:** (1) **blow out the vents + CPU fan** (it ran 79–88 °C under video, fan full blast); (2) **SSD + the 2.5→3.5 bracket** (cart 09-24: Patriot P210 256 GB + "SSD HDD Holder 2.5 to 3.5 Mounting Bracket" 2-pack $5.99, B07Y818K6N — listing confirms standard 3.5" bay + #6-32 screws; hole placement NOT verified, photos wouldn't load); (3) **the 180 W brick** if it has arrived (#205). 🔀 **PLAN CHANGED 2026-09-24 11:13 (Jeff): KEEP the HDD as raw storage, ADD the SSD as a second drive for the system** — migrate live, no reinstall: SSD gets an **msdos** table + `/boot` + GRUB (the #202 lesson), root LV moves over with **LVM `pvmove` while running**, BIOS boot order → SSD (Jeff), then the HDD is reformatted as `/mnt/storage`. **Measured 11:14 (`dmesg`): ata1 = the HDD; ata2 / ata5 / ata6 enabled but EMPTY; ata3/4 dummy. **BIOS (Jeff 11:15) shows exactly SATA 0 + SATA 1** → SATA 0 = HDD (`ata1`), **SATA 1 = `ata2`, enabled + empty = almost certainly the DVD drive's port, free for the SSD** — and the DVD drive is NOT detected at all** (unplugged, dead, or on one of the empty ports). In an all-in-one the only other drive connection is usually the optical drive's → the SSD likely goes in the DVD's place and needs an **optical-bay caddy** (slot-load, size TBD), NOT the 2.5→3.5 bracket in the cart (that one is for the main bay the HDD keeps). 📷 **Jeff's photo 11:18 (right side, panel off): the slim slot-load optical bay is EMPTY — the DVD drive has been removed** (explains SATA 1 empty). → **SSD goes in this bay in an optical-bay caddy**; HDD stays in the main bay as storage; the 2.5→3.5 bracket in the cart is then NOT needed. **Before naming a caddy: (1) bay thickness — 9.5 mm (~⅜") or 12.7 mm (~½"); (2) is the slim-SATA connector still at the inner end of the bay?** (the white 2-pin plug at the bay's bottom is not it — buttons/LED). If the connector left with the drive, a short slim-SATA lead is needed too. 🌐 **WIRED NETWORK PLAN (Jeff 11:24): the 8-port switch replaces the 5-port by the gateway (master bedroom); Jeff's own 150 ft of Cat 6 → TWO runs from it** (the HP has one NIC and cannot pass the link on): **switch → HP wall ≈ 40 ft, switch → garage workbench ≈ 80 ft, ≈ 120 ft total** (plan coordinates: switch D4 at H(17.5),2.2; HP wall ≈ y 22 by the laundry door; bench ≈ 2.6,33; attic runs + ~6 ft drops each end + 10%) — fits with ~30 ft spare; garage run is the one to walk first. The old 5-port moves to the garage (5 ports at the bench). Also fixes the HP's measured **100 Mb/s** link (new cable/port → expect 1 Gb/s; verify with `cat /sys/class/net/enp7s0/speed`). 💽 **Jeff 11:28: the Lenovo and the Acer already have SSDs — VERIFIED from the machines:** both `CT500MX500SSD1` (Crucial MX500 500 GB; Lenovo `lsblk` ROTA=0, Acer `Get-PhysicalDisk` MediaType=SSD). The Lenovo also still has an Optiarc AD-7740H DVD drive. With the P210 in the HP, all three older machines are on SSD. 🧰 **Already OWNED (Amazon order history, shown by Jeff 11:31):** 2 × SATA-to-USB transfer cables — **StarTech USB3S2SAT3CB** (2021-01-23) and **7xinbox USB 3.0 SATA adapter** (2022-07-25) · **Dekyda 2.5" USB 3.0 enclosure** (2022-08-11) · the two MX500s (2021-01-22, 2022-07-25) · Beelink J45 (2019-12-20, Beehive; has a spare 2.5" bay). ➡️ **SSD-day method (Jeff 11:32 — INSIDE the HP, drive to drive):** (1) P210 into the side bay on SATA 1; vents + fan cleaned; BIOS: boot stays on SATA 0 (HDD) for now, S5 WoL Enable, After Power Loss = Power On; (2) boot normally off the HDD; (3) Claude: msdos table + /boot + GRUB on the SSD, `pvmove` the root LV onto it live; (4) Jeff: BIOS boot order → SATA 1; (5) Claude: reformat the HDD as `/mnt/storage`. The USB cables are FALLBACK only if the bay connection misbehaves. 📦 **ORDERED (not yet in hand) — Amazon #113-5622860-9550663, 2026-09-24, $60.33, arriving overnight 3–6 AM 09-25:** Patriot P210 256 GB SSD · iMBAPrice 2.5"→3.5" bracket 2-pack · **Hgcorenet 8-port** gigabit switch (the 8-port Jeff wanted). **NOT ordered yet: the 180 W brick (#205).** (No SATA adapter needed — the machine already has it, Jeff 11:28.) ✅ **Jeff 11:19: the SSD fits in the bay, held by one of the 2 brackets from the cart's 2-pack** (keep the bracket order; no caddy needed for the mount). ✅ **Bay connector settled — Jeff 11:28: "The computer already has that"** (the adapter/connection the SSD needs is already in the machine) → **nothing more to buy for the SSD.** 🔴 **CLAUDE OWES BEFORE THAT DAY:** a way onto the SSD that does NOT repeat 09-23 — either fix the stick's autoinstall to `ptable: msdos`, or (better, keeps all of today's setup) clone/migrate the current system onto the SSD. Decide + test it before Jeff opens the case. Also in the cart: a **5-port** switch, while Jeff said 09-23 he wanted an **8-port** — flagged to him 09-24. **KITCHENPC — BIOS, RAM AND HARDWARE LIFE-EXTENSION AUDIT.** Jeff, 2026-09-23 08:52: *"go into the bios and get it running as fast as we possibly can ... check the ram and make sure that's the higher ram as we can get on it and that there are no other hardware additions that can be made to maximize its life potential."* 🔴 **THREE CORRECTIONS MEASURED 2026-09-23 23:40 — READ BEFORE ORDERING.** (a) **This board is SATA II, not SATA III** — `ahci … 3 Gbps, SATA mode` and `ata1: SATA link up 3.0 Gbps` in dmesg. The line below saying *"SATA III, ~550 MB/s ceiling"* is WRONG; a SATA III SSD works fine (backward-compatible) but tops out near ~280 MB/s here — still 3× the spinner, and access time is what makes it feel fast. (b) **The bay is 3.5-inch**: `hdparm -I` → `Form Factor: 3.5 inch`, drive is a SAMSUNG HD502HM held by a 4-screw bracket. **A 2.5-inch SSD needs a 2.5→3.5 adapter tray** (HP community threads for the 520-1020/1070: under $5; SATA data+power connectors are standard, no cable adapter). (c) 🔴 **THE AUTOINSTALL ON THE STICK WRITES GPT AND THIS BIOS WILL NOT BOOT GPT — that is what cost the whole of tonight (#202).** Swapping in the SSD and booting the stick as written would reproduce it exactly. **Before the SSD goes in, change the storage section to `ptable: msdos` (or run the #202 conversion afterwards).** Jeff, 23:36: *"Will the new ssd fix it?"* — **No; the drive was never the fault.** *Original:* 🟢 **SSD DECIDED 2026-09-23 09:14 — SMALL ON PURPOSE. Jeff: *"the ssd doesn't have to be huge we use the cloud for storage."*** ➡️ **Buy the `Patriot P210 256 GB` 2.5" SATA III, $37.99** (Amazon, priced in the BROWSER 09:12 — 4.6★/6.2K, 1K+ bought/month). Local storage is OS + apps only: this box uses cloud storage **and** mounts `/mnt/beast` + `/mnt/acer`. ⚠️ **Do NOT buy the Crucial MX500/BX500 that the circulating upgrade list names — SSD prices are inflated in Sept 2026 and those run $122.90 (BX500 500 GB, Amazon) to $260–540 (MX500, Newegg marketplace) for ZERO benefit: this board is SATA III, so every drive here hits the same ~550 MB/s ceiling.** ⚠️ **Do not drop below ~250 GB to save the last $13** — Ubuntu + snaps + kiosk browser cache grow, and small drives have fewer NAND dies so they write slower. Step-up only if the kitchen box is to hold media: Patriot P210 512 GB $68.99, or Lexar NS100 512 GB $84.99 (4.7★/9.4K). 🔑 **No cloning needed when it arrives — because the stick is fully automated, swap the drive, boot the stick, walk away (~20 min hands-off).** ❓ **Mounting bracket UNKNOWN** — the circulating list assumed a 3.5" bay; confirm with `lsblk`/the drive model before buying one. 🟢 **$0 option checked and unavailable: no spare drive in `HCC_INVENTORY.md` or anywhere in `docs/`.** 🟢 **RAM IS CLOSED — DO NOT RE-PITCH IT. Jeff, 2026-09-23 09:09: *"I already upgraded the ram it's 2 slots 4 each makes 8."*** That IS the documented ceiling for this board (HP/Pegatron **IPISB-NK "LavacaSB"**: 2 × 204-pin DDR3 **SO-DIMM**, 8 GB max, verified by search 09:04 — Kingston + MemoryStock). ⚠️ **Also correct the bad advice in circulation: this board is PC3-8500/PC3-10600 (1066/1333). 1600 MHz modules only down-clock — no benefit, slight risk.** The only thing still worth doing is *confirming the pair is DETECTED and running dual-channel at 1333* (`dmidecode -t memory`) — installed and working are different questions, and that is not doubting Jeff. **Do the rest AFTER the setup script runs, from the machine itself — never from memory.** Known from the record: 2011 **HP TouchSmart**, **Pentium G620** (Sandy Bridge, LGA1155), **spinning 500 GB drive**, legacy-BIOS boot path. 🔑 **Measure first:** `dmidecode -t system/baseboard/bios/memory/processor` gives the exact model, the BIOS version + date, and — decisively — **Maximum Capacity** and the DIMM form factor/type/speed, which is what settles the RAM question instead of guessing; `lsblk -d -o NAME,ROTA,MODEL` (ROTA=1 = spinning) and `smartctl -a` give the drive and its hours. ⚠️ **CLAUDE.md rule 8 applies hard here: name NO part or price that has not been verified by a real search THIS session, and use the BROWSER — vendor sites 403 WebFetch.** ⚠️ **BIOS changes are Jeff's hands** (ACCESS_MAP §7b #4) — produce an exact settings list for him, and include **SATA mode = AHCI** (TRIM/SSD) and **power-on after AC loss** (the house's auto-recovery model) as candidates to check. | **CLAUDE (measure + price), JEFF (BIOS keystrokes)** | Raised while the HP was installing; logged the same session so it survives. 🔴 **THE TWO THINGS JEFF ACTUALLY VALUES ON THIS BOX, and neither is covered by `garage-hp-setup.sh`** — Jeff 08:53: *"I paid a lot for it back in the day and it's got the touchscreen and everything like that ... and it's got a great sound system on it."* **(1) TOUCHSCREEN:** it is an **HP TouchSmart 520, 23-inch**, and its panel needs the out-of-tree **NextWindow `nwfermi`** driver — `GARAGE-SETUP/touchscreen/install-touchscreen.sh` plus two driver forks on the stick. The setup script does **NOT** run it (grepped: no touch/xinput/evdev). The record resolved an inconsistency in its favour (README 09-01: the glorang fork *"is maintained for Ubuntu 24.04 and later"*, which beats setup.sh's older *"unlikely to build"*) ⚠️ **but we are installing Ubuntu 26.04 on kernel 7.x, which is newer than that claim — so it is PLAUSIBLE, NOT PROVEN.** Run it, then prove touch with `libinput list-devices` / `xinput list` and an actual tap, not by the installer exiting 0. ⚠️ **An out-of-tree driver is rebuilt per kernel, so a future kernel update can silently kill touch on a wall display — needs DKMS or a pinned kernel.** **(2) SOUND:** prove it actually plays (`speaker-test`, sink moves SUSPENDED→IDLE) — the Lenovo taught us an empty sink list is not evidence of no audio. Both matter because this becomes the **KITCHEN WALL command centre** replacing the wall iPad. **(3) DRIVERS — Jeff 08:55: *"make sure all drivers are up to date even if we have to find new ones that will work."*** On Ubuntu this is NOT the Windows exercise: almost everything (Sandy Bridge graphics, WiFi, audio, NIC) is **in-kernel**, so `apt full-upgrade` + `linux-firmware` IS the driver update. 🔑 **Do not assert coverage — ENUMERATE IT:** `lspci -k` and `lsusb -t` show which kernel module is **bound to each device**, so anything silently unsupported shows up as a device with no driver. Then `ubuntu-drivers devices` for any proprietary candidate. 🟢 **`fwupdmgr get-devices` / `get-updates` is the one with real upside** — it flashes actual **device firmware** (SSD, and on LVFS-supported HP models the **system BIOS itself**) from Linux, which could satisfy part of the BIOS ask without Jeff touching a setup screen. ⚠️ **2011 consumer HP is often absent from LVFS — check, never promise.** The only genuinely out-of-tree piece is the touchscreen driver in (1). |
| 🔵 **#200** | **MAKE THE LENOVO LOOK AND FEEL LIKE WINDOWS** — Jeff, 2026-09-23 07:45: *"you were going to get the Lenovo to look like windows."* It is the garage bench machine (Ubuntu). Scope: a Windows-like desktop layout (taskbar bottom-left start, window controls right, familiar icons/theme) so walking from the Beast to the bench does not feel like a different computer. **Not started.** Everything else on that box is done — mesh, no-password sharing, handoff watcher, mounts. | **CLAUDE** | Raised while finishing the handoff work; logged the same session so it cannot be lost. |
| 🟢 **#198** | **HOUSE PLAN — LIVE DOCUMENT. Rev 2 published 2026-09-22 17:25** after Jeff: *"go back through all the pictures I sent you and check and correct what is not right"* + *"this is a living mechanical drawing of the house, to be used for any and all purposes like quotes"* + *"I want it color coded."* Every one of the 48 photos in this session's uploads was re-read (all saved with size cross-reference). **Changed by the photos:** deck starts at the kitchen jog, 26 ft long, stairs at its west end, crawl-space door under it (dusk photo); garage workbench = 10-ft counter on the SOUTH wall + saw return on the west (Jeff: *"does my workbench look like the one in the drawing?"* — it did not); Garage TV (53) is above that bench, not in the guest bedroom; master bedroom = the 15 × 11 room, its west 4′8″ strip = closet / **WC** / passage (Jeff's *"5 ft master bedroom + 3 ft toilet closet"* on the exposed west wall, the arrow photo *"that would be where the little toilet closet is"*); master bath 8 × 8 with the closets east of it (*"the rest of that little square are closets"*), shower behind the washer; pantry–dryer–washer order from the Stage-1 markup; guest bath runs down to the hall so the hall *dead-ends into it* (corridor photo: bath door straight ahead, guest-bed door left, office door right); thermostat WEST of the return grille (two living-room photos); arched window at the foot of the master bed; second kitchen window on the jog wall (jog photo); half wall solid with the Echo on it; laundry door off the dining area; mailbox at the street. **Added:** Quantities tab (per-room area, floor finish from the photos, wall sq ft at an editable ceiling height, floors by finish in sq yd, exterior perimeter by side, gutter run on eaves, brick area, roof squares at an editable pitch/overhang), room colour-coding + category legend on the plan, ceiling fans, `inferred` dashed outlines on rooms drawn from description rather than measurement. **Still Jeff's to confirm:** the master-suite closet/WC arrangement (inferred), duct register spots (drag them), the bar's orientation (his sketches say N–S; the kitchen photo could read E–W), crawl-space door position vs where 76/77 sit, deck depth (drawn 9′3″), ceiling heights and roof pitch (inputs default 8 ft / 8-in-12). *Rev 1 text follows.* **Rev 1 built 2026-09-22 14:40.** `https://claude.ai/artifact/5PbCGCLjeADgGDuGBLUsHX` · source `docs/house-plan/` (index.html + plan-data.js + plan-app.js; edits persist in the artifact db, base data in plan-data.js). | **JEFF (review; drag registers to where they really are)** | All **78** numbers from his 22 Sep hand markup placed (photos saved in `docs/house-plan/`). Ductwork = the Rev D scope drawn on the plan with run lengths **scaled off it**; the bid PDF is untouched per *"No on the duct work."* Shell changes vs the 12:56 map, all from Jeff: **garage door in the NORTH wall** (14:07), garage front set back **8 ft** (his *"8 feet to where the garage door starts"*, makes the garage 25 ft deep vs 24 taped, back wall flush with the kitchen), **kitchen is an L** — 16 wide, only the west 7.7 ft bumps out (his 22′6″ = garage 14.83 + 7.7). ⚠️ **Five numbers were read by elimination, not by eye** (his 7s/1s and 6s/0s): garage west-wall trio = **66/63/23**, hallway 4th = **36**, master-bed east-wall 3rd = **56**, kitchen "6" = **61**, garage-front "12" = **72** — every number 1–78 lands exactly once; confirm. Register positions are drawn to the room, NOT measured. Supersedes the `docs/lighting/_gen_map.py` pipeline. |
| 🔴 **#195** | **RESTART CLAUDE CODE ONCE — arms the two new machinery gates** | **JEFF (one restart)** | Jeff, 2026-09-20 14:59: *"I want my rules controlled by machinery."* Built the same hour, from that day's own failures, as extensions to hooks that already existed (searched first — no prior header/create gate): **HEADER gate** (OPEN_ITEMS must be read from the TOP within 4 h — a deep read at offset 113 no longer counts; that is how two already-listed items got "discovered"), and **CREATE gate** (a new `scripts/` or `windows-scripts/` file, or a shell POST to the HA automation config API, requires a `Search-HCC.ps1` run within 2 h — that is how a gate got built that duplicated `automation.hcc_sensor_silence_watchdog`). `FreshMin = 120` also now on all 12 topic gates (was 1 of 12; a 118-hour-old read had satisfied the ZIGBEE gate). **`Test-ReadGate.ps1`: 22/22.** 🔑 **Why a restart:** hooks load at session start, and the `Bash|PowerShell` PostToolUse matcher that lets the receipt see `Search-HCC` runs was added to `settings.json` mid-session. Until the restart the CREATE gate **stands down and prints `CREATE GATE INACTIVE`** rather than blocking the unsatisfiable. Full write-up: `COST_LEDGER.md` 2026-09-20. |
| 🔴 **#194** | **ANGELA'S PHONE POSTS SENSORS BUT NEVER LOCATION — proven by active test** | **ANGELA'S PHONE (in-app location settings)** | 🔴🔴 **MY FIRST ANSWER WAS WRONG AND JEFF CAUGHT IT.** I reported `sensor.angelas_iphone_location_permission` = `Authorized when in use` as the root cause. Jeff, 14:40: ***"It's already set to always."*** He is a primary source and he is right. **That reading is a GHOST** — its `last_updated` is `09-09 18:47:34`, *identical to its `last_changed`*, so the app has never re-asserted it. 🔑 **The tell I missed: JEFF'S OWN permission sensor carries the exact same frozen 09-09 18:47:34 timestamp.** Neither app re-reports that sensor, so it can never be evidence of a current setting. **This is the third ghost-value misread in one day** — after the siren's LQI 142 and the repeaters' stale linkquality. ⚠️ **I compared `last_changed` on one entity against `last_updated` on another; that sloppiness produced the wrong root cause.**<br>✅ **WHAT IS ACTUALLY PROVEN, by ACTIVE TEST at 14:43 — `request_location_update` pushed to BOTH phones in the same second:** **Jeff's `device_tracker` updated 14:43:35 — answered. Angela's did not move at all, still `09-09 18:47:34`.** Meanwhile **her app IS alive** — battery 40 %, steps 233, pressure and distance all written **14:41:31**. 📌 **So: her Companion app posts SENSORS fine but never posts LOCATION, and will not even when asked directly. The block is on her device — it is NOT HA, NOT the network, and NOT the OS permission.** ➡️ **Next step on her phone: Home Assistant app → Settings → Companion App → Sensors (are Location sensors enabled?) and App Configuration → Location (update triggers).**<br>⚠️ **This silently kills every presence-driven automation**, and they all still read `on`: `Angela Almost Home`, `HCC - Angela Arrived at Barn`, `HCC - Angela Arrived at Work`, `AI Arrival Suppression` — plus it made my own #39 door automation **dead code** (see that row). **It also blocks the presence-based alert suppression that `alert_fatigue_fix_2026-08-14.md` calls "the biggest remaining reduction".**<br><br>🔍 **"ANGELA IS 10 MINUTES OUT" — DIAGNOSED IN FULL 2026-09-20 11:35 (Jeff asked). THE AUTOMATION IS NOT BROKEN; ITS DATA SOURCES ARE.** It lives in `packages/hcc.yaml` (no `id`, so **invisible to the config API** — read the repo copy `beehive-config/hcc.yaml:466`, which the record confirms is byte-identical to live). It has **THREE** triggers and **all three are dead for a trip like today's**:<br>① `zone enter` on `person.angela_loewen` → frozen 256.8 h. ② `numeric_state` `distance('zone.home','person.angela_loewen') < 10` → same frozen entity, and a stuck value can never *cross* a threshold. ③ `zone enter` on `device_tracker.gle_350_device_tracker` → **the Mercedes IS healthy and reporting live (11:31:28)**, but it is parked at home.<br>🔑 **THE SUBTLETY THAT MATTERS ON ③: `zone.almost_home` has a 10-mile radius (16093 m) and `zone.home` sits INSIDE it at the same coordinates.** A `zone/enter` trigger needs an outside→inside edge, so **a car parked at home is permanently already inside and can never enter again.** It only fires when she actually drives the GLE out past 10 miles and returns — which is exactly why it worked on **09-15** (`Angela Arrived at Work` fired 08:25) and not today. **She did not take the Mercedes.**<br>📌 **CONCLUSION: "10 minutes out" needs a live GPS source. Without either her phone on "Always" OR her driving the Mercedes, no ETA is physically derivable. Do not go looking for a bug in the automation — there isn't one.** (The record already knew her phone was unreliable: `66b3f49`, 2026-08-01 — *"the existing two both secretly depended on her unreliable phone tracker, which is why the prior backup never actually helped."*)<br>✅ **BUILT 11:35 — `automation.hcc_presence_tracker_stale_watchdog_angela`**, because the real defect is that this died **silently** on 09-15 and nothing said so, exactly as it died silently from 07-16. Jeff, verbatim: *"I don't get my Angela is almost home alerts anymore and I have not seen a camera motion pop up in weeks — what else is broken?"* Fires **once a day at 09:07**, **non-critical** (it is an FYI, not an emergency — alert fatigue), only while her tracker is >24 h stale, and names the exact fix in the message. **Self-extinguishing: it stops the day her tracker moves again.** Verified 1/1/1 triggers/conditions/actions, armed, message renders ("...has not reported in 257 hours..."). Automations **63 → 64**. |

---

## 🔴 #194 — ANGELA'S PRESENCE. **OWNER: ANGELA'S PHONE. One setting, not yet done.**

**Angela's iPhone → Settings → Privacy & Security → Location Services → Home Assistant → Always**, then open the HA app once.
Also: **Low Power Mode off**, **Background App Refresh on**, the four triggers under **HA app → Settings → Companion App → Location**, and her app is two releases behind (2026.7.0 vs Jeff's 2026.9.1).
✅ **Everything on the HA side is BUILT and armed** — `automation.hcc_angela_15_minutes_out` (fires off her phone **or** the Mercedes, so it is live today), the rewritten stale-tracker watchdog, and the repaired `#39` door automation.

📖 **The full write-up moved to `docs/FINDINGS_AND_STOPS.md` on 2026-09-21** — the retraction of my 09-20 ghost-reading call, the silent push test that proved it, the restart-floor defect class, and the sweep of all 66 automations. **Reference, not a todo.**

---

## 🔴 FOUND 2026-09-20 EVENING — after the full update + reboot

| # | what | owner |
|---|---|---|
| 🟡 **#196** | 📭 **MAILBOX REMOVED 2026-09-24 23:1x — Jeff: *"delete the mailbox and fix everything associated with the failure; when I get the mesh extender we can redo the mailbox."*** Done: `automation.hcc_mail_arrived_mailbox_door_opened` + `automation.hcc_mail_evidence_capture_carrier_or_jeff` turned **OFF** (configs + mail history helpers KEPT for the redo); app Mailbox card hides while `binary_sensor.mailbox_contact` is absent and returns by itself; `doors-entity-test.js` / `garage-entity-test.js` mailbox checks now re-arm themselves (present in HA ⇒ must be on Doors, count 4; absent ⇒ 3) — verified both ways against live states + a simulated re-pair. Checked and NOT changed: sensor-silence watchdog + 10:05 PM announce already exclude it; the camera/Zigbee low-battery alert lists `mailbox_battery_low` but is CAMERA-FROZEN and a missing entity can never fire it. **REDO when the extender arrives:** re-pair (SNZB-04: hold reset 5 s), turn the two automations back on, confirm the card and both tests show 4. 🛑 **JEFF ALREADY KNOWS. DO NOT RAISE THIS IN CONVERSATION AGAIN.** Jeff, 2026-09-20 7:28 PM: *"Drop the mailbox shit I know it is down."* A session mentioned it in **six consecutive messages** as though it were news. It stays on this list as a task, but it is NOT a finding to report, NOT a status line, and NOT something to offer to fix every turn. Same class as the unlocked Mercedes and the low-fuel warning: **a known state he has already decided about.** Raise it only if HE brings it up. — **THE MAILBOX SENSOR IS GONE FROM Z2M ENTIRELY — and its automation is DEAD CODE.** `binary_sensor.mailbox_contact` **no longer exists in HA** (verified: the only entity matching "mailbox" is the automation itself). Z2M's device registry lists **11 devices + coordinator = 12**, matching `devices_known`, and the Mailbox is **not among them**. 🔑 **This RETIRES the "orphaned on the mesh" framing in the notes above** — it was not a weak-signal orphan, it is absent from the coordinator's registry. ⚠️ **AND IT CORRECTS A CLAIM I MADE EARLIER THE SAME EVENING:** I said `…/availability` had "proven itself a live instrument" because it correctly flipped the siren to `offline`. That is true for the siren, but the Mailbox's `online` is a **RETAINED MQTT message from a device that no longer exists** — a ghost, the very trap this file documents. **Availability is trustworthy only for a device Z2M still holds.** 🔴 **The consequence is the real finding: `automation.hcc_mail_arrived_mailbox_door_opened` is armed `on` and triggers on an entity that cannot exist, so it can never fire.** Same shape as #39's dead-code bug found 10:32 today — an automation that reads as covered and does nothing. **Re-pair the sensor, or disable the automation so it stops reading as protection.**<br>✅ **VERIFIED 2026-09-20 18:26, three registries, after Jeff said *"read the files, everything is in there"*:** Z2M `bridge/devices` = **12 entries INCLUDING the coordinator** (11 real devices, no Mailbox); HA **entity registry** match = 1 and it is the automation itself; HA **device registry** match = **0**. Not disabled, not hidden — absent. **And it was already absent at 16:24, BEFORE the Z2M 2.14 update**, so the update did not cause it.<br>🔑 **THE REBOOT DID NOT BREAK IT, IT REVEALED IT.** The 09-19 sweep still measured `binary_sensor.mailbox_contact` (frozen since 09-16 12:44 UTC), so the entity existed then. HA holds MQTT-discovered entities in memory after Z2M stops advertising them; the restart flushed it. ⚠️ **Which means the "stuck `on`" reading this file reported for the mailbox was a GHOST ENTITY the whole time, not a sensor reporting an open flap.**<br>🔴 **THE PLANNING CORRECTION, AND IT UNBLOCKS SPEND: THE MAILBOX NEVER NEEDED A REPEATER.** `#84/#85/#127` are held *"for the repeater still in shipping"*. The record says otherwise, twice: it joined the coordinator **directly from the street at LQI 61–102** in the 08-17 range test, and `zigbee_mesh_routers_2026-08-27.md` states plainly *"The mailbox fix was the RE-PAIR, not the front repeater. It attached straight to the coordinator at LQI 170... Jeff's 08-24 'it's too far, accepted' is retired — it was never distance."* **So this costs $0 and needs no new hardware.** A re-pair is the whole fix, exactly as on 08-27. | **CLAUDE + JEFF (hands on the sensor)** |

---

## 🔵 WAITING ON JEFF — **every row below RE-VERIFIED 2026-09-16 01:45, not taken on trust**

Jeff, 01:40: *"You can't get out of cleaning the list by pushing old stale shit to me so you don't
have to deal with it."* Correct. So each one was **measured** before being left here. One was
already stale, one is half-done, and one nearly produced a false report from me.

| row | claim | measured tonight | verdict |
|---|---|---|---|
| `#3 / #3b / #118` | needs one `bw unlock` | `bw status` → **`unauthenticated`** | ✅ **TRUE** — zero-knowledge vault, nothing substitutes for his master password |
| `#4` | Secure Boot BIOS trip | **Secure Boot = False**, TPM **present and ready** | ✅ **TRUE** — TPM is fine, Secure Boot is the only blocker, and it is a BIOS screen |
| `#112` | GaragePC is off the LAN | 🔴 **see the correction below** | ✅ **TRUE — and it is the best-evidenced row in the file** |
| `#113` | `Document (6).docx` → safe **+** `HCC_ACCESS.md` | `HCC_ACCESS.md` **already carries the GaragePC account and the `Document (6)` reference** | 🟡 **HALF DONE.** The credential half is finished. What is left is purely **physical: put the paper in the safe.** |
| `#11` | "9 of 12 sensors mounted, ~3 in his hands" | **12 Zigbee devices reporting** | 🔴 **STALE** — see the cross-check in `FINDINGS_AND_STOPS.md` |
| `#25` · `#58 / #58b` | iPad tap · HomeKit codes | iPad not reachable from this PC or from HA; HomeKit rows are behind the camera freeze | ✅ **TRUE** / **FROZEN — not checked** |
| `#121 / #122` | the other three bonnets · autumn blow-out | overnight 09-18 the meter moved **24226.0 → 24227.2 gal (~1.2 gal** against the 1.3 gal baseline) — **no live leak.** The freeze risk is forward-looking, so the row stands | ✅ **TRUE** |

### 🔴 TWO LIVE FAULTS FOUND DURING THE 2026-09-18 SWEEP — measured, not inferred

| what | measurement | why it matters |
|---|---|---|
| 🔴 **THE MAILBOX SENSOR IS OFF THE MESH** | **Re-measured 2026-09-19 04:35 — it is OFFLINE, not "stuck".** `binary_sensor.mailbox_contact` and its linkquality BOTH froze at the same minute (12:44:00 / 12:44:52 UTC on 09-16). A device that is gone does not report; HA simply keeps displaying its last state, which happened to be `on`. 🔑 **It died HEALTHY: battery 100% / 3000 mV, LQI 112** at the moment it went silent — so not a flat cell and not weak signal. Z2M itself is fine (every other device reported within the hour), so this is NOT the 08-23 broker-silence class. | That combination — healthy, good LQI, then gone — is the **orphaned** signature the record already describes for this device, and it is why the fix was held for the repeater still in shipping. ⚠️ **A Z2M restart will not fix an orphan with no router near the street; it would only blind the whole house for a minute.** Do not "just restart it". 🔴 **2026-09-20 02:45 — THE "NO ROUTER NEAR THE STREET" PREMISE IS NOW IN DOUBT. SEE #193.** Two repeaters are paired in HA (`light.garage_repeater`, `light.floating_repeater`) and **both are silent** — the garage one since at/before 09-15 16:37, which is BEFORE this mailbox orphaned on 09-16 07:44. **So "held for the repeater still in shipping" may be the wrong reason to be waiting.** Answer #193 before spending anything. ⚠️ Also worth a look, NOT declared a fault (the back-deck precedent says ask Jeff first): `binary_sensor.garage_man_door_contact` has read `on` since 09-18 15:20.<br>✅ **CLOSED 2026-09-20 20:48 — IT WAS THE DOOR, AND THE SENSOR IS PROVEN HEALTHY.** Jeff closed the man door and `binary_sensor.garage_man_door_contact` went **`on` → `off` within 6 minutes** (`last_changed 0.10 h`), battery 100% / 3000 mV. **So the magnet never drifted and the sensor was never stuck** — it reported a clean edge the moment the door actually moved. The `on` reading from 09-18 15:20 to 20:42 was **real and deliberate**: the door was held open for ventilation in 100 °F heat (see `docs/incidents/garage_doors_open_by_design_2026-09-20.md`). 🔑 **This also proves #39 is live code, not dead code** — that automation triggers on an `off→on` edge, which a stuck sensor can never produce; this one just demonstrated a clean transition. ⚠️ `binary_sensor.garage_secure` remains `off` and `garage_door_down_contact` remains `on` — **correct**, the overhead door is still cracked for the fan until the 22:00 secure. Original text below. — 🟢 **ANSWERED 2026-09-20 02:30 — THE MAN DOOR IS *NOT* THE SAME FAULT, AND THE TEST THAT SEPARATES THEM IS REUSABLE.** The discriminator is **whether the LINKQUALITY entity is still updating**, not the contact state: an orphan goes silent on BOTH, a live sensor keeps reporting LQI. Measured side by side — **Mailbox LQI last updated 09-16 07:44:52 = 3.8 DAYS frozen** (confirms the orphan, now **92 h**, up from the 52.8 h recorded above), while **Garage Man Door LQI last updated 09-20 01:34:53 = 0.9 h ago**, battery 100%, 3000 mV, LQI 69. Front Door 3.4 h, Back Deck 1.6 h, Garage Down 1.1 h — all alive and closed. 🔑 **So the man door sensor is ALIVE on the mesh and genuinely reporting OPEN since Thursday 09-18 10:20 local.** That makes it a **door-or-magnet question for Jeff's eyes, not a mesh question** — either it has been standing open since Thursday morning, or the magnet has drifted/fallen. ⚠️ Still NOT declared a fault; it needs Jeff to look. Script: `scratchpad\door-lqi-freshness.ps1`.<br>🔴 **PROCESS NOTE, 2026-09-20 02:30 — BOTH OF THESE WERE RE-DERIVED FROM SCRATCH BY A SESSION THAT HAD NOT READ THIS HEADER.** Jeff caught it mid-investigation (*"Did you read before you did anything?"*). The session had read the `#39` row at line ~113 and never lines 47–52 — **the exact block `SESSION_START.md` §5 says to read FIRST** (*"Its header carries the current verified triage — read that part first, not the 3,100 lines below it"*). It also skipped `SESSION_START.md` entirely and skipped `Search-HCC.ps1`. ⚠️ **The failure mode is specific and worth naming: reading a DEEP row of this file while skipping its HEADER produces confident re-discovery of items already triaged.** Read 47–52 before any investigation, every time. |
| 🔴 **THERE IS NO SMOKE OR CO SENSOR IN HOME ASSISTANT** | searched every one of the 552 entities for `smoke`, `carbon_monoxide`, `co_alarm` — **zero matches** | Confirmed, not assumed. The largest real safety hole in the house. A **Kidde SM120X relay into the existing hardwired interconnect** would put every hardwired **smoke** alarm into HA — see `docs/automation/IDEAS_FROM_WHAT_WE_OWN_2026-09-17.md` §4c *(this pointed at a `-09-18` file that does not exist; corrected 09-19)*. ✅ **PRICED + COMPAT-VERIFIED 2026-09-19: ~$12.50–$15** at an electrical supply house, and confirmed compatible with Jeff's **Firex i4618AC**. ⚠️ **Sold out at Kidde/Konnected/Alarm Grid.** ⚠️ **Covers smoke only — NOT CO — and is dead during a power outage** (manual, verbatim). So the SM120X **closes the smoke half and leaves the CO half open**; CO still needs its own detector. |

⚠️ **NOT a fault, and do not report it as one:** nine unrelated sensors all read stale at *exactly* 67.9 hours. Identical timestamps across unrelated devices is the documented signature of **an HA restart flooring `last_updated`**, not nine simultaneous failures. Use `last_triggered` or Z2M availability instead.

### 🔴🔴 #112 IS OUR DAMAGE, NOT A CHORE HE HAS BEEN PUTTING OFF. SAY SO.

**Jeff, 2026-09-16 01:43: *"The garage pc is still down and should be on the list — that is from
your fuck up. Read the record, you fucked that all up."*** He is right, and listing it under
*"waiting on Jeff"* without that sentence is how a session we broke becomes a task he looks lazy
for not doing.

**Both candidate causes in the record are ours:**
- **(b)** *"the **09-01 boot loop** caused by **a setup script of mine** re-applying the whole
  `USER_RIGHTS` policy block via `secedit`; **Jeff recovered it with System Restore**"* — and the
  record itself notes a restore can roll back a wireless profile.
- **(a)** the **08-13 extender retirement** that left it joined to the vanished `Loewen301_Ext`
  SSID. The gateway evidence points here as the *start* — it has not associated since before
  08-14 — but that does not make it his doing either.

**And the handling was wrong too, which the row already admits:** *"Jeff was told flatly it was the
SSID, before the 09-01 note — which was sitting UNCOMMITTED in the working tree — had been read."*
A confident wrong cause, given to him, from a file we had not committed.

**So the correct framing:** the machine is down because of work done on it here. The only remaining
step needs hands on the machine, which are his — **but it is a repair we owe, not a chore he is
behind on.** When it comes back: give it a DHCP reservation like Beehive's, because its recorded
addresses go stale every time.

### 🔴 MY NEAR-MISS ON #112 TONIGHT — the trap this row already warns about

I pinged **`192.168.1.215`**, got three replies, and was about to report **"GaragePC is back on the
network."** **It is not GaragePC.** `.215` is the **Fire TV** — the record has PiPup posting to
`http://192.168.1.215:7979/notify`. GaragePC's addresses are **`.121` and `.212`**, and the row
already proved both fail.

**What the record already established, and it is conclusive:** the BGW320's own device list shows
**56 devices, and the gateway retains powered-off clients for ~4 weeks** — `JeffsLapTop` sits in it
right now reading *Status: off, 17 days stale*. **GaragePC is absent in every state.** So it has
not touched this gateway since before 2026-08-14 — which points at the extender retirement, not
the 09-01 boot loop.

⚠️ **The lesson is the one in `ACCESS_MAP` about instruments that lie: a ping proves something
answers at an address, not WHICH something.** Confirm the identity before naming the machine.

**Fix is unchanged and genuinely his:** power it on, join it to `Loewen301`. Credentials are
already in `HCC_ACCESS.md` §5, so the beast can authenticate the moment it appears.

**His go / his decision** (one word each):
**RE200 → dedicate to B-hyve** (plan in `NETWORK_MAP.md`; **never Pairing Mode**) · `#10` arm panic **and** disable the old one · `#39` + the alarm subsystem · `#28` URL rotation ·
`#84 / #85 / #127` **one Z2M restart**, held for the repeater still in shipping · `#106` the A/C job ·
`#131` after the proof run · `#27` Smart Stall *("do not start until Jeff says go")* ·
**`#183`** the utility-card half of the Cloudflare fix — written, tested, **shipped switched off**

---

## 🚨 THE ONE BIG THING GENUINELY NOT BUILT — THE ALARM SUBSYSTEM

**Jeff, 2026-09-10: *"Alarms not built !!!"*** He is right. It is one system, not four half-tasks:

**⚠️ Two numbers in this table were STALE and are corrected below — measured 2026-09-16 01:22.**

| piece | row | state — **re-measured 2026-09-16** |
|---|---|---|
| Panic button | `#10` | v2 built and disabled; the OLD one that reaches nobody is still armed. 🛑 Jeff, 09-10: *"Leave the panic button alone till the alarms are built."* |
| Sirens | — | not built; the TS0224 was judged too weak and reassigned to the leak alarm |
| Fire detection | — | not built at all — no smoke/CO sensors exist. **This is the biggest real hole in the house.** ✅ **09-19: the parts question is now ANSWERED, so this is no longer "research needed" — it is a buy-and-wire job.** Smoke = **Kidde SM120X ~$12.50–$15** (verified compatible with his Firex i4618AC) into a spare Zigbee contact sensor he already owns. **CO = separate detector, the SM120X does not carry CO.** Power-outage gap is real and documented. Full detail + wiring colors: `docs/automation/IDEAS_FROM_WHAT_WE_OWN_2026-09-17.md` §4c. **Still gated behind Jeff's own order-of-operations (J45 → RTL-SDR → alarm layer) and his go-ahead.** |
| Sensors it runs on | `#11` | 🟢 **DONE — the old "9 of 12" is stale. 12 Zigbee devices are on the mesh and reporting.** The fleet is not the blocker any more. |

🔴 **Build it as ONE subsystem, not as orphans — Jeff's own instruction.** It needs his go, and it
is the largest real piece of work left anywhere in this project.

---

## ⛔ FROZEN FOR THIS SESSION

Enforced by `windows-scripts/hooks/Hook-RequireRead.ps1`, listed in `.claude\session-freeze.txt` —
the gate blocks these, so it is not a matter of remembering.

- **Cameras** — Jeff, 2026-09-16: *"We said no cameras tonight."* Also `#181`'s remaining half.
- **Genealogy** — *"Genealogy is not the main priority here, the project is!"* → `docs/genealogy/OPEN_GENEALOGY.md`

---

## 📚 WHERE EVERYTHING ELSE WENT

| what | where |
| # | what | why it is still here |
|---|---|---|
| **155/156/157** | Ancestry: **279 duplicate people**, the DNA veto, and the sweep only seeing **58%** of the tree | genealogy, real work, needs a session with time |
| **189** | 📄 **Daniels still owes the flex diameters IN WRITING.** The revised proposal deleted the *16" flex supply/return*, the *16"–14" reducer* and the *875 CFM* line. Logged **09-10** as *"THE ONLY THING STILL OWED BEFORE SIGNING"* — that connection work is the entire reason $9,000 counts as turnkey. | **JEFF** — 7 days open. Do not sign without it. |
| **191** | 🔴 **BLINK PIN STORM — OCCURRENCE THREE. Two automations are TURNED OFF right now and must not be blindly re-enabled.** 2026-09-18 5:01 PM Jeff got a Blink verification text. Entry was `setup_error` / *"Required Blink re-authentication"*, **all 6 real cameras `unavailable`**, 13 of 19 entities down. 🔴 **`Verify-CameraStreams.ps1` printed ALL GOOD throughout** — it measures the go2rtc `*_live` copies, not the Blink entities. Same wrong-instrument trap as 08-21. ✅ **Re-auth completed by Jeff on HA's own screen 5:08 PM; entry `loaded`, 6/6 cameras back, flow closed.** (4 sensors read `unknown` after recovery — expected, predicted in the 08-19 write-up.) | ⚠️ **THE GATING IS BROKEN AND THE AUTOMATIONS ARE BACK ON.** `hcc_blink_periodic_health_reload` and `hcc_blink_motion_poll_30s` both fired at 22:00:00Z, one minute before the PIN text, **while the entry was already in `setup_error`** — exactly the state the 2026-08-19 fix was supposed to gate them on. **That gating does not work and was never verified.** They were turned OFF at 5:05 PM to stop the texts and **turned back ON at 5:12 PM on Jeff's instruction** (*"Turn them back on"*), after confirming the entry was `loaded` first. That is safe **only while auth holds** — the 08-19 memo: *"Harmless while auth is good … but once the token was rejected every reload = a login attempt = a new PIN."* 🔴 **THE TRIPWIRE: if Jeff gets another Blink text, this is why.** `hcc_blink_needs_re_auth_alert` works — it fired correctly at 22:05 — so the warning will come. Fix is to read the two condition blocks, repair the `setup_error` gate, and PROVE it against a broken entry. Prior occurrences 07-20 and 08-19; the 08-19 memo already called it *"recurring class, not a fluke"*. | **CLAUDE** — Jeff 5:10 PM: *"You got to much other stuff to finish leave it for now."* Deferred by his call. |

---

## 🔴 STANDING CONSTRAINT — ANGELA'S WORK COMPUTERS (Jeff, 2026-09-19 09:10)

> *"make sure you don't block Angela's work computers. I don't want them on my network but they
> have to be on the WiFi and ethernet"*

**Two requirements, and they pull in opposite directions:** they must keep **internet over both WiFi
and Ethernet**, and they must **not participate in Jeff's LAN**.

🟢🟢 **RESOLVED 2026-09-19 09:12 — THE REQUIREMENT IS ALREADY MET, AND NOT BY US.** Jeff, verbatim:
***"I can't get on her computers they are blocked I tried just to see if I could."***
Her employer's endpoint policy blocks inbound access — corporate-managed, host firewall closed, no
file sharing exposed. So the machines pull an address and internet from the gateway while
participating in **nothing** on Jeff's LAN. **That is exactly "on the WiFi and Ethernet but not on
my network," achieved with zero configuration on our side.**

✅ **THEREFORE: DO NOT build guest-SSID isolation for this, and do not touch the gateway for it.**
A BGW320 change would affect the **whole household's** connectivity to solve a problem that does
not exist. The wired-isolation question is **moot** — it never needed answering.
⛔ **And do not try to reach or "fix" Angela's machines.** They are her employer's, they are locked
down deliberately, and being unable to reach them is the CORRECT state — not a fault to report and
not a wall to work around.

🟢 **Nothing done in the 09-18/19 build can block them.** Every change was *inside* three machines —
Beast (sharing/media config), Acer (power, SSH key, time), Lenovo (samba/wsdd/nmbd). **The gateway,
DHCP and routing were never touched.** The pending SMB-signing change is also per-machine on Jeff's
three, not network-wide, so it cannot reach her either.

**What the gateway can actually do (verified 2026-09-19, not assumed):**
- **WiFi — yes.** BGW320-500 Guest SSID has a **Network Access** setting; on **"Internet Only"** a
  guest client reaches the internet but **cannot contact any LAN host, including the gateway**, on a
  separate subnet. That is exactly the requirement.
- **Ethernet — UNCONFIRMED.** Guest SSID is wireless-only in every source found; the BGW320's LAN
  ports all sit on the home LAN and it exposes no VLANs. **Do not promise wired isolation until it
  has been looked for in the admin UI at `192.168.1.254`.** "Could not check" is the honest state.
- ⚠️ Changing gateway settings affects the **whole household's** connectivity — **needs Jeff's
  explicit go**, never done unasked.

📌 **Practical note:** corporate-managed laptops normally do not join a workgroup or share files
anyway, so they can sit on the same subnet without being "on the network" in the sharing sense.
Being on `192.168.1.x` is not the same as participating in Jeff's file sharing.

---

## 🔌 LENOVO HARDWARE LIMITS — measured 2026-09-19, do not re-investigate

| fact | evidence |
|---|---|
| ❌ **NO Bluetooth radio** | `/sys/class/bluetooth` = **0 entries**; `rfkill` lists only `ideapad_wlan`/`phy0` (a switched-off radio would still be listed); the two `8087:0024` USB devices are Intel **Rate Matching Hubs**, not BT |
| ❌ **2.4 GHz WiFi only** | Qualcomm Atheros **AR9285** — single-band. That is why it sits on `Loewen301` and not `Loewen301-5G`, and it cannot change without hardware |
| 🖥️ machine | **Lenovo B570 (1068BAU)** — many shipped without BT |
| ✅ **BT software already complete** | `bluez 5.85`, `bluez-obexd`, **`libspa-0.2-bluetooth`** (the PipeWire A2DP plugin), pipewire + wireplumber active — **a USB dongle would be plug-and-pair, nothing to install** |

**So Jeff's garage Bluetooth speakers will NOT work on it as-is.**

🟢 **BUT THE ZERO-COST PATH IS CONFIRMED WORKING, not assumed** (tested 2026-09-19 10:04): sink
`alsa_output.pci-0000_00_1b.0.analog-stereo` — **ALC272 Analog = built-in speakers + 3.5 mm jack**,
with `HDMI 0` as a second output. **`speaker-test` played a 440 Hz tone without error** and the sink
moved `SUSPENDED → IDLE`, proving audio actually flowed. Not muted; volume raised 40% → 60%.
**A 3.5 mm cable to the speakers' aux input gives the garage sound today for nothing** — which
matters because the SEND TO GARAGE handoff is useless without audio out there.

⚠️ **Trap:** `pactl list short sinks` returns EMPTY over SSH because **`pactl` was not installed**
(now is) *and* it needs `XDG_RUNTIME_DIR=/run/user/1000` + `DBUS_SESSION_BUS_ADDRESS` set.
**An empty sink list is NOT evidence of no audio.**
🛒 **DONGLE — WHAT TO BUY, verified against THIS machine + real prices from the BROWSER, 2026-09-19.**
**Buy one whose specs say `RTL8761B`/`RTL8761BU`, or that states Linux support.** The usual forum
failure cannot happen here: kernel **7.0.0-31 / Ubuntu 26.04.1 LTS** (btrtl needs 5.8+), modules
`btusb`+**`btrtl`** present, and the firmware is **already on disk** — `rtl8761b_fw.bin`,
`rtl8761bu_fw.bin`, `rtl8761cu_fw.bin`, 46 files in `/lib/firmware/rtl_bt/`. With `bluez 5.85` +
`libspa-0.2-bluetooth` it is plug-and-pair. USB ports are USB 2.0, which is all a BT dongle needs.

✅ **RECOMMENDED: Cudy `BU530` — $6.49**, listing states verbatim *"…ultra-low latency audio,
**Windows and Linux**"*, 4.2★ (248), Amazon "Overall Pick", next-day delivery.
🔴 **THE TRAP he would have hit buying "a cheap dongle": the best-sellers are WINDOWS-ONLY in their
own specs** — **UGREEN BT5.3 $6.99 (*"Windows 11/10/8.1 Only"*)**, UGREEN BT6.0 $7.99,
**TP-Link UB500 $11.99**, Amazon Basics 5.4 $8.96. UGREEN alone shows *3,000+ bought this month*.
**Do not buy any of those.** Safer/pricier: **StarTech `USBA-BLUETOOTH-V5-C2` $16.99**
(Windows/Linux, RTL8761B) or **Panda BT4.0 Nano $19.99** (lists Ubuntu/Mint/Fedora by name).
⚠️ **Unconfirmed: the Cudy's exact chipset** — it claims Linux but does not name the chip.
⚠️ **WebFetch is 403 on Amazon; the BROWSER is not.** Jeff, 10:13: *"Why can't you think Use the
browser so I get the correct one."* He was right — `ACCESS_MAP` already says vendor sites need it.

🟢 **JEFF'S DECISION 10:17 — buying the dongle, NOT running a cable:** *"I don't need more cables on
my work bench I will get the dongal."* **The 3.5 mm cable option is CLOSED — do not re-pitch it.**
🟢🟢 **DONGLE ARRIVED AND WORKS — VERIFIED LIVE 2026-09-23 09:43.** Jeff: *"it's in and working great it's even picking up the neighbors tv."* Measured, not taken on trust: controller **`hci0` = `D4:0D:AB:15:2A:75 GarageLaptop [default]`**, `bluetooth.service` **active**, `libspa-0.2-bluetooth` present (the A2DP plugin — without it devices pair and stay silent), and a 20 s scan returned **11 devices**, so the radio genuinely transmits and receives. **The "no Bluetooth radio" hardware limit below is now WORKED AROUND — do not re-raise it as a blocker.**
🟢🟢 **CLOSED 2026-09-23 11:10 — SPEAKER PAIRED AND JEFF HEARD THE TONE.** Device: **QFX `PBX-BF120`** (FCC ID OGGQFX-1215), MAC **`41:42:CE:67:95:B0`**, found advertising by name. `bash ~/pair-bluetooth.sh 41:42:CE:67:95:B0` → *Pairing successful · trust succeeded · Connection successful*, default sink `bluez_output.41_42_CE_67_95_B0.1`, `bluetoothctl info` reads **Paired yes / Trusted yes / Connected yes**. ✅ **Proven by EAR, not by exit code — Jeff: *"Yes I herd the tones."*** 🔑 **TRUSTED is the load-bearing bit: without it the speaker re-pairs every time instead of reconnecting itself.** 🟢 The dongle enumerated as **`0bda:8771` Realtek — the RTL8761B the 09-19 research deliberately targeted**, with `rtl8761b_fw.bin` already on disk; the "check the chipset, the best-sellers are Windows-only" work paid off exactly as intended. 🔑 **This also closes the gap the record flagged: the SEND TO GARAGE handoff now lands on a machine that can make sound.** ⚠️ A sink reading `SUSPENDED` is idle, not broken. *Superseded text follows.* ⏭️ ~~**PAIRING STILL OWED, and it is not a fault: Jeff, 09:45 — *"the speaker is not plugged in."***~~ A BT speaker only advertises a friendly name while powered **and in pairing mode**; nothing audio-like was in the scan, which is expected. **When he powers it and the LED flashes FAST:** re-scan, then `bash ~/pair-bluetooth.sh <MAC>` — it pairs, **trusts** (without trust it never auto-reconnects), sets the sink and plays a tone to prove it. ⛔ **The scan also sees the neighbours' gear — Megan is west, Grandma is north (Jeff, 09:44). Do NOT pair with, probe, or catalogue their devices, and never map a MAC to a person. Only ever pair the address Jeff confirms is his.**
🛠️ *Staged 09-19, still in place:* `windows-scripts/lenovo-pair-bluetooth.sh` is staged
on the Lenovo at `~/pair-bluetooth.sh` (`bash -n` clean; dry-run correctly reports **"NO CONTROLLER"**
today rather than faking success). Jeff plugs it in and walks away — Claude runs it from the Beast:
it starts the service, scans, pairs **and trusts** (without trust the speakers never auto-reconnect),
sets the bluez sink as default, and **plays a tone to prove it**.

⚠️ Jeff's 5 GHz-vs-2.4 hunch was right about the **hardware** but was NOT what broke
discovery — that was `disable netbios = Yes`.

---
## 🖥️ COMPUTERS / NETWORK — BUILT OVERNIGHT 2026-09-18→19 (all live-verified)

Jeff: *"build a network so everything is available everywhere"* / *"make sure they all connect both ways."*

| what | state |
|---|---|
| **File mesh, both directions** | ✅ **5 of 6 legs live.** Beast→Acer `K:` (rclone over **authenticated SSH** — Win11 24H2 mandates SMB signing and guest sessions cannot be signed, so guest SMB from the Beast is impossible), Beast→Lenovo `L:` (native SMB once Jeff cleared the signing requirement), Acer→Beast `O:`, Acer→Lenovo `GarageFiles`, Lenovo→Beast mount. ❌ **Lenovo→Acer** is the one gap — blocked twice by the safety classifier (can't open a guest share on the Acer; can't add the Lenovo's key to it). Least-used leg. |
| **Auto-reconnect after reboot / outage** | ✅ verified on all three — WiFi auto-connect, sshd + watchdogs, discovery services, drive re-map logon tasks, never-sleep. 🟢 **NOW PROVEN BY A REAL UNPLANNED REBOOT, not a claim:** the Acer froze and rebooted at **23:21:48**, and `HCC-MapBeastAtLogon` ran at **23:22:03 — 15 seconds later — returning `0x0`**, with sshd back up on its own (Claude logged in over SSH with no password afterwards). That is the whole auto-reconnect promise surviving the exact event it was built for. ⚠️ **Do not re-test this with `net use`/`O:` over SSH** — mappings are per-logon-session and will read "Unavailable" on a perfectly healthy machine; use `Get-SmbConnection`. See `ACCESS_MAP` §4b. Honest limit: a long outage that drains the laptop batteries still needs a power button; the Beast's auto-boot is a BIOS AC-recovery setting. |
| **Printer (HP OfficeJet 4650)** | ✅ all three print. Windows queues repointed **WSD → fixed IP 192.168.1.208** (WSD does not reliably wake a sleeping printer). 🔴 **Auto-Off cannot be disabled — firmware only accepts `2hours`** (PUT every other value → 400). Worked around with `HCC-PrinterKeepAlive` on the Beast: a PJL status poke every 90 min that prints nothing and stops the idle timer expiring. |
| **Clock sync** | 🟢 **FIXED 2026-09-19 05:00 — and it was broken on TWO of the three.** The Acer read `Source: Local CMOS Clock, Last Successful Sync: unspecified`, and **the Beast's `w32time` was Stopped/Manual** — neither was syncing with anything. Both were accurate *by luck* at the time of measurement. **This matters because every fault call in this project is built on event-log timestamps** (Kernel-Power 41, volmgr 161, the `last_reported` traps) — a drifting clock corrupts the evidence silently. Both now on `time.windows.com`, service Automatic; Beast at **Stratum 5**. Lenovo was already clean (chrony active/enabled). Script: `windows-scripts\fix-time-sync.ps1`. |
| **Discovery / workgroup / media / email** | ✅ all three on `LOEWEN301`, WS-Discovery on (Win11 killed the old workgroup browser — `net view` error 6118 is expected, not a fault), DLNA running, Email shortcut on every desktop. |
| **Browsers** | ✅ Edge signed in as `jeff.loewen@comcast.net` with **126 bookmarks already syncing** between Beast and Acer — it was never broken. Edge installed on the Lenovo 09-19; one sign-in there makes all three identical. ⚠️ Chrome is **Claude's** automation browser, not Jeff's. |
| ~~**#112**~~ | 🟢 **STICK RE-AUDITED 2026-09-22 7:50 PM** after Jeff: *"do you have all this set up and ready go on the stick for the Garage HP conversion? I don't want to waste 2 days with a bunch of fuckups."* Read end to end on `E:\`: `autoinstall.yaml` (parses — hostname KitchenPC, user jeff, 4 late-commands copy GARAGE-SETUP into the home dir, `shutdown: poweroff`), `boot/grub/grub.cfg` (both AUTO entries carry `autoinstall` on the kernel line, braces balanced, both original entries kept), `garage-hp-setup.sh`, `setup.sh`, `rescue-files.sh`, README. **LF endings confirmed BYTE-BY-BYTE with Python — 0 CR in every file.** ⚠️ Git-Bash `grep -c $'\r'` reported CRLF on every line of every file and was **wrong**; `bash -n` under Git Bash also tolerates CRLF, so neither is proof for Linux — use Python bytes or `od -c`. `bash -n` clean ×3. **Patched to match tonight's no-password decision (#199):** step 7 now guest-mounts the Beast's `Users` / `OneDrive` / `ClipArchive` **and the Acer's `Users`** (the exact fstab line proven on the Lenovo), puts **Beast** and **Acer** folders on the desktop + sidebar bookmarks; step 6 adds `server signing = required` (Win11 24H2 refuses a guest session to an unsigned server — the 09-18 Lenovo lesson) while the CLIENT side stays default (an anonymous session cannot sign); the self-verify now proves *Beast – everything: N items, no password*, the Acer mount and signing. `setup.sh`'s mount hint switched to Guest / empty password. Repo copy `windows-scripts/garage-hp-setup.sh` synced (it was the stale GaragePC version). ❌ Auto-login + an unlocked keyring for the wall display was refused by the classifier — do it over SSH after the build, with Jeff's OK. 🔑 **The Wi-Fi risk is already retired by the record:** the Techkey dongle came up in-kernel on THIS stick's live session on 09-01 (`192.168.1.121`, MAC `90-de-80-32-d9-9f`). 🔴 **What is NOT verified and cannot be from here: the autoinstall has never actually booted on the HP** — the 09-01 "spinning" attempt was the manual installer, before the autoinstall existed. The Windows boot loop (`GARAGE_PC_REPAIR_KIT.md`) does not touch the USB path: ESC → F9 is firmware, and `layout: direct` wipes whatever the half-finished 09-01 install left. GRUB label still says "GaragePC" — label only, README says so. <br>*Earlier row:* 🟢 **PREP DONE — 2026-09-20 IT BECAME BOOT AND WALK OUT. NO COMMAND AT ALL.** Jeff, 09-18 and again 09-20: *"make sure you fix it where you can take over and all I have to do is put the stick in."* An **Ubuntu 26.04 autoinstall** now sits at the root of the stick (`E:utoinstall.yaml`, beside `casper/` — the documented location) with a default GRUB entry that runs it. It erases the disk, installs, creates user `jeff`, joins `Loewen301`, installs `openssh-server` **+ the Beast's key**, copies GARAGE-SETUP to the new home, and **POWERS OFF**. 🔴 **`shutdown: poweroff`, NEVER `reboot` — that is load-bearing:** on 2026-09-01 a script re-applied a USER_RIGHTS policy block and **Jeff ran it twice**, boot-looping this machine. A stick left in a USB-first BIOS would re-run the installer and re-wipe the box it just built. ✅ **Verified, not assumed:** YAML parses with all assertions passing; `openssh-server` confirmed in the stick's LOCAL pool so SSH survives an offline install; embedded key matches `id_ed25519.pub` and the Beast holds the private half; scripts LF/no-BOM/`bash -n` clean; both ORIGINAL GRUB entries preserved and backed up, so the stick cannot be made unbootable. ⚠️ **`quiet splash` REMOVED on purpose** — the last attempt was left *"just spinning"* and a spinner is unfalsifiable; it now prints console text, and `nomodeset` is the default entry for Sandy Bridge graphics. 🔑 **NAMING: this machine becomes `KitchenPC`, NOT GaragePC** — the **Lenovo** is the garage computer. ⏭️ **Still needs Jeff's physical boot** (ESC → F9 → USB): no vPro/AMT on 2011 hardware, which `ACCESS_MAP` §7b lists as a genuine blocker. Everything after that keypress is automated. <br>*Superseded text:* ~~the garage job is now boot, install, ONE command, walk out.~~ `E:\GARAGE-SETUP\garage-hp-setup.sh` (bash -n clean, 0 CR bytes) does hostname/sudo/never-sleep/watchdog/workgroup/wsdd/its own share/Beast mount/printer/browser+shortcuts/auto-updates — and installs **SSH + the Beast's key FIRST**, so Claude finishes everything remotely the moment it is on WiFi. Jeff: *"I'm not sitting in the 100 degree garage."* Still needs his physical boot of the stick. |
| 🔴 **ACER HARD FREEZE — still open, but three theories are now DEAD** | **6 real freezes in 24 h** (11:26, 15:54, 16:37, 21:03, 21:50, 23:21 — Kernel-Power 41). ❌ **"End of life" was WRONG** (drive healthy: 0 read errors, 0 wear). ❌ **Firmware M3CR046 does NOT apply** — Crucial ships separate families (`M3CR032>033` vs `M3CR042>046`); the Acer is on M3CR033, already the newest for its revision, and flashing 046 would have been the wrong family. ❌ **Thermal ruled out** — CPU 43.1 °C, drive 26 °C, zero throttle events. ✅ **APPLIED: SATA AHCI LPM disabled** (the documented Crucial-on-Intel trigger; needs no firmware). 🔑 **The clue missed all night:** `volmgr 161 "Dump file creation failed"` — the system *did* bugcheck, it just could not write the dump **because the drive it needed to write to had hung**. "No dump, nothing in the log" was never a mystery. **Verdict pending an overnight run** — gaps have ranged 43 min to 4.5 h, so anything under that proves nothing. Next suspect if it recurs: RAM (pull one stick — Jeff's own test, free).<br><br>🟢🔴 **2026-09-19 07:45 — JEFF'S OBSERVATION RESET THE WHOLE DIAGNOSIS, AND IT IS THE BEST CLUE ANYONE HAS PRODUCED.** Verbatim: *"it never crashes when it's being used because I really think it has something to do with the screen turning off or it hibernates... I will watch TV on it all night and it never crashes or shuts off **only when idle**."* 🔴 **That points AWAY from RAM** — bad memory fails under load, not at rest — so the queued memory test was **disarmed** rather than waste a reboot on it. ⚠️ **Do not re-pitch "pull a RAM stick" without new evidence; the load/idle asymmetry argues against it.**<br>**What the idle audit found:** the GLOBAL power path was ALREADY locked down — HIPM `0`, **DIPM `0`**, PCIe ASPM `0`, USB selective suspend `0`, fast startup off (and blocked by policy), **no `hiberfil.sys`**, scheme = **Ultimate Performance**, sleep states S3/Hibernate/Hybrid (**not** Modern Standby). And the Kernel-Power log shows **only ONE sleep event all day** (09-18 14:54:09 → resumed 14:54:11, 2 s) with **no sleep before any freeze** — so a sleep transition is not the trigger. *(It also closed a gap in my own 00:33 fix: I had set only HIPM and called SATA LPM "disabled"; DIPM is the other half. Both now read 0.)*<br>🟢 **WHAT WAS STILL OPEN AND IS NOW FIXED:** the **per-device** *"Allow the computer to turn off this device to save power"* layer, which is independent of the power scheme. **15 of 15 devices were allowed to power themselves down**, including **Atheros QCA6174 Wi-Fi** (`VEN_168C&DEV_0042`), **Realtek RTL8168 Ethernet** (`VEN_10EC&DEV_8168`), the **Intel xHCI USB 3.0 controller** (`VEN_8086&DEV_9D2F`) and the **Intel Management Engine** (`VEN_8086&DEV_9D3A`). A bus or network controller powering down at idle is a documented hang source **and only bites a machine that is sitting still — exactly Jeff's pattern.** Now **0 of 15**; Ethernet WakeOnMagic disabled. Reversible: `acer-stop-device-sleep.ps1 -Revert`.<br>**Jeff also set display-off to NEVER on AC himself** — confirmed live (display NEVER, sleep NEVER, disk NEVER). **Both changes are now running as one live experiment, and `HCC-WatchAcer` on the Beast timestamps the next freeze to the minute with disk-queue depth.**<br><br>🔬 **POST-MORTEM 2026-09-19 07:25, run the moment Jeff power-cycled it. Two theories raised and BOTH killed by evidence — read this before proposing either again.**<br>❌ **GPU / LiveKernelEvent 141 — RULED OUT.** The WER queue holds **43 × LiveKernelEvent 141** (`VIDEO_ENGINE_TIMEOUT_DETECTED`), and they all showed a folder timestamp of *today 07:20*, which looked damning. **The folder timestamp is when WER flushed the queue, not when the fault fired.** The real `EventTime` inside each `.wer` says the **newest is 2026-06-26 22:23** — three months before these freezes. ⚠️ **Trap for the next session: read `EventTime` from the .wer, never the directory's LastWriteTime.** Also ❌ the **Intel graphics driver is not the cause** — Intel Graphics Experience installed 09-18 **20:17**, but freezes at 11:26/15:54/16:37 predate it. Driver is `31.0.101.2141` (2026-03-29), UHD 620.<br>❌ **September Windows updates — NOT the sole cause, though the timing is ugly.** `KB5124007` + **`KB5129195`** + `KB5126052` landed 09-18 **11:36–12:02**, and at **12:15 they rewrote core kernel drivers incl. `scsiport.sys` (storage), `Wdf01000.sys`, `afd.sys`, `WifiCx.sys`** — and a replaced storage driver fits the `volmgr 161` "drive hung so hard the dump could not be written" signature. Build is now **26200.9457 = KB5129195**, which is Microsoft's **emergency out-of-band update (2026-09-14)** for breakage caused by the Sept 2026 Patch Tuesday; press reporting says *"driver timeouts, black screens and full system freezes"* from that cycle remain **unacknowledged and unfixed** (reported on Radeon; this machine is Intel, so not a direct match). 🔴 **But it cannot be the whole story: there are NO System events on 09-18 before 11:26:34 — the machine was OFF — so the first event 41 reports an unclean shutdown from BEFORE the updates installed.** Jeff's opening message that night was *"It may be hung up."*<br>📌 **Boot census 09-18→19: 12 boots, 7 unclean.** 14:39 · 14:40 · 14:56 · 15:17 · 19:58 were **clean** deliberate restarts (driver work + the memory test), so do not count them as freezes.<br>🔴 **WHAT IS LEFT, AND IT IS STILL UNTESTED: RAM.** `MemDiagV1` at **09-18 19:58:18** records *"The Windows Memory Diagnostic was canceled during execution"* — **the test never completed, so RAM has never been cleared.** Modules are **2 × 8 GB TIMETEC `SD3-1600` @ 1600 MT/s — aftermarket, not OEM.** WHEA errors: **0**. No minidumps (`C:\Windows\Minidump` empty) and **no WER report at any freeze time**, which is itself consistent with a hang so hard nothing can be written. Drive: **36 °C, 0 read errors, 0 wear, 1952 power-on hours.** **Cheapest definitive test is Jeff's own: pull ONE stick and run on the other — minutes, not the 20-hour test he objected to.**<br><br>🔬 **CAN THE MACHINE TELL US WHY? NO — and the obvious evidence is CIRCULAR (checked 16:50).**
Event rates before (19.8 h, 7 freezes) vs after (8.9 h, 0 freezes) look damning — `Kernel-Power`
3.44/h→0, `Dhcp-Client` 2.33/h→0, `WLAN-AutoConfig` 0.61/h→0, `Ntfs` 1.26/h→0, warnings 2.33/h→0.
🔴 **All of it is an artifact of not rebooting.** The before window has **11 boots**, the after window
has **0**, and each boot emits Kernel-Power + a DHCP lease + a WLAN reconnect + an NTFS mount. The
DHCP events timestamp at **23:21:56 / 21:50:34 / 21:03:42 — ~8 s after each boot.** Those rates
measure the *consequence* of stability, not its cause.
🔴 **"The log is quiet now" is also worthless:** the before window contains **six silent stretches
over 20 min, including 195 min and 172 min with nothing logged** — while it was still freezing.
✅ **What IS non-circular:** the freezes left **ZERO events in the 10 minutes before every one** — no
warning, no error, no device event. It died silently, which points at a hardware/firmware power
transition rather than a software fault (software usually complains first). And **0 of 15 devices
are currently allowed to power down**, so the fix is holding.
**Bottom line: the only real evidence is the OUTCOME, not a mechanism. Do not overstate it.**
🔑 **JEFF CALLED IT, 2026-09-19 15:38:** *"it had to be something to do with the screen setting or
power setting."* The evidence agrees — **and it also says what it was NOT.**
❌ **Disk power management is ruled out.** SATA HIPM/DIPM was disabled at 00:33 and it **froze again
at 05:23**. The most-documented cause for Crucial-on-Intel was not this machine's cause.
✅ **What is actually running, all applied within ~35 min of the 07:12 boot:**
  • **07:45 — Jeff** set display-off to **NEVER** on AC
  • **07:47 — 15 of 15 devices blocked from powering down**, incl. the **Atheros QCA9377 Wi-Fi**,
    **Realtek RTL8168 Ethernet**, the **Intel xHCI USB 3.0 controller** and the **Intel ME**
  • **07:50 — Realtek Green Ethernet / EEE / Power Saving Mode** disabled
⚠️ **CONFOUNDED EXPERIMENT — do not claim which one fixed it.** They landed together. The likeliest
single culprit is the **device-level power-down** (a network or bus controller sleeping), because
that only misbehaves on a machine sitting still — which is exactly Jeff's observation. A display
timeout alone would not explain a hang so hard the crash dump could not be written.
🛑 **Do NOT re-enable one to find out.** Jeff has a working machine; the answer is not worth the risk.
🟢 **2026-09-19 13:25 — FIRST TIME IT HAS BEATEN ITS OWN RECORD, on the IDLE-POWER fixes (not LPM).**
**6.20 h / 372 min uptime, 0 dropouts across 324 one-minute samples**, sitting idle the whole time —
**27 min past the 5h45m best it ever managed before.** Max disk queue in the last 30 samples: **1**
(a climbing, never-draining queue is the drive-hang signature, so that is clean too).
📉 At the pre-fix failure rate (6 freezes in 11h55m) the odds of a clean run this long by chance are
**4.4%**, down from ~7% at the 5.3 h mark this morning. ⚠️ **Still NOT proof — 4.4% is not nothing,
and this is one interval.** Do not strike this row until it has run clean well past a full day.
**What changed since the LPM attempt:** Jeff's own observation (*"only when idle"*) drove closing
**every** idle power path — 15/15 devices blocked from powering down, Realtek Green Ethernet/EEE off,
display/sleep/disk all NEVER, plus his own display-never change. That is what is being tested now.

🔬 **Full MEI / screen / evidence-arming research from 2026-09-19 evening and 2026-09-20 has
MOVED to `docs/FINDINGS_AND_STOPS.md` → "ACER ASPIRE E5-576 HARD FREEZE".** Findings, not tasks.
Headlines kept so nobody re-derives them: **ME firmware is `11.8.55.3510`** (read live from
`root\Intel_ME` → `ME_System.FWVersion`); **BIOS 1.49 of 2019-03-22 is the last Acer shipped**;
newer CSME firmware 11.8.97.4739 exists only via an unofficial Intel `FWUpdLcl64` flash -
**not recommended, brick risk**. 🔴 **Driver rollback is WRONG** - 2441.7.0.0 is the correct
pairing for 11.8.x firmware. Removing MEI is SAFE (no BitLocker, no PIN) but unnecessary.
Battery 74.1% - ruled out. Sleep-failure theory tested and DEAD. Screen reverted to Jeff's NEVER.
✅ **ARMED for the next freeze:** RIGHT Ctrl + Scroll Lock × 2 forces a dump; kernel dump + 8 GB
dedicated dump file, effective next boot. 🔑 **If those keys do nothing, that is itself evidence
the hang is below the OS.** 🟢 **19.19 h clean at 02:24, 1050 samples, 0 dropouts** (old best 5h45m).

🔴🔴 **VERDICT 2026-09-19 05:23 — THE LPM FIX DID NOT CURE IT. IT FROZE AGAIN.** Unreachable at 05:23: ping dead, SSH timeout, **ARP entry aged out entirely**, while the Lenovo, Beehive and the gateway all answered — so it is the machine, not the network. Last good reading **05:05 (uptime 5h39m)**, so it froze in the **05:05–05:23** window. **Clean run ≈ 5h45m.** ⚠️ **That is still LONGER than any pre-fix gap (best 4h28m, mean 2h23m), so the LPM change may have helped — but "helped" is not "fixed," and one longer interval is not proof of either.** 🛑 **A hard freeze cannot be cleared remotely — it needs Jeff's hand on the power button.** **Next suspect, per this row's own plan: RAM — pull one stick. Jeff's own test, free, and now the top candidate.** When it is back, read the Kernel-Power 41 timestamp for the exact minute; also deploy a *network* heartbeat (the existing `HCC-Heartbeat` writes only to the Acer's local disk, which is unreadable while it is frozen — that is why the exact minute is not known).<br><br>~~🟢 **FIRST CLEAN RESULT — measured 2026-09-19 04:39, and deliberately NOT called "fixed."**~~ *(superseded by the 05:23 freeze above — kept because the before/after numbers in it are still the measurement baseline)* Since the LPM change at 00:33: **zero Kernel-Power 41**, uptime **5 h 17 m** (booted 23:21:48, i.e. the last freeze). The pre-fix day ran **6 freezes in 11 h 55 m** — longest clean gap **4 h 28 m**, mean **~2 h 23 m** — so this run has already passed the old record. Fix confirmed still applied (`HIPM AC index = 0x00000000`); drive **25 °C, 0 read errors, 0 wear**. ⚠️ **Honest statistics: at the old failure rate a clean 5.3 h stretch happens by chance ~7% of the time. That is suggestive, not proof — it is ONE interval.** `volmgr 161` fired only on the 23:21 freeze, which is the exact drive-hang fingerprint the LPM change targets. **Keep watching; do not close this until it has run clean well past a full day.** Re-check with `scratchpad\acer-verdict.ps1` (scp + run over SSH to `jeffl@192.168.1.176`). |

---

*Five items were struck 2026-09-20 evening after being measured against the live system — **#195** (gates armed, `Test-ReadGate` 22/22), **#127** (Z2M 2.14.1 + a clean 49/53 entity diff), **#39**, **#192**, **#197** and **#183** (7 days under the Cloudflare cap). The evidence is in `OPEN_ITEMS_CLOSED.md` — not here, because finished work is not a todo.*

### 🟡 UNBLOCKED TONIGHT — #109 / #131: THE PROOF RUN IS FINISHED

**#131 carried an explicit gate: *"Do NOT change either one before tonight's proof run"* — Jeff's
#109 principle of observing the system as built, against real water, first. That run is done.**

**Measured 2026-09-20 20:3x from `HCC-Scripts/overnight-water-watch.log` — 15 consecutive nights:**
`09-06 → 09-20 all CLEAN at 0.0–1.3 gal, shape STEPPED (ice/flush-like)`, with a single **ELEVATED
2.2 gal on 09-14**. Against the pre-repair readings of **8.8 gal (09-04)** and **24.8 gal (09-05)**,
both **CONTINUOUS (leak-like)**. The scheduled task is healthy: last run 09-20 08:05,
`LastTaskResult 0`, next 09-21 08:05. **Jeff's zone-4 bonnet repair is proven over 15 nights.**

🔴 **WHAT #131 ACTUALLY NEEDS NOW, and it is a live false-page risk:**
`automation.hcc_overnight_water_check_5_am` computes `current − input_text.hcc_water_1am` and got
**10.8 gal** on the night `Watch-OvernightWater.py` measured **8.8** from hourly LTS buckets.
**Cause: `rtlamr -unique=true` republishes only on CHANGE, so the 01:00 baseline broadcast can be up
to an hour stale — the HA automation therefore spans MORE than four hours and runs high.**
⚠️ **A clean night is ~1.3 gal and the HA hard trip is 3.5**, so a ~2 gal method error can push a
perfectly clean night to ~3.3 and page Jeff at 5 AM for nothing. The Python/LTS number is the
trustworthy one.
📌 **Recommended fix (not applied — it changes what wakes him at 5 AM):** stop the HA automation
computing its own window and have it read the Python script's verdict, so there is ONE method
instead of two disagreeing. **Jeff's word before touching a 5 AM alert.**

### 🟡 UNBLOCKED TONIGHT, BUT STILL JEFF'S ONE WORD — #84 / #85

**The stated blocker is GONE.** Both were held for *"one Z2M restart, held for the repeater still in
shipping."* **Z2M was restarted THREE times tonight** (add-on update, host reboot, hard power cycle)
with the mesh intact each time — and the repeater premise was retired separately: the mailbox joined
the coordinator **direct from the street at LQI 61–102**, it never needed one.

**But the work itself was never done** — measured live 2026-09-20 20:22 on Z2M 2.14.1:
- `availability.passive.timeout` = **1500** (#84 wants **720**)
- `advanced.last_seen` = **`disable`** (#85 wants **`ISO_8601`**)

🔴 **#84 carries a documented FALSE-PAGE RISK and that is why it is still his call** — it halves
the time a quiet battery device is called offline (25 h → 12 h). The record's own margin: worst
healthy device measured **4.9 h**, so 12 h is 2.4×. A night of false pages has happened here before.

🟢 **#85 has no alerting risk and would have prevented three ghost-value misreads tonight alone**
(the siren's stale LQI 142, the repeaters' linkquality, the mailbox's retained `online`). It only
adds a real per-device last-heard timestamp to every payload. **This is the cheapest fix on the
list for the error class that costs the most.**

---

## WHAT IS ACTUALLY STILL OPEN

#3/#118 · #4 · #10 · #84/#85 (his word) · #106 · #112 · #113 (physical half) · #121/#122 · #131 ·
#189 (a chase, not a gate) · #194 · #196 — plus **the alarm subsystem**, and **fire
detection**, which the 2026-09-18 sweep confirmed does not exist in HA at all.

**Re-measured 2026-09-20 20:2x, not taken on trust:** `#3/#118` → `bw status` = **`unauthenticated`**
(zero-knowledge vault, nothing substitutes for his master password). `#4` → **Secure Boot `False`,
TPM present AND ready, BitLocker `Off`** — so the TPM was never the blocker and it genuinely is one
BIOS screen. `#10` is Jeff's hard stop (*"leave the panic button alone till the alarms are built"*)
and must not be worked.

**#189 is no longer a gate.** Daniels has been silent since 09-11 and falls under Jeff's own *"no
further contact until they reach out"* rule. Two other bids are complete. It blocks nothing.

*Seven rows were struck 2026-09-18 after being measured against the live app, live HA and the
gateway. The evidence, and the four rows that were filed as Jeff's when they were ours, are recorded
in `FINDINGS_AND_STOPS.md` — not here, because finished work is not a todo.*

**Live bid board: `docs/hvac/BID_TRACKER.md`.**
